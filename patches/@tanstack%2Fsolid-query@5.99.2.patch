--- a/src/useBaseQuery.ts	2026-08-15 08:21:22.735744205 +0000
+++ b/src/useBaseQuery.ts	2026-08-15 08:38:52.814526017 +0000
@@ -376,6 +376,20 @@
       prop: keyof QueryObserverResult<TData, TError>,
     ): any {
       if (prop === 'data') {
+        // CLAXEDO PATCH: on the client, `.data` reads must not go through the
+        // resource. Both resource paths can call solid's `createResource#read`
+        // (`queryResource()` always; `queryResource.latest` whenever the
+        // resource has not RESOLVED its initial load yet), and read() arms a
+        // PERSISTENT computed against the nearest <Suspense> boundary — from
+        // then on, every fetch on that query demotes the ALREADY-RENDERED
+        // subtree to its fallback for a frame (focus loss, popover close,
+        // DOM churn across the session pane). `state` is the reactive store
+        // the observer reconciles into, so reading `state.data` keeps full
+        // reactivity without any Suspense registration. The server path keeps
+        // the resource reads: SSR/streaming needs the Suspense integration.
+        if (!isServer) {
+          return state.data
+        }
         if (state.data !== undefined) {
           return queryResource.latest?.data
         }
--- a/build/dev.js	2026-08-15 08:21:22.736344782 +0000
+++ b/build/dev.js	2026-08-15 08:39:00.106525691 +0000
@@ -276,6 +276,9 @@
   const handler = {
     get(target, prop) {
       if (prop === "data") {
+        if (!isServer) {
+          return state.data;
+        }
         if (state.data !== void 0) {
           return queryResource.latest?.data;
         }
--- a/build/index.js	2026-08-15 08:21:22.736425049 +0000
+++ b/build/index.js	2026-08-15 08:39:02.926525566 +0000
@@ -267,6 +267,9 @@
   const handler = {
     get(target, prop) {
       if (prop === "data") {
+        if (!isServer) {
+          return state.data;
+        }
         if (state.data !== void 0) {
           return queryResource.latest?.data;
         }
--- a/build/dev.cjs	2026-08-15 08:21:22.736097689 +0000
+++ b/build/dev.cjs	2026-08-15 08:39:05.046525471 +0000
@@ -277,6 +277,9 @@
   const handler = {
     get(target, prop) {
       if (prop === "data") {
+        if (!web.isServer) {
+          return state.data;
+        }
         if (state.data !== void 0) {
           return queryResource.latest?.data;
         }
--- a/build/index.cjs	2026-08-15 08:21:22.736151614 +0000
+++ b/build/index.cjs	2026-08-15 08:39:07.262525372 +0000
@@ -268,6 +268,9 @@
   const handler = {
     get(target, prop) {
       if (prop === "data") {
+        if (!web.isServer) {
+          return state.data;
+        }
         if (state.data !== void 0) {
           return queryResource.latest?.data;
         }
