diff --git a/dist/authorize-Crqw4_bR.mjs b/dist/authorize-Crqw4_bR.mjs
index fdd19eb..791a219 100644
--- a/dist/authorize-Crqw4_bR.mjs
+++ b/dist/authorize-Crqw4_bR.mjs
@@ -3502,41 +3502,15 @@ async function revokeRefreshToken(ctx, opts, token, clientId) {
 		error_description: "token not found",
 		error: "invalid_request"
 	});
-	if (refreshToken.revoked) {
-		await invalidateRefreshFamily(ctx, clientId, refreshToken.userId);
-		throw new APIError$1("BAD_REQUEST", {
-			error_description: "refresh token revoked",
-			error: "invalid_request"
-		});
-	}
 	if (!refreshToken.clientId || refreshToken.clientId !== clientId) return null;
-	const iat = Math.floor(Date.now() / 1e3);
-	if (!await ctx.context.adapter.incrementOne({
-		model: "oauthRefreshToken",
-		where: [{
-			field: "id",
-			value: refreshToken.id
-		}, {
-			field: "revoked",
-			operator: "eq",
-			value: null
-		}],
-		increment: {},
-		set: { revoked: /* @__PURE__ */ new Date(iat * 1e3) }
-	})) {
-		await invalidateRefreshFamily(ctx, clientId, refreshToken.userId);
+	if (refreshToken.revoked) {
+		await invalidateRefreshFamily(ctx, refreshToken);
 		throw new APIError$1("BAD_REQUEST", {
 			error_description: "refresh token revoked",
 			error: "invalid_request"
 		});
 	}
-	await ctx.context.adapter.deleteMany({
-		model: "oauthAccessToken",
-		where: [{
-			field: "refreshId",
-			value: refreshToken.id
-		}]
-	});
+	await invalidateRefreshFamily(ctx, refreshToken);
 }
 /**
 * We don't know the access token format so we try to validate it
@@ -3900,7 +3874,7 @@ const schema = {
 	*
 	* Refresh tokens are linked to a session.
 	*/
-	oauthRefreshToken: { fields: {
+		oauthRefreshToken: { fields: {
 		token: {
 			type: "string",
 			required: true,
@@ -3951,6 +3925,25 @@ const schema = {
 			type: "string[]",
 			required: false
 		},
+		familyId: {
+			type: "string",
+			required: true,
+			index: true
+		},
+		parentId: {
+			type: "string",
+			required: false,
+			unique: true
+		},
+		generation: {
+			type: "number",
+			required: true
+		},
+		rotationNonce: {
+			type: "string",
+			required: false,
+			unique: true
+		},
 		expiresAt: { type: "date" },
 		createdAt: { type: "date" },
 		revoked: {
@@ -3981,7 +3974,10 @@ const schema = {
 			type: "string[]",
 			required: true
 		}
-	} },
+	}, indexes: [{
+		fields: ["familyId", "generation"],
+		unique: true
+	}] },
 	/**
 	* An opaque access token sent when there is no resource audience claim
 	* to assigned to the JWT.
@@ -4398,6 +4394,7 @@ const oauthProvider = (options) => {
 		onRequest: handleIssuerMetadataRequest,
 		init: async (ctx) => {
 			if (ctx.options.secondaryStorage && ctx.options.session?.storeSessionInDatabase !== true) throw new BetterAuthError("OAuth Provider requires `session.storeSessionInDatabase: true` when using secondaryStorage");
+			if (opts.requireAtomicRefreshRotation && !ctx.adapter[Symbol.for("claxedo.better-auth.d1.atomic.v1")]?.rotateRefreshToken) throw new BetterAuthError("OAuth Provider requires the configured adapter to provide atomic refresh rotation");
 			await seedResources(ctx, opts);
 			logEnforcePerClientResourcesResolution(opts);
 			if (!opts.disableJwtPlugin) {
diff --git a/dist/introspect-6ew7sakf.mjs b/dist/introspect-6ew7sakf.mjs
index 8786e0d..a89e10f 100644
--- a/dist/introspect-6ew7sakf.mjs
+++ b/dist/introspect-6ew7sakf.mjs
@@ -1478,50 +1478,36 @@ async function createOpaqueAccessToken(ctx, opts, user, client, scopes, payload,
 	});
 	return (opts.prefix?.opaqueAccessToken ?? "") + token;
 }
-/**
-* Tear down the entire refresh-token family for a (client, user) pair, plus
-* any access tokens that reference those refresh rows, per RFC 9700 §4.14.
-* Access tokens are deleted first so the parent rows' foreign-key children
-* do not block the refresh-row delete.
-*
-* TODO(invalidate-family-race): the two `deleteMany` calls are not atomic
-* with respect to each other. Between them, a concurrent rotation in a
-* different worker can `create` a fresh refresh row (and, immediately after,
-* an access-token row referencing it) for the same (client, user) pair,
-* leaving the family partially rebuilt and the new refresh row orphaned of
-* any deletion. Closing this window requires the same transactional adapter
-* contract tracked under FIXME(strict-family-invalidation) in
-* `createRefreshToken`.
-*
-* @internal
-*/
-async function invalidateRefreshFamily(ctx, clientId, userId) {
+/** Tear down one per-login refresh-token family and its cascade-linked access tokens. */
+async function invalidateRefreshFamily(ctx, refreshToken) {
+	if (!refreshToken.familyId || !refreshToken.clientId) throw new Error("refresh token is missing its family binding");
+	const d1Atomic = ctx.context.adapter[Symbol.for("claxedo.better-auth.d1.atomic.v1")];
+	if (d1Atomic?.invalidateRefreshFamily) {
+		await d1Atomic.invalidateRefreshFamily({
+			familyId: refreshToken.familyId,
+			clientId: refreshToken.clientId
+		});
+		return;
+	}
 	const refreshTokens = await ctx.context.adapter.findMany({
 		model: "oauthRefreshToken",
 		where: [{
-			field: "clientId",
-			value: clientId
+			field: "familyId",
+			value: refreshToken.familyId
 		}, {
-			field: "userId",
-			value: userId
-		}]
-	});
-	if (refreshTokens.length) await ctx.context.adapter.deleteMany({
-		model: "oauthAccessToken",
-		where: [{
-			field: "refreshId",
-			operator: "in",
-			value: refreshTokens.map((r) => r.id)
+			field: "clientId",
+			value: refreshToken.clientId
 		}]
 	});
+	if (!refreshTokens.length) return;
 	await ctx.context.adapter.deleteMany({
 		model: "oauthRefreshToken",
 		where: [{
-			field: "clientId",
-			value: clientId
+			field: "familyId",
+			value: refreshToken.familyId
 		}, {
-			field: "userId",
-			value: userId
+			field: "clientId",
+			value: refreshToken.clientId
 		}]
 	});
 }
@@ -1547,7 +1533,10 @@ async function createRefreshToken(ctx, opts, user, referenceId, authorizationCod
 	const exp = payload?.exp ?? iat + (opts.refreshTokenExpiresIn ?? 2592e3);
 	const token = opts.generateRefreshToken ? await opts.generateRefreshToken() : generateRandomString(32, "A-Z", "a-z");
 	const sessionId = payload?.sid;
+	const id = originalRefresh ? generateRandomString(32, "a-z", "A-Z", "0-9") : void 0;
+	const familyId = originalRefresh?.familyId ?? generateRandomString(32, "a-z", "A-Z", "0-9");
 	const newRow = {
+		...id ? { id } : {},
 		token: await storeToken(opts.storeTokens, token, "refresh_token"),
 		clientId: client.clientId,
 		sessionId,
@@ -1560,7 +1549,10 @@ async function createRefreshToken(ctx, opts, user, referenceId, authorizationCod
 		scopes,
 		resources,
 		createdAt: /* @__PURE__ */ new Date(iat * 1e3),
-		expiresAt: /* @__PURE__ */ new Date(exp * 1e3)
+		expiresAt: /* @__PURE__ */ new Date(exp * 1e3),
+		familyId,
+		parentId: originalRefresh?.id,
+		generation: originalRefresh ? originalRefresh.generation + 1 : 0
 	};
 	if (!originalRefresh?.id) return {
 		id: (await ctx.context.adapter.create({
@@ -1572,10 +1564,26 @@ async function createRefreshToken(ctx, opts, user, referenceId, authorizationCod
 	const rotatedAt = /* @__PURE__ */ new Date(iat * 1e3);
 	const rotationUpdate = {
 		revoked: rotatedAt,
-		rotatedAt
+		rotatedAt,
+		rotationNonce: generateRandomString(32, "a-z", "A-Z", "0-9")
 	};
 	const reuseInterval = opts.refreshTokenReuseInterval ?? 0;
 	if (reuseInterval > 0) rotationUpdate.rotationReplayExpiresAt = /* @__PURE__ */ new Date((iat + reuseInterval) * 1e3);
+	const d1Atomic = ctx.context.adapter[Symbol.for("claxedo.better-auth.d1.atomic.v1")];
+	if (d1Atomic?.rotateRefreshToken) {
+		const rotated = await d1Atomic.rotateRefreshToken(originalRefresh, newRow, rotationUpdate);
+		if (!rotated) {
+			await invalidateRefreshFamily(ctx, originalRefresh);
+			throw new APIError("BAD_REQUEST", {
+				error_description: "invalid refresh token",
+				error: "invalid_grant"
+			});
+		}
+		return {
+			id: rotated.id,
+			token: await encodeRefreshToken(opts, token, sessionId)
+		};
+	}
 	if (!await ctx.context.adapter.incrementOne({
 		model: "oauthRefreshToken",
 		where: [{
@@ -2157,7 +2165,7 @@ async function handleRefreshTokenGrant(ctx, opts) {
 				error: "invalid_grant"
 			});
 		}
-		await invalidateRefreshFamily(ctx, client_id, refreshToken.userId);
+		await invalidateRefreshFamily(ctx, refreshToken);
 		throw new APIError("BAD_REQUEST", {
 			error_description: "invalid refresh token",
 			error: "invalid_grant"
diff --git a/dist/oauth-1Ud-hvZY.d.mts b/dist/oauth-1Ud-hvZY.d.mts
index 130fd5d..962c235 100644
--- a/dist/oauth-1Ud-hvZY.d.mts
+++ b/dist/oauth-1Ud-hvZY.d.mts
@@ -345,6 +345,25 @@ declare const schema: {
         type: "string[]";
         required: false;
       };
+      familyId: {
+        type: "string";
+        required: true;
+        index: true;
+      };
+      parentId: {
+        type: "string";
+        required: false;
+        unique: true;
+      };
+      generation: {
+        type: "number";
+        required: true;
+      };
+      rotationNonce: {
+        type: "string";
+        required: false;
+        unique: true;
+      };
       expiresAt: {
         type: "date";
       };
@@ -380,6 +399,10 @@ declare const schema: {
         required: true;
       };
     };
+    indexes: {
+      fields: [string, string];
+      unique: true;
+    }[];
   };
   /**
    * An opaque access token sent when there is no resource audience claim
@@ -1236,6 +1259,11 @@ interface OAuthOptions<Scopes extends readonly Scope[] = InternallySupportedScop
    * @default 0
    */
   refreshTokenReuseInterval?: number;
+  /**
+   * Require the database adapter to expose Claxedo's fixed atomic refresh
+   * rotation capability. Initialization fails closed when it is absent.
+   */
+  requireAtomicRefreshRotation?: boolean;
   /**
    * The amount of time in seconds that the authorization code is valid for.
    *
@@ -2917,4 +2945,4 @@ interface ResourceServerMetadata {
   dpop_bound_access_tokens_required?: boolean;
 }
 //#endregion
-export { OAuthOpaqueAccessToken as A, Prompt as B, OAuthClientAuthenticationStrategy as C, OAuthExtensionGrantHandler as D, OAuthConsent as E, OAuthResource as F, VerificationValue as G, Scope as H, OAuthResourceInput as I, ResourceUriSchema as J, ClientRegistrationRequest as K, OAuthTokenIssueParams as L, OAuthProviderApi as M, OAuthProviderExtension as N, OAuthExtensionGrantHandlerInput as O, OAuthRefreshToken as P, OAuthTokenResponse as R, OAuthClientAuthenticationResult as S, OAuthClientResource as T, StoreTokenType as U, SchemaClient as V, StoredAuthorizationQuery as W, oauthClientMetadataSchema as Y, OAuthAuthorizationQuery as _, GrantType as a, OAuthClientAuthenticationInput as b, ResourceServerMetadata as c, ActiveAccessTokenPayload as d, AuthorizePrompt as f, OAuthAuthenticatedClient as g, InitialAccessTokenAuthorization as h, Confirmation as i, OAuthOptions as j, OAuthMetadataExtensionInput as k, TokenEndpointAuthMethod as l, ClientMetadataResourceFetch as m, AuthServerMetadata as n, OAuthClient as o, ClientDiscovery as p, OAuthClientMetadata as q, BearerMethodsSupported as r, OIDCMetadata as s, AuthMethod as t, TokenType as u, OAuthClaimExtensionInput as v, OAuthClientRegistrationResponse as w, OAuthClientAuthenticationRequest as x, OAuthClientAdministrativeResponse as y, OAuthUserInfoExtensionInput as z };
\ No newline at end of file
+export { OAuthOpaqueAccessToken as A, Prompt as B, OAuthClientAuthenticationStrategy as C, OAuthExtensionGrantHandler as D, OAuthConsent as E, OAuthResource as F, VerificationValue as G, Scope as H, OAuthResourceInput as I, ResourceUriSchema as J, ClientRegistrationRequest as K, OAuthTokenIssueParams as L, OAuthProviderApi as M, OAuthProviderExtension as N, OAuthExtensionGrantHandlerInput as O, OAuthRefreshToken as P, OAuthTokenResponse as R, OAuthClientAuthenticationResult as S, OAuthClientResource as T, StoreTokenType as U, SchemaClient as V, StoredAuthorizationQuery as W, oauthClientMetadataSchema as Y, OAuthAuthorizationQuery as _, GrantType as a, OAuthClientAuthenticationInput as b, ResourceServerMetadata as c, ActiveAccessTokenPayload as d, AuthorizePrompt as f, OAuthAuthenticatedClient as g, InitialAccessTokenAuthorization as h, Confirmation as i, OAuthOptions as j, OAuthMetadataExtensionInput as k, TokenEndpointAuthMethod as l, ClientMetadataResourceFetch as m, AuthServerMetadata as n, OAuthClient as o, ClientDiscovery as p, OAuthClientMetadata as q, BearerMethodsSupported as r, OIDCMetadata as s, AuthMethod as t, TokenType as u, OAuthClaimExtensionInput as v, OAuthClientRegistrationResponse as w, OAuthClientAuthenticationRequest as x, OAuthClientAdministrativeResponse as y, OAuthUserInfoExtensionInput as z };
diff --git a/dist/oauth-D8xpKR0_.d.mts b/dist/oauth-D8xpKR0_.d.mts
index 47f2a9d..5e5863b 100644
--- a/dist/oauth-D8xpKR0_.d.mts
+++ b/dist/oauth-D8xpKR0_.d.mts
@@ -2950,6 +2950,25 @@ declare const oauthProvider: <O extends OAuthOptions<Scope[]>>(options: O) => {
           type: "string[]";
           required: false;
         };
+        familyId: {
+          type: "string";
+          required: true;
+          index: true;
+        };
+        parentId: {
+          type: "string";
+          required: false;
+          unique: true;
+        };
+        generation: {
+          type: "number";
+          required: true;
+        };
+        rotationNonce: {
+          type: "string";
+          required: false;
+          unique: true;
+        };
         expiresAt: {
           type: "date";
         };
@@ -2985,6 +3004,10 @@ declare const oauthProvider: <O extends OAuthOptions<Scope[]>>(options: O) => {
           required: true;
         };
       };
+      indexes: {
+        fields: [string, string];
+        unique: true;
+      }[];
     };
     oauthAccessToken: {
       modelName: string;
@@ -3149,4 +3172,4 @@ declare const oauthProvider: <O extends OAuthOptions<Scope[]>>(options: O) => {
   })[];
 };
 //#endregion
-export { OAuthEndpointErrorResult as a, OAuthFieldErrorCode as c, DEVICE_CODE_GRANT_TYPE as d, OAuthDeviceAuthorizationOptions as f, getIssuer as i, OAuthFieldErrorCodeMap as l, getOAuthProviderState as n, OAuthEndpointRedirectContext as o, oauthDeviceAuthorization as p, oauthProvider as r, OAuthErrorCode as s, DEFAULT_OAUTH_SCOPES as t, OAuthRedirectOnError as u };
\ No newline at end of file
+export { OAuthEndpointErrorResult as a, OAuthFieldErrorCode as c, DEVICE_CODE_GRANT_TYPE as d, OAuthDeviceAuthorizationOptions as f, getIssuer as i, OAuthFieldErrorCodeMap as l, getOAuthProviderState as n, OAuthEndpointRedirectContext as o, oauthDeviceAuthorization as p, oauthProvider as r, OAuthErrorCode as s, DEFAULT_OAUTH_SCOPES as t, OAuthRedirectOnError as u };

diff --git a/dist/index.mjs b/dist/index.mjs
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -52,7 +52,7 @@ async function exchangeOAuthDeviceCode(input) {
 	const body = ctx.body;
 	const deviceCode = body?.device_code;
 	if (!deviceCode) tokenError("BAD_REQUEST", "invalid_request", "device_code is required");
-	const { authorizationContext: { client, confirmation, scopes }, redemptionContext: resources, user } = await redeemDeviceCode({
+	const { authorizationContext: { client, confirmation, scopes }, claimedDeviceCode, redemptionContext: resources, user } = await redeemDeviceCode({
 		ctx,
 		deviceCode,
 		authorizeRedemption: async (record) => {
@@ -95,6 +95,8 @@ async function exchangeOAuthDeviceCode(input) {
 		scopes,
 		user,
 		resources,
-		confirmation
+		confirmation,
+		sessionId: claimedDeviceCode.approvalSessionId,
+		authTime: claimedDeviceCode.approvalAuthTime
 	});
 }
