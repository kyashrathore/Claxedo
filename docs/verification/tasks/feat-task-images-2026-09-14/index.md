# Task creation accepts images — evidence (2026-09-14)

Source: uncommitted working tree on `dev` at `162fd6efab`. Scope `local`, owner `local`.

Environment for the live rows: unsigned local stack from this tree. Server `npm run dev` in `packages/claxedo-server` with `CLAXEDO_SERVER_PORT=2597 CLAXEDO_DATA_DIR=~/test/claxedo-scratch/task-images-data`; app `bun run dev` in `packages/claxedo-app` with `PORT=4447 VITE_CLAXEDO_SERVER_URL=http://127.0.0.1:2597`. Project registered with `POST /api/claxedo/projects` pointing at a scratch git repository. The browser rows were driven with `@playwright/test`'s headless Chromium (`chromium-1228`).

| Requirement | Automated | Live | Result | Notes |
|---|---|---|---|---|
| A create carries images and the store keeps them | `claxedo-tasks` model/service/parse/routes/client tests; store conformance case `attachments_read_back_in_position_order_byte_for_byte_and_only_within_their_task_and_scope` on memory, SQLite and D1 (Miniflare); `local-composition.test.ts` through the real desktop-local app | `POST /api/claxedo/tasks/commands` with a 156-byte PNG; `claxedo_task_attachment` row read back with `sqlite3` | PASS | The D1 case caught a swapped `created_at`/`bytes` value order in the first D1 insert. |
| The bytes come back under their own route | routes + client tests (type, length, `content-disposition`, 404, 403) | `GET /tasks/:id/attachments/:attachmentId` → `image/png`, 156 bytes, bytes equal | PASS | |
| A refused image leaves no task | service + routes + local-composition tests | — | PASS (automated) | `attachments[0].mime unknown_value` → 400, list stays empty. |
| The dialog takes paste, drop and a picker; an oversized image is shrunk | `create-task-form.vitest.tsx` (paste, drop, pick, remove, refusal, cap, server field error); `image-drafts.vitest.ts` | Picker with `mock-red.png` (64×48) and `big-noise.png` (3,288,211 bytes, 1400×900): the second became `big-noise.webp` at 1400×900, 521,438 bytes, under the 1,572,864 cap | PASS | Real canvas `toBlob("image/webp")` path exercised; see `evidence/create-dialog-two-images.png`. |
| Detail shows the images through the authorized client | `task-attachments.vitest.tsx` (fetch, object-URL revoke on unmount, failed read, empty) | After Create the page navigated to `/tasks/tsk_b99cdf32…`; both `<img>` rendered from `blob:` URLs at natural size (64×48 and 653×420 of 1400×900) | PASS | First run showed the small image stretched to the column (flex `align-items: stretch`); fixed and re-measured. See `evidence/task-detail-images.png`. |
| Start hands the images to the session as file parts | bridge conformance case `the_task_s_images_follow_its_text_into_the_first_message_as_file_parts`, run by the local host against the real embedded workspace runtime and by the hosted host against its captured fetch | — | PASS (automated) | A live model turn was not run; the local case proves the `file` part with the `data:` URL reaches the harness adapter's `executeTurn` through the runtime's HTTP route. |
| Agents see the images on a task | `claxedo-mcp` `task_get` test | — | PASS (automated) | `task_get` now returns `attachments` metadata. `task_create` over MCP takes no images yet. |

Gates run: `bun test src` in `claxedo-tasks` (237 pass); vitest `src/tasks-host` in `claxedo-server-core` (67), `src/tasks` in `claxedo-local-server` (28), `src/tasks` + migration ledger + credential sweep + resource closure in `claxedo-server` (182); `claxedo-mcp` (182 pass, 3 pre-existing `session_create` failures reproduced without this change); `claxedo-app` tasks vitest (127) and bun (14); `tsc`/`tsgo` on every touched package (one pre-existing `agent-conversation.ts` error from another lane's uncommitted edit); root `oxlint` (one pre-existing finding in another lane's `timeline-virtualization.test.ts`); `bun run test:architecture-ratchets` after raising `app-local` 1071→1073 and `desktop-renderer-unsigned` 1114→1116 for the two new Tasks modules, both products' `verify:closure` green.
