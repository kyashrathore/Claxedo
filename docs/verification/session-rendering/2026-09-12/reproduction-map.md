# Reproduction coverage for failed live observations

Proposed reproduction families, not a count of independently proven root causes. Existing controls are not closure evidence. Some rows contain secondary observations requiring separate assertions.

88 failed table rows map to 33 proposed test families. No failed row is unmapped. This does not mean those tests exist or have passed.

| Family | Live rows | Proposed test home | Required proof |
| --- | --- | --- | --- |
| Completed reply missing | 2 OpenCode, 3 OpenCode, 4 OpenCode, 14 OpenCode | `real-session-rendering-harnesses.spec.ts` | Drive steering and restart through the real producer; prove stored completion and visible reply with the shared oracle. Intermittent Tier R API-started OpenCode steering reproduced, fixme/register 75: unsigned 1213/1214 has 3 passes; test-user 1215 has 2 failures and 1 pass, zero retries. Canonical full messages complete with FINAL, but both failed browser oracles time out after 20s with two user prompts only; latest-surface/latest-turn reads retain only the steering user. Failed reload assertions are unreached. No browser first-send recovery, event-loss or exact internal-cause claim; desktop/restart/mature-session and other steering variants remain open. |
| Thinking appears under the preceding turn | 5 Codex | `core-busy-abort-errors.spec.ts` | Qualified red (register 78): `Thinking stays with the new prompt while the previous turn's completion envelope is in flight` fails in both auth modes (runs 1272/1273). Seeded previous assistant envelope lacks `time.completed` (parts painted, completion in flight) + busy; after the follow-up send, `activeMessageID` resolves `pending()` to the stale assistant's parent — the OLD user message — and Thinking paints beneath the completed-looking reply, above the new prompt. 496 sampled frames attributed to the old turn in the unsigned run; the anchor relocates when the new assistant envelope lands. The normalized 1112–1114 control used an already-completed seed, so `pending()` never pointed backwards. |
| Thinking disappears before the reply | 5 OpenCode | `core-busy-abort-errors.spec.ts` | Register 59: real follow-up 1119 captures Idle before the completed REST refresh. The shared mock now serves the central WebSocket from its EventBus. Earlier unsigned passes missed the lifecycle transport and are invalid as continuity coverage. Final shared-transport checks 1128/1129 reproduce the continuity failure three times per auth mode (716–733ms), while both normal-turn controls pass three times per mode: 6 intended failures, 12 passes, zero retries. Every run has reviewed transition or busy/final frames. The regression also asserts that the central WebSocket was intercepted, preventing the earlier missing-transport false pass. Full recording review remains outstanding. |
| Existing transcript moves during updates | 6 Codex, 6 OpenCode, 13 Codex, 59 OpenCode, 59 Codex, 60 Codex, 60 OpenCode, 61 Codex, 61 OpenCode, 65 Codex | `core-timeline-rendering-scroll.spec.ts` | Codex first-token variant reproduced red in both auth modes (first-token-1198/1199), fixme/register69: steady58px prompt shift with stationary composer. Send, OpenCode, child-chip and separate tool-transition variants remain unqualified. |
| Stopped work lacks interrupted presentation | 7 OpenCode, 44 OpenCode | `core-busy-abort-errors.spec.ts` | Register 60: live OpenCode probes 1130/1131 show no interruption explanation after Stop, while reload reveals it. Probe 1131 confirms a cancelled abort response and canonical lastTurn metadata. Tier M runs 1132/1133 fail the pre-reload visibility assertion after 15 seconds in both auth modes; ready composer and post-reload divider pass. Screenshots reviewed. Tool-fold, repeated-Stop, and later-turn-retention variations remain uncovered. |
| Tool metadata or live row absent | 12 OpenCode, 20 OpenCode, 21 OpenCode, 22 OpenCode, 26 OpenCode, 41 OpenCode, 49 OpenCode, 50 OpenCode, 51 OpenCode, 56 OpenCode, 74 OpenCode, 96 OpenCode | `real-session-rendering-harnesses.spec.ts` | Exercise real read, shell, edit, MCP and permission tools; assert canonical names, live state, input/output, grouping and exits. Complete Tier M data cannot reproduce absent producer fields. Tier R restored-shell variant reproduced in both auth modes (1208/1209), fixme/register 72: real shell stdout reaches the model, but stored tool data is call_1 with empty input/output and the expanded UI shows only Call 1. API-started session/message, then browser restore and reload; read/edit/MCP/permission, grouping, exits and live-command variants remain unqualified. No browser first-send recovery is claimed. |
| Running shell command is invisible | 12 Codex, 96 Codex | `real-harness-local.spec.ts` | Inspect opacity and hit-test while the command is still running, before assistant reply. Status: initial command Tier R red, register 51; reload recovers, heavy-session rail-return still unproven. |
| Live-fold changes override manual state | 15 Codex, 41 Codex | `core-harness-rendering-matrix.spec.ts` | Tier M red in both auth modes, register 61: enabling live folding hides manually opened shell output. Before/after and final reply screenshots reviewed; completion passes. Disabling folding and later-event retention remain uncovered. |
| Text chunks repeat within one live-streaming turn | 17 Codex; user clarification applies to live streaming | `core-harness-rendering-matrix.spec.ts` | Register 80: `a settled reply does not re-render its streamed text when the turn's deltas replay` fails in both auth modes (1274/1275) — replayed `text-delta` frames drained while the canonical fetch is in flight mint a fresh part id (`000000_<msg>-text` ≠ stored `prt_…`) after the announced `message.updated` un-settles the envelope; `mergeChatParts` keeps both parts and the reply renders twice. The observed completed-turn duplicate (state 17's stored-once/rendered-twice evidence) is qualified. The live-streaming variant (chunks repeating while the stream is in progress) remains unverified — no qualified in-flight regression yet; next proof must compare cumulative incoming text with per-frame rendered chunks during a real stream. |
| Lists and tables paint incrementally | 18 Codex, 18 OpenCode, 66 Codex, 66 OpenCode | `core-timeline-rendering-scroll.spec.ts` | Stream list/table fragments and sample visibility before complete block arrival. Status: table and ordered-list Tier M red in one test, fixme/register 49. |
| Interrupted command returns as Running | 19 Codex | `core-harness-rendering-matrix.spec.ts` | Qualified red (register 77): rail return + reload + reattached runtime stream resends the tool's `tool-start`/`tool-input`; a fresh client projection re-mints the stored part id with `status: "running"`, and the live-event `upsertChatParts` overwrites the stored error part unconditionally (no terminal rank like `mergeChatPart`). Row renders Running with an advancing timer; both auth modes fail (runs 1270/1271). The replay's announced `message.updated` also strips `time.completed`, un-settling the message. |
| Completed command output missing | 20 Codex | `real-harness-local.spec.ts` | Replay real long-output payload and assert visible output and expansion controls. Status: Codex shell output Tier R red, fixme/register 48; edit diff variant open. |
| Failed shell loses command and exit code | 21 Codex, 54 Codex | `real-harness-local.spec.ts` | Use real failed command payload and assert command plus exit chip in collapsed and expanded states. Status: missing command reproduced red, fixme/register 47. Exit 23 passes in the current Tier R run. |
| Control-plane URL becomes Local preview | 22 Codex | `core-harness-rendering-matrix.spec.ts` | Supply captured output containing the control URL and assert it is not a dev-server preview target. Status: Tier M red, fixme/register 50; valid development-server link passes. Navigation remains untested. |
| Existing file image renders alt text | 24 Codex, 24 Codex, 24 OpenCode | `real-session-rendering-harnesses.spec.ts` | Create a real image and render its file reference through web and desktop image paths. Tier R OpenCode web variant reproduced in both auth modes (1209/1210), fixme/register 73: an existing local 512×512 PNG is referenced by stored Markdown, but restoration/reload shows only QA local image alt text and no img element. API-started session/message; 80px tile and full-preview assertions are unreached. Desktop and other-harness variants remain open; no browser first-send recovery is claimed. |
| Resolved question outcome card missing | 25 Codex, 25 OpenCode, 48 Codex, 48 OpenCode, 72 Codex, 72 OpenCode, 64 OpenCode | `real-harness-local.spec.ts` | Drive pending-to-answered/custom/dismissed lifecycle and fold transition; assert retained outcome card. State 25 also observes pending replay, mapped separately below. Status: Codex option retention reproduced red in Tier R, fixme/register 45; remaining variants open. |
| MCP group lacks tool names | 26 Codex, 51 Codex | `core-harness-rendering-matrix.spec.ts` | Stream consecutive named MCP calls and verify names in their group header. Status: Tier M red, fixme/register 53; expanded members pass. |
| Completed fold reopens on return or first visit | 36 Codex, 46 Codex, 58 Codex, 94 Codex | `core-harness-rendering-matrix.spec.ts` | Register 65: fresh live first visit exposes completed command rows for about 0.9 seconds. Captured-runtime return regression fails in both auth modes (1165/1166); transition and all reply screenshots reviewed. Cold first-visit automation, reload, and other harness variants remain uncovered. |
| Selected workspace panel changes across sessions | 38 Codex, 99 Codex, 100 Codex, 100 OpenCode, 100 Cursor, 100 Claude | `core-session-rendering-navigation.spec.ts` | Open distinct child/browser/file panels in two sessions and assert selected panel after switches, with content visible. File-selection variant reproduced red in both auth modes (1186/1187), fixme/register 66. Child/browser variants and live file-content loading remain unverified. |
| Collapsed work omits group count | 43 Codex | `core-harness-rendering-matrix.spec.ts` | Finish three separated tool groups and assert collapsed count. Status: Tier M red in both auth modes, fixme/register 52; manual fold retention passes. |
| Show all changes the output reading position | 63 Codex | `core-timeline-rendering-scroll.spec.ts` | Tier M red in both auth modes, fixme/register 58: the same line moves from y204 to y-5642 after Show all. Before/after screenshots reviewed. Standalone shell reproduces reading-position loss; the original nested-group variant remains uncovered. |
| Answered question reappears as pending | 64 Codex, 97 Codex, 97 OpenCode | `core-docks.spec.ts` | Observe rail return, reload and answer in another tab across the full hydration/replay interval. A genuine session-to-session stream switch plus missed resolution and retained runtime question reproduces both flashes in both auth modes (1150/1151), fixme/register 63. Reload and OpenCode remain unverified. |
| Image loading expands the user row | 67 OpenCode, 67 Codex | `core-timeline-rendering-scroll.spec.ts` | Hold actual image bytes and compare image, existing row and composer rectangles before and after load. Status: red observed; fixme registered in decisions entry 43. |
| Completed todo list disappears | 71 Codex | `core-docks.spec.ts` | Advance all five todos to completed and assert final list persists with completed state. Status: red reproduced, fixme/register 44; final SSE delivered before reload. |
| Resolved permission dock replays | 74 Codex | `core-docks.spec.ts` | Register 64: after one actual UI Allow once and authoritative permission.replied, a session-to-session rail return replays the retained runtime request. Both auth modes fail with a painted approval dock (1158/1159); all first/other/return reply screenshots and return flashes are reviewed. The child-error-panel variation remains uncovered. |
| Child command reports denial without a visible decision | 75 Codex | `real-harness-local.spec.ts` | Register 79: Tier R repro committed (`codex child session's escalated command surfaces an approval dock, not a silent denial`) — real codex `spawn_agent` child armed with `exec_command`/`require_escalated`; it `test.skip`s while the parent turn dies at "no rollout found for thread id" (upstream codex churn defect, same class as the Pi session-file fix `20fdb18874`). Tier M control in `core-docks.spec.ts` passes both auth modes: a `permission.asked` on the child mounts the dock on the parent's composer and Deny posts `reject` to the child's route — the UI decision path works, so the defect is the runtime auto-denying a child approval without asking. Status: qualified red still gated on the missing-rollout fix. |
| Home and End miss transcript endpoints | 84 Codex, 84 OpenCode | `core-timeline-rendering-scroll.spec.ts` | Shared Tier M endpoint regression reproduced in unsigned-local run 1102: Home remains at 12777px and the first user row is absent; End reaches the last prompt. Fixme/register 56. Test-user run 1106 reproduces Home at 12778px and End passes. Transient blanking and OpenCode first-End variation remain uncovered. |
| Expanded tool output collapses after scrolling away | 88 Codex | `core-timeline-rendering-scroll.spec.ts` | Tier M red in both auth modes: Show less before confirmed row unmount becomes Show all on return; outer tool remains expanded. Fixme/register 57; runs 1103–1104. Stage screenshots reviewed; full video review outstanding. |
| Idle return restores middle instead of required top | 93 OpenCode, 93 Codex | `core-timeline-rendering-scroll.spec.ts` | Leave an idle heavy session in the middle, switch and return, then verify first turn at top. Settled position reproduced red in both auth modes (1188/1189), fixme/register 67: top 12492 is retained instead of reaching 0. First-frame and live-session variants remain unverified. |
| Archive does not remove the active session | 102 OpenCode | `core-sidebar-tree.spec.ts` | Current live recheck 1170 succeeds after a 6.4-second request; authoritative archived timestamp and neighbour reply confirmed. Strengthened delayed-success, reload-persistence and rejected-archive control passes three times per auth mode (1177/1178), all reply screenshots reviewed. Narrowed 2026-09-14: the historical no-op (click produced no request, no dialog, unchanged row) matches the pre-fix defect `a2a5b11917` closed — under `updatedAt` ordering any activity moved the row out from under the pointer mid-aim, so the click landed on a displaced/stale row. The rail now sorts by `human_turn_desc` (`SESSION_LIST_SORT_DEFAULT`): a row can move only when a human sends it a message, so agent activity can never displace an aimed row. Residual silent no-op paths remain in `archiveSessionFromRows` (`rowForNavigation` ref miss → early return; `handleArchiveSession`'s `!sessionItem.directory` → `return false`, no toast) but neither is proven against real data. Historical failure still lacks a qualified red reproduction; likely fixed upstream by the ordering change. |
| Browser Back skips rail visits | 103 Codex, 103 OpenCode, 103 Claude | `core-session-rendering-navigation.spec.ts` | Preserve the demonstrated red browser-history assertion and registered fixme; extend only missing visit variants. Status: red observed; fixme registered in decisions entry 42. |
| Sending reorders only the sending tab | 104 Codex, 104 OpenCode | `real-harness-local.spec.ts` | Two browser tabs observe one real send and converge on identical authoritative rail order. Status: Codex Tier R red, fixme/register 46; OpenCode variant open. |
| Desktop Browser tab loses URL and page | 99 Codex; regression variation: native Claude | `desktop-unsigned-embedded.spec.ts` | Initial HTTP page verified, then empty address and about:blank guest on session return. Status: red observed; fixme registered in decisions entry 76. Codex and signed-native variants remain unverified. |

Streaming duplicate family: native Claude first/follow-up controls now have per-frame segment checks and real runtime SSE capture (1255/1256, 12 passes across both auth modes). Historical intermittent duplication and subagent/reconnect variations remain unreproduced.
