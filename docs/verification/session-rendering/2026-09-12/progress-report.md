# Session rendering QA progress — 2026-09-13

> Wrap-up: [what is done and what comes next](WHATS-DONE-AND-NEXT.md). Current test runs are finished; unfinished QA and acceptance gaps are preserved for the next session.

Most of the evidence this record cites — recordings, screenshots, timing files and logs — was never committed. Those citations are written as plain paths; the few files that did land are under `evidence/` and stay linked.

This is an interim report, not completion or release approval. The separately authorized Pi version mismatch is fixed in commit `4c4cd31526`; no session-rendering bugs have been fixed by this QA pass. The authorized deliverable is live evidence and regression tests; application fixes are a separate slice.

## Counts and meaning

- [Observed results](index.md): 165 prior pass/fail observations remain. Setup failures are no longer multiplied into per-state blocker counts. [Fresh prerequisites and actual app checks](current-status.md) supersede the earlier login/version status.
- The 88 failed rows map to 33 candidate issue families; these are not independently proven root causes.
- Twenty-eight numbered-inventory families now have a reproduced regression test, recorded in e2e-decisions entries 42–53, 56–61, 63–67, 69, 72–73 and 75–76. The additional permission-first-render report has two viewport regressions in entry 54, and the wide harness trigger has one in entry 55. Entry 62 is withdrawn after live scrolling recovered the commentary; it was an oracle-navigation problem, not a proven product defect (34 reproduced issue families and 35 failing test cases total, including the additional OpenCode default-agent failure in entry 68, the Pi/Codex first-send failures in entries 70–71, and the boot handoff failure in entry 74). Those failing tests are marked fixme as the brief requires. A skipped red test documents a defect; it is not a fix or green release coverage.
- Five mapped families still lack a qualified failing regression. Some of the twenty-eight reproduced families also have untested harness or navigation variations.
- The qualified regression slice is committed as `576e68805d` (19 explicit QA paths): 35 failing cases and their decision entries, shared helpers, fixtures and qualified controls. Remaining composer, sidebar and boot test changes still need qualification/commit. Native clipboard preservation and Electron video support are committed as `243e5a3aea` (three explicit helper paths). The separately requested Pi pin update is committed as `4c4cd31526`; validation and remaining failures (evidence/pi-upgrade-1220/upgrade.md).

> The sections below preserve prior regression evidence. Use the fresh status for current prerequisites.

## Latest selector feedback

1. Permission selector initially renders nothing, then appears. Now reproduced red at 1280px and 720px; unresolved in the application. The extended test confirms the absent trigger, then verifies unchanged geometry and a successful send after responses arrive. Evidence (evidence/permission-first-1093/result.json). The former hydration test compared only the outer controls/editor bounds and checked permission visibility afterward. It is now extended with a failing pre-response visibility check and a real mocked send/reply oracle. Exact default-label and animation behavior still need further coverage. Required acceptance: render the authoritative known default immediately when available, reserve the final control dimensions, and smoothly update an actual changed value without shifting neighboring controls. Do not display an invented permission mode as though it were effective. First-boot behavior when no authoritative value is available still needs implementation review and a specific test.
2. Harness/model dropdown is too wide and its caret is too far from the label. The closed trigger now reproduces red in both auth modes: 260px wide, Sonnet 4.6 text 65.73px, label-to-caret gap 140.27px. Register 55. Evidence (evidence/caret-1095/result.json). The open menu remains unmeasured. Required acceptance: a compact trigger with a close, consistently spaced caret at supported widths; check the open menu separately from the trigger.

These two additional reports are now reproduced in the browser E2E app and remain outside the original numbered inventory and its 33-family count.

## Reproduced regressions

| Issue | Evidence and current limits |
| --- | --- |
| OpenCode steering intermittently hides a completed reply | Intermittent Tier R API-started OpenCode steering reproduced, fixme/register 75: unsigned 1213/1214 has 3 passes; test-user 1215 has 2 failures and 1 pass, zero retries. Canonical full messages complete with FINAL, but both failed browser oracles time out after 20s with two user prompts only; latest-surface/latest-turn reads retain only the steering user. Failed reload assertions are unreached. No browser first-send recovery, event-loss or exact internal-cause claim; desktop/restart/mature-session and other steering variants remain open. Analysis (evidence/steering-reply-1215/analysis/network-ordering.json) |
| Boot splash disappears before composer readiness | Cold draft entry has three unsigned / six test-user DOM gap samples. All 291 encoded frames reviewed; both recordings paint missing main content between splash and composer, while the signed sidebar remains visible. First UI create/send/reply passes afterward. Register 74; shared-mock browser only, signed Reconnecting remains. Evidence (evidence/boot-handoff-1217/result.json) |
| OpenCode restored shell loses its command and output | Tier R restored-shell variant reproduced in both auth modes (1208/1209), fixme/register 72: real shell stdout reaches the model, but stored tool data is call_1 with empty input/output and the expanded UI shows only Call 1. API-started session/message, then browser restore and reload; read/edit/MCP/permission, grouping, exits and live-command variants remain unqualified. No browser first-send recovery is claimed. Evidence (evidence/api-rendering-1209/result.json) |
| OpenCode local-file image restores as alt text | Tier R OpenCode web variant reproduced in both auth modes (1209/1210), fixme/register 73: an existing local 512×512 PNG is referenced by stored Markdown, but restoration/reload shows only QA local image alt text and no img element. API-started session/message; 80px tile and full-preview assertions are unreached. Desktop and other-harness variants remain open; no browser first-send recovery is claimed. Evidence (evidence/api-image-1210/result.json) |
| Pi first send references a missing session file | **Root cause fixed 2026-09-14**: Pi defers its native session file to the first assistant message, and per-request credential rotation disposed the unpersisted session process; the driver now recreates driver-created ids via `--session-id` and still refuses unknown ones. Journeys rerun (register 70): 0/4 pass — they now reach the model request and fail on a real OpenAI 401 because the brokered `openai` destination is hardcoded, so the scripted endpoint is unreachable. [Diagnosis and rerun](evidence/pi-session-fix/findings.md); original evidence (evidence/native-first-send-1203/) |
| Codex first send references a missing rollout | Existing native journey fails before a model request in both auth modes; reviewed error screenshots, register 71. Evidence (evidence/native-first-send-1203/) |
| First reply token moves the existing prompt | Codex captured-payload regression moves the same prompt58px upward with unchanged composer. Both auth modes fail; register69. Other update triggers remain open. Evidence (evidence/first-token-1199/result.json) |
| OpenCode rejects its advertised default agent | Real browser send submits catalog name Build; engine rejects it before any model request. Both auth modes fail the reply oracle, with both error screenshots reviewed. Register 68. This does not close the original missing tool-data family. Evidence (evidence/opencode-ui-1199/) |
| Answered question reappears as pending | Tier M red in both auth modes, register 63. Session-to-session switching with missed resolution and retained runtime replay paints the resolved question again. Distinct oracle rechecks 1154/1155 confirm the red and provide all six reviewed reply images. Reload and OpenCode remain uncovered. Evidence (evidence/runtime-question-1155/) |
| Approved permission reappears on return | Tier M red in both auth modes (1158/1159), register 64: one painted pending-dock sample among 35 after one successful Allow once. All six reply images and return flashes reviewed. Child-panel opening, repeated returns and full-video review remain uncovered. Evidence (evidence/permission-replay-1159/) |
| Manually closed completed work opens during replay | Tier M red in both auth modes, register 65. Portable-fixture rechecks 1179/1180 reproduce painted tool rows during return; all six reply-oracle images reviewed. Cold first visit, reload and other harnesses remain uncovered. Evidence (evidence/fold-portable-1180/) |
| Workspace file selection leaks across sessions | Tier M red in both auth modes (1186/1187), register 66: return expects first.txt but retains second.txt. All six reply and six selection images reviewed; first-file content loading, child/browser variants and full videos remain unverified. Evidence (evidence/file-scope-1187/) |
| Idle return restores the middle instead of the required top | Tier M red in both auth modes (1188/1189), register 67: before/returned scrollTop is 12492 instead of zero. Six reply and four position images reviewed. Manual top recovery is separate from the failed automatic return; first-frame and active-session variants remain uncovered. Evidence (evidence/idle-return-1189/) |
| Live folding hides manually opened shell output | Tier M red in both auth modes, register 61: enabling live folding hides manually opened shell output. Before/after and final reply screenshots reviewed; completion passes. Disabling folding and later-event retention remain uncovered. Evidence (evidence/live-fold-1135/result.json) |
| Stop omits interruption until reload | Register 60: live OpenCode probes 1130/1131 show no interruption explanation after Stop, while reload reveals it. Probe 1131 confirms a cancelled abort response and canonical lastTurn metadata. Tier M runs 1132/1133 fail the pre-reload visibility assertion after 15 seconds in both auth modes; ready composer and post-reload divider pass. Screenshots reviewed. Tool-fold, repeated-Stop, and later-turn-retention variations remain uncovered. Evidence (evidence/stop-outcome-1133/result.json) |
| Thinking disappears before the reply | Captured live lifecycle ordering reproduced in Tier M; fixme/register 59. The shared mock now serves the central WebSocket from its EventBus. Earlier unsigned passes missed the lifecycle transport and are invalid as continuity coverage. Final shared-transport checks 1128/1129 reproduce the continuity failure three times per auth mode (716–733ms), while both normal-turn controls pass three times per mode: 6 intended failures, 12 passes, zero retries. Every run has reviewed transition or busy/final frames. The regression also asserts that the central WebSocket was intercepted, preventing the earlier missing-transport false pass. Full recording review remains outstanding. Evidence (evidence/thinking-gap-1121/result.json) |
| Running shell command is invisible | Initial running command reproduced intermittent Tier R red (2 fail, 1 pass, zero retries), register 51. Reload recovers; heavy-session rail-return remains unproven. Evidence (evidence/running-1086/result.json) |
| Lists and tables paint incrementally | Table and ordered-list partial rendering reproduced red in one Tier M test; fixme/register 49. Evidence (evidence/blocks-1079/red.log) |
| Completed command output missing | Codex shell output reproduced red in Tier R; fixme/register 48. Native result has 240 lines; stored/UI output only final marker. Edit diff variant remains open. Evidence (evidence/shell-output-1076/red.log) |
| Failed shell loses command and exit code | Codex missing command reproduced red in Tier R, fixme/register 47. Exit code 23 passes in the current run; older live exit-1 variants not repeated. Evidence (evidence/shell-exit-1074/red.log) |
| Control-plane URL becomes Local preview | Tier M red, fixme/register 50. Correct development-server link passes; preview navigation and Browser-tab persistence remain untested. Evidence (evidence/preview-1081/red.log) |
| Resolved question outcome card missing | Codex option retention reproduced red in Tier R; fixme/register 45. Custom, dismissal, and OpenCode variations remain unverified by this test. Evidence (evidence/question-retained-1068/red.log) |
| MCP group lacks tool names | Tier M red, fixme/register 53. Both expanded member rows pass; collapsed header omits names. Evidence (evidence/mcp-group-1091/result.json) |
| Collapsed work omits group count | Tier M red in test-user and local-unsigned, fixme/register 52. Manual fold retention passes; header lacks group count. Evidence (evidence/fold-count-1090/result.json) |
| Home misses the first transcript turn | Tier M red in both auth modes, fixme/register 56. Home stops at 12777px / 12778px; End reaches the last prompt. Endpoint screenshots reviewed; transient blanking remains uncovered. Evidence (evidence/home-end-1102/red.log) |
| Expanded output loses its state after scrolling away | Tier M red in both auth modes, fixme/register 57. Outer accordion remains expanded, but Show less returns to Show all after confirmed row unmount. Stage screenshots reviewed. Evidence (evidence/output-retention-1104/red.log) |
| Show all loses output reading position | Tier M red in both auth modes, fixme/register 58. Line 16 moves 5846px out of view; screenshots show the output end instead. Original nested-group variation remains uncovered. Evidence (evidence/output-reading-1110/red.log) |
| Image loading expands the user row | red observed; fixme registered in decisions entry 43 |
| Completed todo list disappears | red reproduced; fixme registered as decision 44; final completed SSE delivered before reload Evidence (evidence/todo-e2e-1067/red.log) |
| Browser Back skips rail visits | red observed; fixme registered in decisions entry 42 |
| Sending reorders only the sending tab | Codex reproduced red in Tier R; fixme/register 46. OpenCode variant remains unverified by this test. Evidence (evidence/rail-sync-1073/red.log) |

The image regression measures the same message moving upward **416px** while the composer stays fixed. Loading/corrupt placeholders measure about 646 × 37px, and the loaded image expands to 512 × 512px. Clicking it does not open the expected preview. The reference contract calls for approximately 80 × 80 logical-pixel tiles in every state.

The running-command regression is intermittent: two of three real Codex runs keep command opacity at zero beyond a one-second animation window. Reload recovers in that fixture. A separate unsigned run passes; that does not disprove the intermittent failure.

The long-output regression records all 240 lines in Codex's result to the scripted model endpoint, but only the final marker in stored/rendered presentation data. The exact internal producer fault is not yet established.

## Observed failures still without a qualified red regression

These are the original families still lacking a qualified red, rather than all remaining variations of already-reproduced families.

1. **Text chunks repeat within one turn during live streaming** — Live recheck 1136 shows the final list once after restoring the original session and after confirmed unmount/jump return. The completed-turn duplicate (state 17's stored-once/rendered-twice evidence) graduated via register 80: `a settled reply does not re-render its streamed text when the turn's deltas replay` fails in both auth modes (runs 1274/1275). Replayed `text-delta` frames drained while the canonical fetch is in flight mint a fresh part id and append a second copy after the announced `message.updated` un-settles the envelope. The live-streaming variant (chunks repeating while the stream is in progress) remains unverified.
2. **Child command reports denial without a visible decision** — Register 79: Tier R repro committed in `real-harness-local.spec.ts` (spawn_agent child + escalated exec_command). The Tier M control proves the UI decision path works — a child `permission.asked` mounts the dock on the parent's composer in both auth modes — so the defect is the runtime auto-denying without asking. The repro currently `test.skip`s on the upstream codex missing-rollout defect (parent turn dies before the spawn call; same deferred-persistence + config-churn class as the Pi session-file fix). Qualified red is gated on that upstream fix.
3. **Archive does not remove the active session** — The original disposable session archives successfully in live recheck 1170. Delayed-success, reload-persistence and rejection controls pass three times per auth mode (1177/1178). Narrowed 2026-09-14: the click-no-op symptom matches the defect `a2a5b11917` fixed — under `updatedAt` ordering, any session activity moved a row out from under an aiming pointer; the rail now sorts `human_turn_desc` so only a human turn can move a row. Residual silent no-op paths (`rowForNavigation` ref miss, `!sessionItem.directory` → no toast) are noted in the map but unproven. The historical failure still lacks a qualified red reproduction and is likely resolved upstream.

The interrupted-command family graduated from this list via register 77: `an interrupted Codex command stays interrupted when rail-return replay resends its start` fails in both auth modes (runs 1270/1271, zero retries). A reattached `/api/wr/runtime-events` stream replays the tool's `tool-start`/`tool-input`; a fresh client-presentation projection re-mints the stored part id (`000000_<callID>`) with `status: "running"`, and the live-event `upsertChatParts` path overwrites the stored error part unconditionally — no terminal ranking like the REST merge's `mergeChatPart`. The row renders Running with an advancing timer until a later canonical refetch reverts it. The replay's announced `message.updated` also drops `time.completed` (`preserveMessageFields` keeps only author and ranked error), un-settling the message and disabling the late-part guard. The earlier isolated running/error/reload control and live rechecks 1167/1169 never exercised a stale reattach replay, which is why they did not reproduce it. [Evidence](evidence/interrupted-replay-1270/result.json).

The Thinking-anchor family graduated via register 78: `Thinking stays with the new prompt while the previous turn's completion envelope is in flight` fails in both auth modes (runs 1272/1273, zero retries). With the previous turn's assistant envelope un-completed (parts painted, `time.completed` still in flight) and the session busy, `activeMessageID` resolves `pending()` to the stale assistant's parent — the previous user message — so the Thinking row paints inside the previous turn's block, beneath its completed-looking reply and above the newly sent prompt. 496 consecutive post-submit frames carried the previous turn's `data-message-id` in the unsigned run; the anchor relocates once the new turn's `message.updated`(pending) lands. The 1112–1114 normalized control seeded an already-completed assistant, so `pending()` never pointed backwards. [Evidence](evidence/thinking-anchor-1272/result.json).

Already-qualified families still need their uncovered variations: transcript movement on send/OpenCode/tools/child chips; completed-fold cold first visit/reload; file-selection child/browser panels; answered-question reload/OpenCode; approved-permission child-panel/repeated return; idle-return first frame and active-session positioning; and the limits listed for the earlier regressions above. Their outstanding variations do not make their existing qualified tests disappear or add another family.

## Additional findings and original reports

- A real Codex 630-second process exits, but the turn still shows Stop, Thinking, and a running tool after reload. No final reply appears before manual Stop at 16m 22s. This additional soak finding is recorded in the main inventory but is not a separate family in the 33-family map. Its own completion regression remains outstanding.
- Boot-logo flashing, sent-attachment leakage across sessions/restarts, New Terminal switching into chat, and selector appearance remain original acceptance requirements. Existing narrow diagnostic/control passes do not establish that all their reported variations are resolved.
- Answered-question return now reproduces red after a genuine session-to-session stream switch in both auth modes (1150/1151, entry 63). The missed-resolution plus retained-runtime variation is covered; reload and other harnesses remain open. Missing retained answer cards and replaying pending cards are different defects.
- The terminal-creation control passes its covered flow; this does not prove the original failure absent in every desktop lifecycle.
- Permission/model hydration bounds pass at tested widths, but late content appearance and the wide picker trigger now have failing regressions (54–55). Open-menu proportions remain uncovered.

## Fixes and verification

Rendering fixes: **zero in this QA-only pass**. The separately authorized Pi version alignment is committed; its remaining runtime failures are recorded in evidence/pi-upgrade-1220/upgrade.md. Test infrastructure was improved to measure effective ancestor opacity and geometry, retain image-response control through the shared mock, and verify actual reply visibility. These are QA improvements, not product fixes.

Historical full gate outcomes, runs 1218–1219 (before the separately recorded Pi upgrade and subsequent focused regressions):

| Command | Recorded result |
| --- | --- |
| `CLAXEDO_E2E_SERVE_MODE=build-preview bun run build:e2e` | **Pass**, exit 0. Log (evidence/gates-1218/build.log) |
| `bun run lint` | **0 warnings, 0 errors**, 5391 files, exit 0. Log (evidence/gates-1218/lint.log) |
| `bun run typecheck:e2e` | **2 errors**, exit 2, both external to QA: `SandboxSecretBrokering` assignment mismatches at supervisor/config-sync.ts:16 and supervisor/sandbox.ts:114. No QA type error is reported. Log (evidence/gates-1218/typecheck.log) |
| `bun run test:architecture-ratchets` | **Pass**, exit 0; five products, eight policies, 12599 helpers, no baseline change. Log (evidence/gates-1218/architecture-ratchets.log) |
| `bun run test:architecture` | **251 pass, 1 fail**, exit 1; moduleScopeMutableState 45 against baseline 44. Log (evidence/gates-1218/app-architecture.log) |
| `bun run test:e2e:real` | **29 pass, 52 fail, 8 skipped, 0 retries**, exit 1. This precedes the Pi pin correction. Log (evidence/gates-1218/real.log) |
| Requested three-spec core gate | **1 pass, 6 skipped, 0 retries**. The passing terminal case establishes focus only; the terminal shows Reconnecting. Review (evidence/gates-core-1219/review.json) |

Both gates are terminal. Their failures, skips and limited visual acceptance do not establish a release pass. Exact gate commands and recorded results (evidence/gates-1218/result.json). The 1205 root lint rerun previously passed with 0 warnings/errors after generated Trace Viewer JavaScript was preserved in ZIP archives; the initial 2192-error artifact scan remains superseded historical evidence (evidence/first-interaction-controls-1205/root-lint.log), not the current source-lint result.

Fresh prerequisite results are in [current status](current-status.md). The real Claude login and app send/reload now pass; the Pi version refusal is cleared. The earlier credential/version checks are superseded.

The fresh desktop package build and packaging invariants pass, but its ordinary Pi draft → UI first-send journey fails before Browser interaction. Separate native Pi/Codex first-send journeys reproduce missing-session-file/missing-rollout errors in both auth modes (1202/1203), with zero model requests, and are registered as 70–71. On 2026-09-14 the Pi missing-session-file root cause was fixed in the driver and the four journeys rerun: each now reaches a model request and fails on a real OpenAI 401 because brokered provider destinations are hardcoded to vendor origins, leaving the scripted endpoint unreachable ([findings](evidence/pi-session-fix/findings.md)). The Codex missing-rollout failure is unverified against this fix and remains open. The subsequent unsigned native Claude Browser return qualifies entry 76; native clipboard send/new-session/app-restart passes three times in runs 1223–1224. Other harness and signed-native variations remain open. The earlier pinned-Pi usage-fetch failure remains historical evidence, not the latest first-send outcome. Packaged desktop result (evidence/desktop-browser-1200/result.json), unsigned native evidence (evidence/native-first-send-1202/), test-user native evidence (evidence/native-first-send-1203/).

Next work: verify default-label and selector-transition behavior plus open-menu proportions; finish the missing red regressions and live variations; review outstanding videos; run final gates; audit ownership and commit explicit QA paths. The goal remains active.

[Full live inventory](index.md) · [Complete reproduction mapping](reproduction-map.md) · Image reference contract, drafted as image-thumbnail-contract.md and not committed with this record

Historical live-fold regression checks (1134/1135; gates superseded by the table above): one intended failure in each auth mode, zero retries; pre-toggle output and completed reply oracle pass. `bun run lint -- packages/claxedo-app/e2e/playwright/core-harness-rendering-matrix.spec.ts` passes with 0 warnings / 0 errors. `bun run typecheck:e2e` remains blocked by the single registry.ts:667 owner error outside QA. `bun run test:architecture-ratchets` passes: 5 products / 8 policies / 12572 helpers, no baseline changes. An initial package-directory `bunx oxlint` invocation rejected inherited configuration; the root-script lint command above is the valid result. Gate logs (evidence/live-fold-1135/).

Superseded commentary checks (1139/1140): 6 test failures, zero retries; withdrawn as product evidence below. Focused root lint passes (0 warnings / 0 errors); E2E typecheck reports only the existing registry.ts:667 owner error; root architecture ratchets pass without baseline changes. Exact test commands and results (evidence/commentary-clipped-1140/result.json), gate logs (evidence/commentary-clipped-1140/). No green duplicate-reply coverage or application fix is claimed.

Correction to the earlier commentary checks: register 62 is withdrawn. Live Codex probe 1141 recovers opening commentary with ordinary upward scrolling. The prior test relied on scrollIntoViewIfNeeded under a sticky title, and its zero-scroll diagnostic read the outer wrapper. The corrected restoration control uses the shared wheel-to-top interaction and measures the actual scrollable viewport. Runs 1139/1140 remain superseded evidence, not product-failure counts. Live recovery (evidence/live-commentary-1141/scrolled-top.png).

The corrected restoration control passes 3/3 in each auth mode (1142/1143), zero retries. All 48 commentary oracle screenshots and six follow-up oracle frames were reviewed. It proves ordinary scroll recovery and one occurrence per stored passage after restoration/reload; it does not close the historical streaming duplicate. Focused lint: 0 warnings / 0 errors. Typecheck: existing registry.ts:667 error only. Root ratchets: pass, no baseline changes. Commands, results, and limits (evidence/restored-commentary-1143/result.json).


## Latest archive verification

The original disposable OpenCode archive now succeeds after a 6.4-second request. The strengthened existing E2E scenario passes three times per auth mode, zero retries, with all 30 reply screenshots reviewed; it covers pending transcript retention, successful navigation, reload persistence, and rejected archive retention. The adjacent only-session control also passes three times per mode. This does not establish the cause of the historical failure or continuous frame stability. Counts above are unchanged. Final logs and current gate blockers are recorded in index.md under 1177–1180.


## File-selection regression

State 100 now reproduces in both auth modes (1186/1187): returning to the first session leaves the second session's file selected. Registered as fixme 66. All six reply-oracle images and six file-selection images are reviewed. Child/browser selection variants remain uncovered, and failed live previews are not successful content-loading evidence. No production fix is included.


## Idle-return position regression

State 93 now has a settled-position regression in core-timeline-rendering-scroll, fixme 67. Both auth modes return to 12492px instead of the required top; manual recovery subsequently proves the first reply visible. Six oracle screenshots and four before/return screenshots are reviewed. First-frame paint and live-session bottom behavior are outside this test. Root architecture ratchets pass; the full goal remains incomplete.


## Reviewed first-interaction controls: historical runs 1204/1205

These two build-preview runs passed **18 interaction journeys plus 6 boot diagnostics**, with **0 failures and 0 retries**: image-only paste, late FileReader completion after send, and New Terminal each passed three times per auth mode; the separate boot diagnostic also passed three times per mode. Review covered all **36 reply-oracle observations across 23 distinct PNGs**, 251 sampled frames across the 12 boot/terminal videos, and all 766 attached boot DOM samples. Video review used every fifth frame at 25 fps plus the final frame; intervening frames were not reviewed. Unsigned reviewed evidence (evidence/first-interaction-controls-1204/result.json), test-user reviewed evidence (evidence/first-interaction-controls-1205/result.json).

The attachment journeys use synthetic DOM paste and a controlled FileReader completion order. They assert an empty attachment list after send, browser reload, and opening another unsent draft; they do not exercise the OS clipboard or a dev-server/native app restart. New Terminal retains the launcher route through reload and ends on “Start a terminal”; it does not start a PTY, and some launcher cards are still appearing at the recording end. Signed pre-reload reply snapshots show “Reconnecting…” above the empty composer, so these passes do not establish connection health.

The boot diagnostics hold the app-shell asset and check the designated splash's sampled opacity, animation, and single logo → shell transition. The shell has a separate faint draft logo outside that counter. Those six passes establish the historical diagnostic scope only: there is no prompt send in that boot test, no full native boot proof, and no result claimed for the later boot-test upgrade. These controls did not change the then-current **29 reproduced issue families / 30 qualified failing tests** or establish application fixes. The later OpenCode regressions brought the totals to 31 / 32; boot handoff entry 74 subsequently brought them to 32 / 33, before steering entry 75 raised current totals to 33 / 34.

## OpenCode restored tool and file-image regressions: 1208–1210

Two original families now have qualified real-server browser regressions, entries 72–73, bringing coverage at that checkpoint to **31 reproduced issue families / 32 failing tests**, with **26 of 33 original families qualified and 7 without a red regression**. The shell case fails once per auth mode because its executed command and stdout are missing from the restored tool; the local-file image case fails once per mode because only alt text renders. Both completed assistant replies are visible on open and reload before each target assertion fails. The author reviewed all eight reply-oracle images and four target images; the primary agent independently reviewed the unsigned expanded tool and test-user image. Full recordings remain unreviewed. 1208 (evidence/api-tool-1208/result.json), 1209 (evidence/api-rendering-1209/result.json), 1210 (evidence/api-image-1210/result.json).

These sessions and prompts start through the real local-server API, then use browser restoration/reload. They do not show recovery of the separate browser first-send failure. Shell coverage does not qualify read/edit/MCP/permission or running-command variants. Image coverage is restored OpenCode web rendering only; the missing image prevents reaching the 80px tile and full-preview checks, while desktop and other harnesses remain open. The original 487 live verdicts are preserved; these are later regression links, not rewritten historical outcomes. No application fix is included.

## Boot handoff regression: 1216/1217

The existing boot journey now reproduces a visible splash-to-composer gap in both auth modes, register 74. Each run is **0 pass / 1 fail / 0 retries**: three unsigned and six test-user DOM samples have neither splash nor visible composer. The motion review covers all 291 encoded frames across both recordings; splash at 0.720s is followed by missing main content at 0.760s, then composer at 0.800s. The signed sidebar remains painted, so this is not a whole-window blank. Full-frame contact sheets were reviewed throughout, with selected boundary frames additionally inspected at native resolution. Video cadence cannot establish the exact duration or exclude shorter between-frame defects. Unsigned (evidence/boot-handoff-1216/result.json), test-user (evidence/boot-handoff-1217/result.json).

Both journeys then perform one first UI session creation and send, receive the visible completed reply, and record no unhandled mock API requests. Signed “Reconnecting…” persists after turn settlement; connection recovery remains unverified. The earlier 1211/1212 greens used a shell-container readiness check and do not qualify continuity. This browser/shared-mock case does not establish native desktop or real-provider boot reliability.

Boot is an original user report additional to the numbered map: at that checkpoint totals were **32 reproduced issue families / 33 failing tests**, while **26 of 33 numbered families were qualified and 7 lacked a red**. Steering was still a candidate at that checkpoint and is qualified below. No application fix or historical verdict change is included.

## Intermittent steering reply regression: 1213–1215

The real API-started OpenCode steering test is now fixme/register 75: unsigned runs pass **3/3** (1213/1214), while test-user records **2 failures and 1 pass** (1215), all with zero retries. Both failures wait 20s for the completed-reply oracle and show only the two user prompts despite canonical completed assistant text containing FINAL. Browser latest-surface/latest-turn responses instead contain only the steering user. The initial empty-history response in failed repeat 1 completed approximately 47ms before the API prompt, ruling out an unfinished initial history request as the sole explanation. Actual commands/results (evidence/steering-reply-1215/result.json), 24-request comparison and limits (evidence/steering-reply-1215/analysis/network-ordering.json).

All ten outcome images were reviewed by the primary agent: eight passing reply-oracle observations and two failure screenshots. Full videos remain unreviewed. Failed runs never reach reload; API-started steering does not qualify normal browser first-send or native restart. Stream contents and passing-repeat trace timing are unavailable, so no event-loss or exact internal-cause claim is made. The proposed exact-session initial-history guard remains unimplemented. The existing test body is unchanged except its fixme marker.

Current coverage is **33 reproduced issue families / 34 failing tests**, with **27 of 33 original numbered families qualified and 6 without a red**. Boot remains additional to the numbered map. Missing-reply restart, mature-session and other steering variants remain open; historical verdicts are preserved and no application fix is included.

### Actual native image send and app restart: 1223–1224

The new existing-home desktop case passes **3/3, zero retries** using the OS clipboard and real Claude binary against the guarded scripted endpoint. The first submitted prompt carries one image; the next session and a new session after app-only restart carry zero. The original image reply is restored from the retained profile/daemon. Clipboard restoration is acknowledged in all three runs. All 12 reply-oracle observations (eight unique images) were visually reviewed, plus the first attached-image screenshot. Full recordings remain unreviewed; continuous geometry is not claimed. First run (evidence/native-image-1223/result.json), two repeats (evidence/native-image-1224/result.json).

This closes the missing native-paste/app-restart coverage variation for unsigned Claude only. It does not close the historical attachment leak, test the separate development-server restart, or qualify compact-image/preview geometry. Signed-native and other harness variations remain pending. Reproduced-family and failing-case counts remain 33/34.

### Browser session return: qualified regression 76

Run 1225 reaches the intended native Browser round trip: both UI-created Claude sessions reply, and the initial Browser address plus real Electron guest URL/body match the fixture HTTP page. After session A → B → A, the Browser tab survives but the address becomes empty and its guest returns `about:blank`/empty body. The five reply-oracle observations and before/return page screenshots were visually reviewed. The existing desktop test is now marked fixme and registered as 76. Exact command and result (evidence/browser-return-1225/result.json). This qualifies the unsigned Claude variation; original Codex, signed-native, and the earlier immediate-navigation lead remain unverified.

Current totals: **34 reproduced issue families / 35 failing test cases**; **28 of 33 original families qualified / 5 without a reliable red reproduction**. No Browser production fix was made.

## HTTP 404 and image-load sampling — 1228–1229

Runs 1228/1229 extend the existing image regression to an observed HTTP 404 in unsigned and test-user modes (each 0 pass, 1 fail, 0 retries). Loading, corrupt and missing-image boxes are 645.75 × 37.1875px; the valid image becomes 512 × 512px. Thirty post-decode frame samples catch 449px and 474px upward movement respectively, with unchanged composer geometry. Both reply-oracle images and before/after images were reviewed in each mode. Clicking the loaded image still fails to open the preview; closing it remains unreachable. The earlier single-frame run 1227 missed the later scroll correction, which its screenshot exposed; 1228/1229 supersede that measurement method. Full video review remains open. This extends entry 43 without adding a finding or test case. Unsigned evidence (evidence/image-404-1228/result.json), test-user evidence (evidence/image-404-1229/result.json).

Focused lint passes with 0 warnings / 0 errors. `bun run typecheck:e2e` exits 2 with the same two external supervisor `SandboxSecretBrokering` errors. `bun run test:architecture-ratchets` passes: 5 products, 8 policies, 12607 helpers; no baseline change. Counts remain 34 reproduced families / 35 failing cases.

Native evidence helpers are committed as `243e5a3aea test(e2e): support preserved native clipboard and desktop video` (three explicit paths; no push). This contains the clipboard helper and Electron video option only; the Browser and clipboard scenario tests remain pending commit. Root `bun run lint` passes with 0 warnings / 0 errors across 5396 files (log (evidence/image-404-1228/root-lint.log)).

## Child-permission live prerequisite — 1230

The state 75 child-denial recheck did not reach child execution. The documented server recipe initially restored the app, then its listener on 2593 disappeared while the process retained an SSE connection. The UI showed server/model/session-loading failures. Closing the QA tab allowed the server process to exit. No child approval or filesystem write was attempted; the shutdown cause remains unknown. Code inspection identifies immediate denial for an approval without an active-thread entry as a hypothesis, not a qualified reproduction. Observed state and limits (evidence/child-permission-1230/result.json).

Committed `5740bfb2b6 test(e2e): script exact Markdown replies through the model endpoint` (two explicit helper/test paths; no push). `bun test e2e/helpers/scripted-model-server.test.ts` passes three times with 2 pass / 0 fail each; focused lint passes with 0 warnings / 0 errors. This supports real-harness Markdown fixtures and does not qualify the blocked child-permission flow.

Cold-history first-wheel control now passes three times per auth mode with zero retries (1232/1233). The same final paragraph stays mounted across 45 samples and moves at most 1px after a 1px wheel input. All six reply-oracle observations and all 296 encoded video frames were reviewed. This qualifies only seeded cold-history wheel geometry. The initial 1231 selector ambiguity is excluded from defect counts. Unsigned result (evidence/cold-wheel-1232/result.json), test-user result (evidence/cold-wheel-1233/result.json).

QA control qualification (1235–1237): short follow-up geometry, same-tab answered-question return, and restored interrupted-command state each pass three times per auth mode, zero retries. All 36 reply-oracle observations across the 18 cases were reviewed; sampled movement frames were reviewed for the short follow-up and question-return cases. The short follow-up has zero measured row/composer movement across 180 samples and an intercepted central WebSocket. The initial 1234 HTTP-SSE guard failure was a test defect. Test-user Reconnecting remains visible, so overall transport health is not claimed. These controls do not close live replay, heavy-session or original streaming variants. Unsigned geometry (evidence/short-geometry-1235/result.json), test-user controls (evidence/qa-controls-1236/result.json), unsigned navigation controls (evidence/qa-controls-1237/result.json).

Fresh commit checks (1238): root lint 0 warnings / 0 errors; root architecture ratchets pass for five products/eight policies/12607 helpers without baseline changes. E2E typecheck still has the two external supervisor proxy-enum errors; app architecture has 251 pass / 1 fail (mutable state45 versus44). Initial root-ratchet attempts exceeded git-list stdout capacity; verified ZIP preservation of derived video frames restores the passing run. Exact commands and outcomes (evidence/qa-commit-1238/gates.json).

Committed `576e68805d test(e2e): preserve reproduced session-rendering regressions`: 19 explicit QA paths, no production rendering changes and no push. The commit retains the qualified 35 failing cases with decision entries and the reviewed green controls. Remaining test changes and the full live acceptance scope are not complete. Exact changed files (evidence/qa-commit-1238/commit.json).

User clarification: the duplicate-reply report concerns repeated chunks of text within one turn while streaming is in progress. It does not require a duplicate whole reply or persistence after completion. The existing restoration control does not cover that failure. State17 remains without a qualified live-streaming red regression; capture incoming text updates and per-frame rendered text together, then preserve the actual failing sequence. Counts remain unchanged.

Additional requested lifecycle coverage: a running tool followed by disconnection or another failure can remain displayed as Running indefinitely. New Tier R probe `Claude tool completion reconciles after the browser reconnects and reloads` runs a real held command, requires the existing browser event connection to close, finishes the command while offline, checks canonical server completion, then checks the reply/tool after reconnect and reload. The browser-disconnection variant passes three times per auth mode, zero retries (unsigned (evidence/tool-disconnect-1246/result.json), Test User (evidence/tool-disconnect-1247/result.json)). All twelve reply-oracle screenshots and 114 video samples were visually reviewed. This is a green lifecycle control, not reproduction or closure of the historical indefinitely-Running report. Server/runtime crashes, permanent disconnection, other harnesses and the original long-soak failure remain separate variants.

Committed browser-disconnection coverage: `dfed52f19a test(e2e): verify tool completion across browser disconnection` (three explicit QA paths). Gate results (evidence/tool-disconnect-1247/gates.json). No application fix is included.

Native streaming coverage now records actual runtime SSE text alongside animation-frame observations. Claude first and follow-up replies pass three times per auth mode (12 cases, zero retries): Test User (evidence/streamed-segments-1255/result.json), unsigned (evidence/streamed-segments-1256/result.json). All 18 reply-oracle images and 282 video samples were reviewed. Across 8,182 animation-frame observations, the tests record 29–30 distinct partial replies per case, reject duplicated segments in any sampled frame, and require all 16 segments once at completion. The original intermittent duplicate-chunk finding stays open; subagents, reconnect-during-stream, and heavy tool turns are not covered. The Codex probe is blocked before streaming by the already-registered `no rollout found` first-send error.

The model fixture previously ignored pacing for native Messages and Responses; two red HTTP tests reproduced that fixture defect, and all four fixture tests now pass. An initial paint observer wrongly excluded active rows carrying `aria-hidden`; screenshot review showed visibly streaming text, so the missing-partial-reply claim is withdrawn. These setup failures add no product findings. Fixture evidence (evidence/paced-text-1248/result.json).

Committed native streaming coverage: `0dbad3a21e test(e2e): observe native text chunks during live rendering` (four explicit QA paths). Gate results (evidence/paced-text-1248/gates.json). Full real-harness gate rerun 1257 finished: **34 passed / 50 failed / 8 skipped**, zero retries, 40.5 minutes. The two native streaming cases and browser-disconnection case passed. Terminal result and limits (evidence/full-real-1257/result.json). This is not an overall acceptance pass; failure triage and full visual review remain incomplete.

### Mention keyboard diagnostic: 1258–1260

The unresolved composer-controls-1239 flake reproduces again without intermediate waits: 1259 has **9 pass / 1 fail**, zero retries, unsigned. The failing screenshot again shows an open mention popup and the literal `@` instead of an inserted reviewer pill. A passive event recorder confirms ArrowDown, ArrowUp and Enter target the focused editor; this rules out focus loss in that run. The harness-specific agent-catalog response arrives after the key sequence, but causation is not established. Failure and key evidence (evidence/mention-diagnostic-1259/), analysis (evidence/composer-controls-1239/diagnosis.json).

Adding focus/selection waits between keys produced 3 passes in 1258; those waits were removed because they can mask the race. A later diagnostic with post-dispatch observation and the strengthened sent-mention reply oracle has **20 pass / 0 fail** (ten runs of each scenario), zero retries. These later passes do not erase the earlier failure. All ten sent-mention reply screenshots are byte-identical and their unique image was reviewed: the highlighted user mention and completed reply are visible. Videos and the fresh-build/auth twin remain unqualified. Both diagnostics reuse the unsigned static frontend with isolated mock pages and separate output directories; they do not modify or restart the full real gate's server. 1260 results (evidence/mention-diagnostic-1260/result.json).

No new qualified finding is added to the 34-family / 35-case count. The keyboard test remains under investigation and uncommitted. The sent-mention test now requires the shared completed-reply oracle in addition to the existing user-highlight assertion. Root lint passes with 0 warnings / 0 errors; e2e typecheck still has only the two external supervisor brokering errors. Lint (evidence/mention-diagnostic-1259/lint.log), typecheck (evidence/mention-diagnostic-1259/typecheck.log).

### Required core gate: 1262

The three requested core spec files produce **1 pass / 0 fail / 6 skipped**, zero retries, against the current unsigned build with independent outputs. Both reply screenshots and the terminal-launcher screenshot were reviewed. The passing scenario opens New Terminal and reloads its launcher, but does not click Shell or create a PTY. It therefore proves launcher selection only; actual terminal creation and command-output focus retention remain an acceptance gap. The skipped regressions remain failures awaiting separate product fixes. Exact command and scope (evidence/core-gate-1262/result.json), log (evidence/core-gate-1262/run.log).

The mention-catalog delay experiment 1261 passes three times, but the initial visible-menu guard may wait through hydration. It does not explain or close the original intermittent key failure. The temporary delay and request guard were removed; its source is archived as diagnostic evidence. Result (evidence/mention-hydration-1261/result.json).


## Real Shell and busy-draft qualification (1263–1264)

One new green rule test in `real-harness-local.spec.ts` creates a real PTY from a completed Claude chat, executes a split-marker command, reloads with retained output, returns to chat through the shared reply oracle, and returns to the same terminal to execute another command. The older core launcher-only control does not prove this mutation. The existing native busy-draft case now checks canonical `/session/status` before and after reload and expects Send for a nonempty draft, matching `submit-control.tsx`; its retained text, no-unwanted-send, exact payload, reply, and persistence assertions remain.

Unsigned 1263 and Test User 1264 each pass **6/6**, two cases repeated three times, zero retries. All **30 reply oracle screenshots, 18 terminal screenshots, and 222 video samples at two frames/second** were reviewed. Replies and shell output are visibly rendered; terminal output survives reload and chat return. This does not qualify every encoded frame, every terminal launch mode, or the Codex busy-draft case, which fails before its draft assertions in full gate 1257. Unsigned command/result (evidence/terminal-busy-1263/result.json); Test User command/result (evidence/terminal-busy-1264/result.json).

Gates: `bun run lint` passes, 0 warnings / 0 errors across 5403 files. `bun run test:architecture-ratchets` passes, 13 tests / 5 products / 8 policies / 12616 helpers, no baseline changes. Initial ratchet invocations hit the scanner's 1 MiB `git ls-files` buffer because of expanded QA artifacts; byte-verified ZIP archival of those artifacts resolved the environment failure, without modifying the scanner. `bun run typecheck:e2e` fails on the two existing production `secretBrokering: proxy` type errors in `workspace/supervisor/config-sync.ts:16` and `sandbox.ts:114`; no QA errors. `bun run test:architecture` is **251 pass / 1 fail**, the existing `moduleScopeMutableState is exactly pinned` debt ratchet. These failures remain open; no ceiling was raised. Logs (evidence/terminal-busy-1263/).

Committed these two QA paths as `35ad1a76f6 test(e2e): verify real shell navigation and busy draft state`.


## Tool interruption after server restart (1265–1269)

Added one green Tier R regression beside the browser-disconnection case in `real-harness-local.spec.ts`: `Claude running tool becomes interrupted without reload after server restart and a follow-up can complete`. It runs a real held Bash command, verifies Running in both canonical state and UI, restarts the owned local server, requires the same open page to show a tool error instead of Running, sends a successful follow-up through the shared reply oracle, and reloads to verify the interrupted tool stays terminal. No route or runtime event is fabricated. The tool script's start file proves it was executing before restart; the final canonical error is `Tool execution interrupted`.

Final qualification: unsigned 1267 (1 pass) plus 1268 (2 pass), Test User 1269 (3 pass), **6 passes total, zero failures/skips/retries**. All **12 reply oracle screenshots, 12 tool-state screenshots, and 287 video samples at two frames/second** were reviewed. Before restart the row says Running; afterward it shows Shell Failed in the existing page, and the follow-up reply is readable before and after reload. Initial 1265–1266 passed only after an immediate page reload; they are retained as diagnostic controls and excluded from final qualification. Unsigned command/results (evidence/tool-restart-1267/result.json), remaining unsigned repetitions (evidence/tool-restart-1268/result.json), Test User command/results (evidence/tool-restart-1269/result.json).

This complements the committed browser-outage regression `dfed52f19a`: tool completion while disconnected, then automatic reconciliation and reload. Neither case reproduces the historical indefinite-running failure. Independent native-runtime crashes, abrupt SIGKILL, permanent disconnection, other harnesses, and the recorded 630-second Codex process remain unverified variants. Finding counts are now **36 reproduced families / 37 qualified red regression cases** (register 77 added the interrupted-command replay red; register 78 added the Thinking-anchor red).

For the restart slice, `bun run lint` passes with 0 warnings / 0 errors across 5403 files, and `bun run test:architecture-ratchets` passes (13 tests, 5 products, 8 policies, 12616 helpers). `bun run typecheck:e2e` still fails only on the two external credential-broker `proxy` type errors listed above. The app architecture gate remains 251 pass / 1 existing debt-ratchet failure from 1263; this test adds no application import. Full real gate 1257 predates this additional test and remains 34/50/8; the six focused real runs qualify the new case separately. Latest slice logs (evidence/tool-restart-1267/).

Committed the restart regression as `edc362ab08 test(e2e): verify running tool interruption across server restart`.
