# Session rendering verification

> [Current test results](current-status.md) · [Completed work and next steps](WHATS-DONE-AND-NEXT.md).

Most of the evidence this record cites — recordings, screenshots, timing files and logs — was never committed. Those citations are written as plain paths; the few files that did land are under `evidence/` and stay linked.

[Latest actual rerun](current-status.md): Claude's real login and real-provider app journey work; Pi's version check passes, while its native app first-send failure is being tracked once as a startup defect. The full rendering acceptance scope remains incomplete.

The table below retains **165 observed outcomes: 77 passes and 88 failures** from the earlier QA runs. Unexecuted checks are not counted as blockers or passes. Repeated prerequisite rows and the reconciliation matrix have been removed. Use the [QA brief](../../../qa/2026-09-12-session-rendering-qa-brief.md) for untested scenarios and the failure register for actual defects.

The test coverage map, drafted as test-coverage-map.md and not committed with this record, records inspected existing test homes and the missing assertions for the principal rendering reports. Thirty-one numbered-inventory issue families now have reproduced regressions (register entries 42–53, 56–61, 63–67, 69, 72–73 and 75–80); two mapped families still lack qualified red regressions. Entry 62 is withdrawn. Including the two selector families, OpenCode default-agent failure (entry 68), Pi/Codex first-send failures (entries 70–71), and the boot handoff failure (entry 74), the total is 37 reproduced families and 38 failing test cases. The two additional selector reports are registered separately as 54–55. Harness and lifecycle variants remain incomplete. See the [current progress report](progress-report.md), including the latest permission-selector and harness-width feedback.

## Recorded findings

Additional long-run completion finding: Codex's 630-second Python process exits, but the app continues to show Stop, Thinking and a running tool after reload, with no QA_CODEX_SOAK_DONE reply. The turn is still busy when stopped manually at 16m 22s. Its stored progress ends at line 067. Before reload (evidence/heavy-soak/codex-process-exited-still-busy.png), reload recording (evidence/codex-soak-finished-reload/capture.webm), after reload (evidence/heavy-soak/codex-after-reload.png), stored progress (evidence/heavy-soak/codex-stored-progress.json), Stop recovery (evidence/codex-soak-stop-recovery/capture.webm), successful fresh turn (evidence/heavy-soak/recovered.png). The command process and user-visible turn disagree; the underlying runtime cause is not established. This needs its own completion regression in Phase 2.

The same reload additionally reproduces the state 17 duplicate-text finding: several progress paragraphs, including the line 067 paragraph, appear twice in the rendered transcript but once in the stored text parts. It shares the duplicate-row regression rather than becoming a separate duplicate scenario.

## Recorded results

| State | Harness | Surface | Observed verdict | Evidence | Observation |
| --- | --- | --- | --- | --- | --- |
| 2 | Codex | Local web, fixed build | pass | Mid-turn capture (evidence/codex-steer/capture.webm), completed original command and reply (evidence/codex-steer/completed.png) | A follow-up sent during `sleep 35` is incorporated as `QA_INITIAL_DONE BLUE`; the original command finishes and the composer returns to ready with one visible addition and no aborted-turn indication. |
| 2 | OpenCode | Local web, fixed build | fail | Missing reply after reload (evidence/opencode-steer/missing-after-reload.png), stored text (evidence/opencode-steer/stored-visible-text.json), API-started steering regression 75 (evidence/steering-reply-1215/analysis/network-ordering.json) | The foreground steering variation completes in stored session messages with `QA_FOREGROUND_DONE ORANGE`, but the page shows only the two user prompts and an idle composer, still omitting the response after reload and switching back. |
| 3 | Codex | Local web, fixed build | pass | Completed ordered markers (evidence/codex-order/completed.png), three sends (evidence/codex-order-burst/capture.webm) | Three additions sent during the same busy turn appear once each and produce `QUEUE_ONE`, `QUEUE_TWO`, `QUEUE_THREE` in that order after `sleep 45`. |
| 3 | OpenCode | Local web, fixed build | fail | Idle page without reply (evidence/opencode-order/idle-missing-reply.png), stored ordered reply (evidence/opencode-order/stored-visible-text.json) | The stored reply contains OC_ONE, OC_TWO and OC_THREE in order, but the idle page omits that reply entirely, reproducing the missing-response finding from state 2. |
| 4 | Codex | Local desktop, dev recipe | pass | Before quit (evidence/desktop-restart-992/before-restart.png), After relaunch (evidence/desktop-restart-992/after-restart.png), Completion recording (evidence/desktop-restart-complete-992/capture.webm), Visible completed reply (evidence/desktop-restart-992/visible-completed.jpg), Stored reply (evidence/desktop-restart-992/replay.json), Lifecycle observations (evidence/desktop-restart-992/observations.json) | Sent QA_RESTART_ADDITION_992 during a 90-second shell call, quit the desktop, and relaunched with bun run dev. The original process survived, the addition remained visible, and the reply displayed both markers without another send. All 255 completion frames were reviewed. Native AX snapshots stayed stale after visible completion; they are not evidence of a stuck app. This covers app restart with the existing daemon, not server restart. |
| 4 | OpenCode | Local desktop, dev recipe | fail | Before quit (evidence/desktop-restart-993/before-restart.png), After relaunch (evidence/desktop-restart-993/after-restart.png), Recording (evidence/desktop-restart-complete-993/capture.webm), Idle without reply (evidence/desktop-restart-993/visible-idle-no-reply.jpg), Return (evidence/desktop-restart-993/returned.png), Stored reply (evidence/desktop-restart-993/replay.json), Lifecycle observations (evidence/desktop-restart-993/observations.json) | The original shell survives desktop quit and relaunch, and the stored completed reply includes QA_OC_RESTART_ADDITION_993. Both prompts remain visible, but the idle desktop omits the reply, including after switching away and back. This shares the missing-response finding in state 2; the follow-up is not lost in the runtime. App restart was tested with the existing daemon. |
| 5 | Codex | Local web, fixed build | fail | Send recording (evidence/codex-order-start/capture.webm), Thinking beneath completed reply (evidence/codex-thinking-placement/previous-turn.jpg), Thinking beneath new turn (evidence/codex-thinking-placement/current-turn.jpg) | At 0.57–1.05 seconds after send, Thinking appears beneath the previous completed `QA_INITIAL_DONE BLUE` reply, then disappears there and appears beneath the new prompt by 1.81 seconds. The normalized follow-up ownership control passes three times in each auth mode with no wrong owner sampled (runs 1112–1114). Busy/final frames reviewed. This does not reproduce the earlier real-producer timing and does not change this live fail verdict. Control evidence (evidence/thinking-owner-1114/result.json). The defect is now qualified in Tier M (register 78, both auth modes, runs 1272/1273): with the previous turn's assistant envelope un-completed and the session busy, a follow-up send leaves `pending()` anchored to the stale assistant's parent, so the Thinking row paints inside the previous turn's block — beneath its reply and above the new prompt — for 496 sampled frames until the new assistant envelope relocates it. [E2E evidence](evidence/thinking-anchor-1272/result.json). |
| 5 | OpenCode | Local web, fixed build | fail | Continuous send and reply recording (evidence/opencode-shimmer-continuous-991/capture.webm), Thinking removed (evidence/opencode-shimmer-991/thinking-removed.jpg), reply arrival (evidence/opencode-shimmer-991/reply-arrival.jpg), timing (evidence/opencode-shimmer-991/timing.json) | Thinking disappears at 10.7261s while no reply is visible; QA_OC_SHIMMER_DONE_991 first appears at 11.2879s. The indicator therefore does not stay until the reply begins. All 380 captured frames were reviewed; no reappearance is claimed for this fixture. Fresh real follow-up 1118 reproduces the gap: Thinking absent at sampled 6365ms, reply first present at 6873ms (about 508ms). Transition frames 35–39 reviewed. Capture (evidence/live-continuity-1118/capture.webm), samples (evidence/live-continuity-1118/samples.json). Follow-up 1119 captures a second 615ms sampled gap and Idle arriving 691ms before the completed REST response. Captured ordering (live-ordering-1119/result.json). The shared mock now serves the central WebSocket from its EventBus. Earlier unsigned passes missed the lifecycle transport and are invalid as continuity coverage. Final shared-transport checks 1128/1129 reproduce the continuity failure three times per auth mode (716–733ms), while both normal-turn controls pass three times per mode: 6 intended failures, 12 passes, zero retries. Every run has reviewed transition or busy/final frames. The regression also asserts that the central WebSocket was intercepted, preventing the earlier missing-transport false pass. Full recording review remains outstanding. Fixme/register 59. Expected no absent Thinking samples before the reply; run 1121 receives 59 (approximately 722ms). Red assertion (evidence/thinking-gap-1121/red.log). |
| 6 | Codex | Local web, fixed build | fail | Same recorded transition (evidence/codex-order-start/capture.webm), before (evidence/codex-thinking-placement/previous-turn.jpg), after (evidence/codex-thinking-placement/current-turn.jpg) | Existing reply and prompt rows shift upward as Thinking relocates, while the composer remains visibly mounted in the same position; no scroll input occurs during this capture. |
| 6 | OpenCode | Local web, fixed build | fail | Send recording (evidence/opencode-live-fold-start-986/capture.webm), before (evidence/opencode-send-shift/before.jpg), after (evidence/opencode-send-shift/after.jpg), timing (evidence/opencode-send-shift/timing.json) | Sending fixture 986 moves the existing QA_OC_FOLD_DONE_982 reply upward approximately 153px as the new prompt is inserted; the composer also shrinks after clearing its draft. No scroll input occurs. States 6 and 59 share this send-shift reproduction and one geometry regression; it does not establish the stationary-composer variation on OpenCode. |
| 7 | Codex | Local web, fixed build | pass | Stop capture (evidence/codex-stop-action/capture.webm), usable composer and new reply (evidence/codex-stop/new-turn-completed.png) | Clicking Stop during the foreground command, followed by Escape, settles to Interrupted and You stopped; a subsequent prompt completes normally. |
| 7 | OpenCode | Local web, fixed build | fail | Stop capture (evidence/opencode-stop-action/capture.webm), idle without interruption explanation (evidence/opencode-stop/idle-no-interruption.png) | Stop clears Thinking and restores the composer, but the stopped turn has no visible interrupted row or explanation. Register 60: live OpenCode probes 1130/1131 show no interruption explanation after Stop, while reload reveals it. Probe 1131 confirms a cancelled abort response and canonical lastTurn metadata. Tier M runs 1132/1133 fail the pre-reload visibility assertion after 15 seconds in both auth modes; ready composer and post-reload divider pass. Screenshots reviewed. Tool-fold, repeated-Stop, and later-turn-retention variations remain uncovered. Live evidence (evidence/live-stop-1131/result.json), red assertion (evidence/stop-outcome-1132/run.log). |
| 8 | Codex | Local web, fixed build | pass | Immediate button Stop (evidence/codex-stop-race-retry/capture.webm), new send (evidence/codex-stop-new-send/capture.webm), completed (evidence/codex-stop/scoped-stop-completed.png) | Stop clicked immediately after send produces You stopped after 0s; the following prompt completes as QA_AFTER_STOP_TWO rather than being aborted. |
| 8 | OpenCode | Local web, fixed build | pass | Send, Stop, new send (evidence/opencode-stop-race/capture.webm), completed (evidence/opencode-stop/new-send-completed.png) | Stop clicked immediately after send returns the composer to ready, and the new prompt completes as QA_OC_AFTER_STOP. |
| 10 | Codex | Local web, fixed build | pass | Other session (evidence/codex-attachments/other-session-empty.png), new session (evidence/codex-attachments/new-session-empty.png), restored (evidence/codex-attachments/restored-draft-image.png), switch recording (evidence/draft-image-isolation/capture.webm) | An unsent pasted image remains attached only to Codex; OpenCode and a new-session composer are empty, and returning restores the original image. |
| 10 | OpenCode | Local web, fixed build | pass | Restored image (evidence/opencode-attachments/restored-draft-image.png), both sessions (evidence/draft-return-both/capture.webm) | A separately pasted OpenCode image remains in that composer after switching to Codex and back. |
| 10 | Claude | Local web, fixed build, unsent image | pass | Navigation recording (evidence/claude-attachment-isolation-1040/capture.webm), returned image (evidence/attachment-isolation-1040/claude-return.png), observations (evidence/attachment-isolation-1040/observations.json), frame review (evidence/attachment-isolation-1040/review.jpg) | A pasted PNG remains attached to its own session after visiting the other harness session and a new draft. Neither destination shows the image, and returning shows exactly one thumbnail. The QA attachment is removed afterward. All captured frames reviewed (23 Claude, 14 Cursor); this is unsent isolation, not successful delivery to the unavailable harness. |
| 10 | Cursor | Local web, fixed build, unsent image | pass | Navigation recording (evidence/cursor-attachment-isolation-1040/capture.webm), returned image (evidence/attachment-isolation-1040/cursor-return.png), observations (evidence/attachment-isolation-1040/observations.json), frame review (evidence/attachment-isolation-1040/review.jpg) | A pasted PNG remains attached to its own session after visiting the other harness session and a new draft. Neither destination shows the image, and returning shows exactly one thumbnail. The QA attachment is removed afterward. All captured frames reviewed (23 Claude, 14 Cursor); this is unsent isolation, not successful delivery to the unavailable harness. |
| 11 | Codex | Local web, fixed build | pass | Restored draft (evidence/codex-attachments/restored-draft-image.png), both sessions (evidence/draft-return-both/capture.webm) | QA_UNSENT_CODEX_DRAFT_731 restores verbatim in Codex and does not replace OpenCode's different draft. |
| 11 | OpenCode | Local web, fixed build | pass | Restored draft (evidence/opencode-attachments/restored-draft-image.png), both sessions (evidence/draft-return-both/capture.webm) | QA_UNSENT_OPENCODE_DRAFT_842 restores verbatim after a round trip to Codex, whose distinct draft remains unchanged. |
| 11 | Claude | Local web, fixed build, unsent draft | pass | Switch recording (evidence/draft-isolation-1039/capture.webm), draft (evidence/ceiling-prerequisites-1039/claude-draft.png), exact values and cleanup (evidence/ceiling-prerequisites-1039/drafts.json) | Distinct Claude and Cursor drafts containing café 日本語 return verbatim in their own composers; the Cursor destination starts empty. Both markers are then explicitly cleared and empty composers verified. All 22 recording frames reviewed. This tests draft state without a successful harness turn; Cursor remains unconfigured. |
| 11 | Cursor | Local web, fixed build, unsent draft | pass | Switch recording (evidence/draft-isolation-1039/capture.webm), draft (evidence/ceiling-prerequisites-1039/cursor-draft.png), exact values and cleanup (evidence/ceiling-prerequisites-1039/drafts.json) | Distinct Claude and Cursor drafts containing café 日本語 return verbatim in their own composers; the Cursor destination starts empty. Both markers are then explicitly cleared and empty composers verified. All 22 recording frames reviewed. This tests draft state without a successful harness turn; Cursor remains unconfigured. |
| 12 | OpenCode | Local web, fixed build | fail | Live capture (evidence/opencode-tool-running-long/capture.webm), no running command (evidence/opencode-tools/running-no-command.png), expanded completed row (evidence/opencode-tools/completed-tool-open.png), Later restored-shell regression 72 (evidence/api-rendering-1209/result.json) | While the local server's sleep 60 process is running, only Thinking renders; completed tools use opaque Call identifiers and expanding them provides no command/output panel. |
| 12 | Codex | Local web, fixed build | fail | Running and completion recording (evidence/codex-shimmer-end-989/capture.webm), running row (evidence/codex-shimmer-989/before-shimmer-end.jpg) | The live shell row shows only Running and a timer; its actual sleep 8 command is invisible, and stays absent after Ran appears until the reply starts. This fails the command-visible requirement; opening while live was not exercised in this fixture. Tier R run 1086 reproduces initial command opacity zero beyond a 1000ms poll (expected >0.9): 2 failed / 1 passed / 0 retries, screenshot reviewed. Reload restores visibility and both completed reply oracles pass. Evidence (evidence/running-1086/result.json); register 51. |
| 13 | Codex | Local web, fixed build | fail | Open live (evidence/codex-tool-click-live/capture.webm), completion (evidence/codex-tool-settle-open/capture.webm), settled (evidence/codex-tools/expanded-completed.png) | The opened sleep 60 card remains expanded after completion, but the tool and existing content move upward as the final reply appears with no scroll input; the composer remains stationary. |
| 14 | Codex | Local web, fixed build | pass | Settled fold (evidence/codex-folding/settled-folded.png), live folding off (evidence/codex-fold-off-running/capture.webm), enable during run (evidence/codex-enable-live-fold/capture.webm) | Multiple groups remain visible while live folding is off; enabling it during the run folds completed groups while retaining the newest running step, and the settled turn has a fold control with prose still visible. |
| 14 | OpenCode | Local web, fixed build | fail | Idle incomplete turn (evidence/opencode-folding/idle-unfolded-incomplete.png), start (evidence/opencode-fold-groups-start/capture.webm) | The idle page leaves three opaque tool rows unfolded and omits the final step/reply, although stored text confirms all four steps and QA_FOUR_GROUPS_DONE; folding cannot correctly represent this incomplete rendered turn. |
| 15 | Codex | Local web, fixed build | fail | Manually opened pwd (evidence/codex-folding/live-off-expanded-tool.png), automatic fold (evidence/codex-enable-live-fold/capture.webm), hidden card (evidence/codex-folding/live-fold-hides-open-card.png) | A manually expanded pwd output disappears when live folding is enabled during the same turn; automatic folding does not preserve the expanded card. Tier M regression in core-harness-rendering-matrix reproduces this in both auth modes (runs 1134/1135, zero retries): `the manually opened output remains visible when live folding is enabled` fails after 10000ms. Completion passes; screenshots reviewed. Fixme/register 61. E2E evidence (evidence/live-fold-1135/result.json). |
| 16 | Codex | Local web, fixed build | pass | Stop (evidence/codex-interrupted-fold-stop/capture.webm), manual toggle (evidence/codex-interrupted-fold-toggle/capture.webm), expanded interruption (evidence/codex-folding/interrupted-expanded.png) | Stopping a three-group turn leaves it expanded with Interrupted and You stopped, and its Worked control successfully collapses and re-expands the tools. |
| 16 | OpenCode | Local web, fixed build | pass | Stop (evidence/opencode-interrupted-fold-stop/capture.webm), manual expansion (evidence/opencode-interrupted-fold-toggle/capture.webm), expanded (evidence/opencode-folding/interrupted-open.png) | The interrupted multi-group turn retains a Worked control that opens its two completed tool rows; the separate automatic-fold/interruption-label defect is recorded under state 44. |
| 17 | Codex | Local web, parent transcript | fail | Duplicated exit prose (evidence/codex-scroll/stopped.png), stored response once (evidence/codex-scroll/stored-exit-response.json), scroll recording (evidence/codex-scrollbar-stop/capture.webm), long reply (evidence/codex-long-reply/completed-bottom.png) | The completed false/grep turn displays the same two-item exit-code list twice around its failed tool rows; the authoritative assistant text contains that list only once. Qualified 2026-09-14 (register 80): `a settled reply does not re-render its streamed text when the turn's deltas replay` fails in both auth modes (runs 1274/1275) — replayed `text-delta` frames drained while the canonical fetch is in flight mint a fresh part id (`000000_<msg>-text` ≠ stored `prt_…`) after the announced `message.updated` un-settles the envelope; `mergeChatParts` keeps both parts and the reply paints twice. The live-streaming variant (chunks repeating mid-stream) remains unverified. |
| 17 | OpenCode | Local web, parent transcript | pass | Streaming (evidence/opencode-long-reply-stream/capture.webm), top (evidence/opencode-long-reply/completed-top.png), middle (evidence/opencode-long-reply/completed-middle.png), bottom (evidence/opencode-long-reply/completed-bottom.png) | The completed response renders eight distinct SECTION_1–SECTION_8 paragraphs with one completion marker and no duplicated paragraphs in the reviewed response. |
| 18 | Codex | Local web, fixed build | fail | Streaming (evidence/codex-long-reply-stream/capture.webm), partial list (evidence/codex-long-reply/partial-list.png), tail (evidence/codex-long-reply-tail/capture.webm), completed (evidence/codex-long-reply/completed-bottom.png) | The numbered response list is visible while section 6 is incomplete, then gains the remaining text and sections 7–8 rather than painting the complete list at once. Combined table/list test, same fixme 49: red log (evidence/blocks-1079/red.log), list frames (evidence/blocks-1079/artifacts/list-row-frames.json), partial list (evidence/blocks-1079/artifacts/list-first-visible.png), completed list (evidence/blocks-1079/artifacts/list-completed.png). List assertion expects no partial frames; receives 66 frames with five of eight items. Both final reply oracles pass. |
| 18 | OpenCode | Local web, fixed build | fail | Table recording (evidence/opencode-table-stream/capture.webm), partial rows (evidence/opencode-table/partial-fourteen-rows.jpg), completed (evidence/opencode-table/completed.png) | At about 11 seconds the table ends at row 14 while Thinking remains; the completed table later contains 30 rows, so it grows visibly instead of painting whole. Tier M table regression, fixme/register 49: red log (evidence/table-1078/red.log), frame counts (evidence/table-1078/artifacts/table-row-frames.json), partial (evidence/table-1078/artifacts/table-first-visible.png), completed (evidence/table-1078/artifacts/table-completed.png). Expected zero partial frames; received 59 frames with fifteen of thirty rows. Final reply oracle passes. Combined table/list test, same fixme 49: red log (evidence/blocks-1079/red.log), list frames (evidence/blocks-1079/artifacts/list-row-frames.json), partial list (evidence/blocks-1079/artifacts/list-first-visible.png), completed list (evidence/blocks-1079/artifacts/list-completed.png). List assertion expects no partial frames; receives 66 frames with five of eight items. Both final reply oracles pass. |
| 19 | Codex | Local web, fixed build | fail | Idle animation recording (evidence/codex-terminal-shimmer/capture.webm), visible running row (evidence/codex-archive/terminal-shimmer.jpg), DOM (evidence/codex-archive/terminal-shimmer-dom.json), stored terminal tool (evidence/codex-archive/stored-tool-parts.json), reload (evidence/codex-stale-tool-reload/capture.webm) | After returning to the session and reloading, the interrupted QA_LIVE_THREE_985 tool displays Running with an increasing timer and active text shimmer while the composer is idle; the message API reports error and Tool execution interrupted with an end time. The 131-frame idle capture was reviewed, with a full-size check at frame 65. This is a terminal-tool rendering mismatch, not evidence that the process restarted; one regression must cover replay and the terminal animation state. Additional child-tab reproduction: settled view (evidence/live-child-994/settled.png), recording (evidence/codex-child-terminal-994/capture.webm), and message API (evidence/live-child-994/replay.json) show the child command as Running after its stored status is error with Tool execution interrupted and an end time; the child reply and parent continuation are already visible. Recording samples 0, 30, 60, 90 and 120 were reviewed, plus the live full-size screenshot. Tier M regression in core-harness-rendering-matrix reproduces this in both auth modes (runs 1270/1271, zero retries): `an interrupted Codex command stays interrupted when rail-return replay resends its start` fails because a reattached runtime stream replays the tool's start frames and the live-event store overwrites the stored error part to running; the row shows Running with an advancing timer until a canonical refetch reverts it. Fixme/register 77. [E2E evidence](evidence/interrupted-replay-1270/result.json). |
| 20 | Codex | Local web, fixed build | fail | Expanded completed card (evidence/codex-output/missing-long-output.png), command run (evidence/codex-long-output-start/capture.webm) | The completed seq 1 240; sleep 30 card expands to only the command line, with no numbered output, scrollbar, or Show all control. Other long-output tool types remain untested. Native edit reproduction also expands without its diff: screenshot (evidence/diff-loading-1009/open.png), event (evidence/diff-loading-1009/replay.json). Track missing edit details with the tool-content regression; the geometry prerequisite is recorded at state 68. Tier R: `real-harness-local.spec.ts`, fixme/register 48. Red log (evidence/shell-output-1076/red.log), native complete output (evidence/shell-output-1076/artifacts/native-tool-results.json), stored final marker only (evidence/shell-output-1076/artifacts/completed-shell-part.json), reviewed expanded row (evidence/shell-output-1076/artifacts/completed-shell-expanded.png). Expected `OUTPUT_LINE_1`; received only the command and final marker. Codex returns all 240 lines, so loss precedes rendering in this reproduction. |
| 20 | OpenCode | Local web, fixed build | fail | Expanded tool details (evidence/opencode-output/empty-tool-details.png), Later restored-shell regression 72 (evidence/api-rendering-1209/result.json) | The completed 240-line output tool expands without output or Show all; the surrounding reply reports the line count, but the tool remains an opaque Call identifier. This repeats the state 12 tool-rendering finding. Native edit reproduction also expands without its diff: screenshot (evidence/diff-loading-1009/opencode-open.png), event (evidence/diff-loading-1009/opencode-replay.json). Track missing edit details with the tool-content regression; the geometry prerequisite is recorded at state 68. |
| 21 | Codex | Local web, fixed build | fail | Expanded failure (evidence/codex-output/failure-rows.png), execution (evidence/codex-exit-code-start/capture.webm) | Both false and grep with no matches render generic Shell Failed headers without the command or exit chip; expanding false reveals Process exited with code 1, while grep receives the same failure-card treatment. Current Tier R follow-up narrows this: exit 23 is visible and passes, but command remains absent. `real-harness-local.spec.ts`, fixme/register 47: red log (evidence/shell-exit-1074/red.log), stored command (evidence/shell-exit-1074/artifacts/failed-shell-part.json), reviewed header (evidence/shell-exit-1074/artifacts/failed-shell-header.png). Assertion expects `fail.cjs`, receives `ShellFailedexit 23`. |
| 21 | OpenCode | Local web, fixed build | fail | Tool headers (evidence/opencode-output/empty-tool-details.png) | The false and no-match grep tools display opaque Call identifiers without exit-code labels; only assistant prose reports their exit codes. This is the same tool-rendering finding, not independent evidence of incorrect command execution. |
| 22 | Codex | Local web, fixed build | fail | Incorrect preview target (evidence/codex-preview/wrong-preview-target.png) | Expanding the server turn's process-list command exposes Local preview for an unrelated control-plane MCP URL, including serialized header syntax, instead of the requested server at port 8766. The temporary server is no longer listening at inspection, so successful preview loading and Browser-tab persistence remain untested. Tier M regression in `core-harness-rendering-matrix.spec.ts`: expected 0 preview rows for MCP output, received 1; valid development-server href passes. Red log (evidence/preview-1081/red.log), fixme/register 50. Sanitized fixture reconstructed from the screenshot. |
| 22 | OpenCode | Local web, fixed build | fail | Expanded empty tool rows (evidence/opencode-preview-1002/expanded.png), DOM (evidence/opencode-preview-1002/expanded-dom.txt), running server and HTTP 200 (evidence/opencode-preview-1002/server.json), stored parts (evidence/opencode-preview-1002/replay.json) | The real harness starts the declared Astro recipe on port 4612 and the server returns HTTP 200, but expanding the launch and log tool rows produces no output or Local preview row. The replay response already contains opaque call names with empty inputs and outputs, sharing the missing tool-data finding from state 20. The prose URL remains clickable. |
| 24 | Codex | Local web, fixed build | fail | Completed image response (evidence/codex-links/completed.png) | The requested Markdown file image renders an alt-text box reading QA shapes, without the actual image; the PNG exists at the supplied path. Native rendering remains untested. |
| 24 | Codex | Local desktop, dev recipe | fail | Desktop image placeholder (evidence/codex-links/desktop-image.png), same file in Preview (evidence/codex-links/os-preview.png) | The short Markdown response renders QA shapes as an alt-text box on desktop as well; clicking its file link opens the actual red-square/blue-circle PNG in Preview. |
| 24 | OpenCode | Local web, fixed build | fail | Visible placeholder (evidence/opencode-file-image/placeholder.png), Rendered DOM (evidence/opencode-file-image/dom.json), Stored Markdown and file validation (evidence/opencode-file-image/source.json), Later file-image regression 73 (evidence/api-image-1210/result.json) | The heavy reply contains the exact Markdown image reference to the existing 640×400 PNG, but the page renders only a QA shapes markdown-image-fallback span. This shares the file-image rendering finding observed on Codex; the image reference is present in the replay response. |
| 25 | Codex | Local web, fixed build | fail | Answer submission (evidence/codex-question-answer/capture.webm), completed without card (evidence/codex-question/answered.png), return recording (evidence/codex-question-return/capture.webm), pending again (evidence/codex-question/pending-on-return.jpg) | Selecting SAPPHIRE completes as QA_QUESTION_CHOICE SAPPHIRE, but leaves no answered card; navigating to OpenCode and back reopens the same question with unselected choices and Submit/Dismiss for about 0.3 seconds before it disappears. Free-text preserves the exact Unicode answer and dismissal completes without an error, but neither retains its question card; details follow below. Tier R: `real-harness-local.spec.ts`, fixme/register 45. Red log (evidence/question-retained-1068/red.log), real tool result (evidence/question-retained-1068/artifacts/question-tool-results.json), replay (evidence/question-retained-1068/replayed-messages.json), reviewed screenshot (evidence/question-retained-1068/artifacts/answered-question-after-reload.png): `Expected: visible; Error: element(s) not found` for the result card after reload; Staging delivery and reply oracles pass. |
| 25 | OpenCode | Local web, fixed build | fail | Answer (evidence/opencode-question-answer/capture.webm), completed (evidence/opencode-question/answered.png), return (evidence/opencode-question-return/capture.webm), custom answer (evidence/opencode-question/custom-completed.png), dismissal (evidence/opencode-question/dismissed-expanded.png) | AMBER and the custom answer QA café 日本語 972 reach the agent, but each resolved question renders as an opaque Call identifier instead of a question/answer card. Dismissal renders Failed with an aborted error stating The user dismissed this question. The earlier 38-frame round trip shows no pending OpenCode question replay. |
| 26 | Codex | Local web, fixed build | fail | MCP calls (evidence/codex-mcp-group/capture.webm), expanded output (evidence/codex-mcp/expanded.png) | Consecutive sessions_list and processes calls form a group labeled only Used 2 tools, rather than naming the MCP tools. The processes row opens readable output, but its header exposes intent=mcp kind=mcp_tool_call server=claxedo. Skill-tool coverage remains pending. Shared Tier M regression expects both tool names in the header but receives Used 2 tools; both expanded member rows pass. One failed, zero retries, screenshots reviewed; register 53. Evidence (evidence/mcp-group-1091/result.json). |
| 26 | OpenCode | Local web, fixed build | fail | Expanded skill calls (evidence/opencode-tools-1003/skills-expanded.png), DOM (evidence/opencode-tools-1003/skills-expanded-dom.txt), replay (evidence/opencode-tools-1003/skills-replay.json), MCP unavailable (evidence/opencode-tools-1003/mcp-unavailable.png) | Fixture 1004 requests consecutive crabbox and pdf skill loads, which the model reports successful; two completed tool calls render as Used 2 tools with opaque Call identifiers and no output when expanded. The replay response also omits names, inputs and outputs, so the exact original skill payload cannot be independently recovered there. This is the existing missing-tool-data failure, now exercised by skill calls. The requested claxedo MCP tools are reported unavailable in fixture 1003. |
| 28 | Codex | Local web, fixed build | pass | Spawn recording (evidence/codex-child-grouping-start/capture.webm), completion recording (evidence/codex-child-grouping-middle/capture.webm), child A (evidence/child-grouping/child-a.png), child B (evidence/child-grouping/child-b.png), child C (evidence/child-grouping/child-c.png) | All 743 captured frames of the three-child fixture were reviewed: each spawn appears once, no empty or duplicate chip is observed, and each chip opens its distinct QA_CHILD_A_932, QA_CHILD_B_932 or QA_CHILD_C_932 reply. |
| 29 | Codex | Local web, fixed build | pass | First open (evidence/codex-subagents/sapphire-panel.png), reopened (evidence/codex-subagents/reopened.png), second child (evidence/codex-subagents/amber-panel.png) | The chip opens a workspace-panel tab; closing and reopening restores the SAPPHIRE child, and the AMBER chip opens a second tab with the correct separate reply. |
| 32 | Codex | Local web, fixed build | pass | Folded parent with chips (evidence/child-grouping/folded.png), expanded parent (evidence/child-grouping/expanded.png), first child (evidence/child-grouping/child-a.png), second child (evidence/child-grouping/child-b.png), third child (evidence/child-grouping/child-c.png) | The parent completes with two work groups folded under Worked for 27s while all three child chips remain visible; after manual expansion and refolding, each chip still opens its distinct completed child transcript. |
| 33 | OpenCode | Local web, fixed build | pass | Background observation (evidence/opencode-background-shell/capture.webm), idle transcript (evidence/background-shell/opencode-running.png), workspace tabs (evidence/background-shell/opencode-panel.png), process evidence (evidence/background-shell/process-observations.json) | The turn completes while nohup sleep 45 remains alive as PID 62778 under parent PID 1 at elapsed 26 seconds; no subagent chip or Thinking tab appears, and the process later exits. All six captured frames were reviewed; the workspace panel shows Review rather than a phantom agent. |
| 34 | OpenCode | Local web, fixed build | pass | Click recording (evidence/rail-click-geometry/capture.webm), click geometry (evidence/rail-navigation/click-geometry.json), send (evidence/rail-send-order/capture.webm), send geometry (evidence/rail-navigation/send-geometry.json), completed (evidence/rail-navigation/completed-order.png) | Clicking through Codex, OpenCode and Claude preserves row positions at 183/215/247 pixels; sending in OpenCode moves it from second to first, shifts Codex down one slot, and leaves Claude in place. Returning to Codex and OpenCode completing do not reorder them again. |
| 34 | Codex | Local web, fixed build | pass | Send recording (evidence/codex-send-rail-1021/capture.webm), click/send geometry (evidence/codex-rail-1019/send-geometry.json), completed (evidence/codex-rail-1019/send-completed.png), sampled rail frames (evidence/codex-rail-1019/rail-review.jpg) | Clicking Codex leaves it second at y215 behind OpenCode y183. Sending moves Codex to y183 and OpenCode to y215; all three following rows keep y247/279/311. Reviewed send frames 0/10/80/164. Earlier multi-session click coverage is shared with the OpenCode state 34 fixture. |
| 35 | OpenCode | Local web, fixed build, background completion | pass | Recording (evidence/background-rail-completion-1012/capture.webm), before (evidence/background-rail-1012/before.jpg), after (evidence/background-rail-1012/after.png), final geometry (evidence/background-rail-1012/after-geometry.json), completed event (evidence/background-rail-1012/replay.json), review scope (evidence/background-rail-1012/observations.json) | Start OpenCode sleep 25, switch to Codex and send there to put Codex first. OpenCode completes during the recording while Codex stays selected; OpenCode remains second and the other rows retain order. Reviewed frames 0/100/200/300/400/500/550 plus final DOM positions, Codex y183 and OpenCode y215. This covers background completion only; subagent and scheduled-wake variations remain unverified. |
| 35 | Codex | Local web, fixed build, background completion | pass | Recording (evidence/codex-background-rail-1019/capture.webm), before/after geometry (evidence/codex-rail-1019/background-geometry.json), completed event (evidence/codex-rail-1019/background-replay.json), after (evidence/codex-rail-1019/background-after.png), sampled rail frames (evidence/codex-rail-1019/rail-review.jpg) | Codex sleep 25 completes while OpenCode is selected and first. All five rail positions match before and after; Codex stays second at y215. Completion timestamp falls within the recording. Reviewed frames 0/250/500/765. This verifies background completion only; subagent and scheduled-wake variations remain unverified. |
| 36 | Codex | Local web, fixed build | fail | Fold return (evidence/codex-fold-return-981/capture.webm), exposed rows (evidence/question-fold/codex-return-unfolded.jpg), later stable geometry sample (evidence/repeated-switch-1001/geometry.json) | Shares state 46: the previously refolded completed turn exposes its shell rows after return without another click, so folding is not stable across navigation. The later three warm round trips preserve row positions but do not invalidate that recorded fold transition. One regression should cover the shared failure. |
| 36 | OpenCode | Local web, fixed build | pass | Visit a (evidence/switch-opencode-1001-a/capture.webm), Visit b (evidence/switch-opencode-1001-b/capture.webm), Visit c (evidence/switch-opencode-1001-c/capture.webm), fold geometry (evidence/repeated-switch-1001/geometry.json) | The long-session region returns painted and folded in three warm round trips, with a stable title and settled row positions. The composer briefly substitutes Loading models before Big Pickle; this observation does not establish selector stability or cold-start behavior. |
| 37 | Codex | Local web, fixed build | pass | Wheel recording (evidence/codex-fast-scroll-stop/capture.webm), scrollbar recording (evidence/codex-scrollbar-stop/capture.webm), stopped (evidence/codex-scroll/stopped.png) | Fast wheel scrolling and scrollbar dragging end on painted content that remains stable after release. Brief blanking during the largest wheel jump clears while scrolling settles, within the stated fast-fling limit. This is the ordinary-session check, not the Part 11 heavy-session matrix. |
| 37 | OpenCode | Local web, fixed build | pass | Wheel recording (evidence/opencode-long-scroll-stop/capture.webm), scrollbar recording (evidence/opencode-scrollbar-stop/capture.webm), stopped (evidence/opencode-scroll/stopped.png) | Fast wheel scrolling and scrollbar dragging end on painted content that remains stable after release. Brief blanking during the largest wheel jump clears while scrolling settles, within the stated fast-fling limit. This is the ordinary-session check, not the Part 11 heavy-session matrix. |
| 38 | Codex | Local web, fixed build | fail | Round trip (evidence/codex-subagent-tab-return/capture.webm), Review selected on return (evidence/codex-subagents/return-selects-review.png), retained child (evidence/codex-subagents/retained-child.png) | Navigating to OpenCode and back changes the selected workspace panel from the child transcript to Review, although all three child tabs remain open and selecting the original tab still shows its correct transcript. |
| 38 | OpenCode | Local web, transcript URL | pass | Loaded Browser (evidence/opencode-preview-1002/link-result.png), settled DOM (evidence/opencode-preview-1002/link-result-dom.txt) | Clicking the localhost:4612 prose link opens the correct page in the workspace Browser tab. A transient Loading review state precedes the settled Browser; this result covers the final destination of the URL entry, not every file/review entry or panel transition stability. |
| 39 | Codex | Local web, fixed build | pass | Single group (evidence/codex-folding/one-group.png), two folded groups (evidence/codex-folding/two-groups-folded.png), expanded groups (evidence/codex-folding/two-groups-expanded.png) | A single pwd tool stays visible without an automatic turn fold. Two shell calls separated by prose produce a folded completed turn; before, between and after prose markers remain visible. |
| 39 | OpenCode | Local web, fixed build | pass | Single group (evidence/opencode-folding/one-group.png), two folded groups (evidence/opencode-folding/two-groups-folded.png), expanded groups (evidence/opencode-folding/two-groups-expanded.png) | A single pwd tool stays visible without an automatic turn fold. Two shell calls separated by prose produce a folded completed turn; before, between and after prose markers remain visible. |
| 40 | Codex | Local web, fixed build | pass | Clean completion (evidence/codex-three-settle-987/capture.webm), folded (evidence/live-fold/codex-clean-folded.png), expanded (evidence/live-fold/codex-clean-expanded.png), DOM (evidence/live-fold/codex-clean-dom.txt) | Clean fixture 987 automatically hides exactly three completed shell groups; both separating prose paragraphs and QA_COMPLETE_DONE_987 remain outside the fold. Expansion restores the three distinct Ran command rows. Together with state 39, this covers completed one-, two- and three-group thresholds; live-fold behavior is reported separately in state 41. |
| 40 | OpenCode | Local web, fixed build | pass | Completed fold (evidence/live-fold/opencode-completed.png), expanded three groups (evidence/live-fold/opencode-three-expanded.png), DOM (evidence/live-fold/opencode-three-expanded-dom.txt) | The completed 68-second fixture hides exactly three separate tool groups while retaining both prose separators and final reply; expansion reveals all three opaque tool rows. Group-count behavior matches Codex, while names and live folding remain separate failures. |
| 41 | Codex | Local web, fixed build | fail | Enabled setting (evidence/live-fold/enabled.png), first group (evidence/codex-live-fold-start-985/capture.webm), third-group transition (evidence/codex-live-fold-two-985/capture.webm), live group retained (evidence/live-fold/third-running.png), setting-off recording (evidence/codex-live-fold-disabled-985/capture.webm), earlier groups unfolded (evidence/live-fold/disabled-unfolded.png), restored setting (evidence/live-fold/restored.png) | With live folding enabled, the third shell group shows Working while retaining the live sleep command; disabling the setting immediately exposes both earlier groups instead of preserving the existing fold. The original enabled preference is restored, and the fixture completes QA_LIVE_DONE_985. |
| 41 | OpenCode | Local web, fixed build | fail | Enabled setting (evidence/live-fold/opencode-enabled.png), running capture (evidence/opencode-live-fold-third-986/capture.webm), third command absent (evidence/live-fold/opencode-third-running.png), DOM (evidence/live-fold/opencode-third-dom.txt), live process (evidence/live-fold/opencode-process.json), completed fold (evidence/live-fold/opencode-completed.png) | While the third shell command is confirmed running for 19s, only the two completed opaque call rows and separating prose appear: no live third row or Working fold control exists despite enabled live folding. The completed turn folds as Worked for 1m 7s. The missing running row overlaps state 12; setting-off retention cannot be exercised on this absent live fold. |
| 42 | Codex | Local web, fixed build | pass | New-turn capture (evidence/codex-manual-expand-new-turn/capture.webm), still expanded (evidence/codex-folding/expanded-after-new-turn.png) | Manually expanding the completed two-group turn remains respected after a subsequent no-tool turn runs and completes; both earlier tool rows remain visible. |
| 42 | OpenCode | Local web, fixed build | pass | New-turn capture (evidence/opencode-manual-expand-new-turn/capture.webm), still expanded (evidence/opencode-folding/expanded-after-new-turn.png) | Manually expanding the completed two-group turn remains respected after a subsequent no-tool turn runs and completes; both earlier tool rows remain visible. |
| 43 | Codex | Local web, fixed build | fail | Manual fold (evidence/codex-live-manual-fold/capture.webm), settled (evidence/codex-folding/manual-fold-settled.png) | The live turn remains folded after a manual collapse and completion, but its header exposes only Worked for 57s, with no group count for the three tool groups required by this check. Tier M regression proves three groups, manual collapse, and retained collapse on completion, then fails: expected 3 groups, received Worked for 0s. Test-user 1088 and unsigned 1090 each fail once with zero retries; register 52. Evidence (evidence/fold-count-1090/result.json). |
| 44 | Codex | Local web, fixed build | pass | First Stop (evidence/codex-interrupted-fold-stop-983/capture.webm), interrupted open (evidence/interrupted-fold/stopped-983.png), manual fold (evidence/interrupted-fold/manually-folded-983.png), second Stop (evidence/codex-interrupted-fold-second-stop/capture.webm), first turn still expanded (evidence/interrupted-fold/after-second-stop.png), DOM (evidence/interrupted-fold/after-second-stop-dom.txt) | Stopping the two-group fixture leaves Worked for 34s expanded with Interrupted and You stopped; manual collapse and expansion work, and stopping the subsequent sleep 45 turn leaves the first turn expanded with its original tool and interruption message. |
| 44 | OpenCode | Local web, fixed build | fail | Stop transition (evidence/opencode-interrupted-fold-stop/capture.webm) | Stop makes the multi-group turn automatically collapse to Worked for 37s, with no Interrupted or You stopped explanation, rather than leaving interrupted work open. |
| 46 | Codex | Local web, fixed build | fail | Return (evidence/codex-fold-return-981/capture.webm), exposed shell rows (evidence/question-fold/codex-return-unfolded.jpg), reload (evidence/codex-fold-reload-981/capture.webm) | A manually refolded completed turn initially paints folded on return, then both shell rows appear without another click; all 12 return frames and 16 reload frames were reviewed. Reload retains the latest fold but replays the answered question briefly. Native process cold-start coverage remains pending. |
| 46 | OpenCode | Local web, fixed build | pass | Return (evidence/opencode-fold-return-982/capture.webm), reload (evidence/opencode-fold-reload-982/capture.webm) | The completed fixture remains folded from its first transcript paint through all 9 return frames and 7 reload frames. This verdict covers web navigation and page restart; native process cold-start coverage remains pending. |
| 47 | Codex | Local web, fixed build | pass | Live Working label (evidence/fold-label-1006/running.png), Stop transition (evidence/codex-fold-label-stop-1006/capture.webm), Worked after Stop (evidence/fold-label-1006/stopped.png), DOM (evidence/fold-label-1006/stopped-dom.txt) | With live folding enabled, fixture 1006 shows Working while its third shell runs and Worked for 42s after Stop, with Interrupted and You stopped. Reviewed Stop frames 0/10/20/30/45/58 plus full-size live and settled views. This verifies the running-to-interrupted label; an independently failed turn overlapping busy status is not exercised. |
| 48 | Codex | Local web, fixed build | fail | Selected Indigo (evidence/question-fold/selected.png), answer and completion (evidence/codex-question-fold-answer/capture.webm), folded turn (evidence/question-fold/folded.png), expanded turn (evidence/question-fold/expanded.png), DOM (evidence/question-fold/expanded-dom.txt) | The answered turn auto-folds after two shell groups separated by prose; QA_FOLD_DONE_981 Indigo confirms the answer, but no resolved question card remains outside the fold or inside it when expanded. The two shell groups remain distinct. This is the resolved-card retention failure also covered by states 25/72, not an additional independent defect. |
| 48 | OpenCode | Local web, fixed build | fail | Selected Indigo (evidence/question-fold/opencode-selected.png), completion (evidence/opencode-question-fold-answer/capture.webm), folded (evidence/question-fold/opencode-folded.png), expanded (evidence/question-fold/opencode-expanded.png), group contents (evidence/question-fold/opencode-group.png) | The final reply preserves Indigo, but the answered card is absent outside and inside the fold. The first work group contains two opaque tool rows before the separating prose; the empty expanded first tool does not identify its type, so its classification remains unconfirmed. Card retention overlaps states 25/72. |
| 49 | OpenCode | Local web, fixed build | fail | Three reads collapsed (evidence/opencode-context/collapsed.png), expanded (evidence/opencode-context/expanded.png), mixed collapsed (evidence/opencode-context/mixed-collapsed.png), mixed expanded (evidence/opencode-context/mixed-expanded.png) | Three reads form Used 3 tools with opaque Call identifiers rather than a named context group; read → shell → read also collapses into one Used 3 tools group instead of three separate groups. |
| 50 | Codex | Local web, fixed build | pass | Mixed header (evidence/mixed-work/codex-collapsed.png), four entries (evidence/mixed-work/codex-expanded.png), DOM (evidence/mixed-work/codex-dom.txt), edited fixture (evidence/mixed-work-fixture.txt) | Two shell calls, a native file change, then a third shell call render as one group labeled Ran 3 commands · File change, with all four entries in order; the edit changes the disposable fixture to QA_MIXED_BETA_950 and the reply is QA_MIXED_DONE_950. The fixture is subsequently edited again by the OpenCode comparison. This verifies settled grouping, not transition smoothness; the recording attempt was interrupted. |
| 50 | OpenCode | Local web, fixed build | fail | Generic header (evidence/mixed-work/opencode-collapsed.png), opaque entries (evidence/mixed-work/opencode-expanded.png), DOM (evidence/mixed-work/opencode-dom.txt), edited fixture (evidence/mixed-work-fixture.txt) | The four-call sequence forms one group, but its header says only Used 4 tools and its entries are four opaque Call IDs, omitting the command/edit mix; the fixture changes to QA_MIXED_GAMMA_951 and the turn completes QA_OC_MIXED_DONE_951. |
| 51 | Codex | Local web, fixed build | fail | Consecutive MCP calls (evidence/codex-mcp-group/capture.webm), expanded group (evidence/codex-mcp/expanded.png) | The two consecutive read-only MCP calls sessions_list and processes have no intervening shell or prose, but the header only says Used 2 tools; the expanded rows identify the MCP calls. This is the same missing descriptive MCP-group header as state 26 and requires one shared regression test. Shared Tier M regression expects both tool names in the header but receives Used 2 tools; both expanded member rows pass. One failed, zero retries, screenshots reviewed; register 53. Evidence (evidence/mcp-group-1091/result.json). |
| 51 | OpenCode | Local web, fixed build | fail | Two-call skill group (evidence/opencode-tools-1003/skills-expanded.png), ordered replay (evidence/opencode-tools-1003/skills-replay.json) | The two consecutive completed calls in skill fixture 1004 have no intervening prose or shell, but their group says only Used 2 tools and the children show opaque call IDs. Shares the state 26 skill-tool finding; do not add a duplicate regression. |
| 52 | Codex | Local web, fixed build | pass | Separated groups (evidence/codex-folding/two-groups-expanded.png) | pwd and ls -d . remain separate groups with QA_BETWEEN_GROUPS visibly between them; the intervening prose is not swallowed by either group. |
| 52 | OpenCode | Local web, fixed build | pass | Separated groups (evidence/opencode-folding/two-groups-expanded.png) | pwd and ls -d . remain separate groups with QA_BETWEEN_GROUPS visibly between them; the intervening prose is not swallowed by either group. |
| 54 | Codex | Local web, fixed build | fail | Failed shell rows (evidence/codex-output/failure-rows.png), real execution (evidence/codex-exit-code-start/capture.webm), successful named shell row (evidence/interrupted-fold/running-983.png) | Successful Codex shell calls display their command, but false and no-match grep become generic Shell Failed cards without a command label or exit-code chip; opening false shows Process exited with code 1. This failure overlaps state 21; raw local_shell versus command spelling was not captured in this visual check. Current Tier R follow-up narrows this: exit 23 is visible and passes, but command remains absent. `real-harness-local.spec.ts`, fixme/register 47: red log (evidence/shell-exit-1074/red.log), stored command (evidence/shell-exit-1074/artifacts/failed-shell-part.json), reviewed header (evidence/shell-exit-1074/artifacts/failed-shell-header.png). Assertion expects `fail.cjs`, receives `ShellFailedexit 23`. |
| 55 | Codex | Local web, fixed build | pass | Plan creation (evidence/codex-plan-capability/capture.webm), updates (evidence/codex-plan-updates/capture.webm), four complete (evidence/codex-todo/four-complete.jpg) | The confirmed native plan turn renders and updates five named steps above the composer without creating transcript tool rows; final-list retention is separately failing under state 71. |
| 56 | Codex | Local web, fixed build | pass | Collapsed header (evidence/codex-long-command/collapsed.png), expanded command (evidence/codex-long-command/expanded.png) | The long printf command truncates with an ellipsis in its collapsed header and expands to show the complete command and output through QA_LONG_COMMAND_END. |
| 56 | OpenCode | Local web, fixed build | fail | Collapsed header (evidence/opencode-long-command/collapsed.png), expanded row (evidence/opencode-long-command/expanded.png), Later restored-shell regression 72 (evidence/api-rendering-1209/result.json) | The same long command renders only an opaque Call identifier; expanding it reveals no command or output, reproducing the missing tool-detail finding. |
| 57 | Codex | Local web, fixed build | pass | Separated chip rows (evidence/child-grouping/expanded.png), ordered DOM (evidence/child-grouping/expanded-dom.txt) | Two consecutive child spawns form the first chip row, printf QA_BETWEEN_CHILDREN_932 appears as a work row, and the third child forms a second chip row; expanding the folded parent preserves this ordering. |
| 58 | Codex | Local web, fixed build, first visit and reload | fail | First visit (evidence/codex-first-open-1022/capture.webm), reload (evidence/codex-first-reload-1022/capture.webm), creator already settled (evidence/first-open-1022/creator-settled.png), first-visit replay (evidence/first-open-1022/first-open-replayed.jpg), reload replay (evidence/first-open-1022/reload-replayed.jpg), timing (evidence/first-open-1022/timing.json), completed response (evidence/first-open-1022/replay.json) | A two-group session is created and completed in a second tab before its first visit in the original tab. First-visit frames 1–3 show the correct fold, then frames 4–10 unfold/replay the completed turn with Thinking before frame 11 folds it again. Reload frame 4 shows a Running row and Thinking on that completed response; the turn folds at frame 18. All 12 first-visit and 20 reload frames reviewed, with full-size checks of frame 4 in each. Shares the completed-turn replay/fold regression at state 46. This tests a first tab visit within the same app installation, not an isolated fresh installation. |
| 58 | OpenCode | Local web, fixed build, first visit and reload | pass | First visit (evidence/opencode-first-open-1023/capture.webm), reload (evidence/opencode-first-reload-1023/capture.webm), creator settled (evidence/first-open-1023/creator-settled.png), all captured frames (evidence/first-open-1023/review.jpg), stored response (evidence/first-open-1023/replay.json) | A completed two-group session generated in a second tab is first opened from the original rail. The first captured transcript paint is folded with its final title and no Thinking; it stays folded through subsequent captured frames and reload. All six frames from each recording reviewed, with first-visit frames 1–2 checked full-size. Reload includes initial logo/empty-shell loading before transcript paint; this pass covers fold/title/terminal state once transcript content appears, not the broader loading experience. Select agent briefly precedes Big Pickle without a fold change. Same-installation first tab visit, not an isolated fresh installation. |
| 59 | OpenCode | Local web, fixed build | fail | Send recording (evidence/opencode-live-fold-start-986/capture.webm), before (evidence/opencode-send-shift/before.jpg), after (evidence/opencode-send-shift/after.jpg), timing (evidence/opencode-send-shift/timing.json) | Sending fixture 986 moves the existing QA_OC_FOLD_DONE_982 reply upward approximately 153px as the new prompt is inserted; the composer also shrinks after clearing its draft. No scroll input occurs. States 6 and 59 share this send-shift reproduction and one geometry regression; it does not establish the stationary-composer variation on OpenCode. |
| 59 | Codex | Local web, fixed build | fail | Same recorded transition (evidence/codex-order-start/capture.webm), before (evidence/codex-thinking-placement/previous-turn.jpg), after (evidence/codex-thinking-placement/current-turn.jpg) | Existing reply and prompt rows shift upward as Thinking relocates, while the composer remains visibly mounted in the same position; no scroll input occurs during this capture.  Shares the state 6 reproduction and one regression rather than creating a duplicate finding. |
| 60 | Codex | Local web, fixed build | fail | First-token recording (evidence/codex-shimmer-end-989/capture.webm), before (evidence/codex-shimmer-989/before-first-token.jpg), after (evidence/codex-shimmer-989/first-token.jpg), timing (evidence/codex-shimmer-989/timing.json) | Frames 162–163 show the first QA_SH reply token arriving while the existing tool row and earlier prose move upward approximately 58px; the composer remains stationary and no scroll input occurs. This adds the first-token trigger to the transcript geometry finding; its authoritative cause remains unestablished. |
| 60 | OpenCode | Local web, fixed build | fail | Continuous recording (evidence/opencode-shimmer-continuous-991/capture.webm), before reply (evidence/opencode-shimmer-991/before-reply.jpg), reply arrival (evidence/opencode-shimmer-991/reply-arrival.jpg), timing (evidence/opencode-shimmer-991/timing.json) | Frames 363–364 show the first visible reply arriving while the existing tool label and earlier content move upward approximately 86px, with the composer stationary and no scroll input. This is the OpenCode first-reply variation of the transcript geometry finding. |
| 61 | Codex | Local web, fixed build | fail | Open live (evidence/codex-tool-click-live/capture.webm), completion (evidence/codex-tool-settle-open/capture.webm), settled (evidence/codex-tools/expanded-completed.png) | The opened sleep 60 card remains expanded after completion, but the tool and existing content move upward as the final reply appears with no scroll input; the composer remains stationary.  Shares the state 13 reproduction and one regression rather than creating a duplicate finding. |
| 61 | OpenCode | Local web, fixed build | fail | Continuous recording (evidence/opencode-shimmer-continuous-991/capture.webm), before tool (evidence/opencode-shimmer-991/before-tool.jpg), tool arrival (evidence/opencode-shimmer-991/tool-arrival.jpg), timing (evidence/opencode-shimmer-991/timing.json) | The completed opaque Call row first appears in frames 321–322, moving the previous reply and prompt upward approximately 32px while the composer stays stationary. There is no existing live row whose height is preserved. The missing live row shares state 12; the completion shift belongs to the geometry regression. |
| 62 | Codex | Local web, fixed build | pass | Completion recording (evidence/codex-shimmer-end-989/capture.webm), before (evidence/codex-shimmer-989/before-shimmer-end.jpg), after (evidence/codex-shimmer-989/after-shimmer-end.jpg), timing (evidence/codex-shimmer-989/timing.json) | The Running label becomes Ran in frames 13–14 without a visible shift of its icon, label origin, prompt or surrounding text. This passes the observed shimmer-ending transition; the later first-token jump is recorded separately in state 60, and terminal replay shimmer fails state 19. |
| 63 | Codex | Local web, heavy session | fail | Expansion recording (evidence/codex-show-all-geometry/capture.webm), before (evidence/codex-show-all-geometry/before.png), after (evidence/codex-show-all-geometry/after.png), DOM geometry (evidence/codex-show-all-geometry/geometry.json) | With output already scrolled, clicking Show all replaces the visible lines 16–20 with lines 4–14 rather than preserving the reading position. The outer transcript scrollTop stays 5015.5, while the containing tool-group scrollTop changes 846.5 to 861.5 and the control moves from y403 to y6535. All six transition frames were reviewed. This is output-expansion position loss, separate from the offscreen expansion-state loss in state 88. Shared standalone-shell regression now fails in both auth modes: line 16 moves from y204 to y-5642 after Show all, a 5846px displacement. Fixme/register 58; test-user log (evidence/output-reading-1109/red.log), unsigned log (evidence/output-reading-1110/red.log). All four stage screenshots reviewed; original nested-group variation remains uncovered. |
| 64 | Codex | Local web, fixed build | fail | Return recording (evidence/codex-question-return/capture.webm), pending flash (evidence/codex-question/pending-on-return.jpg), settled (evidence/codex-question/settled-return.jpg) | The already-resolved question replaces the composer at approximately 4.016–4.297 seconds, shifting the completed response upward; when it disappears the composer and response return. This independently reproduces the supplied recording's question-flash finding. |
| 64 | OpenCode | Local web, fixed build | fail | Question resolution recording (evidence/opencode-question-answer/capture.webm), completed without question card (evidence/opencode-question/answered.png), return recording (evidence/opencode-question-return/capture.webm) | Resolving AMBER removes the question dock instead of retaining an answered card with the same footprint, and surrounding content changes position. This shares the state 25 answered-card finding. Resolution frames 0/59/118/177 were rechecked; the prior return sample does not show a pending-card replay on OpenCode. |
| 65 | Codex | Local web, fixed build | fail | Chip arrival recording (evidence/codex-child-grouping-start/capture.webm), before (evidence/child-grouping/before-first-chip.jpg), after (evidence/child-grouping/after-first-chip.jpg), timing (evidence/child-grouping/arrival-timing.json) | At the first chip arrival, frames 264–266 (7.7595–7.8177s), the previous QA_CODEX_BG2_STARTED reply and current user prompt shift upward approximately 32px while the composer remains stationary; no scroll input occurs. This verifies the chip-arrival variation, with root cause and DOM geometry regression still unestablished. |
| 66 | Codex | Local web, fixed build | fail | Streaming (evidence/codex-long-reply-stream/capture.webm), partial list (evidence/codex-long-reply/partial-list.png), tail (evidence/codex-long-reply-tail/capture.webm), completed (evidence/codex-long-reply/completed-bottom.png) | The numbered response list is visible while section 6 is incomplete, then gains the remaining text and sections 7–8 rather than painting the complete list at once.  Shares the state 18 reproduction and one regression rather than creating a duplicate finding. Combined table/list test, same fixme 49: red log (evidence/blocks-1079/red.log), list frames (evidence/blocks-1079/artifacts/list-row-frames.json), partial list (evidence/blocks-1079/artifacts/list-first-visible.png), completed list (evidence/blocks-1079/artifacts/list-completed.png). List assertion expects no partial frames; receives 66 frames with five of eight items. Both final reply oracles pass. |
| 66 | OpenCode | Local web, fixed build | fail | Table recording (evidence/opencode-table-stream/capture.webm), partial rows (evidence/opencode-table/partial-fourteen-rows.jpg), completed (evidence/opencode-table/completed.png) | At about 11 seconds the table ends at row 14 while Thinking remains; the completed table later contains 30 rows, so it grows visibly instead of painting whole.  Shares the state 18 reproduction and one regression rather than creating a duplicate finding. Tier M table regression, fixme/register 49: red log (evidence/table-1078/red.log), frame counts (evidence/table-1078/artifacts/table-row-frames.json), partial (evidence/table-1078/artifacts/table-first-visible.png), completed (evidence/table-1078/artifacts/table-completed.png). Expected zero partial frames; received 59 frames with fifteen of thirty rows. Final reply oracle passes. Combined table/list test, same fixme 49: red log (evidence/blocks-1079/red.log), list frames (evidence/blocks-1079/artifacts/list-row-frames.json), partial list (evidence/blocks-1079/artifacts/list-first-visible.png), completed list (evidence/blocks-1079/artifacts/list-completed.png). List assertion expects no partial frames; receives 66 frames with five of eight items. Both final reply oracles pass. |
| 67 | OpenCode | Local web, fixed build | fail | Image transition (evidence/opencode-http-image-1028/capture.webm), placeholder (evidence/http-image-1028/placeholder.jpg), loaded (evidence/http-image-1028/loaded.jpg), timing (evidence/http-image-1028/timing.json), settled dimensions (evidence/http-image-1028/geometry.json) | The sent user Markdown image first paints as an approximately 36px-high alt placeholder, then loads a large logo in frames 2–3. Earlier transcript content moves offscreen while the composer remains fixed, without scroll input. The HTTP PNG returns 200 and ultimately has 512×512 natural dimensions; space is not reserved for it. All 167 capture frames were reviewed. This is an image-loading variation of the geometry failure; the assistant image loads later but its separate transition was not isolated. Regression: `core-timeline-rendering-scroll.spec.ts` → `loading a user Markdown image preserves its reserved space and the existing transcript position`; red assertion **Expected <= 1; Received 474.8125** (height delta), same message row y=276→-79, composer y=691/height=52 unchanged. Red output (evidence/image-e2e-1062/red.log), geometry (evidence/image-e2e-1062/image-geometry.json), reviewed before (evidence/image-e2e-1062/image-before-load.png), reviewed after (evidence/image-e2e-1062/image-after-load.png). Shared fixme/register entry 43. Extended same regression: corrupt/loaded geometry and preview red (evidence/image-corrupt-1070/red.log), measurements (evidence/image-corrupt-1070/artifacts/image-geometry.json). Corrupt tile 645.75 × 37.1875px; valid image 512 × 512px; same row moves upward 416px with unchanged composer. Click opens no preview (`element(s) not found`). |
| 67 | Codex | Local web, fixed build | fail | Loading recording (evidence/codex-http-image-1029/capture.webm), placeholder (evidence/http-image-1029/placeholder.jpg), loaded (evidence/http-image-1029/loaded.jpg), review scope (evidence/http-image-1029/review-scope.json), scroll variation (evidence/codex-image-scroll-1029/capture.webm) | Frames 2–3 reproduce the OpenCode image-space failure: the user-message HTTP image expands from a roughly 36px alt placeholder into the logo and moves earlier content offscreen with the composer stationary. Sixteen loading samples and the full-size transition were reviewed. The later short-session wheel variation keeps visible images painted in all 71 reviewed frames, but does not complete the heavy-session chip/image case. This shares the image-loading geometry regression with OpenCode. Regression: `core-timeline-rendering-scroll.spec.ts` → `loading a user Markdown image preserves its reserved space and the existing transcript position`; red assertion **Expected <= 1; Received 474.8125** (height delta), same message row y=276→-79, composer y=691/height=52 unchanged. Red output (evidence/image-e2e-1062/red.log), geometry (evidence/image-e2e-1062/image-geometry.json), reviewed before (evidence/image-e2e-1062/image-before-load.png), reviewed after (evidence/image-e2e-1062/image-after-load.png). Shared fixme/register entry 43. Extended same regression: corrupt/loaded geometry and preview red (evidence/image-corrupt-1070/red.log), measurements (evidence/image-corrupt-1070/artifacts/image-geometry.json). Corrupt tile 645.75 × 37.1875px; valid image 512 × 512px; same row moves upward 416px with unchanged composer. Click opens no preview (`element(s) not found`). |
| 69 | Codex | Local web, fixed build | pass | Wheel and stop (evidence/codex-fast-scroll-stop/capture.webm), scrollbar and stop (evidence/codex-scrollbar-stop/capture.webm), stopped view (evidence/codex-scroll/stopped.png) | Shares the state 37 live run: the ordinary-session fast wheel/scrollbar movement settles on painted rows that remain stable after release. Rechecked scrollbar samples 0/10/21/31; the separate heavy-session and physical trackpad requirements are not inferred from this run. |
| 69 | OpenCode | Local web, fixed build | pass | Wheel and stop (evidence/opencode-long-scroll-stop/capture.webm), scrollbar and stop (evidence/opencode-scrollbar-stop/capture.webm), stopped view (evidence/opencode-scroll/stopped.png) | Shares the state 37 live run: the ordinary-session fast wheel/scrollbar movement settles on painted rows that remain stable after release. Rechecked scrollbar samples 0/5/10/15; the separate heavy-session and physical trackpad requirements are not inferred from this run. |
| 70 | Codex | Local web, fixed build | pass | Visit a (evidence/switch-codex-1001-a/capture.webm), Visit b (evidence/switch-codex-1001-b/capture.webm), Visit c (evidence/switch-codex-1001-c/capture.webm), row geometry (evidence/repeated-switch-1001/geometry.json) | Three settled round trips return the same measured rows to identical y positions without additional scroll input. Codex prose remains at y=431.5 and 701.5. This checks repeatable landing geometry; the separate answered-question and fold-replay findings remain. All captured transition frames were included in the contact-sheet review, with full-size spot checks. |
| 70 | OpenCode | Local web, fixed build | pass | Visit a (evidence/switch-opencode-1001-a/capture.webm), Visit b (evidence/switch-opencode-1001-b/capture.webm), Visit c (evidence/switch-opencode-1001-c/capture.webm), row geometry (evidence/repeated-switch-1001/geometry.json) | Three settled round trips return the same measured rows to identical y positions without additional scroll input. OpenCode prose remains at y=201 and 701; the folded header remains y=644 with height 32. This checks repeatable landing geometry; the separate answered-question and fold-replay findings remain. All captured transition frames were included in the contact-sheet review, with full-size spot checks. |
| 71 | Codex | Local web, fixed build | fail | Plan updates (evidence/codex-plan-updates/capture.webm), four complete (evidence/codex-todo/four-complete.jpg), settled without list (evidence/codex-todo/confirmed-progress.png) | The five named steps appear and advance, but the to-do surface disappears at completion instead of preserving a final list showing all five completed. E2E: `core-docks.spec.ts` extends the existing todo case, fixme/register 44. Red run (evidence/todo-e2e-1067/red.log), completed SSE (evidence/todo-e2e-1067/artifacts/completed-todo-events.txt), reviewed screenshot (evidence/todo-e2e-1067/artifacts/completed-todos-after-reload.png): `Expected: visible; Error: element(s) not found` after reload despite five completed steps delivered and a passing reply oracle. |
| 72 | Codex | Local web, fixed build | fail | Option selected (evidence/cross-tab-question/958-selected-indigo.png), submitted payload (evidence/cross-tab-question/958-submit-request.json), option result (evidence/cross-tab-question/958-completed.png), free-text result (evidence/codex-question/custom-completed.png), dismissed result (evidence/codex-question/dismissed-completed.png) | Indigo is selected and submitted as Indigo, free text preserves QA café 日本語 731, and dismissal completes without an error; none retains the required question card showing its resolved outcome. This maps the same card-retention finding as state 25, not a separate defect. Tier R: `real-harness-local.spec.ts`, fixme/register 45. Red log (evidence/question-retained-1068/red.log), real tool result (evidence/question-retained-1068/artifacts/question-tool-results.json), replay (evidence/question-retained-1068/replayed-messages.json), reviewed screenshot (evidence/question-retained-1068/artifacts/answered-question-after-reload.png): `Expected: visible; Error: element(s) not found` for the result card after reload; Staging delivery and reply oracles pass. |
| 72 | OpenCode | Local web, fixed build | fail | Option result (evidence/opencode-question/answered.png), custom input (evidence/opencode-question/custom-selected.png), custom result (evidence/opencode-question/custom-completed.png), dismissal recording (evidence/opencode-question-dismiss-submit/capture.webm), Failed dismissal (evidence/opencode-question/dismissed-failed.png), error detail (evidence/opencode-question/dismissed-expanded.png) | Option and Unicode custom answers are delivered but lack resolved question cards; dismissing instead shows Failed, with detail {type:aborted,message:The user dismissed this question}, and no cancellation reply. The dismissal error styling is distinct from the shared card-retention failure. |
| 74 | Codex | Local web, fixed build | fail | Pending command (evidence/codex-permission/pending.png), denial recording (evidence/codex-permission-deny/capture.webm), denied (evidence/codex-permission/denied.png), allow once (evidence/codex-permission-allow-once/capture.webm), allowed (evidence/codex-permission/allowed.png), filesystem results (evidence/codex-permission/filesystem-outcome.json) | In Untrusted mode the dock names the exact shell command and /tmp target; Deny produces QA_PERMISSION_DENIED and no file, while Allow once on a separate request creates its QA file and returns QA_PERMISSION_ALLOWED. However, opening the later child error replays that already-approved parent dock (stale dock (evidence/codex-permission/child-denied.png)); it also flashes repeatedly on a session round trip (recording (evidence/codex-resolved-permission-return/capture.webm), return frame (evidence/codex-permission/stale-return.jpg)) and clears without another answer. |
| 74 | OpenCode | Local web, fixed build | fail | Unnamed dock (evidence/opencode-permission/unnamed-request.png), denial (evidence/opencode-permission-deny/capture.webm), expanded denial error (evidence/opencode-permission/denied.png) | The permission dock displays only Permission required and action buttons, with no tool or target to review. Deny stops the turn and exposes The user declined this tool call inside an opaque failed-call row. |
| 75 | Codex | Local web, child workspace tab | fail | Child outcome and error (evidence/codex-permission/child-denied.png), parent spawn (evidence/codex-child-permission-start/capture.webm) | The child command settles to User declined the command and QA_CHILD_PERMISSION_DENIED without an observed child approval dock or a denial action in this QA run; its read-only tab offers only the completed failure. The source of that denial remains unestablished. Narrowed 2026-09-14 (register 79): a Tier M control proves the dock DOES mount on the parent for a child-session `permission.asked` in both auth modes — the decision path works, so the denial must be the runtime auto-denying without asking. A Tier R repro (spawn_agent child + `require_escalated` exec_command) is committed as fixme in `real-harness-local.spec.ts`; it currently `test.skip`s on the upstream codex missing-rollout churn (same class as the Pi session-file fix `20fdb18874`). |
| 76 | Codex | Local web, fixed build | pass | Refusal recording (evidence/codex-permission-ceiling-1037/capture.webm), clear reason (evidence/permission-ceiling-1037/refused.jpg), retained mode (evidence/permission-ceiling-1037/retained-mode.json), setup and scope (evidence/permission-ceiling-1037/setup-and-scope.json) | A disposable session created through POST /session with an ask ceiling starts Read only. Selecting Workspace write through the real picker produces a toast explaining that the auto mode widens the ask ceiling; both the picker and GET permission-mode retain read-only. All 13 captured frames reviewed. This verifies mode widening refusal, not a filesystem action or harness change under the ceiling. |
| 76 | Claude | Local web, fixed build | pass | Picker refusal (evidence/claude-ceiling-1039/capture.webm), reason (evidence/ceiling-prerequisites-1039/claude-refusal-dom.txt), retained default mode (evidence/ceiling-prerequisites-1039/claude-retained-mode.json), fixture creation (evidence/ceiling-prerequisites-1039/claude.json) | A disposable ask-ceiling session rejects Auto through the real permission picker with a toast explaining that auto widens the ask ceiling. GET permission-mode retains default. All ten frames reviewed. No model turn or filesystem action is claimed; the separate Claude authentication blocker remains. |
| 77 | Codex | Local web, fixed build | pass | Live child (evidence/live-child-994/working.png), live panel recording (evidence/codex-live-child-panel-994/capture.webm), completed child and parent (evidence/live-child-994/settled.png), DOM (evidence/live-child-994/settled-dom.txt) | Fixture 994 produces one child chip opening its own live transcript in the workspace panel; the chip changes to done, QA_CHILD_DONE_994 appears, and the parent continues with QA_PARENT_AFTER_CHILD_994. The child tool status remains wrong after termination, covered by state 19 rather than by this chip-flow verdict. |
| 77 | OpenCode | Local web, fixed build | pass | Task-like tool recording (evidence/opencode-task-like-1005/capture.webm), completed view (evidence/opencode-tools-1003/task-like-completed.png), DOM (evidence/opencode-tools-1003/task-like-dom.txt) | Fixture 1005 invokes one task-like tool and completes with QA_OC_PARENT_1005; no subagent chip or workspace child tab appears, matching the brief's OpenCode surface contract. The tool remains an opaque Call row, already covered by the missing-tool-data failure. Reviewed frames 0/60/140/220/300/372 and the full-size completed view. |
| 81 | Codex | Local web, fixed build | pass | A (evidence/codex-heavy-reading-a/capture.webm), B (evidence/codex-heavy-reading-b/capture.webm), C (evidence/codex-heavy-reading-c/capture.webm), D (evidence/codex-heavy-reading-d/capture.webm), E (evidence/codex-heavy-reading-e/capture.webm), F (evidence/codex-heavy-reading-f/capture.webm), geometry (evidence/codex-heavy-reading-a/traversal.json) | All 960 captured frames were reviewed with no blank rows during the top-to-bottom wheel traversal. The initial 35 inputs use a faster unpaced variation; the remaining 200 use 0.1-page increments with 200ms pacing. The endpoint is the exact 19548px bottom for a 20251px history and 703px viewport. This pass concerns wheel scrolling; Home-key blanking remains a failure in state 84. |
| 81 | OpenCode | Local web, fixed build | pass | Wheel pass 1 (evidence/opencode-heavy-reading-wheel-1/capture.webm), pass 2 (evidence/opencode-heavy-reading-wheel-2/capture.webm), pass 3 (evidence/opencode-heavy-reading-wheel-3/capture.webm), scroll positions (evidence/opencode-heavy/wheel-steps.json), bottom (evidence/opencode-heavy/wheel-bottom.png) | With previous history expanded, 230 small wheel steps traverse the heavy session from scrollTop 0 to 18923, the bottom of a 19626px transcript with a 703px viewport; all 597 captured frames reviewed in contact sheets show populated rows with no observed blanking at reading speed. Tool groups retain their current fold states. |
| 84 | Codex | Local web, fixed build | fail | Full-history Home recording (evidence/codex-heavy-full-home/capture.webm), End recording (evidence/codex-heavy-full-end-last/capture.webm), Home endpoint (evidence/codex-heavy-keys/full-home.png), End endpoint (evidence/codex-heavy-keys/full-end.png), geometry (evidence/codex-heavy-keys/full-geometry.json), blank frame (evidence/codex-heavy-keys/full-home-blank.jpg) | End → Home → End starts at 43427px in the fully loaded 44130px history. Home stops at 39779.5px instead of zero and shows a blank transcript in captured frames 5–10; the history height becomes 42683px. The final End reaches the exact 41980px bottom with the last reply and composer visible. All 14 Home and 35 End frames were reviewed. The earlier recent-tail reproduction remains supplemental evidence. Shared endpoint regression: unsigned-local run 1102 fails Home at 12777px with the first UserMessage absent; End passes. Fixme/register 56; red log (evidence/home-end-1102/red.log). Transient blanking and harness-specific variations remain uncovered. Test-user twin 1106 also fails Home at 12778px and passes End; both endpoint screenshots reviewed. Log (evidence/home-end-1106/red.log). |
| 84 | OpenCode | Local web, fixed build | fail | First End (evidence/opencode-heavy-end-expanded-history/capture.webm), Home (evidence/opencode-heavy-home-expanded-history/capture.webm), second End (evidence/opencode-heavy-end-again/capture.webm), settled positions (evidence/opencode-heavy/keyboard-endpoints.json) | With transcript focus and previous history expanded, End settles at 13080.5 of 19028px, Home at 12847.5 rather than zero, and only the second End reaches the bottom at 18923px; each action has a ten-second observation window. Endpoint navigation fails; the cause remains unestablished. Shared endpoint regression: unsigned-local run 1102 fails Home at 12777px with the first UserMessage absent; End passes. Fixme/register 56; red log (evidence/home-end-1102/red.log). Transient blanking and harness-specific variations remain uncovered. Test-user twin 1106 also fails Home at 12778px and passes End; both endpoint screenshots reviewed. Log (evidence/home-end-1106/red.log). |
| 85 | Codex | Local web, fixed build | pass | Scroll-to-latest recording (evidence/codex-heavy-scroll-latest-full/capture.webm), top (evidence/codex-heavy-latest/top.png), bottom (evidence/codex-heavy-latest/end.png), geometry (evidence/codex-heavy-latest/geometry.json) | With the full 44130px history loaded, Scroll to latest message moves from zero to 43427px, the exact bottom for a 703px viewport. All 12 captured transition frames were reviewed and show content at the landing; QA_COMPLETE_DONE_987 is present. |
| 85 | OpenCode | Local web, fixed build | pass | Scroll-to-latest recording (evidence/opencode-heavy-scroll-latest/capture.webm), settled endpoint (evidence/opencode-heavy/scroll-latest.json), last reply (evidence/opencode-heavy/scroll-latest.png) | From scrollTop zero in the expanded heavy history, Scroll to latest message reaches 18923px, the exact bottom; all 13 captured transition frames show content rather than a blank landing, with QA_OC_SOAK_DONE and the composer visible at rest. |
| 86 | Codex | Local web, heavy session | pass | Streaming scroll (evidence/codex-stream-scroll-995/capture.webm), before (evidence/stream-scroll-995/before.png), settled (evidence/stream-scroll-995/settled.png), message timing (evidence/stream-scroll-995/replay.json) | Eight small wheel movements through older turns during fixture 995 leave the reading region painted; two tool-result timestamps fall within the 17.96-second recording, and settlement leaves the same older messages visible instead of pulling to the latest reply. Reviewed frames 0, 30, 60, 90, 150, 250, 400 and 595, plus full-size before and settled views; this is the observed local sample, not exhaustive frame-by-frame certification of every streaming path. |
| 86 | OpenCode | Local web, heavy session | pass | Streaming scroll (evidence/opencode-stream-scroll-996/capture.webm), before (evidence/stream-scroll-996/before.png), settled (evidence/stream-scroll-996/settled.png), message timing (evidence/stream-scroll-996/replay.json) | Eight small wheel movements through older turns during fixture 996 leave the reading region painted; two tool-result timestamps fall within the 23.98-second recording, and settlement leaves the same older messages visible instead of pulling to the latest reply. Reviewed frames 0, 30, 60, 100, 200, 400, 600 and 797, plus full-size before and settled views; this is the observed local sample, not exhaustive frame-by-frame certification of every streaming path. |
| 87 | OpenCode | Local web, fixed build | pass | Folded scroll (evidence/opencode-dense-folded-scroll/capture.webm), expansion (evidence/opencode-dense-expand/capture.webm), expanded scroll (evidence/opencode-dense-expanded-scroll/capture.webm) | The adjacent completed turns 982 and 986 are traversed folded, then expanded and traversed again using twelve 0.1-page wheel inputs down and twelve up with 200ms pacing. All 234 captured frames were reviewed without blank rows; both work groups remain expanded on return. Existing opaque tool labels and missing resolved question content are separate recorded failures. |
| 87 | Codex | Local web, fixed build | pass | Folded scroll (evidence/codex-dense-folded-scroll/capture.webm), expansion (evidence/codex-dense-expand/capture.webm), expanded scroll (evidence/codex-dense-expanded-scroll/capture.webm) | The region containing fixtures 981, 983 and 985 is scrolled down and back with all three work groups folded, then with those same groups expanded. All 308 captured frames were reviewed: opening and scrolling the groups introduces no blank rows, and all three remain expanded on return. Each scroll uses twelve 0.1-page wheel inputs in each direction with 200ms pacing. |
| 88 | Codex | Local web, heavy session | fail | Large output (evidence/codex-heavy/large-output.png), expansion and round trip (evidence/codex-heavy-output-retention/capture.webm), observed control states (evidence/codex-heavy/output-retention.json), returned (evidence/codex-heavy/output-returned.png) | The 300-line output changes to Show less when expanded, but scrolling four pages away and back restores Show all; the manually expanded output state is lost when its transcript region leaves the viewport. Tier M regression now reproduces the same loss in both auth modes: expected Show less, received Show all after confirmed row unmount; outer accordion stays expanded. Fixme/register 57; test-user log (evidence/output-retention-1103/red.log), unsigned log (evidence/output-retention-1104/red.log). All six stage screenshots reviewed. |
| 89 | OpenCode | Local web, heavy session | pass | Scroll away (evidence/opencode-heavy-image-scroll-1030/capture.webm), Return to images (evidence/opencode-heavy-image-return-1030/capture.webm), Review scope (evidence/heavy-image-1030/review-scope.json) | The loaded HTTP logo in the existing heavy transcript remains painted while visible, leaves view while scrolling older turns, and re-enters fully painted without an empty-image phase. All 125 outgoing and 34 returning frames were reviewed. Inputs were paced at 200ms. OpenCode has no subagent-chip surface per the brief; the separate file-image failure is still recorded under state 24. |
| 89 | Codex | Local web, heavy session | pass | Scroll through chips and images (evidence/codex-heavy-chip-image-scroll-1031/capture.webm), Return (evidence/codex-heavy-chip-image-return-1031/capture.webm), Review scope (evidence/heavy-image-1031/review-scope.json) | The existing heavy session includes a new completed child chip and HTTP image beside older completed child chips. On paced wheel scrolling through older turns and back, chips remain labeled and images re-enter painted without an empty-box phase. All 118 outgoing and 81 returning frames were reviewed; return frame 61 was also checked full-size. This covers loaded HTTP images; file-image loading remains broken under state 24. |
| 90 | Codex | Local web, heavy session | pass | Early scroll (evidence/codex-heavy-soak-early-scroll/capture.webm), ten-minute scroll (evidence/codex-heavy-soak-ten-minute-scroll/capture.webm), early metrics (evidence/heavy-soak/baseline.json), late metrics (evidence/heavy-soak/ten-minute.json) | With the foreground timed stream active for ten minutes, the repeated wheel sample shows no observed worsening or persistent stopped blanking; heap usage is 288.4 MiB early and 73.4 MiB late. The early sample is approximately two minutes into the run, and temperature is unmeasured. |
| 90 | OpenCode | Local web, heavy session | pass | Early scroll (evidence/opencode-heavy-soak-early-scroll/capture.webm), ten-minute scroll (evidence/opencode-heavy-soak-ten-minute-scroll/capture.webm), early metrics (evidence/heavy-soak/baseline.json), late metrics (evidence/heavy-soak/ten-minute.json) | The timed foreground run lasts over ten minutes, with no observed scrolling deterioration or persistent stopped blanking in the repeated sample; heap usage after closing the file panel is 74.0 MiB early and 71.8 MiB late. The early sample is approximately two minutes into the run, and temperature is unmeasured. |
| 93 | OpenCode | Local web, fixed build | fail | Return recording (evidence/opencode-heavy-idle-return/capture.webm), scroll geometry (evidence/opencode-heavy/idle-return.json), returned view (evidence/opencode-heavy/idle-return.png) | The idle heavy session is left at scrollTop 13340 and returns to exactly 13340 after switching to Codex and back, instead of the top required by the brief. All 71 captured frames were reviewed; the workspace panel also closes during the return transition. |
| 93 | Codex | Local web, fixed build | fail | Return recording (evidence/codex-heavy-idle-return/capture.webm), scroll geometry (evidence/heavy-soak/codex-idle-return.json), returned view (evidence/heavy-soak/codex-idle-return.png) | The idle heavy session is left at scrollTop 13127 and returns to exactly 13127 after switching to OpenCode and back, instead of the top required by the brief. All 22 frames were reviewed; frame 4 additionally shows a blank transcript while opening OpenCode, followed by populated content. |
| 94 | Codex | Local web, fixed build, first visit and reload | fail | First visit (evidence/codex-first-open-1022/capture.webm), reload (evidence/codex-first-reload-1022/capture.webm), creator already settled (evidence/first-open-1022/creator-settled.png), first-visit replay (evidence/first-open-1022/first-open-replayed.jpg), reload replay (evidence/first-open-1022/reload-replayed.jpg), timing (evidence/first-open-1022/timing.json), completed response (evidence/first-open-1022/replay.json) | A two-group session is created and completed in a second tab before its first visit in the original tab. First-visit frames 1–3 show the correct fold, then frames 4–10 unfold/replay the completed turn with Thinking before frame 11 folds it again. Reload frame 4 shows a Running row and Thinking on that completed response; the turn folds at frame 18. All 12 first-visit and 20 reload frames reviewed, with full-size checks of frame 4 in each. Shares the completed-turn replay/fold regression at state 46. This tests a first tab visit within the same app installation, not an isolated fresh installation. |
| 94 | OpenCode | Local web, fixed build, first visit and reload | pass | First visit (evidence/opencode-first-open-1023/capture.webm), reload (evidence/opencode-first-reload-1023/capture.webm), creator settled (evidence/first-open-1023/creator-settled.png), all captured frames (evidence/first-open-1023/review.jpg), stored response (evidence/first-open-1023/replay.json) | A completed two-group session generated in a second tab is first opened from the original rail. The first captured transcript paint is folded with its final title and no Thinking; it stays folded through subsequent captured frames and reload. All six frames from each recording reviewed, with first-visit frames 1–2 checked full-size. Reload includes initial logo/empty-shell loading before transcript paint; this pass covers fold/title/terminal state once transcript content appears, not the broader loading experience. Select agent briefly precedes Big Pickle without a fold change. Same-installation first tab visit, not an isolated fresh installation. |
| 95 | Codex | Local web, fixed build, five-session burst | pass | Recording (evidence/five-session-rapid-1024/capture.webm), setup/action times/final drafts (evidence/rapid-switch-1024/states.json), all captured frames (evidence/rapid-switch-1024/review.jpg) | Two cycles across five QA sessions complete ten clicks in 750ms, with individual actions 58–105ms. Every captured title, transcript and draft corresponds to its selected session; all five stored drafts match their own QA markers on subsequent visits. All 17 captured frames reviewed. Markers are then cleared and each empty composer verified. This covers warm idle sessions and draft/title isolation; live-session Thinking isolation and cold unvisited-load variations remain unverified. Codex own-session folding replay is covered separately at states 46/58/94. |
| 95 | OpenCode | Local web, fixed build, five-session burst | pass | Recording (evidence/five-session-rapid-1024/capture.webm), setup/action times/final drafts (evidence/rapid-switch-1024/states.json), all captured frames (evidence/rapid-switch-1024/review.jpg) | Two cycles across five QA sessions complete ten clicks in 750ms, with individual actions 58–105ms. Every captured title, transcript and draft corresponds to its selected session; all five stored drafts match their own QA markers on subsequent visits. All 17 captured frames reviewed. Markers are then cleared and each empty composer verified. This covers warm idle sessions and draft/title isolation; live-session Thinking isolation and cold unvisited-load variations remain unverified. Codex own-session folding replay is covered separately at states 46/58/94. |
| 96 | OpenCode | Local web, fixed build | fail | Switch recording (evidence/opencode-running-navigation-return/capture.webm), returned view (evidence/running-navigation/opencode-return.png), destination snapshots (evidence/running-navigation/opencode.json), live process (evidence/running-navigation/opencode-process.json), completed (evidence/running-navigation/opencode-settled.png) | Switching away and back during sleep 60 restores Thinking and Stop but no running tool row, while PID 97962 is observed alive at 48 seconds; after completion a Call f5c3981f84894a339a3357e6 row and QA_OC_NAV_COMPLETE_946 appear. The idle destination has no Thinking; running-row retention fails before shimmer or folding can be assessed. |
| 96 | Codex | Local web, fixed build | fail | Return recording (evidence/codex-running-return-1018/capture.webm), idle destination (evidence/running-navigation-1018/away.png), returned (evidence/running-navigation-1018/returned.png), DOM (evidence/running-navigation-1018/returned-dom.txt), completed (evidence/running-navigation-1018/completed.png), stored tool (evidence/running-navigation-1018/replay.json) | During sleep 35, switching to idle OpenCode shows no busy indicator; returning restores Running, its timer, Thinking and Stop but the command is visually absent although present in the DOM. Reviewed captured frames 0/30/60/90/120/165 and the full-size returned screenshot. The command and final reply become visible on completion. Shares the state 12 invisible running-command regression. This single-group fixture does not establish multi-group folding retention. The exact-process query returned no match and is not used as liveness evidence. |
| 97 | Codex | Local web, fixed build | fail | Return recording (evidence/codex-question-cross-tab-return/capture.webm), pending before leaving (evidence/cross-tab-question/pending.png), submitted in second tab (evidence/cross-tab-question/answered-in-second-tab.png), first flash (evidence/cross-tab-question/first-pending-flash.jpg), second flash (evidence/cross-tab-question/second-pending-flash.jpg), frame timing (evidence/cross-tab-question/return-timing.json), settled (evidence/cross-tab-question/settled.jpg) | After submitting the pending question in a second tab while the original tab displays another session, returning repaints the question twice (frames 6–37 and 50–77) before settling to the completed answer; all 131 captured frames were reviewed. |
| 97 | OpenCode | Local web, fixed build, answer in second tab | fail | Return recording (evidence/opencode-question-return-1011/capture.webm), already answered elsewhere (evidence/question-navigation-1011/answered-other-tab.png), pending replay (evidence/question-navigation-1011/pending-replayed.jpg), resolved (evidence/question-navigation-1011/resolved.jpg), observations (evidence/question-navigation-1011/observations.json) | With the original tab visibly on Codex, Beta is submitted in a second OpenCode tab and its completed reply is visible before return. On returning, captured frames 1–8 restore the pending question with options and Submit (0.062–0.308s); frame 9 at 0.632s replaces it with the completed Beta reply. All 13 captured frames reviewed. Shares the stale-question navigation regression with Codex state 97; missing answered-card rendering remains state 25. No second answer submitted. |
| 98 | Codex | Local web, fixed build | pass | Navigation recording (evidence/codex-permission-navigation/capture.webm), returned dock (evidence/codex-permission/returned-pending.png), denied afterward (evidence/codex-permission/denied.png) | Switching to OpenCode and back preserves the pending approval command and Deny/Allow controls; denying after return completes the turn with the actual denial reported. |
| 98 | OpenCode | Local web, fixed build | pass | Switch away (evidence/opencode-permission-away-998/capture.webm), other session visibly loaded (evidence/permission-navigation-998/other-session.png), return (evidence/opencode-permission-return-998/capture.webm), returned dock (evidence/permission-navigation-998/returned.png), deny (evidence/opencode-permission-deny-998/capture.webm), denial result (evidence/permission-navigation-998/denied.png), file absence (evidence/permission-navigation-998/filesystem-outcome.json) | Fixture 998 preserves the pending dock through a visibly completed switch to Codex and back; Deny clears it, returns an idle composer, and exposes The user declined this tool call without creating the requested file. The unnamed dock remains the separate state 74 failure. Fixture 997 was excluded from navigation proof because its rapid round trip never visibly painted the other session. Reviewed away frames 0/5, return 0/40/127, and deny 0/12/24 plus live full-size views. |
| 99 | Codex | Local web, workspace subagent panel | fail | Selected child before leaving (evidence/child-grouping/child-c.png), leave recording (evidence/codex-child-tab-navigation/capture.webm), return recording (evidence/codex-child-tab-return/capture.webm), settled panel (evidence/child-grouping/navigation/returned.png), DOM (evidence/child-grouping/navigation/returned-dom.txt) | Returning from the fifth QA session preserves all three child tabs but selects Review instead of the previously selected QA_CHILD_C_932 transcript; all 19 return frames were reviewed and the settled panel still shows Review. This repeats the selection defect in state 38; Browser-tab persistence remains unverified. |
| 99 | Codex | Local desktop, dev recipe, Browser panel | fail | Loaded local page (evidence/child-grouping/browser/desktop-before.png), other session (evidence/child-grouping/browser/desktop-other.png), returned blank Browser (evidence/child-grouping/browser/desktop-return.png), recorded repeat (evidence/desktop-browser-tab-return/capture.webm), returned state (evidence/child-grouping/browser/desktop-return-state.txt) | A Browser tab opened through Add workspace tab loads http://localhost:4446; switching to the fifth QA session and back retains the tab but clears its address and page, reproduced twice. The five-frame repeat documents settled destinations, not transient-frame absence. Entry through a transcript Local preview row remains unverified. |
| 100 | Codex | Local web, workspace file panel | fail | Original package file (evidence/file-session-scope/codex-file.png), OpenCode file (evidence/file-session-scope/opencode-file.png), Codex returned (evidence/file-session-scope/codex-returned.png), return recording (evidence/file-panel-session-return/capture.webm) | Codex opens packages/agent-event-runtime/package.json; after OpenCode opens CLAUDE.md, returning to Codex displays CLAUDE.md instead of its prior file. |
| 100 | OpenCode | Local web, workspace file panel | fail | Original CLAUDE.md (evidence/file-session-scope/opencode-file.png), OpenCode returned (evidence/file-session-scope/opencode-returned.png) | Selecting the package.json tab in Codex and returning to OpenCode replaces its CLAUDE.md selection with packages/agent-event-runtime/package.json, confirming the same shared-selection finding in reverse. |
| 100 | Cursor | Local web, workspace file panel | fail | Cursor original (evidence/file-scope-1041/cursor-first.png), Claude selection (evidence/file-scope-1041/claude-selected.png), Cursor returned (evidence/file-scope-1041/cursor-return.png), recording (evidence/file-scope-return-1041/capture.webm), DOM (evidence/file-scope-1041/cursor-return-dom.txt) | Cursor opens CLAUDE.md; Claude then selects README.md; returning to Cursor restores README.md rather than its own prior selection. All ten return frames reviewed. This shares the existing state 100 selection-isolation finding. Both previews show Failed to fetch, so successful file-content rendering is not claimed. |
| 100 | Claude | Local web, workspace file panel | fail | Claude original (evidence/file-scope-1041/claude-selected.png), Claude returned (evidence/file-scope-1041/claude-return.png), reverse recording (evidence/file-scope-reverse-1041/capture.webm), DOM (evidence/file-scope-1041/claude-return-dom.txt) | After Cursor selects CLAUDE.md, returning to Claude replaces its README.md selection with CLAUDE.md. All five reverse frames reviewed. This is the same shared file-selection finding as Codex/OpenCode, not a separate regression; failed file-preview loading remains an additional observed limitation. |
| 101 | Codex | Local web, fixed build | pass | Direct URL recording (evidence/codex-session-deep-link/capture.webm), selected session (evidence/deep-links/codex.png), rail positions (evidence/deep-links/rail.json) | Opening the session URL from the other session reaches the correct transcript and highlights its rail row; the three QA rail rows remain at y=183, 215 and 247 before and after navigation. This verdict covers destination and rail stability after load; the recording also shows startup spinner/composer transitions before the transcript settles. |
| 101 | OpenCode | Local web, fixed build | pass | Direct URL recording (evidence/opencode-session-deep-link/capture.webm), selected session (evidence/deep-links/opencode.png), rail positions (evidence/deep-links/rail.json) | Opening the session URL from the other session reaches the correct transcript and highlights its rail row; the three QA rail rows remain at y=183, 215 and 247 before and after navigation. This verdict covers destination and rail stability after load; the recording also shows startup spinner/composer transitions before the transcript settles. |
| 102 | Codex | Local web, fixed build | pass | Archive recording (evidence/codex-archive-click/capture.webm), before (evidence/codex-archive/before.png), after (evidence/codex-archive/after.png), URL and rail snapshots (evidence/codex-archive/states.json) | Archiving the completed disposable Codex fixture 988 removes its row, selects the neighbouring heavy Codex session, and preserves the relative order of all remaining rail rows. All 56 captured frames show populated transcript content rather than a blank pane. Stale tool-state replay in the destination is recorded separately below. |
| 102 | OpenCode | Local web, fixed build | fail | After archive click (evidence/archive-navigation/opencode.png), unchanged route and rail (evidence/archive-navigation/opencode.json), capture (evidence/opencode-archive-active-session/capture.webm) | After the disposable QA_ARCHIVE_READY_619 session completes, clicking its rail Archive button leaves that row, the active URL and its transcript unchanged through a ten-second observation window; no JavaScript confirmation dialog is present. The capture has one unchanged frame, so it proves no transition timing. Archive completion and neighbour selection are not reached. |
| 102 | Claude | Local web, fixed build | pass | Archive recording (evidence/claude-archive-visible-1045/capture.webm), destination (evidence/archive-1045/claude-after.png), destination DOM (evidence/archive-1045/claude-after-dom.txt), frame review (evidence/archive-1045/claude-archive-visible-1045-review.jpg) | Archiving the disposable Claude fixture 1039 removes its row, selects the neighbouring populated Codex fixture 1038 and preserves the relative order of the remaining rail rows. All 16 captured frames reviewed. This establishes the archive destination and rail-order rule; delayed permission-control appearance is not covered by this pass. |
| 102 | Cursor | Local web, fixed build | pass | Archive recording (evidence/cursor-archive-1046/capture.webm), destination (evidence/archive-1045/cursor-after.png), destination DOM (evidence/archive-1045/cursor-after-dom.txt), frame review (evidence/archive-1045/cursor-archive-1046-review.jpg) | Archiving the disposable Cursor fixture 1039 removes its row, selects the neighbouring populated Codex fixture 1038 and preserves the relative order of the remaining rail rows. All 10 captured frames reviewed. This establishes the archive destination and rail-order rule; delayed permission-control appearance is not covered by this pass. |
| 103 | Codex | Local web, fixed build | fail | History destinations (evidence/rail-navigation/history.json), blank destination (evidence/rail-navigation/back-leaves-app.png) | After rail navigation OpenCode to Codex, browser Back leaves Claxedo for about:blank rather than restoring OpenCode; Forward reloads the current Codex URL. This repeats twice. The attempted recording stops because CDP is unavailable on non-HTTP pages, so only the observed destination log and screenshot are retained.  Regression: `core-session-rendering-navigation` / `browser Back and Forward preserve a chat visit made through the rail`. Red assertion (evidence/history-e2e-1049/red.log): expected `/w/ws_mock_runtime/session`, received `/s/ses_browser_history` (10000ms timeout); one failed, no retries. Shared fixme, register 42. |
| 103 | OpenCode | Local web, fixed build | fail | Navigation destinations (evidence/opencode-browser-history/steps.json), Back leaves app (evidence/opencode-browser-history/back.png), Forward recovery (evidence/opencode-browser-history/forward.png) | After rail selections through heavy OpenCode, archive fixture and fifth fixture, Back goes to about:blank instead of the archive fixture; Forward reloads the fifth fixture and shows QA_FIFTH_READY_827. This is static destination evidence; no motion claim is made because the capture API cannot record the non-HTTP destination.  Regression: `core-session-rendering-navigation` / `browser Back and Forward preserve a chat visit made through the rail`. Red assertion (evidence/history-e2e-1049/red.log): expected `/w/ws_mock_runtime/session`, received `/s/ses_browser_history` (10000ms timeout); one failed, no retries. Shared fixme, register 42. |
| 103 | Claude | Local web, fixed build | fail | Sequence (evidence/history-1047/result.json), Back recording (evidence/history-sampled-1048/capture.webm), all sampled frames (evidence/history-1047/sampled-review.jpg), destination (evidence/history-1047/second-back.png), Forward (evidence/history-1047/forward.txt) | After visiting Claude and selecting Codex 1038 then 1037 through the rail, two Back steps skip Claude and reach New Session. Forward returns to 1038. A repeated second Back is captured in 25 screenshots over 0.67 seconds; all sampled frames reviewed. This shares the history-entry failure already observed on Codex/OpenCode. The capture does not prove absence of shorter flickers.  Regression: `core-session-rendering-navigation` / `browser Back and Forward preserve a chat visit made through the rail`. Red assertion (evidence/history-e2e-1049/red.log): expected `/w/ws_mock_runtime/session`, received `/s/ses_browser_history` (10000ms timeout); one failed, no retries. Shared fixme, register 42. |
| 104 | Codex | Two local web tabs, fixed build | fail | Sender (evidence/two-tab-sync/codex-sender.png), receiver (evidence/two-tab-sync/codex-receiver.png), rail geometry (evidence/two-tab-sync/codex-rail-geometry.json), receipt recording (evidence/codex-second-tab-receive/capture.webm) | Both tabs show one QA_TWO_TAB_CODEX_731 reply, but sending moves Codex from y=215 to y=183 only in the sender; the receiver retains OpenCode first and Codex second after completion. Tier R regression in `real-harness-local.spec.ts`, fixme/register 46: red log (evidence/rail-sync-1073/red.log), before/after order (evidence/rail-sync-1073/artifacts/rail-orders.json), sender (evidence/rail-sync-1073/artifacts/rail-sender.png), receiver (evidence/rail-sync-1073/artifacts/rail-receiver.png). Both reply oracles pass; receiver first-row assertion expects older `dca246b4-2544-4b91-9d45-87cb38f39039`, receives newer `8fb8a83f-1be5-45c1-98c5-653cbdf3b3db` for 15s. |
| 104 | OpenCode | Two local web tabs, fixed build | fail | Receiver recording (evidence/opencode-second-tab-receive-1000/capture.webm), sender (evidence/two-tab-sync-1000/sender.png), receiver (evidence/two-tab-sync-1000/receiver.png), before order (evidence/two-tab-sync-1000/before.json), after order (evidence/two-tab-sync-1000/after.json), exact counts (evidence/two-tab-sync-1000/counts.json) | Fixture 1000 delivers one prompt and one QA_TWO_TAB_OC_1000 reply to each tab. Sending moves OpenCode from y=215 to y=183 in the sender, but the receiver retains it at y=247 after completion. The tabs already differ on initial rail order; receipt does not reconcile them. This shares the Codex cross-tab rail regression. Reviewed receiver frames 0/20/40/60/80/100/120/129 and the live completed view. |











These verdicts cover the specified steering behavior only, not all layout invariants. Codex session: `/s/239d35f0-b59f-4178-be03-874a29361a59`. Claude session: `/s/ef231b35-5a36-41b7-8ace-3c177122f07a`.

OpenCode state 2 uses `/s/ses_f68e2f7d2ffepUgk0se1qGgGi0` and the configured Big Pickle model. The initial attempt accepts the addition but backgrounds the command. The foreground variation is the decisive failure: the read-only `session_messages` entrypoint returns the final text while computer-use inspection shows it absent. The evidence JSON retains only ordinary text parts, not reasoning. The local server listener remains PID 71888 during these observations; no server restart was performed by this pass.

The missing foreground reply becomes visible during the next turn's three-message burst, captured in `evidence/opencode-order-burst/capture.webm`. Reload and switching back had not recovered it. The next ordering reply then exhibits the same missing-at-idle behavior. These are two state failures backed by two concrete reproductions of one response-rendering finding; the regression should exercise that actual implementation path rather than invent an out-of-order model response (stored ordering is correct).

States 5 and 6 were observed in the recording of state 3's initiating send, not in a separate synthetic setup. This is a concrete reproduction of the user's composer-independent shift: compare frames 26 (0.57 seconds) and 79 (1.81 seconds), captured without scrolling. The composer position stays visually stable while the Thinking row relocates and prior transcript content moves. DOM geometry measurement and causal diagnosis remain for the regression work. State 4 has been attempted by closing and reopening the web tab during an in-flight addition; Codex restores the addition and renders RECOVERED_PURPLE. Native process termination and backend restart have not been tested, so the full restart criterion remains outstanding.

An additional tool-rendering failure was observed during that run: the row is named `Call 27c8b6e7ec8c41eba4205026`, and expanding it reveals no command or output. Expanded row evidence (evidence/opencode-steer/tool-details.png). The surrounding response claims `sleep 35` was started. The tool's authoritative identity still needs inspection before choosing its regression fixture. This finding will be mapped to the appropriate tool-row state when that state is exercised; it must not be counted twice.

### User-supplied regression evidence

| State | Harness | Surface | Verdict | Evidence | Observation |
| --- | --- | --- | --- | --- | --- |
| 25, 64 | Not identified from recording | Local desktop | fail | Recording (evidence/question-return/user-recording.mov), pending question (evidence/question-return/pending-flash.png), after disappearance (evidence/question-return/stale-running.png) | Returning to the session titled “there is a minimize button on question dock, that is not working” briefly renders an already-answered question with unselected options and Submit/Dismiss controls at approximately 2.4–2.5 seconds, then removes it. |
| 58 | Not identified from recording | Local desktop | fail | Second recording (evidence/content-shift/user-recording.mov), loading title (evidence/content-shift/title-loading.png), settled title (evidence/content-shift/title-settled.png) | At 4.12–4.49 seconds the title-loading logo disappears and the same title text moves left by approximately 20 pixels in the 1280-pixel-wide extracted frames, while the question and sidebar stay in place. |
| 70 | Not identified from recording | Local desktop | fail | Answered content (evidence/content-shift/answered-before.png), pending again (evidence/content-shift/pending-again.png), composer returns (evidence/content-shift/composer-return.png) | At 12.73–13.48 seconds on return, the answered card paints, the large pending question replaces it, then the composer returns and the same answered card lands approximately 84 pixels higher in the extracted frames. |

The user supplied this recording on September 13. Its frames were inspected; independent live reproduction and the failing test remain pending. These two checks share one finding and will receive one regression test. State 97 requires a different sequence (resolving while away) and is not verified by this recording.

The same recording also shows “Running command” and “Thinking” after the question disappears, alongside a title-loading indicator. Their correctness requires checking the session's actual lifecycle; they are observations, not yet separately confirmed failures.

## Harness prerequisites

Use the [fresh app and version checks](current-status.md). Do not reuse earlier Claude authentication failures or the old Pi version refusal as current blockers. Other harnesses were not freshly qualified by this targeted rerun.

The matching existing test home is `core-docks.spec.ts`; its ordinary answer test waits for the question dock to disappear after submitting. That eventual-state assertion does not establish absence of a transient pending dock on session return. The new test must observe the entire navigation transition and fail on any visible pending frame for the answered request, even if the final state is correct.

Historical note: this observation predates Phase 2. Later work records 34 reproduced families and 35 qualified failing regression cases, plus scoped green controls; see the handoff for the retained results. No full acceptance gate passes.

The second recording is 14.98 seconds long and was inspected using 80 evenly sampled frames plus full-size inspection of the transitions above. Pixel estimates describe those extracted frames, not a DOM geometry measurement. Its answered-to-pending transition is additional evidence for the original question finding, not an additional independent question bug. The application shows active work elsewhere in this clip, so ordinary streaming growth must be distinguished from the navigation shifts when reproducing it.

The user additionally reports upward transcript movement while the composer is already visible. Composer remounting is therefore not an established explanation for the transcript movement finding. Reproduction must track a named transcript row and the composer rectangle independently, including a transition in which the composer stays mounted at the same height. The Codex state 6 recording above now reproduces this variation with the composer visibly stationary. The causal mechanism and DOM geometry regression remain unverified; this is not yet a separately proven root cause.

## Historical run notes

The sections below retain the sequence of observations and experiments. Present-tense wording and intermediate totals belong to the run described, not to current status. Use the fresh status and handoff above for the latest retained conclusions.

## Launch and recording notes

The `claxedo-app` development recipe starts successfully but concurrent hot reloads twice crashed the page before sending with `Permission context must be used within a context provider`. Neither interrupted attempt counts as a transcript verdict. The added `claxedo-app-qa-preview` recipe runs the existing production build and preview commands on port 4446 against the real local server; build completed in 16.22 seconds. This is a QA launch adjustment, not an application fix.

Live browser captures use Chrome's supported screencast through computer-use, with per-frame timestamps. Video is resampled to 30 fps using those timestamps. Selected frames and final screenshots are visually reviewed; sampling alone does not establish absence of a one-frame flash. No application or runtime events are mocked in these live runs.

The separate Codex Escape attempt in `evidence/codex-stop-race/capture.webm` did not stop the 60-second command; its follow-up remained an unsent draft until Return was pressed later. This is not counted as a scoped-Stop pass and needs a keyboard-path regression investigation. The button-path verdict above uses a fresh reproduction.

OpenCode web-tab closure preserved RECOVERED_TEAL in stored messages, but it was absent from the idle page after returning: page (evidence/opencode-recovery/idle-missing-reply.png), stored text (evidence/opencode-recovery/stored-visible-text.json). Starting the later Stop test revealed it. Native app termination and server restart remain untested.

Codex state 12: the running card can be opened by clicking its visible row and shows `$ sleep 60`. At one observed frame the collapsed row showed only Running and elapsed time despite the DOM snapshot containing the command. The exact first-frame naming criterion needs frame review before a verdict. The semantic locator attempt timed out, then a screenshot-grounded click succeeded; that locator failure is not counted as an application bug.

Attachment removal observation: in OpenCode, clicking Remove attachment then immediately replacing the draft and sending left clipboard.png on the submitted QA running-row prompt. In Codex, explicitly observing removal before sending produced no attachment on the next prompt. This race needs a dedicated reproduction; it is not yet conflated with the passing unsent cross-session ownership checks.

The Fold a running turn preference was initially on, was turned off for the comparison run, and was restored to on during that run. This QA pass leaves it at its original value. The first Codex four-group recording was interrupted by a browser observation timeout; its settled screenshot is evidence only for the settled state. The following preference-change recording is intact.

OpenCode state 14 recovery note: on the next interaction the previously incomplete four-group turn becomes complete and folded, including QA_FOUR_GROUPS_DONE. The original idle-page failure remains valid; this strengthens the connection to stale/incomplete transcript rendering rather than proving a separate group-count bug.

State 23 partial evidence: all three URI schemes render links in the short Codex reply. Desktop file opening reaches Preview with the exact QA PNG; vscode opening reaches VS Code with that image (evidence (evidence/codex-links/vscode-open.png)). VS Code displayed two identical per-open prompts before opening; the cause is not established. The Claxedo URI targets the current session, so unchanged UI after clicking does not prove dispatch. Long native-rendered reply links and a distinguishable Claxedo navigation target remain outstanding.

The fresh Codex question return recording was reviewed as a contact sheet of all 39 captured frames and at full size for the pending frame. The resolved answer and pending controls are simultaneously visible. This supplies independent live evidence for the supplied question-return recording; it does not establish a root cause.

Codex state 25 variants: the free-text answer `QA café 日本語 731` is returned exactly (submission (evidence/codex-question-custom/capture.webm), completed (evidence/codex-question/custom-completed.png)). Dismiss reaches the agent as cancellation and produces QA_QUESTION_DISMISSED (submission (evidence/codex-question-dismiss/capture.webm), completed (evidence/codex-question/dismissed-completed.png)). Neither leaves a question card. The subsequent round trip again flashes a pending Choose one label question at approximately 4.632–4.837 seconds (recording (evidence/codex-question-dismiss-return/capture.webm), frame (evidence/codex-question/pending-after-dismiss.jpg)); the visual label is shared by two requests, so this does not identify which resolved request was replayed. All 33 captured frames were reviewed as a contact sheet and the pending frame at full size.

Earlier state 28 evidence: the two real Codex child sessions appear as two settled chips, and both open distinct transcripts with their unique replies (spawn recording (evidence/codex-subagent-spawn/capture.webm), settled chips (evidence/codex-subagents/two-chips.png)). The verdict now rests on the fully reviewed three-child fixture 932 described below; this older recording remains unreviewed for transient duplicate chips.

State 99 subagent variant: all three child tabs survive a session round trip and the original child content remains available. The selected tab changes to Review, recorded under state 38. The Browser-tab variant is still outstanding. Child question output appears only as a generic Called tool row after returning; expanding it exposes no error detail, so the underlying question-tool unavailability is not established from the renderer.

State 35 completion variant: OpenCode finishes its real sleep command while Codex is selected; the measured rail order remains OpenCode, Codex, Claude. The completed reply QA_OC_RAIL_DONE is visible when returning. Scheduled-wake and subagent-completion variants remain outstanding. The first rail geometry probe incorrectly used text content instead of accessible labels and is discarded; only click-geometry.json and send-geometry.json contain the corrected measurements.

State 37 partial evidence: rapid wheel input and scrollbar dragging reach painted stopped views on OpenCode (wheel (evidence/opencode-long-scroll-stop/capture.webm), drag (evidence/opencode-scrollbar-stop/capture.webm), stopped (evidence/opencode-scroll/stopped.png)) and Codex (wheel (evidence/codex-fast-scroll-stop/capture.webm), drag (evidence/codex-scrollbar-stop/capture.webm), stopped (evidence/codex-scroll/stopped.png)). The captures still require transition-frame review before a full no-blank/no-swap verdict. Codex additionally exposes the confirmed repeated exit-code prose recorded under state 17. These sessions do not satisfy the hundreds-of-tool-calls setup required by Part 11.

State 37 frame review complete: all 32 Codex scrollbar, 16 OpenCode scrollbar, 27 OpenCode wheel, and 46 Codex wheel frames were reviewed in contact sheets, with stopped screenshots inspected at full size. Codex wheel frames 3–11 show transient blanking during the largest movement, clearing by frame 12; no persistent stopped blank or content replacement appears in these runs.

State 47 partial evidence: the Codex live fold header changes from Working to Worked for 57s at successful completion. Interrupted/failed and busy-but-failed label variants remain to be checked explicitly.

The upward transcript movement in states 6 and 13 is independent of composer mounting in the recorded reproductions: the composer is already visible and stationary while existing transcript content moves. Its regression oracle must measure both a named existing transcript row and the composer before and after the triggering update, with no scroll input. A composer-presence assertion alone cannot cover this finding. The authoritative cause remains unestablished.

The first Codex attempt at states 55 and 71 was inconclusive: the five-step plan prompt finishes with QA_PLAN_DONE 19 48 22 9, but no to-do surface is visible (settled (evidence/codex-todo/settled.png), initial recording (evidence/codex-todo-progress/capture.webm)). The structured message response contains only ordinary text and does not establish that update_plan ran. This attempt therefore does not prove a rendering failure or a passing plan lifecycle; the subsequent confirmed plan-producing turn supplies the verdicts above. The recording covers the first 19.96 seconds, not the entire 37-second turn.

The Codex permission test temporarily selected Untrusted and restored the original Workspace write mode after its allow/deny and child checks. The stale resolved-permission round-trip recording was reviewed across all 32 captured frames; the resolved parent dock appears again around 3.395–3.674 seconds and briefly at 4.337 seconds. No second answer was submitted.

The initial heavy-fixture preparation was recorded in the existing Codex and OpenCode QA sessions (Codex start (evidence/codex-heavy-fixture-start/capture.webm), OpenCode start (evidence/opencode-heavy-fixture-start/capture.webm)). Each prompt requests 200 separate numbered shell calls, prose between five-call groups, two large outputs, and two edits to a disposable /tmp file; Codex also creates two child sessions. Codex has reached the 016–020 progress marker in the latest observed structured response, and its two completed child chips are visible. OpenCode has emitted its fixture-creation introduction. These are preparation snapshots, not proof of the requested final counts. The early Codex scroll (evidence/codex-heavy-early-scroll/capture.webm) was recorded while the fixture was still growing. The later sustained-run comparison is recorded under state 90; the other Part 11 checks remain incomplete.

Blocked prerequisite rows share the current harness-availability evidence; they do not claim each unavailable scenario was executed. Composer-only and navigation-only states are not blocked solely because a harness cannot run. The availability recheck used a fresh Claude send, actual Cursor/Pi selection, and current CLI/version checks.

Heavy-fixture validation now confirms 205 Codex and 203 OpenCode normalized tool parts (message IDs and counts (evidence/heavy-fixture-validation.json)). Codex exposes six five-command groups and one expanded 170-command group, whose rendered rows run from QA_HEAVY_CODEX_031 through QA_HEAVY_CODEX_200. Its 300-line command output was opened and inspected. OpenCode created and twice edited only the owned evidence/opencode-heavy-fixture.txt artifact, whose final contents are alpha-prime, bravo, charlie-prime. The initial OpenCode outside-workspace write was denied before this successful fixture turn.

The sustained-run processes were observed live as PIDs 86985 and 87106 at elapsed 09:59 and 09:46 before the late recordings, and both had exited afterward. Early and late scroll contacts were inspected across the initial movement frames and the settled final frame. Codex has brief blanking during the large wheel jump, clearing immediately as the movement proceeds; no persistent stopped blank is observed. pmset -g therm returns no recorded thermal warning level, performance warning level, or CPU power status; this does not establish a temperature measurement.

Heavy OpenCode scrollbar follow-up: desktop-width dark capture (evidence/opencode-heavy-scrollbar-dark/capture.webm) contains five successive thumb drags, ending with a large jump back down; all 73 captured frames were reviewed with no observed blank region. Phone-width capture (evidence/opencode-heavy-phone-scrollbar/capture.webm) contains four drags at a DOM-verified 390×844 viewport; all 26 frames were reviewed with no observed blank region, and the final position is 27091 of 27091px. Phone screenshot (evidence/opencode-heavy/phone.png). These captures are partial evidence for states 82, 91 and 92: the computer-use drag API provides endpoints without a duration parameter, so a deliberately slow held drag and a physical trackpad fling are not established by these recordings. The phone-width top-to-bottom reading-speed pass remains outstanding. The temporary viewport override was cleared afterward.

Phone-width reading pass for OpenCode: first segment (evidence/opencode-heavy-phone-wheel-1/capture.webm), middle segment (evidence/opencode-heavy-phone-wheel-2/capture.webm), last segment (evidence/opencode-heavy-phone-wheel-3/capture.webm), scroll samples (evidence/opencode-heavy/phone-wheel.json), last reply (evidence/opencode-heavy/phone-wheel-bottom.png). At a verified 390×844 viewport, 280 small wheel steps traverse the full expanded history from zero to the exact final bottom at 27519px (28209px content, 690px viewport). All 902 captured frames were reviewed in contact sheets; no blank transcript region was observed. This completes the reading-speed portion of state 91 for OpenCode. The slow held-drag portion remains open, so the complete state is not yet marked pass. The temporary page viewport was cleared after capture.

Archive diagnostic follow-up: a second click on the same disposable OpenCode fixture again leaves the row and route unchanged. The bounded network capture (evidence/archive-navigation/opencode-network.json) contains a process-list GET but no session archive request. The inspected source path is `rail-sidebar.tsx:archiveSessionFromRows` → optional `onArchiveSession` → `session-actions.tsx:handleArchiveSession`; the handler has no confirmation step and returns before its update when the item has no directory. That is a candidate failure point, not a confirmed cause: the live callback input was not inspected, and no production code was changed.

Rapid-navigation setup: created the fifth QA-owned session `ses_f683973d8ffe2hUmAzjg3yc10N`, which completed QA_FIFTH_READY_827 without tools. Five distinct unsent draft markers were seeded into the fifth fixture, archive fixture, heavy OpenCode, heavy Codex and failed-auth Claude QA sessions. The three-burst recording call timed out and reset the computer-use kernel; its in-memory frames were lost, so state 95 receives no verdict from that attempt. Recovery through a fresh Chrome tab found all five markers still associated with their original sessions. The initial empty-fill cleanup did not remove them; the subsequent recorded retry and explicit select-all/Backspace cleanup below supersede that cleanup claim.

State 95 idle retry: five-session burst (evidence/five-session-rapid-navigation-retry/capture.webm) preserves 42 frames over 12.05 seconds, with successive rail clicks through all five fixtures. All captured frames were reviewed, including full-resolution title/composer strips: the rendered title, transcript and distinct draft marker remain associated with the same session. Before/after draft observations (evidence/five-session-rapid-navigation-retry/drafts.json) confirm all five markers return in their original session; explicit select-all/Backspace then leaves only the editor's empty newline in each. This is partial evidence only: the destinations are warm idle sessions, so overlapping loads and different fold states remain unverified. State 95 is not marked pass from this narrower run.

Running navigation follow-up for states 95/96: Codex executes the bounded `sleep 90; printf QA_NAV_RUNNING_DONE_936` fixture while the rail visits the other four QA sessions and returns. Start recording (evidence/codex-running-navigation-start/capture.webm), switch and return recording (evidence/codex-running-navigation-return/capture.webm), per-destination DOM observations (evidence/running-navigation/codex.json), before (evidence/running-navigation/codex-before.jpg), returned (evidence/running-navigation/codex-return.jpg), completed (evidence/running-navigation/codex-settled.png), settled recording (evidence/codex-running-navigation-settle/capture.webm). Sampled transition frames and the five destination snapshots show Thinking/Stop only on Codex; the returned current turn retains its single tool group and completes QA_NAV_COMPLETE_936 in 1m 44s. The command label is intermittently absent visually while still present in the return DOM; no cause is assigned. The preceding old soak group also changes from Ran 2 tools to Used 3 tools plus a generic tool row after return. Full frame review and an explicitly folded multi-group running variation remain necessary before assigning the complete state 96 verdict.

Question-selection diagnostic: the earlier cross-tab fixture replied Amber after an Indigo click, but the selected state and request were not captured in that run. A controlled follow-up captures Indigo checked (evidence/cross-tab-question/958-selected-indigo.png), the POST body (evidence/cross-tab-question/958-submit-request.json) containing answers [[Indigo]], and the completed reply (evidence/cross-tab-question/958-completed.png) preserving Indigo. No wrong-answer transport defect is established by the earlier discrepancy. The resolved-card absence and pending-card replay remain independently observed failures.

Codex background fixture remains unverified: the first command reports nice(5) failed: operation not permitted (tool output (evidence/background-shell/codex-first-command-error.png)); its reported PID 65314 is absent at the first process probe. A Bash retry reports PID 71151 (output (evidence/background-shell/codex-bash-output.png), DOM (evidence/background-shell/codex-bash-dom.txt)), but the first probe again finds no sleep 45 process. No phantom chip was observed, but without a demonstrated persistent background process this does not establish state 33 passing. The OpenCode fixture independently satisfies that prerequisite.

Child fixture 932 closes the earlier partial state 28 verdict: all 405 spawn frames and 338 completion frames were reviewed, with full-size checks of the third-chip arrival and settled fold. No duplicate or empty chip appears in these captured frames. The three distinct child routes open QA_CHILD_A_932, QA_CHILD_B_932 and QA_CHILD_C_932. This review covers the newer spawn recording (evidence/codex-child-grouping-start/capture.webm) and completion recording (evidence/codex-child-grouping-middle/capture.webm); it does not claim review of the older two-child recording or establish that surrounding prose remains stationary.

Browser prerequisite and scope: the web build explicitly displays Browser tabs are unavailable and directs the user to desktop (web notice (evidence/child-grouping/browser/web-unavailable.png)). The desktop Browser round trip above establishes URL/page loss through the native Add workspace tab entrypoint. It does not establish the untested local-preview-row entrypoint or claim a fixed-build desktop result.

Additional question-replay evidence: full review of the Codex fixture 981 reload shows its already-answered pending question in frame 9 (frame (evidence/question-fold/codex-reload-question-replay.jpg)), followed by the composer returning. This reproduces the existing pending-question replay defect on page reload as well as session switching; it is not a new independent finding.

State 47 partial observation: interrupted fixture 983 correctly uses Worked for 34s, not a working verb, and completed fixtures use Worked. The two-group running fixture does not expose a live fold control, so it does not establish the running-label or busy-but-failed cases. Those variations remain unverified.

State 47 follow-up: fixture 985 exposes the live label Working (running (evidence/live-fold/third-running.png)) and settles to Worked for 1m 15s (completed (evidence/live-fold/completed.png)); interrupted fixture 983 uses Worked for 34s. The busy-but-failed variation remains unverified, so the full label matrix is not marked passing.

Completed three-group inspection: OpenCode fixture 986 contains exactly three separate tool rows, with both prose separators and the final reply outside the fold (expanded (evidence/live-fold/opencode-three-expanded.png), DOM (evidence/live-fold/opencode-three-expanded-dom.txt)). Codex fixture 985 also exposes three groups (expanded (evidence/live-fold/codex-three-expanded.png)), but the third is Shell Failed with Tool execution interrupted (error (evidence/live-fold/codex-third-error.png), DOM (evidence/live-fold/codex-third-error-dom.txt)). QA_LIVE_DONE_985 therefore proves a final reply, not successful completion of all three commands. Escape was used while dismissing Settings during this fixture and may have triggered interruption; its cause is unconfirmed and is not counted as a new defect. The setting-off fold change was observed earlier while the third command was still running and remains independently established. The clean Codex fixture 987 below supplies the separate completed-group verification.

Codex state 81 traversal now reaches the exact bottom via 235 wheel inputs from scrollTop zero. The first 35 inputs are an unpaced faster variation; the remaining 200 use 0.1-page increments with 200ms pacing. The six recordings capture 960 frames in total, with segment geometry (evidence/codex-heavy-reading-a/traversal.json). The final endpoint is 19548px of a 20251px history with a 703px viewport. All 960 captured frames have now been visually reviewed and contain transcript content, closing the recording review for state 81. The eighteen low-content candidates also received larger visual checks. Recordings: A (evidence/codex-heavy-reading-a/capture.webm), B (evidence/codex-heavy-reading-b/capture.webm), C (evidence/codex-heavy-reading-c/capture.webm), D (evidence/codex-heavy-reading-d/capture.webm), E (evidence/codex-heavy-reading-e/capture.webm), F (evidence/codex-heavy-reading-f/capture.webm).

Additional navigation finding: after archiving fixture 988, the destination heavy Codex session revives the previously interrupted fixture 985 shell as `Running sleep 60; printf QA_LIVE_THREE_985`, with its timer advancing from 6s to 14s. The composer remains idle with Send disabled, and the later completed fixture 987 is visible. Earlier evidence shows the same tool as `Shell Failed — Tool execution interrupted`. The post-navigation DOM (evidence/codex-archive/states.json), later DOM (evidence/codex-archive/settled-dom.txt), screenshot (evidence/codex-archive/settled.png), and process probe (evidence/codex-archive/process-check.json) support a stale terminal-tool rendering finding; the probe finds no matching live process. Stored event inspection and the regression fixture remain pending. This is not evidence that the shell actually restarted.

The stale-tool finding is now grounded in the real message API: stored tool parts (evidence/codex-archive/stored-tool-parts.json) returns assistant `msg_097fbf50e001oS8114phtFElTC_r` completed at 1789256343104 and bash call `exec-fdaa5454-5dca-4811-a9b2-e90c1b7c3f06` with `state.status = error`, an end timestamp, and `Tool execution interrupted`. At the same visit the UI timer reaches 2m 5s with an idle composer. Reload recording (evidence/codex-stale-tool-reload/capture.webm), reloaded screenshot (evidence/codex-archive/after-reload.png) and reloaded DOM (evidence/codex-archive/after-reload-dom.txt) show the tool still Running, now with the timer reset to 6s. This establishes the terminal API state versus running UI mismatch, including reload persistence; the reload video is retained for timing and has not received exhaustive frame review. The regression should deliver this actual normalized error tool and completed assistant through the message-page entrypoint, then assert terminal rendering across navigation/reload. No production fix has been made.

The OpenCode heavy fixture repeats the missing-output prerequisite: opening the fifth call in the 046–050 group produces an empty card. Its replay API part (evidence/opencode-heavy-output/stored-tool.json) has completed status, empty input and output, and the call ID as the tool name. Thus the replay response already lacks display data; the evidence does not establish which upstream normalization stage lost it. States 63 and 88 are blocked by the state 20 failure, rather than passing or becoming duplicate findings.

## Environment sweep: locale labels

These Part 6 checks are separate from numbered state 6 (send geometry) and are not included in the numeric row counts. They establish static localization failures, not animation or full locale coverage.

| Locale | Harness | Surface | Verdict | Evidence | Observation |
| --- | --- | --- | --- | --- | --- |
| German | Codex | Local web, fixed build, system dark theme | fail | Screenshot (evidence/environment-sweep-1014/german.png), DOM (evidence/environment-sweep-1014/german-dom.txt) | After selecting German, New Project, Copy response and the failure status translate, but the visible composer prompt Ask anything, / for commands, @ for context..., Workspace write, and File change remain English. One shared untranslated-label regression across locales; fixture prose is intentionally English and is not a finding. |
| Japanese | Codex | Local web, fixed build, system dark theme | fail | Screenshot (evidence/environment-sweep-1014/japanese.png), DOM (evidence/environment-sweep-1014/japanese-dom.txt) | After selecting Japanese, New Project, Copy response and the failure status translate, but the visible composer prompt Ask anything, / for commands, @ for context..., Workspace write, and File change remain English. One shared untranslated-label regression across locales; fixture prose is intentionally English and is not a finding. |
| Arabic | Codex | Local web, fixed build, system dark theme | fail | Screenshot (evidence/environment-sweep-1014/arabic.png), DOM (evidence/environment-sweep-1014/arabic-dom.txt) | After selecting Arabic, New Project, Copy response and the failure status translate, but the visible composer prompt Ask anything, / for commands, @ for context..., Workspace write, and File change remain English. One shared untranslated-label regression across locales; fixture prose is intentionally English and is not a finding. |

The locale switch uses Settings → General → Language. English is restored and verified through the visible English controls and document lang=en. Color scheme remains System and theme remains Codex; no security setting changed. Arabic document lang=ar was observed with no document dir attribute, but this alone does not establish a separate RTL defect. Live Queued, Show all, exit-code labels, the OpenCode comparison, theme variations and narrow-layout checks remain unverified in this sweep.

Additional model-selection finding (OpenCode): in fixture 1036, selecting Big Pickle and immediately sending the recovery prompt still stores and executes cloudflare-workers-ai / @cf/aisingapore/gemma-sea-lion-v4-27b-it, returning another 401 while the picker displays Big Pickle. The later repeat, with no further model change, stores opencode / big-pickle and completes. First send after selection (evidence/opencode-failure-recovery-1036/capture.webm), picker and failed turn (evidence/provider-failure-1036/recovery-failed.png), DOM (evidence/provider-failure-1036/recovery-dom.txt), stored messages (evidence/provider-failure-1036/replay.json), later repeat (evidence/opencode-model-mismatch-1036/capture.webm), recovered (evidence/provider-failure-1036/recovered.png). This is one observed selection-to-send mismatch, not a claim that every subsequent send uses the wrong model. The authoritative cause and regression remain pending; the regression must assert the submitted model after selecting it through the real picker. No credentials were changed.

Additional model-display finding (Codex): fixture 1038 is created with codex/qa-invalid-model-1038, but the composer displays GPT-5.6-Sol before and after the rejected send while the stored user and assistant messages identify qa-invalid-model-1038. Failure screenshot (evidence/model-failure-1038/failed.png), DOM (evidence/model-failure-1038/failed-dom.txt), stored models (evidence/model-failure-1038/replay.json). The error-card recovery chooser correctly uses gpt-5.6-terra and the reply completes. This exposes unavailable-model display disagreeing with actual submission; it is not evidence that selecting GPT-5.6-Sol caused the invalid configuration, because the fixture was created with that invalid model through the public API. Regression and producer diagnosis remain pending.

QA environment recovery: after port 2593 loses its listener, the still-running QA desktop launcher repeats daemon lease renewal failed. Stopping that owned launcher (exit 130) and running the documented packages/claxedo-desktop bun run dev recipe starts a healthy replacement daemon at PID 43007. The saved Claude fixture is returned by the session API and renders after browser reload. Recovery record (evidence/server-recovery-1043/result.json). The prior exit cause is unknown; this is environment recovery, not evidence for in-flight turn recovery. File-preview Failed to fetch observations near this outage require revalidation before attribution to a separate product defect.

Browser tooling recovery: the history attempt times out and resets the CUA kernel, after which the old tab reports a detached debugger. A fresh tab in the same Chrome browser opens the saved Claude fixture and renders its empty ready composer. Recovery record (evidence/browser-recovery-1044/result.json). No history or archive verdict is inferred from the interrupted attempt.

Additional browser-history evidence: after opening the saved Claude session and clicking Codex fixtures 1038 then 1037 in the rail, the first Back reaches 1038 but the second Back reaches the new-session page instead of Claude. Forward returns to 1038. Action sequence and limitations (evidence/history-1047/result.json), second Back (evidence/history-1047/second-back.png), destination DOM (evidence/history-1047/second-back.txt), forward DOM (evidence/history-1047/forward.txt). This supports the existing skipped-history-entry finding. The concurrent recording times out with only one saved frame, so timing and flicker are unverified; the full state 103 evidence and Cursor variation remain outstanding. No extra numbered pass is inferred.

History regression: `core-session-rendering-navigation.spec.ts` → `browser Back and Forward preserve a chat visit made through the rail @core` fails at the intended Back URL assertion after both reply-oracle checks pass. Assertion: `Expected: http://localhost:4455/w/ws_mock_runtime/session; Received: http://localhost:4455/s/ses_browser_history; Timeout: 10000ms`. Exact command/result (evidence/history-e2e-1049/result.json), red output (evidence/history-e2e-1049/red.log), reviewed failure screenshot (evidence/history-e2e-1049/test-failed-1.png), video (evidence/history-e2e-1049/video.webm). One failed, zero retries. The test is now marked fixme with register entry 42; Forward was not reached. This is the rail-history regression for state 103; it does not establish a harness-specific root cause or complete the broader live navigation matrix.

Gate run: `bun run typecheck:e2e` from `packages/claxedo-app` exits 0. Output (evidence/history-e2e-1049/typecheck.log). This is type validation, not a passing behavioral regression or full gate completion.

Geometry control: `core-timeline-rendering-scroll.spec.ts` → `a short follow-up preserves the existing transcript row with a stationary composer` passes three times with zero retries in build-preview. The shared geometry helper samples the same mounted row and composer for 180 frames and checks that busy and subsequent ready states occur. Exact result and scope (evidence/geometry-e2e-1050/result.json), output (evidence/geometry-e2e-1050/repeat.log), run 1 (evidence/geometry-e2e-1050/0-geometry-after-send.png), run 2 (evidence/geometry-e2e-1050/1-geometry-after-send.png), run 3 (evidence/geometry-e2e-1050/2-geometry-after-send.png). All three screenshots were reviewed and show both replies, but also Reconnecting; mock transport qualification is pending, so this is not yet accepted green coverage. The short text case does not reproduce or close the reported tool/image/first-token movement.

Geometry transport diagnostics: the strengthened control passes 3/3 with no retries, zero unhandled API requests, successful SSE responses, and 0px movement for both sampled elements in all runs. Exact command and measurements (evidence/geometry-transport-1051/result.json), output (evidence/geometry-transport-1051/repeat.log), sample and network data (evidence/geometry-transport-1051/0-geometry-and-transport.json). The mock deliberately ends each fulfilled SSE batch, unlike a persistent live stream, so reconnect UI can appear during its batch gaps. Two ERR_ABORTED prompt requests per run remain logged and need attribution. These diagnostics strengthen the short-text control but do not reproduce the reported live tool/image shifts or complete visual acceptance. `bun run typecheck:e2e` exits 0 (output (evidence/geometry-transport-1051/typecheck.log)).

Interrupted-tool control: `core-harness-rendering-matrix.spec.ts` → `Codex interrupted command remains terminal after message-page reload` passes 3/3 with no retries. It uses the captured error tool part with fixture identifiers and command marker, persists it through the shared mock, reloads, and renders Shell Failed. Exact command/scope (evidence/interrupted-e2e-1052/result.json), output (evidence/interrupted-e2e-1052/repeat.log), reviewed screenshot (evidence/interrupted-e2e-1052/2-interrupted-command-reloaded.png). All three post-reload screenshots reviewed. This isolated terminal snapshot does not reproduce the live state 19 stale-Running failure; the full running-to-error sequence and surrounding heavy-session state remain to be added to this test. No fixme is added for a passing control. `bun run typecheck:e2e` exits 0 (output (evidence/interrupted-e2e-1052/typecheck.log)).

Interrupted-tool lifecycle extension: the same test now first displays Running, delivers the captured error state and completed message, verifies a terminal label, then reloads. It passes 3/3 with zero retries; all six running/post-reload screenshots reviewed. Result and exact command (evidence/interrupted-lifecycle-1053/result.json), output (evidence/interrupted-lifecycle-1053/repeat.log), review sheet (evidence/interrupted-lifecycle-1053/review.jpg). This rules out the isolated running-to-error/reload sequence in the current fixture; it does not close state 19 because the heavy-session surrounding data and rail navigation remain absent. Typecheck exits 0 (output (evidence/interrupted-lifecycle-1053/typecheck.log)).

Required gates: app `bun run test:architecture` has **251 pass / 1 fail**, with `moduleScopeMutableState` at 45 against baseline 44. The current production diff adds a module-scope signal in `src/app/shell-revealed.ts:24`; that is outside this QA slice, and neither production code nor the baseline was changed. Root `bun run lint` reports **0 warnings / 0 errors**. Root `bun run test:architecture-ratchets` reports product boundary and helper ratchets passed. Exact commands and attribution (evidence/gates-1054/result.json), architecture output (evidence/gates-1054/architecture.log), lint output (evidence/gates-1054/lint.log), ratchet output (evidence/gates-1054/ratchets.log). These gates do not establish E2E completion.

Answered-question rail-return control: `core-docks.spec.ts` → `an answered question stays absent on every frame when returning through the rail` passes **3/3 with zero retries**, observing the UI return through the next successful SSE response. Per-frame measurements and scope (evidence/question-navigation-1056/result.json), command output (evidence/question-navigation-1056/repeat.log), return screenshots (evidence/question-navigation-1056/review.jpg), typecheck (evidence/question-navigation-1056/typecheck.log). This does not reproduce or close states 25/70/97: reload, stale directory responses and live producer replay are not represented. Earlier 14-frame-only results are retained in `question-navigation-1055` and superseded by this longer observation.

Scope correction for the preceding control: the next SSE response arrives quickly, so its actual measured windows are only **146–167ms (18–21 frames)**. It proves immediate return behavior only, not delayed reconnect/replay. The live answered-question flash remains open; a held stale directory response or real producer replay is required for the next meaningful reproduction.

Stale-question response experiment: **0 pass / 1 setup failure**, timing out at 60s before the product assertion. The held read has no successful response in the trace (status -1); newer reads return 200. Cancellation was initially inferred, but no explicit abort event is retained, so its cause remains unproven. The unused mock hook was removed and the immediate-return test restored. Exact result and remaining instrumentation (evidence/question-stale-1057/result.json), output (evidence/question-stale-1057/run.log), trace (evidence/question-stale-1057/trace.zip), typecheck (evidence/question-stale-1057/typecheck.log). This attempt does not justify a fixme or close any live finding.

[Failed-row reproduction map](reproduction-map.md): all **88 failed table rows** now have proposed test homes and explicit required proof, grouped into **33 candidate families**. This is not an implementation or acceptance count. Duplicate recorded observations (such as Codex states 12/96 and 21/54) share their intended regression. Desktop Browser content loss stays distinct from panel-selection persistence.

Required real gate started with `CLAXEDO_E2E_SERVE_MODE=build-preview` exported, then `bun run test:e2e:real`. Its 84 scenarios remain in progress; the original exec handle expired but OS PID 80710 was verified alive at scenario 42. Running output snapshot (evidence/real-gate-1058/running.log), run state (evidence/real-gate-1058/run-state.json). Early failures include a missing model-search field and an explicit Pi version mismatch (0.85.1 versus required 0.85.0). An isolated `/tmp/claxedo-qa-pi-0.85.0/node_modules/.bin/pi` now reports **0.85.0**; it is available for a subsequent process via `PI_EXECUTABLE`, but the current run retains its original environment. No Pi live state is promoted to pass from this version check.

Real gate completed: **31 passed / 12 failed / 41 did not run**, **9.4m**, script exit **1**. Complete output (evidence/real-gate-1058/complete.log), counts and failure categories (evidence/real-gate-1058/run-state.json), retained artifacts (evidence/real-gate-1058/artifacts/). Failures include five missing model-search selectors, explicit Pi version mismatch, a second Pi create failure, question-isolation count 2 instead of 1, missing Claude question dock, missing Claude child chip, a worker SIGABRT, and subsequent port 4317 EADDRINUSE. Passed tests still require visual review; the 41 unrun tests are not pass evidence.

Visual review of the real Pi bash-tool failure shows the picker explicitly reporting **Unsupported Pi version 0.85.1; expected 0.85.0**. The model-search locator therefore fails downstream of the version gate; it is not established as a selector defect. Reviewed screenshot (evidence/real-gate-1058/artifacts/playwright-real-harness-lo-13b20-ool-and-persists-its-output-chromium/test-failed-1.png). The isolated 0.85.0 binary is being exercised in a targeted real three-turn/reload follow-up.

Pinned Pi real follow-up: **0 pass / 1 fail**, zero retries. The trace completes all three reply oracles and post-reload duplicate/visible-reply checks; the reviewed failure screenshot shows T1/T2/T3 rendered. The later operation times out after reload checks, where source next calls the usage endpoint with a 10s AbortSignal. Exact command and limits (evidence/pi-real-1059/result.json), output (evidence/pi-real-1059/run.log), trace and failure evidence (evidence/pi-real-1059/artifacts/). This establishes progress beyond the version gate, not a passed full journey or live-provider qualification.

Real-suite qualification gap found during source review: `e2e/helpers/question-isolation.ts:67` injects a synthetic question-reply 500 using `page.route`, called by four concurrent-question scenarios in `real-harness-local.spec.ts`. Their complete flows do not satisfy the zero-route Tier R contract. The observed Codex windows count failure happens before this interception. Passing variants must not be reported as wholly Tier R; no production behavior or existing assertion was weakened.

User-selected image design: compact thumbnail contract, drafted as image-thumbnail-contract.md and not committed with this record, using the supplied Codex screenshot (evidence/image-reference/codex-reference.png). State 67 now targets approximately **80 × 80 CSS-pixel tiles**, identical through loading/failure, opening full-size on click; it does not require reserving a full-size image inline. The updated regression runs red (**0 pass / 1 fail**, zero retries): loading width 645.75px versus target 80px, deviation **565.75px**. Output (evidence/image-reference-1063/red.log), scope (evidence/image-reference-1063/result.json), typecheck (evidence/image-reference-1063/typecheck.log). Corrupt-image and preview-close-position coverage remain outstanding; the preview assertions are not reached past the known size failure.

Required build and combined core command: explicit `CLAXEDO_E2E_SERVE_MODE=build-preview bun run build:e2e` exits **0**. The exact three-path Playwright command exits **0**, with **4 passed / 1 skipped / 0 failed** in 30.1s. Only the navigation file exists and matches; `core-session-rendering-turns.spec.ts` and `core-session-rendering-composer.spec.ts` remain absent, so this gate does not cover those missing files or the extended existing specs. Commands and scope (evidence/core-gate-1064/result.json), build output (evidence/core-gate-1064/build.log), core output (evidence/core-gate-1064/core.log), reviewed static diagnostics (evidence/core-gate-1064/review.jpg). Splash and toolbar checks alone are not full first-interaction acceptance.

Unsigned-local navigation repetition: **12 passed / 3 expected history skips / 0 failed**, **zero retries**, 48.1s. Exact command and limits (evidence/core-unsigned-1065/result.json), output (evidence/core-unsigned-1065/run.log), build auth manifest (evidence/core-unsigned-1065/build-manifest.json), per-run videos (evidence/core-unsigned-1065/artifacts/). Each of four runnable scenarios passed three times. Static screenshot filenames retain only the last repetition, and full video review remains outstanding.

QA continuation 1081–1083: the incorrect MCP Local preview now has a red regression and register entry 50; the valid development-server link passes. The shared PNG mock route registration now passes its architecture guard. `bun run test:architecture` remains red with 251 pass / 1 fail on the production module-state baseline (45 against 44), unchanged. The compact-image regression again fails on tile sizes, 416px same-row movement with stationary composer, and absent preview after clicking; screenshots reviewed, video review outstanding. Evidence: preview (evidence/preview-1081/result.json), architecture (evidence/architecture-1082/result.json), image (evidence/image-1083/result.json). No application fix or completion claim.

Current QA gates 1083: `bun run lint` passes with zero warnings/errors; `bun run typecheck:e2e` passes; `bun run test:architecture-ratchets` passes all five products and eight policies without a baseline change. Logs and outcomes (evidence/gates-1083/result.json). These do not supersede the failing app architecture gate or incomplete live coverage.

QA continuation 1084–1087: added `Codex running shell paints its command before completion` beside the existing real long-command tests. Immediate snapshots in 1085 were rejected because they could catch the entrance animation. The bounded 1086 check yields 2 failed / 1 passed / 0 retries: initial command opacity stays zero beyond one second; both failing screenshots were reviewed. Reload restores the command and completion/persistence checks pass. Register 51 records the intermittent regression; only this presentation variant is fixme. The unsigned-local control yields 1 passed / 0 retries, with visual review outstanding. Heavy-session rail-return remains unproven. Qualified red evidence (evidence/running-1086/result.json), unsigned control (evidence/running-1087/result.json). `bun run lint` passes with zero warnings/errors and `bun run typecheck:e2e` passes. No production changes or commits in this continuation.

QA continuation 1088–1090: the three-group fold header regression fails in test-user and unsigned-local modes, once each with zero retries. The unsigned setup explicitly observes Working, verifies all three groups, manually collapses, then confirms Worked and retained collapse before the missing-count assertion. Both unsigned screenshots were reviewed. Register 52 and the coverage map now reference this evidence. Run 1089 failed before the count assertion and is excluded. Qualified unsigned result (evidence/fold-count-1090/result.json). No application code was changed.

QA continuation 1091–1092: the consecutive MCP-group regression fails on missing sessions_list and processes names in both auth modes (1 failed / 0 retries each). Both expanded members pass. Collapsed and expanded screenshots from both runs were reviewed; full videos remain unreviewed. Register 53 backs states 26 and 51 with one test. The stored assertion allows underscore/space normalization; neither form appears in the observed Used 2 tools header. `bun run lint` passes with zero warnings/errors and `bun run typecheck:e2e` passes. Test-user evidence (evidence/mcp-group-1091/result.json), unsigned evidence (evidence/mcp-group-1092/result.json). No production changes or commits.

Additional user report — permission selector appears late: the extended hydration test now fails on the absent permission trigger before delayed reports arrive. Test-user 1280px: 1 failed; unsigned-local 1280px/720px: 2 failed; zero retries. Geometry and subsequent send/reply checks pass. Register 54 covers the two viewport parameters of this additional issue outside the original 33-family map. Test-user evidence (evidence/permission-first-1093/result.json), unsigned evidence (evidence/permission-first-1094/result.json). Typecheck passes; app architecture remains 251 pass / 1 fail on the unchanged production module-state baseline.

Additional user report — distant harness caret: a successful Claude send is followed by trigger geometry measurement. In both test-user and local-unsigned modes, the trigger is 260px wide, its Sonnet 4.6 text is 65.734375px, and the caret gap is 140.265625px (QA adjacency target ≤16px). Each run fails once with zero retries. Register 55; test-user evidence (evidence/caret-1095/result.json), unsigned evidence (evidence/caret-1096/result.json). The test-user screenshot is reviewed; unsigned image and full-video review remain outstanding. Open-menu width is not covered. Lint is zero warnings/errors; E2E typecheck passes. No application fixes or commits.

Visual follow-up: unsigned permission screenshots at both widths (1094) and unsigned harness-trigger screenshot (1096) are now reviewed. They confirm the absent permission control before hydration, readable replies afterward, and the distant caret. Full videos remain unreviewed.

### Focused regression gates, runs 1103–1106

- `bun run typecheck:e2e` (app): pass, exit 0. Log (evidence/gates-1106/typecheck.log).
- `bun run lint` (root): pass, 0 warnings / 0 errors. Log (evidence/gates-1106/lint.log).
- `bun run test:architecture` (app): fail, 251 pass / 1 fail, moduleScopeMutableState 45 versus baseline 44. No baseline changed. Log (evidence/gates-1106/app-architecture.log).
- `bun run test:architecture-ratchets` (root): pass, 5 products / 8 policies, 12566 helpers. Log (evidence/gates-1106/root-ratchets.log).
- `bun run build` (agent-runtime-contract): pass, refreshed its existing dist exports after the Home twin setup failed on a missing export. No production source changed. Log (evidence/gates-1106/contract-build.log).

Home endpoint and output-retention regressions are registered as 56–57 after red runs in both auth modes. These focused gates are not the final full-scope acceptance run.

### Dependency gate failures, runs 1107–1109

The latest `bun run typecheck:e2e` fails with two workspace-runtime declaration errors (ProviderBindingOverlay and bindProviders). `bun run lint` fails with 0 warnings / 1 error because egress-broker cannot resolve its declared @tsconfig/node-lts configuration. A frozen dependency refresh fails the repository minimum-release-age policy for @vercel/sandbox@3.3.0 and modal@0.10.1; no policy was bypassed. Exact commands, results, and owner follow-up (evidence/gates-1109/qualification.md). These supersede the earlier focused green lint/typecheck result; browser regressions still run.

### Thinking ownership control, runs 1112–1114

`Thinking belongs to the new prompt throughout a follow-up send` is a new enabled rule test in core-busy-abort-errors. The shared frame sampler now records visible elements' containing message IDs. A normalized Codex session starts with a completed prior reply and submits a follow-up through the shared HTTP/event mock; its completed reply passes the canonical oracle. Test-user runs pass 1 + 2 times; unsigned passes 3 times; all runs use zero retries. Each run samples 485–584 visible Thinking frames, all under the new user message. Busy and final video frames were reviewed for all six runs. Full video review remains outstanding.

This is a green control, not a reproduced regression for the earlier live wrong-turn Thinking. The original real producer sequence and timing still need capture and reproduction. First run (evidence/thinking-owner-1112/result.json), test-user repeats (evidence/thinking-owner-1113/result.json), unsigned repeats (evidence/thinking-owner-1114/result.json). Run 1111 stopped at the draft-only URL assertion and is excluded.

### Thinking continuity: green mock control and fresh live failure

The enabled `Thinking stays painted until the follow-up reply begins` test samples both Thinking and the new reply through the shared geometry helper, including viewport hit-testing and text presence. Runs 1115–1117 pass three times per auth mode, zero retries; busy/final frames were reviewed for each. Test-user first (evidence/thinking-gap-1115/result.json), test-user repeats (evidence/thinking-gap-1116/result.json), unsigned repeats (evidence/thinking-gap-1117/result.json).

The real OpenCode follow-up in the configured preview still fails: sampled Thinking disappearance precedes reply presence by about 508ms. This does not increase the family count; it freshly confirms state 5 OpenCode. The model replies correctly after the gap. The raw producer sequence remains to capture before a qualified red automated regression can replace the green control. No new E2E build ran during that follow-up. An earlier first-send capture was excluded because its open page requested an old asset after the concurrent E2E build replaced dist; reload recovered the completed first reply without resending. Fresh live evidence (evidence/live-continuity-1118/result.json), excluded first send (evidence/live-continuity-1117/qualification.txt).

### Withdrawn E2E classification: opening commentary under the title

Register 62 is withdrawn. Failures 1139/1140 showed that Playwright scrollIntoViewIfNeeded left the opening text under the sticky title; they did not prove ordinary scrolling could not reveal it. Real Codex probe 1141 completes two turns, expands the earlier work, and recovers the opening commentary with an upward wheel gesture. The earlier scrollTop=0 diagnostic read the outer wrapper rather than the scrolling viewport. The test now uses the shared wheel-to-top helper; the diagnostic reads the measured element's actual viewport. No application code changes. Expanded position (evidence/live-commentary-1141/expanded.png), readable after scrolling (evidence/live-commentary-1141/scrolled-top.png). Historical duplicate-reply streaming reproduction remains open.

Corrected control `restored Codex failure commentary renders each reply passage once` passes three times in both auth modes (1142/1143, zero retries). All commentary screenshots and follow-up oracle frames were reviewed. This is one green restoration rule test, not closure of the historical streaming duplicate. Evidence and command (evidence/restored-commentary-1143/result.json).

### Answered-question return: fresh cross-tab reproduction 1144

Real Codex session `5b95bdc3-1903-4b5f-b3c2-61226f320e89` asked “Which QA color?”. The original tab left while it was pending; another tab answered TEAL and visibly completed `QA_COLOR_1144 TEAL`. Returning through the original rail painted the pending question twice: sampled at 39–341ms and 723–1430ms, with a composer visible between those intervals. The final settled view has the completed answer and ready composer. This confirms the existing states 64/97 finding again; it is not a new issue family.

Evidence: recording (evidence/question-cross-tab-1144/capture.webm), reviewed frames (evidence/question-cross-tab-1144/review-contact.jpg), timing (evidence/question-cross-tab-1144/timing.json), result and limitations (evidence/question-cross-tab-1144/result.json), actual return responses (evidence/question-cross-tab-1144/return-responses.json), separate retained-runtime read (evidence/question-cross-tab-1144/runtime-replay.json). The six selected pending/answered/return/settled images were reviewed; all 177 frames have not been reviewed. Screenshots precede DOM samples, so the timestamps are approximate.

Both observed `/question` responses were empty. Four message responses contained the completed final answer and no pending question tool. A separate fresh HTTP subscription after completion still replayed the original normalized runtime `question` event. CDP could not retrieve the browser return stream body, so that separate subscription does not prove precisely what its reader applied. Cached state and retained replay remain candidate causes. The central WebSocket capture contained lifecycle changes and no question resolution, which does not establish absence on every other transport.

The first Tier M variants (1145 same-tab answer, 1146 immediate away/return) each passed once, but they are not accepted coverage of this live failure. A stricter runtime-body guard in 1147 timed out: the request resumed with `Last-Event-ID: 1`, receiving heartbeats rather than replaying the question. That is a scenario/setup failure, not a product regression. These experiments are archived under `evidence/runtime-question-1145/`, `runtime-question-1146/`, and `runtime-question-1147/`; no additional red finding or release pass is counted from them.

### Answered-question regression qualified: 1150/1151

The preceding draft-return experiment did not change session event scope: `session-event-scope.ts` retains the created session while the route is a draft. Waiting longer in 1148 still did not replay. Experiment 1149 exposed a mock setup gap: the second root session existed in the workspace list but not the rail's control-plane navigation projection. Neither failure is counted as a product defect.

`core-docks.spec.ts` now visits another existing session through the rail and waits for its scoped runtime response and completed reply before returning. The shared mock serves that same stored session through both the workspace and navigation projections. Runtime envelopes are filtered by the requested parent, and the return guard inspects the response body for the retained question ID instead of accepting any heartbeat. A separate snapshot operation models resolution in another client without notifying this client. This is a deliberate missed-event variation, not proof that a specific live event was lost.

- `CLAXEDO_E2E_AUTH_MODE=local-unsigned CLAXEDO_E2E_SERVE_MODE=build-preview CLAXEDO_E2E_SUITE=core npx playwright test --config playwright.config.ts e2e/playwright/core-docks.spec.ts --grep 'Codex questions answered while away' --workers=1 --retries=0 --trace=on`: **0 passed, 1 failed**, the no-visible-question assertion. Three painted samples among 98, including two distinct flashes. Log (evidence/runtime-question-1150/run.log), trace frames (evidence/runtime-question-1150/review/contact-2.jpg), captured responses (evidence/runtime-question-1150/replay-and-questions.json).
- Same command with `CLAXEDO_E2E_AUTH_MODE=test-user`: **0 passed, 1 failed**, same assertion. Three painted samples among 53. Log (evidence/runtime-question-1151/run.log), first return flash (evidence/runtime-question-1151/review/contact-0.jpg), second return flash and recovery (evidence/runtime-question-1151/review/contact-1.jpg).

The return frames and settled assistant were visually reviewed in both modes. Full recordings and all pre-return frames were not reviewed. The first-send and other-session reply oracles pass; the evidence completion below supplies separate reviewed images for those calls. This adds one reproduced original issue family (entry 63): **19/33 original families**, plus the two additional selector families, giving **21 reproduced families / 22 failing test cases**. Fourteen original families still lack a qualified red regression. The new red is marked fixme as requested; it is not a production fix or green release coverage.

Focused QA lint passes with zero warnings/errors. `bun run typecheck:e2e` remains blocked by the unrelated `CredentialMetadata.owner` type mismatch in `claxedo-server-core/src/credentials/registry.ts:667`; no QA type errors are reported. `bun run test:architecture-ratchets` passes: five products, eight policies, 12,575 helpers, no baseline change. Gate logs (evidence/runtime-question-1150/).

Evidence completion: 1154/1155 repeat the red once per auth mode with distinct first/other/return oracle paths, avoiding the earlier screenshot overwrite. Both fail the same pending-question assertion (2/39 and 3/48 visible samples respectively). All six saved reply-oracle screenshots were reviewed: the expected text is legible with a ready composer. Unsigned oracle images (evidence/runtime-question-1154/oracles/), test-user oracle images (evidence/runtime-question-1155/oracles/). These are additional runs of the same regression, not extra findings.

Adjacent controls in 1152 pass same-tab question return and denied runtime subscription, but the existing Codex child-open test fails before clicking: it looks specifically for a `button`, while the actual chip is an accessible link with `/s/ses-child-codex-native`. Repeating with the new navigation projection and stream filtering temporarily reverted (1153) produces the same selector failure. Those mock changes are restored. The spec's existing chip helper now accepts an anchor with an href or a button; its transcript-opening and visibility assertions are preserved. This is a stale QA selector, not an additional application defect. Corrected control validation is recorded separately below.

Corrected adjacent controls, 1156/1157:

```bash
CLAXEDO_E2E_AUTH_MODE=local-unsigned CLAXEDO_E2E_SERVE_MODE=build-preview CLAXEDO_E2E_SUITE=core npx playwright test --config playwright.config.ts e2e/playwright/core-docks.spec.ts e2e/playwright/core-harness-rendering-matrix.spec.ts --grep 'an answered question stays absent|subagents — Codex native opens|subagents — unauthorized parent' --workers=1 --retries=0 --trace=on
```

**3 passed, 0 failed** (1156). Same command with `CLAXEDO_E2E_AUTH_MODE=test-user`: **3 passed, 0 failed** (1157). The six saved final oracle images were reviewed, including the visible child transcript beside the parent after link activation. Unsigned controls (evidence/question-controls-1156/), test-user controls (evidence/question-controls-1157/). Some signed mock screenshots show a Reconnecting banner during finite SSE response turnover; these controls do not establish transport stability. No new application defect is classified from that mock-only observation. The child-open test retains its existing child-text assertion and parent reply oracle; this is adjacent control validation, not new comprehensive child-oracle coverage.

Final focused lint for the three touched QA source files is **0 warnings, 0 errors**. Final E2E typecheck still reports only the unrelated registry owner mismatch. The architecture ratchet result above remains the applicable passing run; no production imports or baselines changed. No production fix, commit, push, or PR is included in this slice. The overall 105-state/remaining-regression task stays incomplete.

### Approved permission replay: regression 1158/1159

The existing Codex state 74 failure now has its own regression beside the other permission cases in `core-docks.spec.ts`: `an approved Codex permission stays absent after switching sessions and replaying runtime events`. The previously captured live return frame (evidence/codex-permission/stale-return.jpg) was reviewed again before this reproduction.

The test clicks Allow once and verifies exactly one successful `once` request, delivers `permission.replied`, and observes the dock disappear. It visits another existing session through the rail and waits for that session's scoped runtime response and visible completed reply. On return it waits for the retained permission request body on the runtime stream, samples each frame, and verifies the settled reply through the shared oracle. The no-pending-dock assertion fails: the approved command and Allow/Deny controls briefly replace the composer again. No extra permission response is sent and no request escapes the shared mock.

```bash
CLAXEDO_E2E_AUTH_MODE=local-unsigned CLAXEDO_E2E_SERVE_MODE=build-preview CLAXEDO_E2E_SUITE=core npx playwright test --config playwright.config.ts e2e/playwright/core-docks.spec.ts --grep 'an approved Codex permission stays absent' --workers=1 --retries=0 --trace=on
```

**0 passed, 1 failed** (1158), `approved permission repaints during rail return`: expected `[]`, received one painted pending-dock sample among 35. Same command with `CLAXEDO_E2E_AUTH_MODE=test-user`: **0 passed, 1 failed** (1159), same assertion and one painted sample among 35. Neither run retries.

Evidence: unsigned log (evidence/permission-replay-1158/run.log), unsigned flash (evidence/permission-replay-1158/review/frame-0206.jpg), unsigned transition (evidence/permission-replay-1158/review/contact-1.jpg), recorded permission/runtime responses (evidence/permission-replay-1158/permission-responses.json), test-user log (evidence/permission-replay-1159/run.log), test-user transition (evidence/permission-replay-1159/review/return-contact.jpg), and separate first/other/return reply-oracle screenshots in each run's `oracles/` directory. Both return transitions and all six oracle screenshots were visually reviewed. Full videos and all pre-return frames were not reviewed. The finite mocked SSE turnover can show Reconnecting on the signed first-send screenshot; these tests do not prove transport stability.

The regression is marked fixme and registered as entry 64. This covers the permission round-trip variant, not the child-error-panel or repeated-round-trip variants. No production code changed. Counts now stand at **20/33 original families with qualified red regressions**, plus two additional selector families: **22 reproduced families / 23 failing test cases**. Thirteen original families still lack a qualified red. Focused lint passes with zero warnings/errors; E2E typecheck still fails only on the unrelated credential registry owner mismatch. No production imports changed, so the existing passing architecture-ratchet run remains applicable. The full goal remains incomplete.

### Completed fold replay: corrected regression 1160–1166

Experiments 1161 and 1162 each passed once unsigned, but neither is accepted regression coverage: the first added a follow-up and tested an older turn; both watched only an expanded fold button. The current live failure removes the fold itself. Their archived traces remain diagnostic evidence. A fresh read of the original 1022 session retained no events in 1163, so it could not supply a current replay fixture.

Real Codex probe 1164 runs two separate successful printf calls with prose between them in a second browser tab. The original tab first visits the completed session through its rail. It initially shows folded work; samples 31–42 expose both command rows at approximately 2506–3307ms, and sample 43 is folded again at 3408ms. The composer remains mounted. Three before/during/after frames are visually reviewed. Recording (evidence/fold-first-open-1164/capture.webm), timing (evidence/fold-first-open-1164/timing.json), qualification (evidence/fold-first-open-1164/result.json). The separate HTTP replay read does not prove precisely which events the browser subscription applied.

The fixture now stores that successful turn and its 35 normalized rendering events. The test uses the existing shared runtime and geometry sampler, expands and explicitly closes the latest completed turn, visits another existing session through the rail, waits for its scoped stream and completed reply, and returns. It requires the retained runtime body to contain the assistant ID. The assertion samples painted tool rows, including when no fold button exists.

```bash
CLAXEDO_E2E_AUTH_MODE=local-unsigned CLAXEDO_E2E_SERVE_MODE=build-preview CLAXEDO_E2E_SUITE=core npx playwright test --config playwright.config.ts e2e/playwright/core-harness-rendering-matrix.spec.ts --grep 'a manually closed latest Codex turn' --workers=1 --retries=0 --trace=on
```

**0 passed, 1 failed** (1165): `closed completed work exposes tool rows during runtime replay`, expected `[]`, received 14 painted samples among 99. Same command with `CLAXEDO_E2E_AUTH_MODE=test-user`: **0 passed, 1 failed** (1166), 12 painted samples among 87. Both fail the observed application assertion, with no unhandled mock requests. All six before/other/return reply-oracle images and selected return transition frames were reviewed. The return oracle proves readable completed reply text while the work is exposed; the final folded-state assertion after the failure is not reached. Full videos are not fully reviewed. Unsigned evidence (evidence/fold-replay-1165/), test-user evidence (evidence/fold-replay-1166/).

The test is marked fixme and registered as entry 65. It covers the manually closed latest-turn return variant; cold first-visit automation and reload remain uncovered. Counts are now **21/33 original families with qualified red regressions**, plus two selector families: **23 reproduced families / 24 failing test cases**. Twelve original families still lack a qualified red regression. No application code changed and no fix is claimed.

Final focused checks for this slice: `bun run lint -- packages/claxedo-app/e2e/playwright/core-harness-rendering-matrix.spec.ts` passes with zero warnings/errors; `bun run typecheck:e2e` fails only on the unrelated credential registry owner mismatch. Lint (evidence/fold-replay-1166/lint.log), typecheck (evidence/fold-replay-1166/typecheck.log). No production import changed; the passing root architecture-ratchet run recorded under 1150 remains applicable. No commit, push, or PR is included. The full QA goal remains incomplete.

### Interrupted command return: fresh controls 1167–1169

The original heavy Codex session now restores the interrupted fixture 985 as Shell Failed; expanding it shows Tool execution interrupted. Its current message API returns 142 rows, but a fresh runtime subscription yields no events before its 3-second read timeout. The reviewed terminal screenshot (evidence/interrupted-heavy-1167/current-terminal.png) and qualification (evidence/interrupted-heavy-1167/result.json) establish current terminal restoration, not replay of the historical failure.

Probe 1168 is excluded as an interruption attempt: the 45-second command completed normally before Stop took effect. Probe 1169 starts a 120-second command, confirms tool-start/tool-input on the real runtime stream, clicks Stop, and settles to Shell Failed with You stopped after 29s. The stored tool is error and the retained sequence includes tool-error. It visits the completed 1164 session and returns through the rail. All 90 sampled return states remain terminal. Recording (evidence/interrupted-replay-1169/capture.webm), stopped (evidence/interrupted-replay-1169/stopped.png), return (evidence/interrupted-replay-1169/frame-0045.jpg), qualification (evidence/interrupted-replay-1169/result.json). The stopped frame and two return frames were visually reviewed; full video was not.

This bounded fresh return passes, but the exact browser return stream body was not captured. It does not close the historical heavy-session Running-after-interruption finding or supply a failing regression. No test or application code changed in this investigation; remaining-family counts are unchanged. The earlier isolated lifecycle/reload control remains the existing coverage.

### OpenCode archive recheck and test strengthening 1170–1172

The original disposable OpenCode fixture 619 now archives successfully through its rail button. The observed PATCH returns HTTP 200 after 6382ms (`server-timing` reports 6305ms); its row disappears and the populated neighbouring OpenCode session opens. A subsequent session GET contains the requested `time.archived`. Request and response (evidence/archive-recheck-1170/network-later.json), persisted row (evidence/archive-recheck-1170/session-after.json), reviewed destination (evidence/archive-recheck-1170/settled.png), qualification (evidence/archive-recheck-1170/result.json). This is a static successful recheck, not proof of smooth transition timing or of the historical click's cause.

The existing sidebar archive test is being strengthened with an actual first send, separate reply oracles before/pending/after archive and reload, a 6500ms archive response delay, and a rejected archive that retains the current populated session. Its scenario now uses the shared runtime exclusively. Archive timestamps belong to that mock's session store and feed its workspace rows and canonical navigation projection; rejected writes do not mutate that store.

Experiment 1171 fails before the mutation because a session title beginning with Archive makes the old broad accessible-name regex match both navigation and archive buttons. The test now uses the observed archive control's dedicated data-slot. Experiment 1172 reaches reload but exposes a fixture mapping mistake: the canonical navigation builder accepts `archived`, while the mock passed its output spelling `archivedAt`. The shared projection input is corrected; these failures are QA setup defects, not new product findings. Their traces remain archived.

Archive control qualification: 1173 reaches all archive/reply assertions but fails the no-unhandled-request guard because restored session metadata is absent from the shared mock. The added metadata route derives known sessions from the same store and follows SessionMetaRoutes' response shape. Runs 1174/1175 then pass three times per auth mode, zero retries; unsigned reply images are reviewed, signed images remain archived pending final-current review. These are intermediate passes. Extending the mock's experimental session list to honor its canonical archived filter initially exposes a local URL-scope typo in 1176 (six setup failures); it is corrected before the final runs. None of these fixture failures count as application findings.


### Final archive controls and portable replay fixtures: 1177–1180

The strengthened populated OpenCode archive scenario and the existing only-active-session control each pass three times in local-unsigned and test-user modes: **6 passed, 0 failed per mode**, zero retries. All 30 populated-flow reply screenshots were reviewed across the two modes. They show the original reply before and during the delayed request, the neighbour after success and reload, and the same neighbour with an error toast after rejection. These checkpoint assertions do not prove continuous transition geometry or transport stability; finite mocked SSE turnover displays Reconnecting in some screenshots. The historical live archive failure remains without a qualified red reproduction.

```bash
CLAXEDO_E2E_AUTH_MODE=local-unsigned CLAXEDO_E2E_SERVE_MODE=build-preview CLAXEDO_E2E_SUITE=core npx playwright test --config playwright.config.ts e2e/playwright/core-sidebar-tree.spec.ts --grep 'archiving a populated OpenCode session|archiving the only active session' --workers=1 --retries=0 --trace=on --repeat-each=3
```

Same command with `CLAXEDO_E2E_AUTH_MODE=test-user`. Unsigned final evidence (evidence/archive-control-1177/), test-user final evidence (evidence/archive-control-1178/). Earlier setup failures 1171–1173 and 1176 are excluded from product counts.

The completed-fold replay fixture replaces captured machine paths with the scenario's portable directory; raw live evidence remains unchanged. Re-running the existing fold command above produces **0 passed, 1 failed** in each mode: unsigned 1179 has 15 painted tool-row samples among 76; test-user 1180 has 12 among 122. All six reply-oracle images are reviewed and visibly show the work rows exposed on return. The failure remains `closed completed work exposes tool rows during runtime replay`; its fixme is restored. Unsigned (evidence/fold-portable-1179/), test-user (evidence/fold-portable-1180/). No new defect family is added.

`bun run lint` passes: **0 warnings, 0 errors, 5390 files** (1178). `bun run test:architecture-ratchets` passes (1174), five products/eight policies, without baseline changes. `bun run typecheck:e2e` currently fails on two external `SandboxSecretBrokering` assignment errors in `claxedo-server/src/workspace/supervisor/config-sync.ts:16` and `sandbox.ts:114`; the previous credential-owner mismatch is no longer the current blocker. App architecture initially also identified machine paths in the QA fixtures; both fixtures now use their scenario directories. The final `bun run test:architecture` result (1180) is **251 passed, 1 failed**, solely the existing module-scope mutable-state count 45 against baseline 44. No production code or baseline changed. The separately normalized commentary fixture is being revalidated before accepting its portability edit.


Portable commentary fixture revalidation 1181/1182: the existing restored-commentary control passes **3/3 per auth mode**, zero retries. All 48 distinct restored/reloaded passage screenshots and the saved final first-send screenshot from each mode are reviewed: commentary, both failed commands and both exit-code passages remain legible. The first-send screenshot uses an existing shared filename across repeats, so earlier repeat first-send images are overwritten; this recheck does not claim separate review of those images or full videos. This is path-only revalidation of existing coverage, not a new green rule test. Archived oracle directories also contain older files; only `restored-failure-*.png` and the scenario's named first-send PNG belong to this review.

```bash
CLAXEDO_E2E_AUTH_MODE=local-unsigned CLAXEDO_E2E_SERVE_MODE=build-preview CLAXEDO_E2E_SUITE=core npx playwright test --config playwright.config.ts e2e/playwright/core-harness-rendering-matrix.spec.ts --grep 'restored Codex failure commentary renders' --workers=1 --retries=0 --trace=on --repeat-each=3
```

Same command with `CLAXEDO_E2E_AUTH_MODE=test-user`. Unsigned log and reviewed contacts (evidence/commentary-portable-1181/), test-user log and reviewed contacts (evidence/commentary-portable-1182/). Final focused lint of mock-runtime.ts, core-sidebar-tree.spec.ts and core-harness-rendering-matrix.spec.ts passes with zero warnings/errors. The architecture result remains 251 pass/1 external debt failure. No temporary fixme removal remains, no production fix or commit is claimed, and the full goal remains active.


### Workspace file selection recheck 1183 and regression setup 1184–1186

The current live web app still shares the selected file across sessions in the same workspace. Codex session `a8e275cd-bdcc-4971-9d0a-04923de0d505` selects README.md. The visible neighbouring Codex session `e54d4988-2a23-44d0-9540-1f551be75484` selects CLAUDE.md. Returning through the rail to the first session keeps CLAUDE.md selected, including after settling. All three relevant screenshots are reviewed. Before (evidence/file-scope-1183/b-before.png), other session (evidence/file-scope-1183/c-before.png), returned (evidence/file-scope-1183/b-settled.png), qualification (evidence/file-scope-1183/result.json). This is a static recheck of state 100; prior state-100 recordings remain the movement evidence. Both previews show Failed to fetch, so successful file loading is not claimed. The initial OpenCode return was unavailable after Load more and Retry failed; using two visible sessions avoids inventing a successful rail transition.

The existing panel working-set key intentionally includes server/workspace/review identity and excludes session ID. This explains a policy conflict with the brief's per-session file-selection requirement; it does not settle the requested product behavior. No production code is changed here.

A regression is being added to the approved session-rendering-navigation home. The split-pane spec owns shell splits and has no file-panel selection scenario to extend. Experiment 1184 is interrupted after identifying an incorrect checkbox locator (the native accessibility tree calls it a checkbox, but the DOM control is a pressed button). Experiment 1185 times out on the file tree because the shared file handler does not strip `/api/wr/`; trace requests to `/api/wr/file` and `/api/wr/file/status` fall through. That mock path normalization is corrected before 1186. Both are setup failures, excluded from product defect counts.


Final file-selection regression 1186/1187: **0 passed, 1 failed per auth mode**, zero retries. The final assertion expects `file://first.txt` and receives `file://second.txt` throughout its ten-second polling interval. All six reply-oracle images and six selection screenshots are reviewed. The return visibly contains second.txt contents beside the first session's reply. The first-selected screenshot precedes content loading; no first-file content-rendering claim is made. Full videos were not reviewed. The test is marked fixme and registered as entry 66. This qualifies the file-selection variant of the existing panel-selection family; child and browser variants remain outstanding.

```bash
CLAXEDO_E2E_AUTH_MODE=local-unsigned CLAXEDO_E2E_SERVE_MODE=build-preview CLAXEDO_E2E_SUITE=core npx playwright test --config playwright.config.ts e2e/playwright/core-session-rendering-navigation.spec.ts --grep 'workspace file selection returns' --workers=1 --retries=0 --trace=on
```

Same command with `CLAXEDO_E2E_AUTH_MODE=test-user`. Unsigned evidence (evidence/file-scope-1186/), test-user evidence (evidence/file-scope-1187/). Counts: **22/33 original families with a qualified red variant**, plus two additional selector families, **24 reproduced families / 25 failing test cases**. Eleven original families lack a qualified red. No production fix or commit is claimed. The goal remains incomplete.


Focused gate: `bun run lint -- packages/claxedo-app/e2e/playwright/core-session-rendering-navigation.spec.ts packages/claxedo-app/e2e/helpers/mock-runtime.ts` passes with zero warnings/errors (1187). `bun run typecheck:e2e` fails with the same two external SandboxSecretBrokering assignment errors in supervisor/config-sync.ts:16 and supervisor/sandbox.ts:114 (1185); no QA type error is reported. No production import changed, and no architecture baseline changed; the previously recorded root ratchet pass and app architecture debt failure remain the latest results. The file-prefix correction is exercised by the new scenario's real routed file requests; broad file-browser/cloud-suite revalidation has not run in this slice.


### Idle heavy-session return: regression 1188/1189

The original state 93 live recordings and scroll measurements remain the live evidence: OpenCode returns to 13340px and Codex to 13127px rather than the top. The Codex return screenshot was reviewed again before implementing this regression. No fresh live run is claimed in this slice.

The new test extends the existing heavy-scroll spec, reusing its normalized 60-turn fixture and shared geometry/rail helpers. It sends and verifies a completed follow-up, loads all 61 user turns, moves to the middle, visits another existing populated session through the rail, and returns while idle. Both runs report **before.top = returned.top = 12492**, maximum 23475; the required `top <= 2` assertion fails after ten seconds. Only after recording that failure and its screenshot does the test manually scroll to the top and verify the first reply. This recovery oracle is not evidence of automatic top positioning. No mock request is unhandled.

```bash
CLAXEDO_E2E_AUTH_MODE=local-unsigned CLAXEDO_E2E_SERVE_MODE=build-preview CLAXEDO_E2E_SUITE=core npx playwright test --config playwright.config.ts e2e/playwright/core-timeline-rendering-scroll.spec.ts --grep 'returning to an idle heavy session' --workers=1 --retries=0 --trace=on
```

**0 passed, 1 failed** (1188). Same command with `CLAXEDO_E2E_AUTH_MODE=test-user`: **0 passed, 1 failed** (1189). All six first/other/manual-top reply-oracle images and all four middle/returned screenshots are reviewed. Full videos are not reviewed. Unsigned evidence (evidence/idle-return-1188/), test-user evidence (evidence/idle-return-1189/). First-frame blanking, fold/title timing and active-session bottom behavior remain uncovered by this test.

The test is marked fixme and registered as entry 67. Counts now stand at **23/33 original families with a qualified red variant**, plus two selector families: **25 reproduced families / 26 failing test cases**. Ten original families lack a qualified red. The full 105-state goal remains incomplete; no production fix or commit is claimed.

Gates: `bun run lint -- packages/claxedo-app/e2e/playwright/core-timeline-rendering-scroll.spec.ts` passes with zero warnings/errors (1188). `bun run typecheck:e2e` still fails on the two external SandboxSecretBrokering assignment errors in supervisor/config-sync.ts:16 and supervisor/sandbox.ts:114, with no QA type error (1188). `bun run test:architecture-ratchets` passes (1189): five products, eight policies, 12599 helpers, no baseline change. The broader app architecture suite was not rerun in this slice; its last recorded result is 251 pass/1 debt failure.


### OpenCode real producer isolation: 1190–1194

The isolated real-server probe now executes the SDK's advertised `shell` tool against the shared scripted model server. The model's next request receives `QA_TOOL_RESULT_1190\nCommand exited with code 0.` as the tool result, while the public stored-message endpoint returns the same call as `tool: "call_1"`, `input: {}`, `output: ""`, and empty metadata. The assistant final token is stored. This proves real execution followed by loss of tool name/input/output at the public transcript boundary; it is not yet a Playwright rendering regression or an on-screen verification. Counts remain 25 reproduced families / 26 qualified failing tests until the rendering assertion is run.

Command (from packages/claxedo-server, with GIT_INDEX_FILE and GIT_AUTHOR_DATE unset): `node --import tsx /tmp/qa-opencode-producer-1194.mts`. Exit 0. Stored messages (evidence/opencode-producer-1194/messages.json), model requests including the actual tool result (evidence/opencode-producer-1194/model-requests.json), run log (evidence/opencode-producer-1194/run.log). The disposable server and scratch workspace were closed and removed in finally.

Excluded setup attempts: 1190 used a nonexistent provider catalog route; 1191 lacked an initialized scratch repository; 1192 used an unsupported unprefixed provider package; 1193 requested `bash`, while this SDK advertises `shell`. They do not count as product defects. Probe 1194 uses the installed SDK's `aisdk:@ai-sdk/openai-compatible` package form and advertised tool name. The next step is to exercise this real producer through the approved Tier R browser spec, preserving the real tool result as the assertion source.


### Local image recheck prerequisite: 1195

The live browser reports the local server unavailable. `lsof -nP -iTCP:2593 -sTCP:LISTEN` returns no listener, and the process connector returns fetch failed. The existing `claxedo-server` launch recipe (`env npm run dev` from packages/claxedo-server) starts successfully and listens on 2593 using the default ~/.claxedo database. The original OpenCode heavy image session then returns HTTP 404 `session_not_found` through the public session endpoint. This recheck cannot recover the original image transcript; it does not supersede the original web/desktop image failures or count as a new rendering failure. A fresh isolated real-server image scenario is required.


### Scoped parallel QA and image response fixture: 1196

The inventory consistency audit reconciles stale report/JSON statuses against the existing register and logs: 23 qualified original families, ten without a qualified red, plus two selector families. Historical row verdicts are preserved. The OpenCode default-agent browser failure and first-token geometry reproduction are delegated and remain unqualified until their actual results/evidence are reviewed.

The shared scripted model endpoint gains a one-shot, prompt-marker-matched text response for real Markdown image scenarios. Its focused HTTP test confirms unrelated requests do not consume the response, exact multiline/file-URI Markdown survives, and the response is consumed once. `bun test ./e2e/helpers/scripted-model-server.test.ts` from packages/claxedo-app: **2 pass / 0 fail**. Focused root lint for scripted-model-server.ts and its test: **0 warnings / 0 errors**. Logs (evidence/image-scripted-text-1196/). These are test-infrastructure checks, not a product image regression. The initial missing-method red and subsequent test transport/parser corrections are not application defects.

The native Browser persistence test was scoped against the existing real desktop scaffold. A Chromium Tier M Browser test cannot prove Electron guest-page persistence. The real desktop regression must verify a served local page before and after A→B→A, including visible guest content and retained address; preserving guest identity is not required. Original native evidence remains valid historical evidence, but current packaged freshness and exclusive launch prerequisites still need verification. No new desktop pass/fail is claimed.


Additional first-send finding, OpenCode browser probe opencode-ui-1195: the user prompt renders, followed by “That turn didn't complete” and `Agent not found: "Build"`. The primary agent has reviewed the failure screenshot (evidence/opencode-ui-1195/playwright/playwright-real-session-re-6acf1--core-tier-real-surface-web-chromium/test-failed-1.png). This occurs before model/tool execution, so it does not qualify the original missing-tool-data rendering case. The delegated test is being narrowed to this catalog/submission identity failure and validated in both auth modes; qualified regression totals remain unchanged until that run and registration finish.


### OpenCode default-agent regression qualified: opencode-ui-1198/1199

The narrowed test submits exactly one prompt through the browser after real history/model hydration. The public catalog names the default agent `Build`; the submission carries that exact value; the stored assistant error says `Agent not found: "Build"`. The scripted model receives zero requests. Both auth modes fail the mandatory reply oracle (`Expected: visible`, `Timeout: 20000ms`) and visibly show the same error. Both failure screenshots were reviewed by the author and primary agent. Full videos are not reviewed. The fixme is registered as entry 68.

Commands from packages/claxedo-app:

```bash
CLAXEDO_E2E_SERVE_MODE=build-preview CLAXEDO_E2E_SUITE=core CLAXEDO_TIER_REAL_E2E=1 CLAXEDO_TIER_REAL_BACKEND_PORT=14317 CLAXEDO_E2E_AUTH_MODE=local-unsigned PLAYWRIGHT_VIDEO=1 npx playwright test --config playwright.config.ts e2e/playwright/real-session-rendering-harnesses.spec.ts --project=chromium --workers=1 --retries=0
CLAXEDO_E2E_SERVE_MODE=build-preview CLAXEDO_E2E_SUITE=core CLAXEDO_TIER_REAL_E2E=1 CLAXEDO_TIER_REAL_BACKEND_PORT=14317 CLAXEDO_E2E_AUTH_MODE=test-user PLAYWRIGHT_VIDEO=1 npx playwright test --config playwright.config.ts e2e/playwright/real-session-rendering-harnesses.spec.ts --project=chromium --workers=1 --retries=0
```

**Each: 0 passed / 1 failed / 0 retries.** Unsigned evidence (evidence/opencode-ui-1198/), test-user evidence (evidence/opencode-ui-1199/). Catalog, submission, stored messages and empty model-request logs corroborate the visible failure. Typecheck retains the same two external supervisor secretBrokering errors; focused oxlint reports zero warnings/errors.

This is an additional default-agent failure, not the original tool-data family: the latter still needs a browser regression that reaches tool execution. Counts: **26 reproduced families / 27 qualified failing tests**; **23/33 original families** have a qualified red variant and ten do not. No production fix is included. The broader goal remains incomplete.


### Harness prerequisite refresh: 1200

A real Claude CLI prompt now completes successfully with `QA_CLAUDE_AUTH_RECHECK` (one turn, exit0), so the historical CLI/authentication failure must not be treated as a current blanket machine blocker. The current desktop's selected Claude `api_key` account still shows rejection; this is a different configured credential path from CLI claude.ai login. Codex CLI login remains available; Cursor CLI reports logged in but cannot fetch user details and the app still offers Connect; pinned isolated Pi reports0.85.0 while PI_EXECUTABLE is unset. No account selection or credential was changed. Exact commands and scoped results (evidence/harness-recheck-1200/result.json), reviewed current desktop provider screenshot (evidence/harness-recheck-1200/desktop-providers.png). This refresh does not convert historical blocked rendering rows to passes; a successful app send is still required.


### Codex first-token geometry regression qualified: first-token-1198/1199

The recorded Codex first-token variation now has a failing test in the existing heavy-scroll home, with a portable fixture extracted from the actual preceding turns and completed shell. After the prompt settles, emitting the first text moves that same prompt from y517 to y459 while the composer remains y752/height52; scrollTop changes388→446. Both before/arrival pairs were reviewed by the primary agent, and the author reviewed all four reply-oracle images as well. The preceding and final reply oracles pass in each mode.

```bash
CLAXEDO_E2E_AUTH_MODE=local-unsigned CLAXEDO_E2E_SERVE_MODE=build-preview CLAXEDO_E2E_SUITE=core PLAYWRIGHT_VIDEO=1 npx playwright test --config playwright.config.ts e2e/playwright/core-timeline-rendering-scroll.spec.ts --grep 'a Codex first reply token' --workers=1 --retries=0
CLAXEDO_E2E_AUTH_MODE=test-user CLAXEDO_E2E_SERVE_MODE=build-preview CLAXEDO_E2E_SUITE=core PLAYWRIGHT_VIDEO=1 npx playwright test --config playwright.config.ts e2e/playwright/core-timeline-rendering-scroll.spec.ts --grep 'a Codex first reply token' --workers=1 --retries=0
```

**Each: 0 passed / 1 failed / 0 retries.** The assertion reports `Expected <=1; Received180` because one sampled transient frame deviates180px; the other89 samples settle58px upward. The transient's visual identity and full videos remain unreviewed; the reviewed steady58px movement is the supported visual claim. Unsigned result (evidence/first-token-1198/result.json), test-user result (evidence/first-token-1199/result.json). Initial wrong-helper-argument setup failure is excluded. Focused lint passes; typecheck retains only the two external supervisor secretBrokering errors.

Registered fixme69. This qualifies the first-token variant of the original transcript-movement family; send, OpenCode, child-chip and separate tool transitions remain open. Counts now: **27 reproduced families / 28 qualified failing test cases**, comprising **24/33 original families** plus three additional families. Nine original families still lack a qualified red. No application fix is included.


### Fresh real image and packaged Browser prerequisites: 1200/1201

The native Browser test is authored in the existing real desktop home, using ordinary New session in main → composer → Submit, not a test-created session payload. Pinned Pi0.85.0 is verified. `bun run package -- --dir` passes, including packaging invariants, and the fresh isolated package launches alongside the untouched existing dev app. Its first Pi turn displays `Pi session file is missing…`; the reply oracle fails before Browser interaction. Exact commands, results and archived evidence (evidence/desktop-browser-1200/result.json). This is not a qualified Browser red and no Browser fixme is added.

Two independent real web image attempts likewise use normal draft → UI first send. Codex fails with `no rollout found for thread id …`; pinned Pi fails with `Pi session file is missing for …`. Each has zero model requests and a failed reply oracle, so neither reaches image rendering. Codex evidence (evidence/codex-local-image-1200/), Pi evidence and prepared image test source (evidence/pi-local-image-1201/). Both screenshots were reviewed by the author. The image case is preserved as an attempted-regression source artifact, not counted as an executable qualified image regression. A separate bounded task is qualifying the native first-send failures through existing first-send test homes; the original file-image and Browser families remain open.

Root `bun run test:architecture-ratchets` passes after these QA additions: five products, eight policies,12599 helpers; no baseline changes. Log (evidence/harness-recheck-1200/architecture-ratchets.log). This is a fresh root ratchet run, not a rerun of the broader app architecture gate.


### Duplicate progress reproduction evidence audit:1202

The historical busy-reload screenshot visibly duplicates progress054/060/067, while the extracted stored strings contain each once. The available full13-part replay is from a later completed state (11text parts,2completed tools). It does not retain the busy response or ordered runtime events surrounding the failure. The duplicate investigator reviewed the original screenshot and compared both sources; no faithful failing replay can yet be built from them. No duplicate test is added or counted. Scoped audit and concrete follow-up (evidence/duplicate-reproduction-audit-1202/result.json). A real working Codex first-send path is needed to capture the missing transport sequence; inventing events or adding another already-passing terminal restoration test would not reproduce the reported failure.


### Native first-send failures qualified in existing journeys: 1202/1203

The existing Pi and Codex native tests reproduce the failures encountered by the image and desktop attempts. Both use their ordinary draft → UI first-send flow. Pi reports a missing session file; Codex reports a missing rollout. Both fail the first mandatory reply oracle with zero model requests, in both auth modes. All four error screenshots were reviewed by the author and primary agent. Actual browser message responses were extracted from traces; an additional diagnostic404 caused by an omitted directory is excluded and its query is corrected.

```bash
PI_EXECUTABLE=/tmp/claxedo-qa-pi-0.85.0/node_modules/.bin/pi CLAXEDO_E2E_SERVE_MODE=build-preview CLAXEDO_E2E_SUITE=core CLAXEDO_TIER_REAL_E2E=1 CLAXEDO_TIER_REAL_BACKEND_PORT=14317 CLAXEDO_E2E_AUTH_MODE=local-unsigned PLAYWRIGHT_VIDEO=1 npx playwright test --config playwright.config.ts e2e/playwright/real-harness-local.spec.ts --grep '(pi-workspace harness|codex native SDK harness) completes exact turns' --project=chromium --workers=1 --retries=0
```

From packages/claxedo-app. Unsigned1202: **0 passed / 2 failed**. The same command with `CLAXEDO_E2E_AUTH_MODE=test-user` (1203): **0 passed / 2 failed**. Zero retries. Unsigned evidence (evidence/native-first-send-1202/), test-user evidence (evidence/native-first-send-1203/). Full videos are unreviewed; visible error screenshots and stored-message evidence establish the static failure. Focused lint passes and typecheck retains only the two existing supervisor secretBrokering errors.

Registered entries70–71, marking only these two existing tests fixme. No duplicate first-send tests or application fixes were added. The scratch-workspace helper now stages/commits only its own README.md and omits inherited git index/date values. Totals: **29 reproduced issue families / 30 qualified failing test cases**, with **24/33 original families** qualified and nine still missing a red. The two native failures are tracked by harness, without claiming independently proven internal root causes. Original image, native Browser, and missing-runtime-event duplicate regressions remain open.


### First-token measurement refinement:1206/1207

Reviewing every decoded frame60–139 in each original first-token recording (160frames total), including full-size adjacent insertion frames, corroborates the steady58px prompt movement. QA_SH first appears at unsignedframe85 and signedframe83. The earlier180px maximum is exclusively the helper's synchronous initial sample, before its first requestAnimationFrame wait; no reviewed video frame establishes that larger movement as painted. At25fps the recording cannot exclude a shorter transient.

The test now retains all raw measurements but excludes only that synchronous sample from the painted-frame assertion. Requalification uses the same two build-preview commands as first-token1198/1199: **0 passed / 1 failed / 0 retries per mode**, now `Expected <=1; Received58` across89 animation-frame samples. All eight final before/arrival and reply-oracle images were reviewed by the author. Unsigned result (evidence/first-token-1206/result.json), test-user result (evidence/first-token-1207/result.json). Register69 is updated; counts do not change.

Copied third-party Trace Viewer JavaScript caused a root lint failure when archived as unpacked source under evidence. Those generated viewer directories are now preserved in trace-viewer.zip alongside each report, with report data/oracle images and original trace archives retained. No source lint rule or baseline was relaxed. Unzip the viewer archive into its report directory to restore the report's local Trace Viewer assets.

### OpenCode restored shell and local-file image qualified: 1208–1210

Two API-started real OpenCode journeys qualify original rendering families while leaving browser first-send unresolved. The shell executes `printf OPENCODE_API_TOOL_RESULT`, and its stdout reaches the model. Stored presentation data nevertheless contains `call_1` with empty input/output, and expanding the restored tool renders only “Call 1.” Both completed-reply oracles pass before the command/output assertions fail. Register 72 covers this restored shell variant; read/edit/MCP/permission, grouping, exit-code and running-command variants remain open.

The image journey creates a real 512×512 PNG, receives Markdown referencing that existing file, and opens/reloads the session in the browser. The reply is visible, but the image renders only “QA local image” alt text; no matching `img` exists. Register 73 covers restored OpenCode web rendering. The 80px tile and full-preview assertions are unreached, and desktop/other-harness variants remain unqualified.

Unsigned 1208: **0 pass / 1 fail** (shell). Test-user 1209: **0 pass / 2 fail** (shell and image). Unsigned 1210: **0 pass / 1 fail** (image). Zero retries. The author reviewed eight reply-oracle images and four target images; the primary agent additionally reviewed the unsigned expanded shell and test-user restored image. Full recordings are unreviewed. Exact commands, case outcomes, data and limitations: 1208 (evidence/api-tool-1208/result.json), 1209 (evidence/api-rendering-1209/result.json), 1210 (evidence/api-image-1210/result.json).

Both source tests are fixme after these reds. Totals at the 1208–1210 checkpoint were **31 reproduced issue families / 32 failing test cases**, with **26/33 original families qualified and 7 without a red**. No original live verdict changes and no production fix is claimed. A successful API-started turn is not evidence that the separate Build-agent browser first-send rejection is fixed.

### Boot splash-to-composer handoff qualified: 1216/1217

The strengthened cold explicit-draft journey now checks visible composer readiness, including ancestor opacity, rather than only the shell container. Both auth modes fail: **0 pass / 1 fail / 0 retries each**. Unsigned records three DOM samples with neither splash nor visible composer; test-user records six. The full 291 encoded frames across both recordings were reviewed using contact sheets, with boundary frames additionally reviewed at native resolution. Both videos show splash at 0.720s, missing main content at 0.760s, and composer at 0.800s. Unsigned is nearly blank apart from a faint divider; test-user retains the sidebar while its main region is empty. DOM sample counts and encoded video frames are different clocks and are not treated as exact gap-duration measurements. Unsigned evidence (evidence/boot-handoff-1216/result.json), test-user evidence (evidence/boot-handoff-1217/result.json).

Each test then successfully creates one session and sends the first prompt through the UI, with one completed reply and no unhandled mock API requests. The signed recording retains “Reconnecting…” after visual turn settlement, so connection recovery is not established. This is a browser/shared-mock boot handoff regression, not real-provider or packaged-desktop proof. Earlier 1211/1212 greens tested the weaker shell-container condition and are not continuity coverage.

Register 74 marks the existing boot test fixme. Totals at the 1216/1217 checkpoint: **32 reproduced issue families / 33 failing test cases**. The boot report is an additional original user report outside the numbered map, so **26/33 numbered families qualified / 7 without a red** remains unchanged. Historical live rows and verdicts are unchanged; no application fix is claimed.

### Current gate results: 1218

The build-preview E2E build passes (exit 0); root lint passes with 0 warnings/0 errors across 5391 files; root architecture ratchets pass for five products, eight policies and 12599 helpers without baseline changes. E2E typecheck still reports the two external supervisor `SandboxSecretBrokering` mismatches (exit 2). App architecture is **251 pass / 1 fail**, `moduleScopeMutableState` 45 versus baseline 44 (exit 1). The full real gate finished with 29 pass / 52 fail / 8 skipped / zero retries. The core gate finished with 1 pass / 6 skipped / zero retries; its artifacts are preserved in gates-core-1219 (evidence/gates-core-1219/run.log). These are raw runner counts, not full visual acceptance. Exact commands and gate records (evidence/gates-1218/result.json). The steering candidate was still excluded at this gate checkpoint; its subsequent evidence qualification is recorded below.

### Intermittent OpenCode steering reply absence qualified: 1213–1215

The API-started real OpenCode steering journey passes unsigned in 1213 (**1 pass / 0 fail**) and 1214 (**2 pass / 0 fail**), but test-user 1215 records **1 pass / 2 fail**; all runs have zero retries. In both failures the shared first completed-reply oracle times out after 20s with two user prompts visible, while canonical full message reads contain the completed `OPENCODE_STEERING_INITIALOPENCODE_STEERING_FINAL` assistant text. The failed runs never reach reload, so they do not qualify reload failure. 1213 command/results (evidence/steering-reply-1213/result.json), 1214 command/results (evidence/steering-reply-1214/result.json), 1215 command/results (evidence/steering-reply-1215/result.json).

The 24-request network-order analysis (evidence/steering-reply-1215/analysis/network-ordering.json) shows later browser `latest-surface` / `latest-turn` reads containing only the steering user. In failed repeat 1 the initial empty history response finished approximately 47ms before the API prompt began, so a still-pending initial history request alone does not explain both failures. The runtime stream body is not retained, and the passing repeat has no retained network trace; event loss or the precise producer/consumer cause is not established. The proposed exact-session initial-history guard is not implemented and is not counted as coverage.

The primary agent reviewed all ten outcome images: eight passing reply-oracle observations and two failure screenshots. Full videos remain unreviewed. Register 75 marks the unchanged regression body fixme after this intermittent red. Coverage is the real API-started live steering variation only, not UI first-send, failed reload, desktop restart or every historical missing-reply variation. Current totals are **33 reproduced issue families / 34 failing test cases**, including **27/33 original numbered families qualified / 6 without a red**. Historical live verdicts and the source behavior remain unchanged.

### Pi version alignment: 1220

The explicitly requested version fix is committed separately as `4c4cd31526`: runtime, fixtures and sandbox image definitions now require Pi 0.85.1, matching the installed binary and npm latest. The compiled runtime was rebuilt. The executable regressions pass, but the two unsigned native-harness browser tests still visibly fail with `Pi session file is missing`. No session-rendering family is closed by the version fix. Exact commands and outcomes (evidence/pi-upgrade-1220/upgrade.md).

### Native Browser prerequisite: 1221–1222

A fresh `bun run package -- --dir` passes. The first Browser attempt stops at an obsolete credential-fixture document (`unreadable_secret`); it is excluded as a product finding. Removing that fixture lets both actual native Claude sends complete. The next attempt reaches Browser navigation, but its initial URL does not load: the real guest stays `about:blank` with an empty body. The round-trip assertions remain unreached. First attempt (evidence/browser-return-1221/result.json), second attempt (evidence/browser-return-1222/result.json), reviewed video frame (evidence/browser-return-1222/initial-navigation-blank.png). Native video recording is now confirmed operational. Counts remain unchanged; initial-navigation timing is a new diagnostic lead, not a qualified regression.

### Actual native image send and app restart: 1223–1224

The new existing-home desktop case passes **3/3, zero retries** using the OS clipboard and real Claude binary against the guarded scripted endpoint. The first submitted prompt carries one image; the next session and a new session after app-only restart carry zero. The original image reply is restored from the retained profile/daemon. Clipboard restoration is acknowledged in all three runs. All 12 reply-oracle observations (eight unique images) were visually reviewed, plus the first attached-image screenshot. Full recordings remain unreviewed; continuous geometry is not claimed. First run (evidence/native-image-1223/result.json), two repeats (evidence/native-image-1224/result.json).

This closes the missing native-paste/app-restart coverage variation for unsigned Claude only. It does not close the historical attachment leak, test the separate development-server restart, or qualify compact-image/preview geometry. Signed-native and other harness variations remain pending. Reproduced-family and failing-case counts remain 33/34.

### Browser session return: qualified regression 76

Run 1225 reaches the intended native Browser round trip: both UI-created Claude sessions reply, and the initial Browser address plus real Electron guest URL/body match the fixture HTTP page. After session A → B → A, the Browser tab survives but the address becomes empty and its guest returns `about:blank`/empty body. The five reply-oracle observations and before/return page screenshots were visually reviewed. The existing desktop test is now marked fixme and registered as 76. Exact command and result (evidence/browser-return-1225/result.json). This qualifies the unsigned Claude variation; original Codex, signed-native, and the earlier immediate-navigation lead remain unverified.

Current totals: **34 reproduced issue families / 35 failing test cases**; **28 of 33 original families qualified / 5 without a reliable red reproduction**. No Browser production fix was made.

## HTTP 404 and image-load sampling — 1228–1229

Runs 1228/1229 extend the existing image regression to an observed HTTP 404 in unsigned and test-user modes (each 0 pass, 1 fail, 0 retries). Loading, corrupt and missing-image boxes are 645.75 × 37.1875px; the valid image becomes 512 × 512px. Thirty post-decode frame samples catch 449px and 474px upward movement respectively, with unchanged composer geometry. Both reply-oracle images and before/after images were reviewed in each mode. Clicking the loaded image still fails to open the preview; closing it remains unreachable. The earlier single-frame run 1227 missed the later scroll correction, which its screenshot exposed; 1228/1229 supersede that measurement method. Full video review remains open. This extends entry 43 without adding a finding or test case. Unsigned evidence (evidence/image-404-1228/result.json), test-user evidence (evidence/image-404-1229/result.json).

Focused lint passes with 0 warnings / 0 errors. `bun run typecheck:e2e` exits 2 with the same two external supervisor `SandboxSecretBrokering` errors. `bun run test:architecture-ratchets` passes: 5 products, 8 policies, 12607 helpers; no baseline change. Counts remain 34 reproduced families / 35 failing cases.

Native evidence helpers are committed as `243e5a3aea test(e2e): support preserved native clipboard and desktop video` (three explicit paths; no push). This contains the clipboard helper and Electron video option only; the Browser and clipboard scenario tests remain pending commit. Root `bun run lint` passes with 0 warnings / 0 errors across 5396 files (log (evidence/image-404-1228/root-lint.log)).

## Child-permission live prerequisite — 1230

The state 75 child-denial recheck did not reach child execution. The documented server recipe initially restored the app, then its listener on 2593 disappeared while the process retained an SSE connection. The UI showed server/model/session-loading failures. Closing the QA tab allowed the server process to exit. No child approval or filesystem write was attempted; the shutdown cause remains unknown. Code inspection identifies immediate denial for an approval without an active-thread entry as a hypothesis, not a qualified reproduction. Observed state and limits (evidence/child-permission-1230/result.json).

Committed `5740bfb2b6 test(e2e): script exact Markdown replies through the model endpoint` (two explicit helper/test paths; no push). `bun test e2e/helpers/scripted-model-server.test.ts` passes three times with 2 pass / 0 fail each; focused lint passes with 0 warnings / 0 errors. This supports real-harness Markdown fixtures and does not qualify the blocked child-permission flow.

Cold-history first-wheel control now passes three times per auth mode with zero retries (1232/1233). The same final paragraph stays mounted across 45 samples and moves at most 1px after a 1px wheel input. All six reply-oracle observations and all 296 encoded video frames were reviewed. This qualifies only seeded cold-history wheel geometry. The initial 1231 selector ambiguity is excluded from defect counts. Unsigned result (evidence/cold-wheel-1232/result.json), test-user result (evidence/cold-wheel-1233/result.json).

QA control qualification (1235–1237): short follow-up geometry, same-tab answered-question return, and restored interrupted-command state each pass three times per auth mode, zero retries. All 36 reply-oracle observations across the 18 cases were reviewed; sampled movement frames were reviewed for the short follow-up and question-return cases. The short follow-up has zero measured row/composer movement across 180 samples and an intercepted central WebSocket. The initial 1234 HTTP-SSE guard failure was a test defect. Test-user Reconnecting remains visible, so overall transport health is not claimed. These controls do not close live replay, heavy-session or original streaming variants. Unsigned geometry (evidence/short-geometry-1235/result.json), test-user controls (evidence/qa-controls-1236/result.json), unsigned navigation controls (evidence/qa-controls-1237/result.json).

Fresh commit checks (1238): root lint 0 warnings / 0 errors; root architecture ratchets pass for five products/eight policies/12607 helpers without baseline changes. E2E typecheck still has the two external supervisor proxy-enum errors; app architecture has 251 pass / 1 fail (mutable state45 versus44). Initial root-ratchet attempts exceeded git-list stdout capacity; verified ZIP preservation of derived video frames restores the passing run. Exact commands and outcomes (evidence/qa-commit-1238/gates.json).

Committed `576e68805d test(e2e): preserve reproduced session-rendering regressions`: 19 explicit QA paths, no production rendering changes and no push. The commit retains the qualified 35 failing cases with decision entries and the reviewed green controls. Remaining test changes and the full live acceptance scope are not complete. Exact changed files (evidence/qa-commit-1238/commit.json).

User clarification: the duplicate-reply report concerns repeated chunks of text within one turn while streaming is in progress. It does not require a duplicate whole reply or persistence after completion. The existing restoration control does not cover that failure. State17 remains without a qualified live-streaming red regression; capture incoming text updates and per-frame rendered text together, then preserve the actual failing sequence. Counts remain unchanged.

Additional requested lifecycle coverage: a running tool followed by disconnection or another failure can remain displayed as Running indefinitely. New Tier R probe `Claude tool completion reconciles after the browser reconnects and reloads` runs a real held command, requires the existing browser event connection to close, finishes the command while offline, checks canonical server completion, then checks the reply/tool after reconnect and reload. The probe is not yet qualified. Server/runtime crashes, permanent disconnection, other harnesses and the original long-soak failure remain separate variants.

Browser-disconnection tool lifecycle coverage: real Claude command starts, the established event WebSocket is forcibly closed, the command completes while offline, and the UI settles after reconnect and reload. Three passes per auth mode, zero retries; twelve reply screenshots and 114 video samples reviewed. Unsigned evidence (evidence/tool-disconnect-1246/result.json), Test User evidence (evidence/tool-disconnect-1247/result.json). This does not close the runtime-crash, permanent-outage or historical long-soak Running findings.

Committed browser-disconnection coverage: `dfed52f19a test(e2e): verify tool completion across browser disconnection` (three explicit QA paths). Gate results (evidence/tool-disconnect-1247/gates.json). No application fix is included.

Native streaming coverage now records actual runtime SSE text alongside animation-frame observations. Claude first and follow-up replies pass three times per auth mode (12 cases, zero retries): Test User (evidence/streamed-segments-1255/result.json), unsigned (evidence/streamed-segments-1256/result.json). All 18 reply-oracle images and 282 video samples were reviewed. Across 8,182 animation-frame observations, the tests record 29–30 distinct partial replies per case, reject duplicated segments in any sampled frame, and require all 16 segments once at completion. The original intermittent duplicate-chunk finding stays open; subagents, reconnect-during-stream, and heavy tool turns are not covered. The Codex probe is blocked before streaming by the already-registered `no rollout found` first-send error.

The model fixture previously ignored pacing for native Messages and Responses; two red HTTP tests reproduced that fixture defect, and all four fixture tests now pass. An initial paint observer wrongly excluded active rows carrying `aria-hidden`; screenshot review showed visibly streaming text, so the missing-partial-reply claim is withdrawn. These setup failures add no product findings. Fixture evidence (evidence/paced-text-1248/result.json).

Committed native streaming coverage: `0dbad3a21e test(e2e): observe native text chunks during live rendering` (four explicit QA paths). Gate results (evidence/paced-text-1248/gates.json). Full real-harness gate rerun 1257 finished: **34 passed / 50 failed / 8 skipped**, zero retries, 40.5 minutes. The two native streaming cases and browser-disconnection case passed. Terminal result and limits (evidence/full-real-1257/result.json). This is not an overall acceptance pass; failure triage and full visual review remain incomplete.

### Mention keyboard diagnostic: 1258–1260

The unresolved composer-controls-1239 flake reproduces again without intermediate waits: 1259 has **9 pass / 1 fail**, zero retries, unsigned. The failing screenshot again shows an open mention popup and the literal `@` instead of an inserted reviewer pill. A passive event recorder confirms ArrowDown, ArrowUp and Enter target the focused editor; this rules out focus loss in that run. The harness-specific agent-catalog response arrives after the key sequence, but causation is not established. Failure and key evidence (evidence/mention-diagnostic-1259/), analysis (evidence/composer-controls-1239/diagnosis.json).

Adding focus/selection waits between keys produced 3 passes in 1258; those waits were removed because they can mask the race. A later diagnostic with post-dispatch observation and the strengthened sent-mention reply oracle has **20 pass / 0 fail** (ten runs of each scenario), zero retries. These later passes do not erase the earlier failure. All ten sent-mention reply screenshots are byte-identical and their unique image was reviewed: the highlighted user mention and completed reply are visible. Videos and the fresh-build/auth twin remain unqualified. Both diagnostics reuse the unsigned static frontend with isolated mock pages and separate output directories; they do not modify or restart the full real gate's server. 1260 results (evidence/mention-diagnostic-1260/result.json).

No new qualified finding is added to the 34-family / 35-case count. The keyboard test remains under investigation and uncommitted. The sent-mention test now requires the shared completed-reply oracle in addition to the existing user-highlight assertion. Root lint passes with 0 warnings / 0 errors; e2e typecheck still has only the two external supervisor brokering errors. Lint (evidence/mention-diagnostic-1259/lint.log), typecheck (evidence/mention-diagnostic-1259/typecheck.log).

### Required core gate: 1262

The three requested core spec files produce **1 pass / 0 fail / 6 skipped**, zero retries, against the current unsigned build with independent outputs. Both reply screenshots and the terminal-launcher screenshot were reviewed. The passing scenario opens New Terminal and reloads its launcher, but does not click Shell or create a PTY. It therefore proves launcher selection only; actual terminal creation and command-output focus retention remain an acceptance gap. The skipped regressions remain failures awaiting separate product fixes. Exact command and scope (evidence/core-gate-1262/result.json), log (evidence/core-gate-1262/run.log).

The mention-catalog delay experiment 1261 passes three times, but the initial visible-menu guard may wait through hydration. It does not explain or close the original intermittent key failure. The temporary delay and request guard were removed; its source is archived as diagnostic evidence. Result (evidence/mention-hydration-1261/result.json).


## Real Shell and busy-draft qualification (1263–1264)

One new green rule test in `real-harness-local.spec.ts` creates a real PTY from a completed Claude chat, executes a split-marker command, reloads with retained output, returns to chat through the shared reply oracle, and returns to the same terminal to execute another command. The older core launcher-only control does not prove this mutation. The existing native busy-draft case now checks canonical `/session/status` before and after reload and expects Send for a nonempty draft, matching `submit-control.tsx`; its retained text, no-unwanted-send, exact payload, reply, and persistence assertions remain.

Unsigned 1263 and Test User 1264 each pass **6/6**, two cases repeated three times, zero retries. All **30 reply oracle screenshots, 18 terminal screenshots, and 222 video samples at two frames/second** were reviewed. Replies and shell output are visibly rendered; terminal output survives reload and chat return. This does not qualify every encoded frame, every terminal launch mode, or the Codex busy-draft case, which fails before its draft assertions in full gate 1257. Unsigned command/result (evidence/terminal-busy-1263/result.json); Test User command/result (evidence/terminal-busy-1264/result.json).

Gates: `bun run lint` passes, 0 warnings / 0 errors across 5403 files. `bun run test:architecture-ratchets` passes, 13 tests / 5 products / 8 policies / 12616 helpers, no baseline changes. Initial ratchet invocations hit the scanner's 1 MiB `git ls-files` buffer because of expanded QA artifacts; byte-verified ZIP archival of those artifacts resolved the environment failure, without modifying the scanner. `bun run typecheck:e2e` fails on the two existing production `secretBrokering: proxy` type errors in `workspace/supervisor/config-sync.ts:16` and `sandbox.ts:114`; no QA errors. `bun run test:architecture` is **251 pass / 1 fail**, the existing `moduleScopeMutableState is exactly pinned` debt ratchet. These failures remain open; no ceiling was raised. Logs (evidence/terminal-busy-1263/).

Committed these two QA paths as `35ad1a76f6 test(e2e): verify real shell navigation and busy draft state`.


## Tool interruption after server restart (1265–1269)

Added one green Tier R regression beside the browser-disconnection case in `real-harness-local.spec.ts`: `Claude running tool becomes interrupted without reload after server restart and a follow-up can complete`. It runs a real held Bash command, verifies Running in both canonical state and UI, restarts the owned local server, requires the same open page to show a tool error instead of Running, sends a successful follow-up through the shared reply oracle, and reloads to verify the interrupted tool stays terminal. No route or runtime event is fabricated. The tool script's start file proves it was executing before restart; the final canonical error is `Tool execution interrupted`.

Final qualification: unsigned 1267 (1 pass) plus 1268 (2 pass), Test User 1269 (3 pass), **6 passes total, zero failures/skips/retries**. All **12 reply oracle screenshots, 12 tool-state screenshots, and 287 video samples at two frames/second** were reviewed. Before restart the row says Running; afterward it shows Shell Failed in the existing page, and the follow-up reply is readable before and after reload. Initial 1265–1266 passed only after an immediate page reload; they are retained as diagnostic controls and excluded from final qualification. Unsigned command/results (evidence/tool-restart-1267/result.json), remaining unsigned repetitions (evidence/tool-restart-1268/result.json), Test User command/results (evidence/tool-restart-1269/result.json).

This complements the committed browser-outage regression `dfed52f19a`: tool completion while disconnected, then automatic reconciliation and reload. Neither case reproduces the historical indefinite-running failure. Independent native-runtime crashes, abrupt SIGKILL, permanent disconnection, other harnesses, and the recorded 630-second Codex process remain unverified variants. Finding counts stay **34 reproduced families / 35 qualified red regression cases**.

For the restart slice, `bun run lint` passes with 0 warnings / 0 errors across 5403 files, and `bun run test:architecture-ratchets` passes (13 tests, 5 products, 8 policies, 12616 helpers). `bun run typecheck:e2e` still fails only on the two external credential-broker `proxy` type errors listed above. The app architecture gate remains 251 pass / 1 existing debt-ratchet failure from 1263; this test adds no application import. Full real gate 1257 predates this additional test and remains 34/50/8; the six focused real runs qualify the new case separately. Latest slice logs (evidence/tool-restart-1267/).

Committed the restart regression as `edc362ab08 test(e2e): verify running tool interruption across server restart`.
