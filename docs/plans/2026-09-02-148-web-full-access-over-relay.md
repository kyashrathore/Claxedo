
### Release 59 — workspace sharing on staging (2026-09-02)

- `release-acc-shares-260902-201500-3851` cut over and dev-opened (ledger rev 154, phase `open`). Desktop host `host_YVi6…` stayed enrolled across the lock window.
- Hosted control plane now mounts `workspaceShareRoutes` (`POST/DELETE /api/workspace/:id/shares`, commit `1d09e8dd88`).
- Owner-side grant, live: from the owner's signed-in browser, `POST /api/workspace/5f39af3e-…/shares` with `{ role: "viewer", target: { kind: "user", userId: "usr_5ca0f278…" } }` → `200 {"grantId":"grant_aq-2TXhmX0afJdehgFH7mw","created":true}` in 913 ms. Control D1 `workspace_share_grants` holds the row (org `org-acc-260830-232009-3851`, `target_user_id` = the test agent's control user, role `viewer`, created by the owner's actor, `revoked_at` null).
- Contract note: the share target takes the CONTROL user id (`usr_…`, from `auth_identities.user_id`), not the auth subject. Posting the auth subject answers `403 workspace_authorization_denied "Workspace share target belongs to another tenant"`, which is the tenant check doing its job, not a defect.
- Grantee-side (catalog row with the `viewer` role label, session list over the relay as viewer): NOT verified. Blocker: it needs a browser signed in as the test agent; signing in as another user is out of bounds for this session. The grant is left active so the owner can verify it from a second browser profile. Owner: user.

### Core e2e matrix, first run after consolidation (2026-09-02)

Command (CI shape, prebuilt preview): `CLAXEDO_E2E_SERVE_MODE=build-preview VITE_AUTH_ENABLED=true VITE_CLAXEDO_SERVER_URL=http://127.0.0.1:3001 VITE_CLAXEDO_E2E=1 bun run test:e2e` in `packages/claxedo-app`.

- `test-user` leg: 67 failed / 265 passed / 10 did not run. Onboarding grep: 3 passed. `local-unsigned` leg: stopped after 40 tests (30 failed, all at boot, one cause), see B.
- Cause groups (owner → disposition):
  - A. Mock harness vocabulary (`e2e/helpers/mock-runtime.ts` still names `claude-acp`/`codex-acp`/`cursor-acp`; the normalizer accepts `acp:<slug>` or the native ids) — 22 tests — DEV-INHERITED (mock and normalizer byte-identical to dev; retired by dev commit `2d0c988fc4`) — fix: migrate the mock and fixtures to the canonical vocabulary.
  - B. Boot awaits browser-auth initialization before `render()`; a non-HTTPS origin makes the descriptor load reject, so the shell never mounts and `/login` cannot render — every `local-unsigned` test plus 5 `core-settings-auth` tests — REAL REGRESSION vs dev (`0025eb0f46`, `455d564387`) — fix: the shell renders unconditionally; auth is a signal; loopback central is anonymous by contract; descriptor failure is anonymous, not a startup failure.
  - C/D. `GET /api/workspace` list and `GET /api/claxedo/services` unmocked (the retired hosted boot aggregate carried the inventory) — 2 direct, ~11 downstream (offline roles, composer `@` agents, Settings providers, workspace lifecycle, documents index) — BRANCH MOCK DRIFT — fix: contract-bound mock handlers mirroring the server routes.
  - E/E2. Cloud/relay send never reaches the mock's cloud lane (`cloudPromptCount` stays 0) and first-turn replies never appear; suspect the stream gate that refuses a workspace stream without a `sessionID` on a relay-backed workspace (`903b2d4dec`) — 9 tests — undecided mock vs app; instrumented single-spec run decides.
  - J/K/L. Rail rewrite (`4db2c6015e`) and ingress narrowing (`e7e942fd2e`): archive action missing on rail rows, `session.lifecycle created` no longer materialises a row, terminal status/title dead — 9 tests + 3 singles — REAL REGRESSION candidates — app fixes in rail/session-source/event-ingress owners.
  - G1. Draft-default v2 shape (`byHarness`/`lastHarness`) not followed through in two `core-harness-ownership-local` assertions — spec repin. G2/H/I/F/M — re-classify after C/D land.
- Fix order executed: B, A, C/D, J/K/L, E/E2 in parallel by owner; then a full re-run of both legs.
- A landed: mock and fixtures speak `acp:<slug>` for ACP harnesses; structured patches send `{ id, access: "acp" }` as the client does. 123 passed / 6 failed across the 8 affected specs (the 6 belong to G2, J and the relay ref-count single). Dev-inherited follow-up, unowned: the committed ACP traces under `e2e/fixtures/harness-traces/` are stale against their own generator (the ACP translator now emits `permission.asked` as a `permission` tool part), and the generator passes `client: "claude-acp"` where production passes the bare slug; regenerating changes what `core-harness-rendering-matrix` behaviour 12 proves, so it needs a deliberate owner.
- Second full matrix, committed tree `fadeadfbce`, detached worktree, CI shape: `test-user` 339 passed / 3 failed; onboarding 3/3; `local-unsigned` 329 passed / 13 failed; onboarding 3/3.
  - `test-user` failures: core-settings-auth:1492 (disconnecting a config-declared provider has no scoped write — unmet, in progress), core-sidebar-tree:993/:1027 (archive affordance latches when a row is re-created under the pointer — in progress).
  - `local-unsigned`-only failures share one cause: the central event stream is read only for a signed account (`claxedo-events.tsx` `includeCentral: accountSigned`), so a loopback surface with no account never receives the daemon's global events (terminal status, `worktree.ready`, process crash) — core-terminal ×5, core-processes:915, core-session-actions:939, core-first-prompt-local:330, core-workspace-lifecycle:839. Central-stream participation must derive from the deployment (loopback → always). In progress at the owner. Plus core-user-hosted-workspace:816 (`GET /path` escapes on the relay origin when anonymous) — in progress.
- Remaining suites on the committed tree (detached worktree, port 4455, sequential): `test:e2e:mobile` 5/5 in both auth modes; `test:e2e:marketing` 2/2; `test:e2e:workgraph` gated on the `@claxedo/mcp` dist (`workgraph-tools`), built, re-run pending; `test:e2e:desktop` gated on a packaged desktop binary (packaging in the detached worktree trips the desktop's own boundary manifest because linked node_modules resolve app sources outside that worktree — runs from the main worktree after the final commit); `test:e2e:live` 1 passed / 8 failed / 17 did not run — `live-claxedo-mcp-tools` and `live-agent-extensions-materialization` gate on an ambient claxedo-server at :3001 (not started), `live-user-hosted-relay` fixture crashed seeding a session snapshot without a fencing token (this branch's private-session invariant; fixture fix in progress), `live-real-harness-smoke` needs real claude/codex/opencode turns (codex quota exhausted; claude and opencode cases to re-measure); `test:e2e:real` (Tier R, real server + real harnesses) 5 passed / 15 failed / 1 skipped — the composer's model control stays at "Select model" on a real harness (×5) with downstream turn/usage/goal failures; real-producer contract triage in progress.
- `test:e2e:workgraph` re-run with the mcp dist: 4 passed / 1 failed / 15 did not run at `core-workgraph.spec.ts:414` (real journey; title "Resolve #101" never visible). Diagnosis (read-only, dev-inherited: spec, harness and every owner in the title chain are byte-identical to dev): the producer is correct (session meta carries the title); the pane's authoritative title was overwritten by the route bridge's unresolved-session fallback (`route-bridge.tsx` `openSession(..., "Session")`) and only repaired after an engine round-trip; what fails the assertion is a ~24 s head-of-line stall of every directory-scoped engine request on first session-page mount when the file runs in sequence (6 s in isolation; passes alone). Applied: `routeSessionPaneTitle` in `route-bridge-resolution.ts` is the one rule for both branches (a resolved title is kept). Not applied, unowned: the from-source engine's cold-start stall under the real WorkGraph harness.
- `live-user-hosted-relay.spec.ts` (real relay, real host tunnel, real server, zero route mocks): fixture now seeds through the real host path (reserve → register → acquire turn/fence → snapshot → release) and mints runtime tokens with `principal_kind`; the spec keeps the fixture alive over its stdin pipe. Result 2 passed / 1 failed / 3 skipped: behaviour 1 (register + tunnel-up → ready) and behaviour 7 (viewer-role tokens are really denied writes and PTY, allowed reads) pass; behaviour 2 fails on a real server gap — PTY create on a managed workspace runtime answers 503 `terminal_capability_authority_unavailable` because the embedded managed session policy lacks `authorizeStream` (self-hosted composition; the desktop daemon uses the unbound local flavour) — fix in progress at the owner.
- Managed session policy (`23d5218b24`): `authorizeRuntimeSessionStream` is the one owner of stream authorization + lease minting for both the remote route and the embedded managed policy; a managed policy is built from one all-or-nothing authority bundle so `managed-private` always carries stream authorization. Live relay spec: behaviours 1, 2 (health, file read, PTY create/list/delete through the real relay lane — was 503) and 7 (viewer role denied writes/PTY, allowed reads) pass. Behaviours 5/6 (tunnel pause → offline view) stay red: in this fixture the browser's central is the same server that also lists the workspace as a LOCAL project, so the client classifies it local (local wins by design when the directory is on this machine) and the connect/offline gate never mounts; the fixture must serve the frontend from a central that does not own the directory locally (spec follow-up, not a product defect — on staging the web client classifies the host's workspace as user-hosted and reads over the relay). Behaviours 3/4 stay `fixme`: their bodies are comments only and 3 needs a model provider wired into the fixture's embedded engine.
- Session rows from events: `sessionRowDirectory` in `session-source.ts` is the one owner of the directory a row carries per workspace kind (local keeps the producing host's path; cloud and user-hosted carry `workspace:<id>`); both event-ingress stamping sites use it; tests feed the producer's real frame shape (host path + workspace id) and bite. Follow-up in progress: user-hosted ids are caller-chosen UUIDs, so the `ws_` gate must give way to classification through the resolved catalog.
- Third core matrix (`00f6ed33b9`, detached worktree, CI shape): `test-user` 337 passed / 1 failed / 4 did not run — the failure is `mobile-smoke` behaviour 1 on `Cannot find module …/agent-sdk-runtime/dist/chunks/chunk-*.mjs` (a dist rebuild by a concurrent agent removed the chunk mid-run; the 4 did-not-run are its serial siblings) — re-run of mobile-smoke + documents-core on the same HEAD: 16 passed; onboarding 3/3; `local-unsigned` 342 passed / 0 failed; onboarding 3/3.
- Tier R after the second pass (`1ef12e67e3`): opencode Goal, OpenCode subagent parentID and Codex native subagent fixed at their owners (all dev-inherited); Pi Goal / Pi subagent trace to the self-hosted composition's central session meta (write side and read side are different stores) — in progress. Trap: `test:e2e:real` builds only agent-sdk-runtime and opencode; `@claxedo/workspace-runtime` resolves to `dist` under every condition, so rebuild it before Tier R sees a runtime change.
- claxedo-server suite after the wave: 4 failed / 3564 passed — the four inherited from dev at the merge (author attribution, abort admission, auth-gate events route, session-grouping ref) — in progress.
- Inherited unit failures fixed (`275f50c1a1`): workspace-runtime store characterizations (create the session through the real route before updating its config), desktop spawn inventory (Codex app-server process file; Claude turn's injectable query seam; one pin derived from the inventory), app workspace-panel disposal (terminal surface switch; partial global-SDK mock). Remaining inherited, unowned: `claxedo-desktop/scripts/opencode-compile-cache-boot.test.ts` asserts the deferred `@claxedo/server` bundle is above 8 MiB and measures 5.9 MB (a size floor from before the local/cloud package split; owner: the server bundle).

### PR grouping addendum (commits `9efc2f56ce`..`80be5590b2`, 86 files)

Settled-tree gates on `80be5590b2`: `packages/claxedo-app` `bun run typecheck` exit 0; repo `bun run test:architecture-ratchets` holds (5 products, 10 policies); `packages/claxedo-server` suite 3573 passed / 14 skipped; agent-sdk-runtime 618/0; workspace-runtime 1001+37+63/0; claxedo-app bun 6102/0 and vitest 1196/0.

- Group 1 — web client boot, catalog and session sources (`packages/claxedo-app/src`): `9efc2f56ce` route pane title, `bd9e784610` rail engagement, `fc752e40c1` session streams before first prompt (+ loopback central stream), `0159af84c0` session row directory per kind, `04c5801ed0` (app half: Settings disconnect through the scoped write), `275f50c1a1` (app half).
- Group 2 — workspace runtime and control-plane contracts (`packages/workspace-runtime`, `packages/claxedo-server`, `packages/claxedo-server-core`, `script/product-boundary`): `04c5801ed0` provider config route, `23d5218b24` managed session policy authorizes streams (PTY over relay), `1450ddc715` share routes in the shared set, `80be5590b2` (server half), `1d09e8dd88` hosted share routes (earlier).
- Group 3 — harness runtime (`packages/agent-sdk-runtime`, `packages/opencode`, `packages/claxedo-local-server`): `9d73656385` Goal capabilities follow availability + runtime typed 502, `00f6ed33b9` ACP default accepted, `1ef12e67e3` Goal state from the engine's announcement, child sessions know their parent, Codex driver delegates, `80be5590b2` (author on the prompt).
- Group 4 — e2e infrastructure and fixtures (`packages/claxedo-app/e2e`, relay fixture): `bfd85790d1` Tier M mocks serve per-surface reads, `5e932890e8` relay fixture real host path, `275f50c1a1` (characterizations), `fadeadfbce` partial solid-query mocks.
- Group 5 — docs and plans: `e6008f3e8e`, `3c0dee0a4c`, this addendum.

### Release 60 — consolidated tree on staging (2026-09-02, `80be5590b2`)

- `release-acc-final-260902-220029-3851`: `release60.sh` (cutover + deploy, exit 0) then `devopen60.sh` → ledger revision 156, phase `open`; `/health` reports phase open.
- Final core matrix on `80be5590b2` (detached worktree, CI shape): `test-user` 342 passed / 0 failed, onboarding 3/3; `local-unsigned` 342 passed / 0 failed, onboarding 3/3. Exit 0.
- Live on release 60 (web, owner, Chrome): the rail's default "Projects" view lists each user-hosted project through the CENTRAL source, which omits user-hosted workspaces (their blocks only exist in the "Workspaces" view), so every user-hosted project reads "No sessions match the current view" while the host answers 70 root sessions over the relay (list 947 ms) — DEFECT, fix in progress (the project view composes each workspace's own source).
- Boot stall observed on release 60 (web, owner's Chrome, ~25 min after dev-open): three consecutive page loads stopped at the index placeholder — `GET /api/claxedo/auth/descriptor` answered (0.5–0.7 s) but the cookie-bearing `GET /api/auth/get-session` never completed; a page-issued probe of the same route with the cookie aborted at 20 s while `/global/health` on the same connection answered in 0.6 s; fresh-connection anonymous requests answered in 0.4 s throughout. A worker tail attached afterwards recorded the next cookie-bearing probe as `ok` in 96 ms (204 ms end to end) and the app then booted. The hung invocations were not recorded (the tail attached after they started). Class: a cookie-bearing request that reaches the auth database hangs on the browser's pinned isolate/connection and later recovers; the composition-readiness gate covers construction, not a mid-life stall. No code change without a recorded cause; next occurrence must be captured with the tail attached BEFORE the load (see `reference_cp_host_requests_never_reach_worker`).
- Live pass on release 60 (owner's Chrome, dev desktop enrolled as `host_YVi6…`, workspace 5f39af3e…):
  - Req 1 enroll: `window.api.hostConnector.start()` → `enrolled` in 3.2 s; daemon `/api/claxedo/host-serving` serving with the relay URL and 4 workspace ids.
  - Req 2/3 (Workspaces view): boot descriptor 168 ms, get-session 201 ms, catalog lists 722/771 ms, workspace connection mint 1.1 s, `GET <relay>/workspaces/<ws>/session?roots=true&limit=55` 834 ms; rail shows "Claxedo — Published by this machine" with its sessions (host answers 70 roots).
  - Req 5: from the draft route, typed prompt → click; route carried the new session id at 2.2 s; first assistant content at 10.9 s after click, of which the harness reported 6 s of generation (model time, not ours); reply persisted on the host (daemon `/session/<id>/message`).
  - Req 6 (attach): turns started on the host reached the attached web pane only as completed turns 8–10 s later through periodic `message?view=latest` refetches; the attached session opens no `runtime-events?parentSessionId=` lane — DEFECT, fix in progress (attach-by-route must open the same live lanes as create-by-composer).
  - Req 4 via the rail: clicking a user-hosted row routed to bare `/s/<id>` with the host path and the pane never loaded — DEFECT, fix in progress (row identity from the source row). Direct navigation to `/w/<ws>/session/<id>` loads the transcript (message read over the relay 957 ms).
  - Project view defect (above) and a transient boot stall (above) recorded.
  - Req 7 terminal: from the web, "New terminal" → the draft opens at `/w/<ws>/terminal/new`, the terminal mounts and the host creates `pty_fec47b…` (running, cwd = the workspace) in 3.7 s — but the terminal renders blank: output rides the workspace event stream and the client refuses that stream for a non-local workspace without a session id (`claxedo-event-targets.ts`), while the host actually serves the unscoped stream over the relay (probe: 200 `text/event-stream`, first heartbeat 922 ms). DEFECT, fix in progress: the connection mint carries the runtime's stream scope; the client stops guessing from the workspace kind. Rail lists the PTY row twice (project + workspace sections) — observation.
- `test:e2e:desktop` (packaged `Claxedo Dev.app` built from `80be5590b2`, main worktree, port 4457): 12 passed / 3 failed / 2 skipped (was 4 / 11 / 2). The e2e app build environment has one owner (`e2eAppViteEnvironment`, `126d189e4e`); desktop specs hover the project header before its action cluster; the daemon-lifetime test pins the real contract (lease held for the process, released on quit). Newly reachable real defects: B5/B6 a re-prompted session does not re-sort to the top (rail list order owner, in progress); C1 a fresh OpenCode draft never resolves a model on the local surface; C2/C4 picking Sonnet in the native Claude picker lands on the harness default (model selection owner, in progress).
- Fix wave three (uncommitted while owners finish): (a) stream scope is a property the control plane states on the connection mint (`sessionAuthority: local | managed-private`); the stream owner opens the workspace-wide stream for local-authority hosts and session-scoped streams only for managed-private runtimes; `session-event-scope` owns the route input for both lanes (terminal over relay unblocked; user-hosted attach needs both lanes: the workspace bus carries pty/process/session lifecycle, `runtime-events` carries message parts). (b) Project view composes each workspace's source (`composed` session source, one cache entry, per-member paging); rail rows resolve their workspace through one lookup (`projectWorkspaceForRef`) so a user-hosted row routes to `/w/<ws>/session/<id>`; the rail's default order is `updated_desc` and the query's sort is the order authority (re-prompt re-sorts). (c) In progress: attached deltas render (ingress directory identity; projection emits the assistant row before parts); user-hosted workspaces addressed by `workspace:<id>` at the catalog and route owners (host path is display only); model selection (fresh OpenCode draft resolves a model; an explicit pick is authoritative per (workspace, harness)).
- Model selection (`27439e44bf`): C1 — OpenCode has no harness-config-options surface (real local server answers 404 `harness_config_options_unavailable`; the model comes from the provider catalog), so a fresh draft resolves the catalog's default into the draft without persisting it (`restoreOpenCodeDraftDefault(resolvedDefault)`, `firstConnectedModel` now has a production caller). C2/C4 — the real Claude catalog is `default, opus[1m], claude-fable-5[1m], sonnet, haiku`; the app's pick path is correct at every layer (store, options loader, writer, Tier M with the truthful 5-row catalog); the desktop spec matched the default row's description with `getByText(/Sonnet/i)` and now picks the list item. Rule enforced at the store: only an explicit pick is remembered (`completeRememberedHarness` and `draftDefaultWritePending` removed; `protectDraftModel` protects chosen models only).
- Addressing (`d6a6ad91c2`, `048f831c9d`, `8ea2a5f50e`): a relay-backed workspace is addressed by `workspace:<id>` at the catalog owner (`workspaceRowDirectory`), the route owner, every reader, the session inventory producers, and the hosted `/project` projection; the host's path is location metadata (`remote_directory`). Tier M asserts that after a rail click the pane's directory is `workspace:<id>` and no central request carries the host path (positive-controlled: the host-path fixture records `GET /api/claxedo/agent-config/harness`, the exact live escape).
- Wave three committed: `62d3e65ab4` project list composes its workspaces' sources; `eca6ea250d` live streams reach an attached pane (stream scope on the connection mint, route scope owner for both lanes, live-session events under the workspace address, projection announces the assistant row; Tier M user-hosted behaviours 8–10 bite, contract version pinned to the runtime's); `244e74887b`, `e9acfdaf17` (closures 965 / 1051 for `route-event-scope.ts`). Release 61 (`release61.sh`, predecessor 60 rev 156) and a core matrix on `e9acfdaf17` in progress; one agent on the last red (`core-panes-split-tabs` behaviour 11: one `session.idle` reaches the ingress three times — a duplicate stream subscription).

### Release 61 — live pass (2026-09-03, tree `e9acfdaf17`, `release-acc-live2-260902-232704-3851`, ledger rev 158 open)

- Req 1: desktop re-enrolled after the daemon rebuild (`hostConnector.start()` → `enrolled` 3.5 s).
- Req 2/3 (Projects view, the default): boot get-session 210 ms; catalog 679/773 ms (a second round 3.1/2.9 s — the boot double-fetch open item); connection mint 1.16 s; relay session list 1.19 s; the "Claxedo" project lists the host's sessions (page of 5, including one created on the desktop by the user). Pane directory `workspace:5f39af3e-…`.
- Req 4: rail click → `/w/<ws>/session/<id>`; the pane loads 8 messages / 4 turns entirely over the relay (`/ws/session/<id>/message…` 0.5–0.7 s per read); zero central requests with a host path. The central `agent-config/harness?directory=workspace:…` read still takes 2.7–3.8 s (open perf item; it is not on the transcript path).
- Req 7 terminal: draft → `/w/<ws>/terminal/pty_e052…` in 8.8 s once the host's `pty.created` arrived on the workspace stream; the rail row title updated from the shell's output, so bytes flow both ways (the canvas renderer keeps the screen text out of DOM reads).
- Req 6: NOT MET on the real path. A host-side turn rendered in the attached pane as one whole message ~3 s after completion. The producer streams: the relay `runtime-events?parentSessionId=` lane carried `text-delta` frames within 0.5 s of generation (connection `sessionAuthority: "local"`), so the client's attach path drops or never subscribes on the real path while Tier M passes — a Tier L proof with a scripted model is in progress at the owner.
- Core matrix on `e9acfdaf17` (release 61 tree): `test-user` 346 passed / 1 failed, onboarding 3/3; `local-unsigned` 346 passed / 1 failed, onboarding 3/3 — the one failure in both modes is `core-panes-split-tabs` behaviour 11 (one `session.idle` reaches the ingress three times), fix in progress.
- Live (user testing on release 61): terminal pane does not mount for `/w/<ws>/terminal/<pty>` (no `.xterm`, no rail row) while the host lists the PTY running; sessions do not stream. One seam: frames from a user-hosted host carry the HOST's filesystem `directory`; the web addresses the workspace as `workspace:<id>`; the translation belongs at the relay stream boundary (a stream is opened for exactly one workspace). In progress with the Tier L proof; the Tier M mock must emit host-path frames like the real daemon.
- Real-relay streaming slice (`d219d917d1`): Tier L behaviour 3 drives a host-side turn against a scripted model through the real relay and samples the attached pane; producer proven (contract v7 frames with the host's path, deltas within a second). Fixed owners: embedded managed session policy carries turn authority (prompts on an embedded managed-private host answered 503 before); the self-hosted bootstrap addresses a relay-backed workspace by id; relay-backed private sessions project from the runtime-events lane alone; frames on a relay-backed workspace's streams are addressed by that workspace at the stream boundary (both lanes); a retarget restarts only the session-scoped lane. `aa9b002997`: the session event scope settles by value (one `session.idle` no longer delivered twice). Remaining owner: the route-attached pane's own directory (host path) — in progress with a Tier L terminal behaviour.
- Relay/tunnel slice (`ff8a0535ac`), found by fronting the live relay spec by hostname (a loopback central address made the app take the desktop transport, so the harness had been proving the wrong path): the workspace relay compiles one origin matcher from the deployment's allowed origins for every browser-facing path (a configured origin passed the WebSocket check and failed every preflight; tunnel responses passed the host's CORS through); both hosts start their tunnel with the loopback replay headers the host's gate requires (the claxedo-server tunnel started with none, so a remote browser's `Origin`/`Host` reached the host's loopback gate → `401 workspace_relay_local_loopback_required` — the production web-client path against a self-hosted tunnel); a browser terminal on a managed runtime is created for the workspace's session. Tier L: 5 passed / 1 failed (behaviour 3) / 1 skipped; new behaviour 9 (terminal round trip through the real relay, output read via xterm's screen-reader mode) green. Behaviour 3's last seam: the compat projection announces a viewer's assistant row parented on the session; the timeline renders replies parented on a user message — fix in progress (message-id convention owner).
- Release 62 (`release-acc-live3-260903-033847-3851`, ledger rev 160 open) = `109f89d377`, handed to the user for retest (terminal output expected to work; attach streaming pending the reply-row parent). Matrix 6 on `109f89d377`: `test-user` 346 / 1 (`core-user-hosted-workspace:1024` deep-equality), `local-unsigned` 311 / 6 / 30 did-not-run — five of the six and the 30 are `Cannot find module …agent-sdk-runtime/dist/chunks/…` from a concurrent dist rebuild (environment), leaving `core-terminal:850` (custom command terminal launch) real; both under triage.

### Phase 2 audit findings (read-only Sonnet audits per PR group, 2026-09-03)

- G1 web client: (high) `global-sdk/route-event-scope.ts` re-implements `workspaceKind` and `live-session.ts` carries a second `USER_HOSTED_WORKSPACE_KIND` constant — use the workspace-kind owner; (medium) `workspace-catalog.ts:202` `kind: access ?? backing ?? "cloud"` papers over a missing kind although `listControlPlaneWorkspaces` knows the access list per call — stamp the kind there, no fallback; (low) `settings-scope-options.ts:76` builds `workspace:<id>` inline — use `sessionRowDirectory`; (low) `event-ingress.ts` comment cites "issue #14" — state the invariant; (low) `settings-scope.tsx` bare `catch` around `useSDK()` (pre-existing pattern). Verdict: the user-hosted abstraction earns its place (no new literal comparisons; one addressing invariant across catalog, session rows and stream frames; three rail state machines collapsed into one).
- G2 runtime/control plane: no high findings. (low-medium) turn admission (`acquireTurn`/`renewTurn`/`releaseTurn`) is attached to the managed policy after construction by one composer while `ManagedSessionAuthority`'s comment claims one bundle — fold turn admission into the bundle and build it in the factory; (low) `routes/provider-config.ts:94` is the fifth copy of the `harness || runner || default` query resolution — one helper; (informational) `HOST_SERVING_WORKSPACE_SQL` exists per DB engine, cross-referenced. Verdict: user-hosted is a first-class runtime/authority kind — `sessionAuthority` is produced once per connection kind, threaded through a typed contract, consumed by the client's stream owner; embedded and relay-host runtimes share one stream authorization with two lease bindings; the host-pull merge mechanism is gone in favour of the runtime as the session authority.
- G4 e2e infrastructure: contracts verified against the real handlers (session reservation regex/codes byte-identical; provider config drives the real router; list row types bound to the D1 authority; central stream on three spellings, fenced by a test that reads the server source); hygiene: one test title named a past bug (renamed), `e2e/INVARIANTS.md` named the retired harness ids (updated), two stale line pointers in `mock-runtime.ts` comments (to drop). Verdict: Tier M proves the user-hosted path truthfully on frames, catalog rows, connection mint and addressing; the terminal spec's central-stream mount was a one-sided seam, now closed and fenced.
- G3 harness runtime: (medium) the self-hosted app mounts `/api/claxedo/events` after the compat router that already serves it — one owner (in progress with G2 consolidation); (low) unused `AgentConfigOption` imports in the pi and opencode adapters after `probeConfigOptions(): Promise<never>`; (low) `claxedo-local-server/src/agent-config/harness.ts` redeclares `ResolvedHarnessModel` instead of importing the runtime's. Clean: Codex server-request handling has one owner; Goal monitoring reads the engine's announcement (metadata key mirrored and pinned); `resolvedModel` is one shape across ACP, native SDK and the local proxy. PR note: group 3's `announcesAssistantMessage` option is set only by group 1's caller and the turn-author fix is covered by group 2's server tests — say so in the PR descriptions.
- Consolidation commits: `0b6f9f6c4c`, `7e84dea8a5` (the workspace address rule is a platform identity owner — `platform/identity/workspace-address.ts`; feature-to-feature import removed), `d08e5fbba0` (turn admission inside `ManagedSessionAuthority`; one harness query helper; one owner for `/api/claxedo/events` in the self-hosted app), `38b1e8196d` (the user-hosted mock models the app's central boot reads — the matrix's remaining `:1024` was the mock's oracle racing boot, `core-terminal:850` was dist-rebuild collateral). G1 `route-event-scope.ts` duplicate `workspaceKind` + `live-session.ts` constant: DONE — both, plus a third copy in `global-sdk-event-fetch.ts`, now import `platform/runtime/agent/workspace-kind.ts`.
- Reply-row slice (`85029da478`): the turn message-id convention has one owner (`agent-event-runtime/src/contracts/turn-message-ids.ts`); the OpenCode publisher stamps every frame with the turn's stable reply id, carries the prompt as a user-message delta and closes the turn; the projection parents a viewer's reply on the turn's user message. Tier L behaviour 3's streaming subject is proven on the real path: six paced deltas → six strictly increasing rendered states (+1.1 s … +4.6 s), no message refetch in the window, one reply slot, prompt row present. Its settled oracle stays red for one contract mismatch: the user-hosted connection mint hard-codes `sessionAuthority: "local"` while a host's runtime may compose managed-private (the fixture does) → the client opens a workspace-wide stream that the runtime refuses (400 `session_event_scope_required`) and the pane is torn down. Decision: the HOST declares its runtime's composition at enrollment; the mint reports it; nothing is guessed — DONE, see the host-declared-composition slice below. Release 63 (`release63.sh`, predecessor 62 rev 160) from `85029da478` and matrix 7 in flight. `55716db5ed`: route audit reads the projection owner; agent-sdk-runtime `runtime.ts` ceiling re-measured at 992.
- Host-declared composition slice (req 6, the last seam): a runtime's session-access composition is a fact only the machine serving it holds — the same product composes either flavour depending on whether a session authority was injected — so the HOST declares it and the control plane records what it said. The declaration rides the enrollment heartbeat (`heartbeatHostEnrollment` arg `sessionAuthority`, HTTP `POST /api/claxedo/host/enrollments/heartbeat`), is stored on the enrollment row (`host_enrollments.session_authority`: D1 migration `0016_host_session_authority.sql`, the SQLite store's `addColumn`, `convex/schema.ts`), rides back on `activeWorkspaceHost` as `session_authority`, and `connections/user-hosted-connection.ts` mints `sessionAuthority` from that and nothing else. Deliberately outside the heartbeat signature: that signature proves machine CONSENT to serve a workspace set, while this describes a composition that can neither grant nor widen access (the runtime itself admits or refuses every stream), so the v2 payload is unchanged. Undeclared stays undeclared all the way to the client: the mint omits the field, `claxedoEventStreamTargets` opens no workspace stream, and no layer substitutes a default. Producers: `embeddedWorkspaceRuntimeSessionAuthority()` in `claxedo-local-server` reads the policy its own runtimes are mounted with; the self-hosted node's `createRemoteAccessService` beats with it; the desktop reads it from the daemon's `/api/claxedo/host-serving` and hands it to the Host Connector child in the bootstrap, before its first beat. Tier L behaviour 3 is green end to end (6 passed / 1 `test.fixme` / 0 failed); reverting the mint to the hard-coded `"local"` turns it red again.
- Release 63 (`release-acc-live4-260903-045121-3851`, ledger rev 162 open) = `85029da478`, handed to the user for the attach-streaming retest. Matrix 7 on `85029da478`: `test-user` 347 / 0, onboarding 3/3; `local-unsigned` 347 / 0; its onboarding leg hit `Cannot find module …/workspace-runtime/dist/…` during a concurrent dist rebuild (1 failed / 2 did not run) — re-run on a quiet tree: 3 passed. Matrix 7 is fully green.
- Host-declared session authority (`273e014c77`): the process that composes a workspace runtime declares `sessionAuthority` on every enrollment heartbeat (outside the consent signature — it grants nothing); the control plane records it (D1 migration `0016_host_session_authority.sql`, sqlite `addColumn`, Convex optional field); the user-hosted mint reports only what was declared (undeclared → no scope, the client opens no workspace stream and says why); the desktop reads the daemon's declaration and carries it through the host connector. Tier L `live-user-hosted-relay`: 6 passed / 1 skipped (behaviour 4 fixme) / 0 failed, twice. Release 64 (`release64.sh`, predecessor 63 rev 162) from `273e014c77` and matrix 8 in flight; the desktop daemon bundle rebuilt for the relaunch after dev-open.
