# Round 10 — client review of `1270592b2c` (rebased onto dev `d94e4250d9`)

Round-9 fixes re-verified: `reconcileAgentStatuses` reads every owned terminal, applies per directory behind the generation guard, idles-and-clears only gone terminals, runs at boot (state hydrates synchronously); parked reopen for S→S, S→draft→S, S→other→S is one open per navigation and the `granted` share notice has a real producer reaching the grantee; per-attempt AbortController covers every path; the frames split has one emitter instance; batch-autotab's cwd rule matches the runtime's `PtyInfo.cwd`; the Tier M body equals the mock's and the handler's. Rebase: `git range-diff` shows 30 commits identical except one plan-doc hunk; dev's 11 commits touch no reader/ingress/controller/resync seam.

Findings:
1. MINOR — `agent-status-listener.ts`: a live `agent.lifecycle` Idle landing between a terminal's recorded read and its directory's apply is overwritten by the recorded Busy (window = N sequential round trips per directory, on every stream return).
2. MINOR — `claxedo-events.tsx`: a re-grant notice arriving while a reconnect attempt is in flight is ignored; the attempt's stale 403 then parks the target until a navigation.
3. MINOR — a control-plane gap that swallowed the `granted` notice leaves the target parked; the gap reset re-reads share lists but re-probes nothing.
4. MINOR — `core-harness-rendering-matrix.spec.ts:1524-1537` is a raw in-page fetch against the mock; it proves the mock's body while its comment describes the reader.
5. NIT — the reader re-exports `PtyInfo`/`ClaxedoDirectoryEvent` with no importers and the emitter helpers reach production only through the frames module.
6. NIT — the plan ends "Round 10: see below" with nothing below.

Verified clean: only the two routes (+ daemon proxy, desktop bridge) opened; single emitter; consumers on the right stream; pre-existing architecture reds at dev's levels.
