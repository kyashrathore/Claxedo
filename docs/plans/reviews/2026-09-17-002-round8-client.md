# Round 8 — client review of `856d7a8a81`

Round-7 fixes re-verified: 403 narrowing/refused path (no timer armed at the refusal; `stop` from `connecting` legal; snapshot cleared; lane closed; a session named later reopens; a return to the draft route refuses once, no loop; unmount clean); `workspaceReconnects` removal (every `wr` return is a level edge: stale keys closed before opening, same-key retarget sets false before `connect()`, one `wr` target per result); union arms retired with no feeder/reader; comment rewrites consistent; account bridge's post-handshake AbortError reaches only the reader's `.catch`; `lane`/`streamId` split typed.

Findings:
1. MAJOR — reader header `claxedo-events.tsx:8-12` and the round-7 record claim a non-routed local workspace's pane is healed by the switch reconcile or the rail batch. Neither carries agent state: `reconcileAgentStatuses` idles only terminals ABSENT from `/pty`; the rail batch reads session status only; `pty.exited`/`pty.created` are not replayed on a cursor-less reopen. On the desktop (dev carried every local workspace's `agent.lifecycle`/`pty.*` on the central stream), a Claude terminal in non-routed workspace B finishing means no completion sound and a "working" dot that stays until B's NEXT lifecycle frame or pty exit — a permanent loss recorded as a delay.
2. MINOR — a session-scoped stream refused by the runtime itself (grant revoked, session deleted) is treated as a transport outage: backoff to 15 s, `console.error`, `markWorkspaceReconnecting`; it ends only when the fail-soft `session.share.changed revoked` notice completes. A missed notice = a grantee on `/s/<revoked>` loops on 403 forever with "Reconnecting…".
3. MINOR — `mock-runtime.ts:1981` "Tier R proves that side": no Tier R spec exercises the narrowing; the cursor drop is proven by the reader vitest and the server route/acceptance tests.
4. MINOR — `agent-status-listener.ts:486-528`: the reconciliation now runs on every workspace switch; it sweeps EVERY tracked non-idle terminal across directories and `catch → clearAllAgentIndicators` resets every indicator on one failure (a tracked cloud workspace whose host is offline clears the routed workspace's live "working" dot).
5. NIT — desktop `index.ts:785` / `child-protocol.ts:54` "never infers": `submitTransportForPlacement` is `controlPlaneSession || sessionAuthority === "managed-private"`, so signed web infers registration from placement.
6. NIT — stale comments: `stream-sync-lifecycle.ts:3` names `context/claxedo-events.tsx`, `:13` cites line-number history; `workspace-connection.ts:616` same stale path; plan DoD "rounds 1–4 recorded … round 5 runs on the round-4 fixes".

Verified clean: only the two routes (and the daemon proxy / desktop bridge) opened; retired-name grep clean; single reader/emitter; consumers on the right stream; e2e mock serves exactly the two routes; round-7's new vitest cases fail without their fixes.
