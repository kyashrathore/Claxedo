# Round 9 — server review of `3d6a5559ab`

Round-8 fixes re-verified: tombstone hole read before attach; a 403 retained frame never holes; `authorizedSessions` seeding gone with live revocation still detected for this connection's sessions; `verified` unconditional (no claims type lacks an actor); child deletion carries and reads the parent; `session_event_stream_denied` minted at one place; `eventSessionId` three cases + default; docs match code.

Findings:
1. MAJOR — the hole was a boolean reset by the first attach: two tabs of one actor, plane away, frame F undecidable → both ended; tab A reconnects → gap (correct) → attach clears the flag; tab B reconnects with the same cursor → contiguous, F never reaches B. Fix shape: a per-scope hole watermark, `hasGap` true for any cursor at or below it.
2. MINOR — during a plane outage the stream still ends once per session's first frame after each reconnect (never-granted + away → terminate → hole → rebuild → gap → resync), at the authority-timeout rate; chosen over silent omission in round 7, stated nowhere.
3. MINOR — `event-delivery.ts:531-542` says a fresh ring numbers from 1; it numbers from the clock; `:63` describes one of the flag's three writers.
4. NIT — `sessionAccessContext` still guards `actor_id && actor_kind`, and `requireActor` still names a case both claim types make impossible.
5. NIT — `deletedSessionParent` reads `.properties.info.parentID` non-defensively; a partial frame would throw inside a decision and end every connection.

Checked clean: producer→bus→handler→policy→transport on all four deployments; no private frame on the unscoped arm without a grant; session-scoped ring only its session; local scope unreachable on managed runtimes; proxy forwards the cursor; cp notices one publisher per kind, default-deny visibility; retired-name grep zero outside dated records and the engine adapter.
