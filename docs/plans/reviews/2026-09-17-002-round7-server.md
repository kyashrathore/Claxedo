# Round 7 — server review of `daeb9114c0` (unactioned; substance recorded)

Round-6 server fixes re-verified: `granted` flag; `holdSession` seeding (one authority call at open); `sessionScope` scope key; hard expiry with renewal in flight; 401 on never-granted → held omit, granted refused at renewal → terminate; abort release on `stream.onAbort` + `c.req.raw.signal` + post-attach check (load-bearing on Hono 4.10.7; @hono/node-server 1.19.11 aborts `req.signal` on close; Bun ≥1.2 natively); `forgetSession` direct path; `actorId` scoping leaves `created` on the session authority and unmanaged-local untouched; heartbeat cadences match code.

Findings:

1. MAJOR — `event-delivery.ts:230-241` + `:208`: round 6 turned a 503/thrown authority on a never-granted session into a 5 s held omit. Every reconnect starts with an empty grants map, so every session is "never granted" again; a plane fault on a session's first frame after reconnect makes that frame omit → not rung → the cursor stays contiguous over it → silent loss, no gap, no resync (scratch `reconnect-503.test.ts` scenario A: `prt-2` gone, `prt-3` arrives as id 2 with no gap). A lost tool `completed` part pins a row "Running" and the terminal reserve cannot help.
2. MAJOR — `event-delivery.ts:317-320` + `:237`: a 503/timeout at renewal on any granted session terminates the stream though the lease is valid; one plane blip (Worker cold start, D1 spike, the 5 s authority timeout) tears down every stream in every workspace within one cadence, each holed → reconnect → gap → resync storm against the plane that just faulted (+ finding 1 if the fault persists). Round 3 ruled a plane fault at open is not a refusal; renewal reads it as a revocation. Suggested: on 503/thrown at renewal keep the grant until `expiresAt` (the hard-expiry clock already bounds a stall). `event-delivery.test.ts:567-585` currently pins the terminate.
3. MAJOR — desktop/self-hosted-node proxy path never resumes by cursor and is torn down every ≤15 min: `localWorkspaceRelayProxy` → `proxy()` mints a fresh RAT per request (`runtime-dispatch/internals.ts:230-236`), the relay's host token carries `parentJti` = that RAT, the runtime keys the replay scope by `rat:<parent_jti>` → every reconnect lands in a scope with no tombstone → any positive `Last-Event-ID` is a gap → full resync per blip; at RAT expiry lease renewals 401 → terminate. The round-6 client note ("cursor forwarded") and the DoD "resumed by cursor" are wrong for this path; the browser relay path caches the RAT (`workspace-relay-connection.ts:376-401`), the proxy does not. Undocumented hosted-vs-desktop divergence.
4. MINOR — the `session.deleted` → forget fix has no production trigger (no production caller sets `deleted_at`; a deleted session stays readable at the authority — separate matter) and an ordering hole: forget runs at publish time, the frame's decision at dequeue; a deletion queued behind another session's in-flight first-frame round trip is decided after its grant was forgotten → 403 → omit → `authorizedSessions.has` → disconnect (scratch scenario B).
5. MINOR — `event-delivery.ts:644-647`: a second connection into a live scope seeds `authorizedSessions` from the ring, so its first transient 503 on a ring session disconnects it (scratch `two-tabs-503.test.ts`); recovers by ring replay, but the "never a terminate" comment at `:230-235` is not true of the source.
6. MINOR — `remote-session-authority.ts:226` maps 429/500/502/504 to a 403 `session_private` on the session arm (round 3 fixed `authorizeHost` only), so an edge 5xx on a grantee's `?sessionID=` open reads as final.
7. MINOR — `agent-sdk-runtime/src/compat-events.ts:123-163` `eventSessionId` is fail-open: unenumerated presentation types return `undefined` → delivered workspace-wide; `message.removed`/`message.part.removed` carry `sessionID` but fall to `default`. Reading `properties.sessionID` as the default closes it.
8. NIT — `IdentityAwareEventSource.open().decide` has no production reader (was the `allows` filter's API).
9. NIT — a session-scoped grantee whose session is deleted keeps a stream that renews nothing until the client closes.
10. NIT — docs floor: `/api/wr/runtime-events` in `docs/plans/2026-09-09-003-session-transcript-defect-log.md:1872`, `docs/verification/session-rendering/2026-09-12/progress-report.md:74`; `runtime-event-projection` in `docs/verification/session-rendering/2026-09-15-live-replay-duplicates.md` — dated records, but the DoD row claims zero. `runtime-session-authority.ts:327-329` keyless-plane sentence describes the pre-round-6 terminate (now: stream stays up, session frames held-omitted every 5 s).

Checked clean: no private-session leak; no retired route/helper in `packages/*/src`; create-protocol order registration → `created`; embedded self-hosted policy coherent.
