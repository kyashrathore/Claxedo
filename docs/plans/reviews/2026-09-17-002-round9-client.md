# Round 9 — client review of `3d6a5559ab`

Round-8 fixes re-verified: per-directory reconcile isolation; `/pty` gone → idle; live → recorded lifecycle; loopback/relay request shapes; hook auth; session-arm parking (no timer/escalation, `close()` clean, revocation navigates to the workspace route and stays parked); mock refusal code; comment rewrites.

Findings:
1. MAJOR — `agent-status-listener.ts:550-553`: healing a live terminal to recorded `Idle` clears `seen`, so the done dot never appears (the live Idle path keeps `seen` unless cancelled). `clearSeen` belongs to the gone arm only.
2. MAJOR — `terminalReconnectTargets` skips idle-tracked terminals, so idle→working/permission is never healed (a pending ask in an un-routed workspace shows nothing until its next frame — never); `useReconnectReconciliation` skips the first stream-up, so persisted dots are not reconciled at boot. Header/plan overclaim.
3. MINOR — outcomes snapshotted across all directories and applied after the loop with no generation token: a slow directory lets a stale recorded `Busy` overwrite a live `Idle`; overlapped reconciles (A→B→A) likewise.
4. MAJOR — `claxedo-events.tsx:749`: a parked session re-granted never reopens (the `granted` share notice reaches only the ingress); before round 8 the retry loop self-healed.
5. MAJOR — `e2e/playwright/core-harness-rendering-matrix.spec.ts:1536` asserts the mock's old `session_access_denied` body; red on the merge commit; its comment says the reader does not reopen on it.
6. MINOR — `terminalSessionHookPath` and the response read duplicate `terminal-session-preview.ts`'s `previewPath`/`parse`.
7. NIT — the mock note cites `routes/events.test.ts` for the session ring's separate numbering; no test asserts it. A retarget landing during the `await streamDenied()`/`res.text()` window turns the AbortError into a failure that nulls the new `state.abort` and schedules a second `connect()`.

Verified clean: only the two routes (+ proxy, bridge) opened; retired-name grep clean; single emitter; consumers name union members; desktop diff comment-only.
