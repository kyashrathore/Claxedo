# Round 8 — server review of `856d7a8a81`

Round-7 fixes re-verified: `refusal()` matrix (403 held omit; 503/thrown keeps a granted lease until expiry; never-granted → terminate; 401 → terminate; workspace lease same rule; 403 of a granted session at renewal ends the stream); `scopeKey` actor-at-role (no cross-actor/role leak); per-connection `forgetSession` after the decision + renew terminates a session-scoped connection whose grant is gone; `deniedFromResponse` 401/403/409 only; `eventSessionId` default; `open().decide` gone.

Findings:
1. MAJOR — `event-delivery.ts:575-611` tombstone rebuild coerces every non-`deliver` fill result (503, thrown, 10 s deadline) into a silent skip, rings nothing, leaves `attached: true`, so a cursor reconnect while the authority is still away sees a contiguous ring over the hole: no gap, the frame is gone (scratch R8-A). When the authority is back the fill re-decides and delivers — a replay, not a gap; round 7's "reconnect reads the hole as a gap" and the `refusal` comment describe a mechanism that did not exist.
2. MINOR — two connections of one actor share a ring: tab A revoked at renewal reconnects by cursor into the scope tab B holds; `attachSseFanout` replays without a per-connection decision (frames from B's ≤5 s cached-grant window reach A), and `authorizedSessions` seeded from the whole ring (`:653-660`) turns A's next 403 into a disconnect → reconnect → seeded → disconnect churn until the scope evicts. The seeding's stated purpose is served by renewal now.
3. MINOR — self-hosted node: embedded `authorizeHost` grants by stamped role with no lease, so `holdHost` is skipped and `renewHost` never re-asks; a member removed from the workspace keeps every session-less frame until they close the tab. Undocumented divergence from the relay path.
4. MINOR — `signed-unattributed` is unreachable (every host token / embedded identity / owner grant carries an actor); `replayKey` is computed for all and read only on that arm; `defaultEventDeliveryPolicy`'s arm, the `unattributed:` scope branch and the `:113-118` comment describe keying no reader uses.
5. MINOR — comments: source docblock `:366-368` ("keyed by … the runtime access token"), acceptance test `:655` ("the scope is the access token's"), round-7 plan text and `refusal` comment (finding 1).
6. MINOR — every `wr/events` connection lives at most one RAT lifetime (host-lease renewal checks the parent RAT's expiry → 401 → terminate; the daemon proxy mints 10-minute RATs); the reconnect resumes by cursor since round 7, but the periodic tear-down is stated nowhere.
7. MINOR — a subagent child's `session.deleted` is scoped to the child: the store row is deleted before publish, so `parentSessionIdFor(child)` is undefined at decision; a session-scoped grantee of the parent never receives it (pre-existing).
8. NIT — the kept round-7 repro tests import `./pump`/`hono`/`solid-js` unresolvable from `docs/`; only one of four loads.
9. NIT — `eventSessionId`'s enumerated `session.updated`/`session.deleted`/`message.updated` cases still return undefined on a malformed frame instead of falling through to `properties.sessionID`.

Checked clean: no cross-actor/role replay; renew matrix at both leases; create registers before `created`; cp handlers coherent; retired-name grep clean in code; acceptance test covers the real RHT/lease chain with revocation at 200 ms cadence.
