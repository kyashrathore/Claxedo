# Round 7 — client review of `daeb9114c0` (unactioned; recorded verbatim in substance)

Round-6 client fixes re-verified: per-kind tracker; reconciliation single-run; `emit(event)` signature at every call site; terminal provider always receives the emitter in production (`directory-scope.tsx:396` under `app/entry/app.tsx:453`); `workspaceAddress` rename complete; loopback+cloud test proves the client seam (not the proxy end to end).

Findings (all MINOR unless noted):

1. `claxedo-events.tsx:574-580` — narrowing to `?sessionID=` after `workspace_event_stream_denied` keeps `state.lastEventId` from the workspace ring; the session arm is its own ring since round 6, so the cursor reads as a gap → spurious `stream.replay-gap` → repair pull + subagent-registry reset. Retarget (line 688) already nulls the cursor; the narrowing path must too. Tier M mock (`mock-runtime.ts:1966-1980`) uses one numbering for both arms, so it cannot see this.
2. `claxedo-events.tsx:574` + retarget 682-700 — a grantee (session arm) opening the workspace's draft route reopens unscoped → 403 with no session to narrow to → backoff loop to 15 s, `console.error` at the 3rd failure, `markWorkspaceReconnecting`; navigating back to the session waits out the backoff timer before narrowing.
3. `stream-connectivity.ts:15-24`, its test 118-119, `agent-status-listener.ts:430-437` — `workspaceReconnects` can never increment: `reconcileTargets` (`claxedo-events.tsx:762-776`) closes stale keys before opening new ones, so `up.wr` never exceeds 1; every wr return is a level edge. The counter arm and the "retarget overlaps" comments are dead. Every workspace switch is a level edge → one `/pty` reconcile per tracked directory per switch.
4. `platform/api/claxedo-api-types.ts:128-138` + `presentation-frames.ts:27-37` — `properties`-shaped `pty.*` / `session.share.changed` union arms have no producer and, since the terminal fallback was deleted, no consumer; the plan's DoD "Repository" row still cites the deleted fallback as the reason to keep them.
5. Comments contradicting landed code: `session-event-scope.ts:63-68` (two stacked docblocks on `live`); `session-controller.ts:634-638` (workspaceId "threaded so the stream routes through the relay" — the reader reads only `live.workspaceAddress`); `e2e/playwright/core-user-hosted-workspace.spec.ts:168-174` names deleted `runtime-envelope.ts`, and line 1002 asserts the mock's own constant (tautology); `session-event-scope.test.ts:87-96` ("lanes really are session-scoped… both lanes"); `claxedo-desktop/src/main/index.ts:780-784`, `host-connector/child-protocol.ts:49-56`, `child-supervisor.ts:143-147` ("clients open no workspace stream"); `claxedo-server/src/connections/cloud-connection.ts:121-125` ("unscoped answers 400 session_event_scope_required"); `session-list-events.ts:14-16` (wrong path, history); `workspace-connection.ts:279` ("provision SSE stream").
6. Undocumented desktop-local divergence: before, the daemon's central stream carried every local workspace's `pty.*`/`agent.lifecycle`; now only the routed workspace's `wr` does, so a non-routed workspace's pane settles only on the switch reconcile or the rail batch. Reader header (`claxedo-events.tsx:1-7`) should name it.

NIT — `account-stream-fetch.ts:126-147`: abort between `streamOpen`/`streamStart` resolves a Response with an errored body; a stale `cp:account` snapshot write 45 s later; no reader.

Verified clean: only `/api/cp/events`, `/api/wr/events` (and the daemon proxy / desktop `controlPlane.events`) opened anywhere; no retired spelling survives except the comments above; heartbeats are `data:` frames on all three producers; guards and consumer/stream pairing hold; Tier R specs assert on the wire.
