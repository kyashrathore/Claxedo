# Round 10 — server review of `1270592b2c` (rebased onto dev `d94e4250d9`)

Round-9 fixes re-verified: `holeBelow` watermark in every direction (cursor at the hole after a terminate, second tab at the same cursor, restore with the plane back re-decides, rolled retained ring, fresh-scope boundary); `refusal()` outage statement matches code; actor unconditional in both claim types; `deletedSessionParent` defensive; the two-arms route test discriminates a shared ring from the session ring. Rebase coherent: overlapping files change disjoint regions; dev's transcript/session commits touch no stream seam.

Findings:
1. MAJOR — `event-delivery.ts` tombstone stored `retainedCursor` only when the scope had RUNG a frame; a scope that rang nothing (a grantee opening on an idle session, a member whose workspace held only others' private sessions) was restored as continuing after the retained ring rolled: `hasGap(undefined)` is never a gap, `holeBelow` 0, the rebuild pushed the surviving 256 frames, and the reader's bootstrap cursor resumed contiguously over everything that rolled out — no gap notice. Reproduced with a probe against the real module. Fix shape: the tombstone must record the retained position the scope had DECIDED through (the hosted room already does), and `continues` must test that.
2. MINOR — the local `cp/events` handler has the same hole (`retainedCursor` only from visible frames); three deployments held three tombstone semantics with nothing documenting it.
3. MINOR — `sessionBus` on `createSessionRoutes`' options is read by nothing (its one event kind lost its publisher in round 2); `RuntimeSessionBusEvent` re-exported; two tests assert an empty bus.
4. NIT — a fresh scope's own bootstrap cursor equals `initialSequence` equals the hole watermark, so a second same-actor connection keeping the scope alive makes the first's reconnect at that cursor a spurious gap and resync; no loss.
5. NIT — the local cp handler kept the attach-cleared `unknownSequence` boolean round 9 rejected for `wr`; equivalent to the watermark except at a cursor equal to the ring start after another tab attached; divergence undocumented.
6. NIT — `routes/events.ts` `deletedSessionParent` comment claimed a wire and a protection the code does not give: frames are in-process only, and a frame without `payload` throws earlier; the backstop is `evaluate`'s try/catch, which ends the connection.

Checked clean: producer → policy → apply → fanout on all four deployments; session ring omits session-less and actor-only frames; child deletion scoped to parent; forgetSession after this connection's decision; renewal/overflow terminates evict via the fanout unsub; remote authority 401/403/409 vs 503; retired-name grep zero in production outside finding 3; rebase overlaps disjoint; hosted cp fallback and room untouched by round 9.
