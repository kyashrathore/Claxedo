.DS_Store
node_modules
packages/claxedo-server/bench-reports/
# Vite fixture generated by packages/session-ui/script/verify-mermaid-wiring.mjs.
# The script removes it in a `finally`; this is only here so a killed run cannot
# leave something committable behind.
packages/session-ui/script/.mermaid-wiring/
packages/claxedo-server/src/cloud/cloudflare-worker/.sandbox-build/
.worktrees
.claude/worktrees/
.sst
.env
.env.local
.idea
.vscode
.codex
*~
playground
tmp
dist
dist-*
build
out
ts-dist
.turbo
# Every `wrangler deploy`/`dev` — including `--dry-run` — writes a local state
# dir next to the wrangler.toml it ran against. packages/workspace-relay had
# its own rule; packages/claxedo-server and the sandbox/bench workers did not,
# so a dry-run left untracked junk in the tree.
.wrangler/
.astro
.cache
.next
.output
.svelte-kit
.vite
coverage
playwright-report
test-results
# JUnit reports written by every package's `test` script and published by
# .github/workflows/test.yml (packages/*/.artifacts/unit/junit.xml).
.artifacts
**/.serena
.serena/
/result
refs
Session.vim
/opencode.json
a.out
target
sidecars
.scripts
.direnv/

# Local dev files
opencode-dev
UPCOMING_CHANGELOG.md
logs/
*.bun-build
*.tsbuildinfo

# Claxedo development artifacts
**/.dev-docs/
packages/claxedo-app/dev-docs/

# Auto-generated schema (derived from code, regenerated on boot)
.opencode/processes.schema.json

# Claxedo runtime data
.claxedo/
.opencode/processes.jsonc

# Playwright test artifacts
docs/screenshots/
docs/verification/**/*.png
packages/claxedo-app/e2e/playwright/report/
packages/claxedo-app/e2e/playwright/screenshots/
packages/claxedo-app/e2e/playwright/test-results/

# Electron build outputs
packages/claxedo-desktop/resources/acp/
packages/claxedo-desktop/resources/claxedo-server/
packages/claxedo-desktop/resources/claxedo-server.js
packages/claxedo-desktop/resources/opencode-cli

# Build artifacts
packages/workspace-runtime-bundle.tar.gz
*.tar.gz
*.zip
*.dmg
*.pkg
*.AppImage
*.deb
*.rpm
*.exe
*.msi
packages/opencode/src/provider/models-snapshot.ts
.collaborator
.vercel
.env*.local
.claude/ralph-loop.local.md
.claude/scheduled_tasks.lock

# Sandbox image host bundles (generated by build-sandbox-image.ts)
packages/claxedo-server/scripts/sandbox/.build/
packages/claxedo-server/scripts/sandbox/cloudflare-worker/.build/

# Extracted renderer bundles used for icon spelunking — never commit these,
# they are multi-megabyte third-party application code.
app-initial-*.js
