Read CLAUDE.md at the repo root and follow it (comment rules, honest completion, ratchets). Branch: dev at 342e26247e; work on a feature branch `fix/pi-agent-dir-precedence`, never push, never create PRs on upstream.

Defect (pre-existing on dev, found while running Tier R on this machine): `packages/agent-sdk-runtime/src/harnesses/pi/agent-dir.ts` `piAgentDir()` resolves `options.agentDir ?? storeRoot/pi/agent ?? env PI_CODING_AGENT_DIR ?? ~/.claxedo/pi/agent/<ws>`. The embedded workspace runtime always passes a `storeRoot`, so the scripted `PI_CODING_AGENT_DIR` that `packages/claxedo-app/e2e/helpers/scripted-model-server.ts` writes (a `models.json` pointing the `openai` provider at the scripted server, plus `settings.json`) is never read, and Pi's turns go to the real api.openai.com with `test-key` ("Incorrect API key provided") instead of the scripted model. `real-harness-local.spec.ts` therefore fails most Pi cases locally (31/98 on dev and on the branch). Reproduce first with `packages/claxedo-app/e2e/playwright/real-harness-local.spec.ts` "local new-worktree session receives its first reply" (`cd packages/claxedo-app && DISABLE_UPDATE_PROMPT=true PLAYWRIGHT_PORT=4477 CLAXEDO_TIER_REAL_BACKEND_PORT=4319 CLAXEDO_E2E_SERVE_MODE=dev CLAXEDO_TIER_REAL_E2E=1 CLAXEDO_E2E_SUITE=core PLAYWRIGHT_VIDEO=0 npx playwright test --config playwright.config.ts e2e/playwright/real-harness-local.spec.ts --workers=1 --retries=0 --grep "new-worktree session receives its first reply"`), read the page snapshot in test-results for the "Incorrect API key" status.

Decide the precedence from what each source means: the workspace-scoped store root exists so one workspace's broker placeholder never reaches another (read the docblock); an explicit environment override is the operator's or the harness's own profile. Make the resolution state one rule, keep the per-workspace isolation for the broker overlay, and prove it with a unit test in `agent-dir.test.ts` (or the pi driver test) that fails without the change, then rerun the Playwright case above until it passes 3/3. Gates: `bun run typecheck` and `bun test src` in packages/agent-sdk-runtime; `bunx oxlint <changed files>` from the root; `bun run test:architecture-ratchets`. Commit path-scoped with the trailer `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`. Write `.lane-reports/DEVIN-PI-AGENT-DIR.md` with the cause as a flow, the rule you chose and why, every command and result.
