Read CLAUDE.md at the repo root and follow it. Branch: dev at 342e26247e; work on `fix/harness-renewal-session-loss`, never push, never create PRs on upstream. Read `.lane-reports/BROKER-FIX.md` §3 and §7 first (in `.worktrees/host-machine/.lane-reports/` or the session scratchpad copy).

Defect (pre-existing): the local credential broker now re-mints a binding's placeholder only at the projection's renewal half-life (commit 352ec21ffe), but when it does, `packages/agent-sdk-runtime/src/harnesses/pi/driver.ts` `applyConfig` sees `piProviderOverrides(auth)` change and calls `closeProcesses()`, and `harnesses/codex/driver.ts` `restartProcess()` does the same. A session created and not prompted across that renewal has no session file yet (Pi writes it on the first assistant message; Codex has no rollout yet), so its next turn fails with "Pi session file is missing" / "no rollout found for thread id". A renewal is not an account switch: the placeholder changes, the account and lease generation do not.

Fix at the owner: carry the lease generation (or an authority-chosen epoch) on the projection row in `packages/agent-sdk-runtime/src/provider-projection.ts` and the runtime config schema (`packages/workspace-runtime/src/routes/config.ts` normalizer, `packages/claxedo-local-server/src/credentials/broker.ts` producer), so a driver can tell a renewal (same generation: keep an idle process whose session is not yet persisted, it serves until it persists, and re-key the overlay for the next spawn) from a switch or withdrawal (generation moved: close, as today). Keep the "Cannot rotate Pi credentials during an active turn" guard. `packages/agent-sdk-runtime/src/test-utils/fake-pi-rpc.mjs` writes the session file at process start unlike real Pi; give it a mode that writes on the first assistant message so a test can reach the unpersisted window, and add the discriminating tests in the pi and codex driver tests plus one at the embedded runtime level (see `packages/claxedo-local-server/src/deployments/local/embedded-workspace-runtime.projection-stability.test.ts` for the pattern of driving the real broker through the real runtime with a moved clock). No compatibility bridge: producers and readers of the new field move together. Gates: typecheck and tests in agent-sdk-runtime (`bun test src`), workspace-runtime (`bun test src/routes src/workspace`), claxedo-local-server (`node ./node_modules/vitest/vitest.mjs run src/credentials src/deployments/local`); `bun run build` + `bun scripts/verify-publish.ts` in agent-sdk-runtime if its public surface changes (13 declaration-hash drifts already exist on dev); `bunx oxlint <changed files>`; `bun run test:architecture-ratchets`. Commit path-scoped with the trailer `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`. Write `.lane-reports/DEVIN-PI-RENEWAL.md`: cause as a flow, design, red/green proofs, every command and result.
