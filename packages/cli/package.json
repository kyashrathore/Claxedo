{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "@opencode-ai/cli",
  "version": "1.17.13",
  "type": "module",
  "license": "MIT",
  "bin": {
    "lildax": "./bin/lildax.cjs"
  },
  "files": [
    "bin"
  ],
  "scripts": {
    "build": "bun run scripts/build.ts",
    "dev": "bun run src/index.ts",
    "test": "mkdir -p .artifacts/unit && node ../../scripts/test-deadline.mjs bun test src --reporter=junit --reporter-outfile=.artifacts/unit/junit.xml",
    "typecheck": "tsgo --noEmit"
  },
  "dependencies": {
    "@claxedo/helpers": "workspace:*",
    "@claxedo/host-connector": "workspace:*",
    "@claxedo/host-serving": "workspace:*",
    "@claxedo/workspace-runtime": "workspace:*"
  },
  "devDependencies": {
    "@tsconfig/bun": "catalog:",
    "@types/bun": "catalog:",
    "@types/node": "catalog:",
    "@typescript/native-preview": "catalog:",
    "@tsconfig/node-lts": "22.0.4",
    "esbuild": "0.25.12"
  }
}
