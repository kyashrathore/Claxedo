dist/
node_modules/
reports/
# Auto-calibrated budgets/baselines/trends are regenerated per run and depend on
# dev-server noise today. Track them again once we gate against a stable
# (production-build) target.
data/
