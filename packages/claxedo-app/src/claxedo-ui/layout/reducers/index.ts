import * as contentsR from "./contents"
import * as panesR from "./panes"
import * as splitR from "./split"
import * as navigationR from "./navigation"

export const reducers = {
  contents: contentsR,
  panes: panesR,
  split: splitR,
  navigation: navigationR,
}
