# Session Feature

Session lifecycle, conversation projection, composer and submission, harness
selection, session providers, actions, navigation, timeline, and session UI.

```json
{
  "owns": "Session lifecycle, conversation, composer, harness, providers, actions, and UI",
  "writerOf": [],
  "mustNotImport": [
    "@/app/*",
    "@/features/browser/*",
    "@/features/extensions/*",
    "@/features/processes/*",
    "@/features/terminal/*",
    "@/features/workspaces/*",
    "@/shell/*",
    "@/context/*",
    "@/components/*",
    "@/pages/*",
    "@/claxedo-ui/*",
    "@/pane/*",
    "@/shared/*",
    "@/terminal/*"
  ]
}
```
