# Workspaces Feature

Workspace connection state, project/workspace lifecycle, panels, dialogs, actions, and display utilities.

```json
{
  "owns": "Workspace connection state, lifecycle, actions, panels, dialogs, and UI",
  "writerOf": [],
  "mustNotImport": [
    "@/app/*",
    "@/features/browser/*",
    "@/features/documents/*",
    "@/features/extensions/*",
    "@/features/processes/*",
    "@/features/review/*",
    "@/features/session/*",
    "@/features/settings/*",
    "@/features/terminal/*",
    "@/shell/*",
    "@/context/*",
    "@/components/*",
    "@/pages/*",
    "@/claxedo-ui/*",
    "@/pane/*",
    "@/shared/*"
  ]
}
```
