# WorkGraph Feature

The personal WorkGraph client, canonical record projection, and WorkGraph workbench surface form one vertical feature.

```json
{
  "owns": "WorkGraph transport client, projection, and user interface",
  "writerOf": [],
  "mustNotImport": ["@/features/browser/*", "@/features/documents/*", "@/features/extensions/*", "@/features/processes/*", "@/features/review/*", "@/features/session/*", "@/features/settings/*", "@/features/terminal/*", "@/features/workspaces/*"]
}
```
