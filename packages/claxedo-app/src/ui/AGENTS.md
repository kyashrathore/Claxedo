# Reusable UI

Presentation-only controls and primitives with demonstrated reuse across
features and application workbench surfaces.

```json
{
  "owns": "Reusable presentation controls and UI primitives",
  "writerOf": [],
  "mustNotImport": ["@/app/*", "@/features/*", "@/shell/*", "@/context/*", "@/components/*", "@/claxedo-ui/*"]
}
```
