# Library Primitives

Dependency-light language and browser primitives without product-domain or
application-service ownership. Files here may use platform APIs and external
utility packages, but they do not import from app, features, platform, or UI.

```json
{
  "owns": "Dependency-light language and browser primitives",
  "writerOf": [],
  "mustNotImport": ["@/app/*", "@/features/*", "@/platform/*", "@/ui/*", "@/shell/*", "@/context/*", "@/components/*", "@/claxedo-ui/*"]
}
```
