# Sync Capability

Headless event ingress, refresh policy, bootstrap coordination, projection,
query-cache writing, and live-resource synchronization.

```json
{
  "owns": "Headless synchronization, event routing, and cache projection",
  "writerOf": [],
  "mustNotImport": ["@/app/*", "@/features/*", "@/shell/*", "@/context/*", "@/components/*", "@/claxedo-ui/*", "@/ui/*"]
}
```
