# Files Capability

Headless file-path encoding, request caching, tree state, content state, watcher
invalidation, and shared file-view contracts.

```json
{
  "owns": "Headless file access, cache, tree, and watcher behavior",
  "writerOf": [],
  "mustNotImport": ["@/app/*", "@/features/*", "@/shell/*", "@/context/*", "@/components/*", "@/claxedo-ui/*", "@/ui/*"]
}
```
