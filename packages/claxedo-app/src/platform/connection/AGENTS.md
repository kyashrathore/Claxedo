# Connection Platform

Transport-independent server connection identity and contracts.

```json
{
  "owns": "Server connection identity and contracts",
  "writerOf": [],
  "mustNotImport": ["@/app/*", "@/features/*", "@/ui/*", "@/context/*", "@/shell/*", "@/claxedo-ui/*"]
}
```
