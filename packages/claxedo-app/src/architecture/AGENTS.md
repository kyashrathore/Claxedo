# Architecture Guards

Owns static guard tests and generated baselines. This directory is test and
tooling infrastructure only.

```json
{
  "owns": "Static architecture guards, ratchets, baselines, ownership manifests",
  "writerOf": [],
  "mustNotImport": ["solid-js", "@tanstack/*", "@opencode-ai/sdk*", "@/components/*", "../shell/*"]
}
```
