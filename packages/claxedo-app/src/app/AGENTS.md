# Application Composition

Application entry, routing, provider assembly, boot, connection orchestration, feature integrations, workbench composition, and app styles.

```json
{
  "owns": "Application composition, routing, providers, integrations, boot, connection orchestration, and workbench assembly",
  "writerOf": [],
  "mustNotImport": ["@/architecture/*"]
}
```
