---
title: "Install"
description: "Install the ten npm-published Claxedo packages in one command — each layer is independently usable."
---

import Card from "../../../../components/framework/compat/Card.astro"
import CodeGroup from "../../../../components/framework/compat/CodeGroup.astro"


Install only the layers you need — each package can be used independently. Most
full products start with `@claxedo/workspace-runtime`.

## Published on npm

All ten framework packages are live on the npm registry. Install the ones you
need — or the whole set in one line:

<CodeGroup>

```sh npm
npm install @claxedo/workspace-runtime @claxedo/agent-sdk-runtime @claxedo/agent-event-runtime @claxedo/agent-extensions @claxedo/workspace-relay @claxedo/workspace-relay-protocol @claxedo/sandbox-manager @claxedo/channels @claxedo/connections @claxedo/workgraph
```

```sh pnpm
pnpm add @claxedo/workspace-runtime @claxedo/agent-sdk-runtime @claxedo/agent-event-runtime @claxedo/agent-extensions @claxedo/workspace-relay @claxedo/workspace-relay-protocol @claxedo/sandbox-manager @claxedo/channels @claxedo/connections @claxedo/workgraph
```

```sh bun
bun add @claxedo/workspace-runtime @claxedo/agent-sdk-runtime @claxedo/agent-event-runtime @claxedo/agent-extensions @claxedo/workspace-relay @claxedo/workspace-relay-protocol @claxedo/sandbox-manager @claxedo/channels @claxedo/connections @claxedo/workgraph
```

</CodeGroup>

| Package | Install |
| --- | --- |
| `@claxedo/workspace-runtime` | `npm install @claxedo/workspace-runtime` |
| `@claxedo/agent-sdk-runtime` | `npm install @claxedo/agent-sdk-runtime` |
| `@claxedo/agent-event-runtime` | `npm install @claxedo/agent-event-runtime` |
| `@claxedo/agent-extensions` | `npm install @claxedo/agent-extensions` |
| `@claxedo/workspace-relay` | `npm install @claxedo/workspace-relay` |
| `@claxedo/workspace-relay-protocol` | `npm install @claxedo/workspace-relay-protocol` |
| `@claxedo/sandbox-manager` | `npm install @claxedo/sandbox-manager` |
| `@claxedo/channels` | `npm install @claxedo/channels` |
| `@claxedo/connections` | `npm install @claxedo/connections` |
| `@claxedo/mcp` | `npm install @claxedo/mcp` (or `npx -y @claxedo/mcp` for the bridge) |
| `@claxedo/workgraph` | `npm install @claxedo/workgraph` |

## Next step

<Card title="See it in action" href="/framework/guides/see-it-in-action" icon="play">
  The quickstart: boot a Workspace Host, configure a harness, and send your
  first prompt.
</Card>
