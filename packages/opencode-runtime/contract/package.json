{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "@claxedo/opencode-sdk-contract",
  "private": true,
  "type": "module",
  "description": "Executable contract probes for the pinned public OpenCode embedded SDK. Deliberately NOT a workspace member: `packages/*` matches one level, so nesting under `packages/opencode-runtime/` keeps this install isolated from the repo's legacy @opencode-ai/* workspace packages.",
  "scripts": {
    "probe": "bun run probe.mjs",
    "probe:node": "bun run build-node-bundle.ts && node probe-node.mjs",
    "check:node-import": "node node-loadability.mjs"
  },
  "dependencies": {
    "@opencode-ai/sdk": "0.0.0-beta-18314"
  }
}
