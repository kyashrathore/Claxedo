research
dist
dist-*
gen
app.log
script/build-*.ts
# ...but never their tests: `script/build-*.ts` also matched
# `script/build-node.test.ts`, so a guard written there would be silently
# excluded from the repo and never run in CI.
!script/build-*.test.ts
temporary-*.md
.artifacts
